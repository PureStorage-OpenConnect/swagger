allOf:
- $ref: ../../models/FB1.8/_built-in.yaml
- readOnly: true
  properties:
    async:
      description: Total bytes transmitted or received per second for asynchronous
        replication.
      allOf:
      - $ref: ../../models/FB1.9/_replication_performance.yaml
    time:
      description: Sample time in milliseconds since UNIX epoch.
      type: integer
      format: int64
