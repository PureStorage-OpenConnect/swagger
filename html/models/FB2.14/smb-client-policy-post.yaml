allOf:
- $ref: ../../models/FB2.14/policy-base.yaml
- properties:
    rules:
      type: array
      maxItems: 200
      items:
        $ref: ../../models/FB2.11/smb-client-policy-rule-post-in-policy.yaml
      description: All of the rules that are part of this policy. The order is the
        evaluation order.
