allOf:
- $ref: ../../models/FB2.10/smb-client-policy-rule-base.yaml
- properties:
    encryption:
      description: Specifies whether the remote client is required to use SMB encryption.
        Valid values are `required`, `disabled`, and `optional`.
      type: string
      example: optional
