allOf:
- $ref: ../../models/FB1.3/array.yaml
- properties:
    timezone:
      description: >-
        The timezone to use for the array. In particular this affects
        the CLI formatting and snapshot policies.
      type: string
      example: America/Los_Angeles
