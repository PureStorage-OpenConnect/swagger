type: object
properties:
  start_date:
    description: Date when the support contract started. Represented as a timestamp
      of 00:00 on that date in UTC, in milliseconds since UNIX epoch.
    type: integer
    format: int64
    example: 1550966400000
  end_date:
    description: Date when the support contract ended. Represented as a timestamp
      of 00:00 on that date in UTC, in milliseconds since UNIX epoch.
    type: integer
    format: int64
    example: 1582502400000
  resource:
    $ref: ../../models/Pure1-1.0/_fixed-reference.yaml
