type: object
properties:
  key:
    description: Key of the tag.
    type: string
    example: example_key
  namespace:
    description: Namespace of the tag. Namespace identifies the category of the tag.
    type: string
    example: pure1
  resource:
    $ref: ../../models/Pure1-1.0/_fixed-reference.yaml
  tag_organization_id:
    description: Org id of the tag.
    type: integer
    format: int64
    example: 101
  value:
    description: Value of the tag.
    type: string
    example: value123
