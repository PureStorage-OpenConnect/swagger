allOf:
- $ref: ../../models/FB1.9/file-system.yaml
- properties:
    multi_protocol:
      allOf:
      - $ref: ../../models/FB1.11/_multi-protocol.yaml
      description: Configuration options governing the interactions of different protocols
        when multiple are enabled on the file system.
    nfs:
      allOf:
      - $ref: ../../models/FB1.11/_nfs-patch.yaml
      description: NFS configuration.
    smb:
      allOf:
      - $ref: ../../models/FB1.11/_smb.yaml
      description: SMB configuration.
