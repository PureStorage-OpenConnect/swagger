allOf:
- $ref: ../../models/FB2.0/_resource.yaml
- properties:
    created:
      type: integer
      readOnly: true
      format: int64
      description: Creation timestamp of the server.
    dns:
      type: array
      maxItems: 1
      items:
        $ref: ../../models/FB2.0/_reference.yaml
      description: The DNS config to be used by this server.
    directory_services:
      type: array
      maxItems: 1
      items:
        $ref: ../../models/FB2.0/_reference.yaml
      description: The directory service config to be used by this server.
