allOf:
- type: object
  properties:
    group:
      description:
        The host group in which the host is a member.
      allOf:
      - $ref: ../../models/FA2.0/_reference-no-id.yaml
    member:
      description:
        The member host of the host group.
      allOf:
      - $ref: ../../models/FA2.0/_reference-no-id.yaml
