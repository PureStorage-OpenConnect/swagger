allOf:
- $ref: ../../models/FA2.0/_space.yaml
- properties:
    snapshots_effective:
      description: The effective space contributed by data unique to one or more snapshots,
        measured in bytes.
      format: int64
      minimum: 0
      readOnly: true
      type: integer
    total_effective:
      description: The total effective space contributed by customer data, measured
        in bytes.
      format: int64
      minimum: 0
      readOnly: true
      type: integer
    unique_effective:
      description: The effective space contributed by unique customer data. Unique
        data does not include shared space, snapshots, and internal array metadata,
        measured in bytes.
      format: int64
      minimum: 0
      readOnly: true
      type: integer
