properties:
  policies:
    description: A list of policies to apply to the resource. The `id` and `name`
      field in each `policy` parameter is required, but cannot be set together.
    items:
      properties:
        policy:
          allOf:
          - $ref: ../../models/FA2.0/_reference.yaml
          description: Reference to the policy to apply to the resource.
          required: true
      type: object
    type: array
type: object
