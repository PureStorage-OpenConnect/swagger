allOf:
- $ref: ../../models/FB2.12/hardware-connector.yaml
- properties:
    port_speed:
      description: Configured speed of each port in the connector in bits-per-second.
      readOnly: true
      type: integer
      format: int64
      example: 40000000000
