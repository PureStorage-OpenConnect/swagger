allOf:
- $ref: ../../models/FA2.17/software-patch.yaml
- properties:
    ha_reduction_required:
      description: If set to `true`, indicates reduction in high availability is required
        for Software Patch installation. If set to `false`, indicates such a reduction
        is not required.
      type: boolean
      example: false
    alert_code:
      description: Alert code for the Software Patch. This code is used to uniquely
        identify the Software Patch.
      type: integer
      format: int64
      example: 5200
