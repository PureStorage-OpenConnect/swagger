allOf:
- type: object
  properties:
    protection_group:
      $ref: ../../models/FA2.0/_fixed-reference.yaml
    target:
      $ref: ../../models/FA2.0/_fixed-reference-no-id.yaml
    allowed:
      type: boolean
      description: If set to `true`, the target array has allowed the source array
        to replicate protection group data to the target array. If set to `false`,
        the target array has not allowed the source array to replicate protection
        group data to the target.
      example: true
