allOf:
- $ref: ../../models/FA2.23/protection-group-snapshot.yaml
- $ref: ../../models/FA2.0/_resource.yaml
- properties:
    space:
      description: Returns provisioned size and physical storage consumption data
        for each protection group.
      allOf:
      - $ref: ../../models/FA2.26/_space.yaml
