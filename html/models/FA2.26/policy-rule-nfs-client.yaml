allOf:
- $ref: ../../models/FA2.21/policy-rule-nfs-client.yaml
- properties:
    nfs_version:
      description: The NFS protocol version allowed for the export. Valid values are
        `nfsv3` and `nfsv4`.
      type: array
      items:
        type: string
        example: nfsv3
