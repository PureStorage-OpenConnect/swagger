allOf:
- $ref: ../../models/FA2.15/policy-nfs-patch.yaml
- properties:
    nfs_version:
      description: NFS protocol version allowed for the export to set for the policy.
        This operation updates all rules of the specified policy. Valid values are
        `nfsv3` and `nfsv4`.
      type: array
      items:
        type: string
        example: nfsv3
