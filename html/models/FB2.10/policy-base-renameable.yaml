allOf:
- $ref: ../../models/FB2.4/policy-base-renameable.yaml
- properties:
    policy_type:
      description: Type of the policy. Valid values are `nfs`, `object-access`, `smb-client`,
        `smb-share`, and `snapshot`.
      readOnly: true
      type: string
