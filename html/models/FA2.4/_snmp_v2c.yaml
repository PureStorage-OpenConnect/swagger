description: The v2c configurations of SNMP.
properties:
  community:
    description: Manager community ID under which Purity//FA is to communicate with
      the specified managers.
    example: '****'
    maxLength: 32
    type: string
type: object
