allOf:
- $ref: ../../models/FA2.4/certificate.yaml
- type: object
  properties:
    days:
      description: The number of days that the self-signed certificate is valid. If
        not specified, defaults to 3650.
      type: integer
      format: int32
      example: 3650
    key:
      description: The text of private key.
      type: string
    passphrase:
      description: The passphrase used to encrypt `private_key`.
      type: string
