type: object
properties:
  component_name:
    description: The name of the FM.
    type: string
  status:
    description: The status of the remote-assist session on the local FM. Valid values
      are `reconnecting`, `connected`, `disconnected`, and `unknown`.
    type: string
    example: connected
