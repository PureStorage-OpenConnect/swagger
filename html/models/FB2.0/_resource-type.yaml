description: Type of the object (full name of the endpoint). Valid values are the
  unique part of the resource's REST endpoint. For example, a reference to a file
  system would have a `resource_type` of `file-systems`.
readOnly: true
type: string
