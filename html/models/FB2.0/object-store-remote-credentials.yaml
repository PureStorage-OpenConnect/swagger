allOf:
- $ref: ../../models/FB2.0/_resource.yaml
- properties:
    access_key_id:
      description: Access Key ID to be used when connecting to a remote object store.
      example: PSFBIKZFCAAAKOEJ
      type: string
    remote:
      allOf:
      - $ref: ../../models/FB2.0/_fixed-reference.yaml
      description: Reference to the associated remote, which can either be a `target`
        or remote `array`. If it is an `array`, then the `resource-type` field will
        not be populated.
    secret_access_key:
      description: Secret Access Key to be used when connecting to a remote object
        store.
      example: 0BEC00003+b1228C223c0FbF1ab5e4GICJGBPJPEOLJCD
      type: string
