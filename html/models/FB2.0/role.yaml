allOf:
- $ref: ../../models/FB2.0/_built-in.yaml
- properties:
    permissions:
      description: A list of permissions that the role can perform.
      type: array
      items:
        $ref: ../../models/FB2.0/_permission.yaml
