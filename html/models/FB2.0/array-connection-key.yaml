type: object
readOnly: true
properties:
  connection_key:
    description: Connection-key, used on another array to connect to this array. After
      creation, listing will only show ****.
    type: string
    example: 6207d123-d123-0b5c-5fa1-95fabc5c7123
  created:
    description: Creation time in milliseconds since UNIX epoch.
    type: integer
    format: int64
  expires:
    description: Expiration time in milliseconds since UNIX epoch.
    type: integer
    format: int64
