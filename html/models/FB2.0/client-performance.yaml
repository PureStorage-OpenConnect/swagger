allOf:
- $ref: ../../models/FB2.0/_built-in-no-id.yaml
- properties:
    bytes_per_op:
      description: Average operation size (read bytes+write bytes/read ops+write ops).
      format: int64
      minimum: 0
      type: integer
    bytes_per_read:
      description: Average read size in bytes per read operation.
      format: int64
      minimum: 0
      type: integer
    bytes_per_write:
      description: Average write size in bytes per write operation.
      format: int64
      minimum: 0
      type: integer
    others_per_sec:
      description: Other operations processed per second.
      format: int64
      minimum: 0
      type: integer
    read_bytes_per_sec:
      description: Bytes read per second.
      format: int64
      minimum: 0
      type: integer
    reads_per_sec:
      description: Read requests processed per second.
      format: int64
      minimum: 0
      type: integer
    time:
      description: Sample time in milliseconds since UNIX epoch.
      format: int64
      type: integer
    usec_per_other_op:
      description: Average time, measured in microseconds, it takes the client to
        process other operations.
      format: int64
      minimum: 0
      type: integer
    usec_per_read_op:
      description: Average time, measured in microseconds, it takes the client to
        process a read request.
      format: int64
      minimum: 0
      type: integer
    usec_per_write_op:
      description: Average time, measured in microseconds, it takes the client to
        process a write request.
      format: int64
      minimum: 0
      type: integer
    write_bytes_per_sec:
      description: Bytes written per second.
      format: int64
      minimum: 0
      type: integer
    writes_per_sec:
      description: Write requests processed per second.
      format: int64
      minimum: 0
      type: integer
  readOnly: true
