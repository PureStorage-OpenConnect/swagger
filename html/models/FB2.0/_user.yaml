allOf:
- type: object
- properties:
    id:
      type: integer
      format: int64
      description: The numeric user id that represents the user's identity in a POSIX
        system.
    name:
      type: string
      description: The user name that represents the user's identity, and which resolves
        to the user's id in a configured directory service.
