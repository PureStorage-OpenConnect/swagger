allOf:
- $ref: ../../models/FA2.0/_resource.yaml
- $ref: ../../models/FA2.0/_snapshot.yaml
- properties:
    remote:
      allOf:
      - $ref: ../../models/FA2.0/_fixed-reference.yaml
      description: Remote target where this volume snapshot is located.
      readOnly: true
  type: object
- description: A volume snapshot on a remote target.
