allOf:
- $ref: ../../models/FA2.0/_resource.yaml
- description: Remote pod that's not directly stretched to this array.
  type: object
  properties:
    arrays:
      type: array
      items:
        $ref: ../../models/FA2.0/_resource.yaml
      minItems: 1
