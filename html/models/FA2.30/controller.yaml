allOf:
- $ref: ../../models/FA2.2/controller.yaml
- properties:
    mode_since:
      description: The time at which the current mode of the controller was last set.
        It is measured in milliseconds since the UNIX epoch. If the controller mode
        is `not present` or `offline`, this field will be null.
      type: integer
      format: int64
      readOnly: true
      example: 1687086901996
