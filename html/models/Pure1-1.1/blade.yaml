allOf:
- $ref: ../../models/Pure1-1.0/blade.yaml
- properties:
    arrays:
      description: A list of arrays that contain this blade. Each blade should only
        exist on one array.
      items:
        $ref: ../../models/Pure1-1.1/_blade-array-status.yaml
      minItems: 1
      type: array
