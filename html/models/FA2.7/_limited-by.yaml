type: object
readOnly: true
properties:
  member:
    description: Reference to the resource to which the effective quota policy is
      attached.
    allOf:
    - $ref: ../../models/FA2.3/_fixed-reference-with-type.yaml
  policy:
    description: Reference to the effective quota policy.
    allOf:
    - $ref: ../../models/FA2.3/_fixed-reference-with-type.yaml
