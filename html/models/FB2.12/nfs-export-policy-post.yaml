allOf:
- $ref: ../../models/FB2.12/policy-base.yaml
- properties:
    rules:
      type: array
      maxItems: 200
      items:
        $ref: ../../models/FB2.10/nfs-export-policy-rule-in-policy.yaml
      description: All of the rules that are part of this policy. The order is the
        evaluation order.
