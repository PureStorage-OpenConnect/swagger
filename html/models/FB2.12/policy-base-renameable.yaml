allOf:
- $ref: ../../models/FB2.10/policy-base-renameable.yaml
- properties:
    policy_type:
      description: Type of the policy. Valid values are `bucket-access`, `cross-origin-resource-sharing`,
        `nfs`, `object-access`, `smb-client`, `smb-share`, and `snapshot`.
      readOnly: true
      type: string
