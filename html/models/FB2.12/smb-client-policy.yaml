allOf:
- $ref: ../../models/FB2.12/policy-base-renameable.yaml
- $ref: ../../models/FB2.10/_version.yaml
- properties:
    rules:
      type: array
      maxItems: 200
      items:
        $ref: ../../models/FB2.11/smb-client-policy-rule-in-policy.yaml
      description: All of the rules that are part of this policy. The order is the
        evaluation order.
