type: object
properties:
  export_enabled:
    description: If set to `true`, the export is enabled. If not specified, defaults
      to `true`.
    type: boolean
    example: false
  export_name:
    description: The new name of the export. Export names must be unique within the
      same protocol.
    type: string
