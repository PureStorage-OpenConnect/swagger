type: object
properties:
  enabled:
    description: Returns a value of `true` if and only if the export policy that manages
      this export is enabled AND this export is enabled.
    type: boolean
    readOnly: true
  export_name:
    description: The export name for accessing this export.
    type: string
  status:
    description: Explains why `enabled` is `false`, or whether there is a name conflict
      with another export. Valid values include `policy_disabled`, `export_disabled`,
      and `name_conflict`.
    type: string
    readOnly: true
