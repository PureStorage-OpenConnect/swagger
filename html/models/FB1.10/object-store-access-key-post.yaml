allOf:
- $ref: ../../models/FB1.3/object-store-access-key-post.yaml
- properties:
    user:
      allOf:
      - $ref: ../../models/FB1.8/_reference.yaml
    secret_access_key:
      type: string
      description: The secret access key to import from another FlashBlade. To import
        a set of credentials, this field must be specified with the `names` query
        parameter. If both of these not specified, the system will generate a new
        set of credentials.
