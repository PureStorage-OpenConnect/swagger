properties:
  decryption_credential:
    allOf:
    - $ref: ../../models/FA2.0/_reference-no-id.yaml
    description: The credential used by the service provider to decrypt encrypted
      SAML assertions from the identity provider. The credential is managed by the
      `certificates` endpoint and `purecert` CLI commands.
  signing_credential:
    allOf:
    - $ref: ../../models/FA2.0/_reference-no-id.yaml
    description: The credential used by the service provider to sign SAML requests.
      The credential is managed by the `certificates` endpoint and `purecert` CLI
      commands.
type: object
