allOf:
- $ref: ../../models/FB2.1/resource-performance-replication.yaml
- readOnly: true
  properties:
    continuous:
      description: Object backlog information and total bytes transmitted or received
        per second for continuous replication. Continuous replication includes object
        replication.
      allOf:
      - $ref: ../../models/FB2.2/_continuous-replication-performance.yaml
