allOf:
- $ref: ../../models/FB2.0/array.yaml
- properties:
    smb_mode:
      description: The current SMB mode of the array. This controls the type of authentication
        that is used by the array for SMB. Possible values include `ad-auto`, `ad-rfc2307`,
        `guest`, and `native`. Modifying this value requires the assistance of Pure
        Storage Support.
      type: string
      readOnly: true
