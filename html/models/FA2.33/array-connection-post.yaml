allOf:
- $ref: ../../models/FA2.4/array-connection-post.yaml
- properties:
    type:
      example: async-replication
      type: string
    encryption:
      description: If `encrypted`, all traffic over this array connection will be
        encrypted. If `unencrypted`, all traffic over this array connection will be
        unencrypted. Defaults to `unencrypted`.
      type: string
      example: encrypted
