allOf:
- $ref: ../../models/FA2.0/_resource-no-id.yaml
- description: The period of time during which maintenance will occur. Alerts are
    suppressed during the maintenance window.
  properties:
    created:
      description: The maintenance window start time, measured in milliseconds since
        the UNIX epoch.
      format: int64
      readOnly: true
      type: integer
    expires:
      description: The maintenance window end time, measured in milliseconds since
        the UNIX epoch.
      format: int64
      readOnly: true
      type: integer
  type: object
