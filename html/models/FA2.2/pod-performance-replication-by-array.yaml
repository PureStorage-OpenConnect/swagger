allOf:
- $ref: ../../models/FA2.2/_pod-performance-replication.yaml
- properties:
    array:
      allOf:
      - $ref: ../../models/FA2.0/_resource.yaml
      description: Array that shares this pod.
