properties:
  paused:
    description: Returns a value of `true` if the replica link is to be created in
      a `paused` state. Returns a value of `false` if the replica link is to be created
      not in a `paused` state. If not specified, defaults to `false`.
    type: boolean
type: object
