allOf:
- $ref: ../../models/FA2.4/array-performance.yaml
- description: Array-level front-end I/O performance per link. A link represents a
    set of arrays that an I/O operation depends on.
- properties:
    link:
      description: List of IDs and array names for the link
      type: array
      items:
        $ref: ../../models/FA2.0/_resource.yaml
