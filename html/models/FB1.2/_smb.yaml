type: object
properties:
  acl_mode:
    type: string
    description: >-
      Valid values are `shared` and `native`.
      If not specified, defaults to `shared`.
  enabled:
    type: boolean
    description: Is the protocol enabled? If not specified, defaults to `false`.
