type: object
properties:
  acl_mode:
    type: string
    description: Valid values are `shared` and `native`. If not specified, defaults
      to `shared`.
  enabled:
    type: boolean
    description: If set to `true`, enables access to the file system over the SMB
      protocol. If not specified, defaults to `false`.
