allOf:
- $ref: ../../models/FA2.23/_space.yaml
- properties:
    snapshots_effective:
      description: Deprecated. The effective space contributed by data unique to one
        or more snapshots, measured in bytes. Please use the `snapshots` field in
        the future, as it contains the same information for Evergreen//One arrays.
      type: integer
      format: int64
      minimum: 0
      readOnly: true
