allOf:
- $ref: ../../models/FB1.2/file-system-post.yaml
- properties:
    hard_limit_enabled:
      type: boolean
      description: >-
        Is the file system's size a hard limit quota?
        If not specified, defaults to `false`.
