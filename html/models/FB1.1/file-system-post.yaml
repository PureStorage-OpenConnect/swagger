allOf:
- $ref: ../../models/FB1.0/file-system-post.yaml
- properties:
    destroyed:
      type: boolean
      description: >-
        Allowed by rest, not supported by backend during create,
        so 400 error.
