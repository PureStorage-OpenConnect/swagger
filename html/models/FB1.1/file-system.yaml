allOf:
- $ref: ../../models/FB1.0/file-system.yaml
- properties:
    destroyed:
      type: boolean
      description: >-
        Is the file system destroyed?
    time_remaining:
      type: integer
      format: int64
      description: >-
        Time in milliseconds before the file system is eradicated.
        `null` if not destroyed.
      readOnly: true
