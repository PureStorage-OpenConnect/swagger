allOf:
- properties:
    eradication_timer:
      description: The eradication configuration timer in millisecond.
      type: integer
      format: int64
      example: 86400
    status:
      description: Status of the eradication configuration. Valid values are `all-enabled`,
        `all-disabled`,
      type: string
      readOnly: true
      example: all-enabled
