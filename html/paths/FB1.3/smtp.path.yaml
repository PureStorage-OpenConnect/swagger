get:
  parameters:
  # there is only 1 smtp so sort, start and token don't make much sense,
  # but since we eventually want them and they are implemented, docing them here
  - $ref: ../../queries/FB1.0/filter.query.yaml
  - $ref: ../../queries/FB1.0/limit.query.yaml
  - $ref: ../../queries/FB1.0/sort.query.yaml
  - $ref: ../../queries/FB1.0/start.query.yaml
  - $ref: ../../queries/FB1.0/token.query.yaml

  responses:
    200:
      description: OK
      schema:
        $ref: ../../responses/FB1.3/smtp-get-response.yaml

  tags:
  - smtp

patch:
  parameters:
  - name: smtp
    in: body
    required: true
    schema:
      $ref: ../../models/FB1.3/smtp.yaml

  responses:
    200:
      description: OK
      schema:
        $ref: ../../responses/FB1.3/smtp-response.yaml

  tags:
  - smtp
