get:
  summary: GET login_banner
  description: 'Get the login banner for the array. No authentication is required
    to access this endpoint.

    '
  parameters:
  - $ref: ../../queries/FB2.14/x-request-id.header.yaml
  security: []
  responses:
    200:
      description: OK
      headers:
        $ref: ../../responses/FB2.14/headers-all-default.response.yaml
      schema:
        $ref: ../../responses/FB2.0/login-banner-get-response.yaml
  tags:
  - Authorization
