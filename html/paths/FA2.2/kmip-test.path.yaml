get:
  summary: Lists KMIP connection tests
  description: Displays communication data between a FlashArray and KMIP server.
  parameters:
  - $ref: ../../queries/FA2.0/authorization.header.yaml
  - $ref: ../../queries/FA2.0/x-request-id.header.yaml
  - $ref: ../../queries/FA2.0/names_required.query.yaml
  responses:
    200:
      description: OK
      schema:
        $ref: ../../responses/FA2.2/kmip-test-result-get-response.yaml
  tags:
  - KMIP
