get:
  description: Displays the connection key for the array.
  responses:
    200:
      description: OK
      schema:
        $ref: ../../responses/FA2.4/array-connection-key-get-response.yaml
  summary: List connection key
  tags:
  - Array Connections
