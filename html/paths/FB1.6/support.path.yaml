get:
  responses:
    200:
      description: OK
      schema:
        $ref: ../../responses/FB1.6/support-get-response.yaml

  tags:
  - support

patch:
  parameters:
  - name: support
    in: body
    required: true
    schema:
      $ref: ../../models/FB1.6/support.yaml

  responses:
    200:
      description: OK
      schema:
        $ref: ../../responses/FB1.6/support-response.yaml

  tags:
  - support
