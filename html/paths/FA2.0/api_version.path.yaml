get:
  summary: Get available API versions
  description: >
    Get available API versions. No authentication is required to access this
    endpoint.
  security: []   # No security

  responses:
    200:
      description: OK
      schema:
        $ref: ../../responses/FA2.0/api_version-response.yaml

  tags:
  - Authorization
