get:
  summary: List available API versions
  description: 'Returns a list of available API versions. No authentication is required
    to access this endpoint.

    '
  security: []
  parameters:
  - $ref: ../../queries/FA2.0/x-request-id.header.yaml
  responses:
    200:
      description: OK
      schema:
        $ref: ../../responses/FA2.0/api_version-response.yaml
  tags:
  - Authorization
