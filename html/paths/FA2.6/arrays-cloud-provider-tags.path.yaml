delete:
  description: Deletes user tags from deployed cloud resources of a CBS array.
  parameters:
  - $ref: ../../queries/FA2.0/authorization.header.yaml
  - $ref: ../../queries/FA2.0/x-request-id.header.yaml
  - $ref: ../../queries/FA2.2/keys.query.yaml
  responses:
    200:
      description: OK
  summary: Delete user tags from the cloud.
  tags:
  - Arrays
get:
  description: Displays the list of user tags on deployed cloud resources of a CBS
    array.
  parameters:
  - $ref: ../../queries/FA2.0/authorization.header.yaml
  - $ref: ../../queries/FA2.0/x-request-id.header.yaml
  - $ref: ../../queries/FA2.1/continuation_token.query.yaml
  - $ref: ../../queries/FA2.0/filter.query.yaml
  - $ref: ../../queries/FA2.0/limit.query.yaml
  - $ref: ../../queries/FA2.0/offset.query.yaml
  - $ref: ../../queries/FA2.0/sort.query.yaml
  - $ref: ../../queries/FA2.0/total_item_count.query.yaml
  responses:
    200:
      description: OK
      schema:
        $ref: ../../responses/FA2.6/cloud-provider-tag-get-response.yaml
  summary: List user tags on the cloud.
  tags:
  - Arrays
