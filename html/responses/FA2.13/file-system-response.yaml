properties:
  items:
    description: Displays a list of all items after filtering. If applicable, the
      values are displayed for each name.
    items:
      $ref: ../../models/FA2.13/file-system.yaml
    type: array
type: object
