type: object
properties:
  items:
    type: array
    items:
      $ref: ../../models/FA2.13/protection-group-snapshot.yaml
    description: Returns a list of all items after filtering. If applicable, the values
      are displayed for each name.
