type: object
properties:
  items:
    type: array
    items:
      $ref: ../../models/FA2.23/host-group.yaml
    description: Returns a list of all items after filtering. The values are displayed
      for each name, if applicable.
