allOf:
- $ref: ../../responses/FA2.1/page-info.yaml
- $ref: volume-group-response.yaml
- properties:
    total:
      type: array
      items:
        $ref: ../../models/FA2.23/volume-group.yaml
      description: The aggregate value of all items after filtering. If applicable,
        the average value is displayed instead. If applicalbe, the values are displayed
        for each field.
