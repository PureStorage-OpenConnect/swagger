description: UNAUTHORIZED
type: object
schema:
  type: object
  properties:
    error:
      type: string
      example: invalid_client
    error_description:
      type: string
      example: 'Invalid identity: JWT validation failed.'
