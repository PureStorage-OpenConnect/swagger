type: object
properties:
  items:
    type: array
    items:
      $ref: ../../models/FA2.2/pod-performance-replication.yaml
    description: A list of pod performance replication objects.
  total:
    type: array
    items:
      $ref: ../../models/FA2.2/pod-performance-replication.yaml
    description: The aggregate value of all items after filtering. When applicable,
      the average value is displayed instead. The values are displayed for each field
      if meaningful.
