type: object
properties:
  items:
    type: array
    items:
      $ref: ../../models/FA2.33/volume.yaml
    description: Displays a list of all items. The values are displayed for each name,
      if meaningful.
