allOf:
- $ref: ../../responses/FB1.0/page-info.yaml
- type: object
  properties:
    items:
      type: array
      items:
        $ref: ../../models/FB1.12/file-system-performance.yaml
      description: A list of file system performance objects.
    total:
      type: array
      items:
        $ref: ../../models/FB1.12/file-system-performance.yaml
      description: Total of all records after filtering. If `total_only` query param
        is `true`, then no items will be returned.
