allOf:
- $ref: ../../responses/FA2.1/page-info.yaml
- properties:
    items:
      description: Returns a list of all items after filtering. The values are displayed
        for each name where meaningful.
      items:
        $ref: ../../models/FA2.1/remote-pod.yaml
      type: array
  type: object
