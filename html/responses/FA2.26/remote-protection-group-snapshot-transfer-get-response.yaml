allOf:
- $ref: ../../responses/FA2.1/page-info.yaml
- $ref: remote-protection-group-snapshot-transfer-response.yaml
- properties:
    total:
      type: array
      items:
        $ref: ../../models/FA2.26/remote-protection-group-snapshot-transfer.yaml
      description: The aggregate value of all items after filtering. Where it makes
        more sense, the average value is displayed instead. If applicable, the values
        are displayed for each field.
