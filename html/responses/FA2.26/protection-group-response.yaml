type: object
properties:
  items:
    type: array
    items:
      $ref: ../../models/FA2.26/protection-group.yaml
    description: Displays a list of all items after filtering. If applicable, the
      values are displayed for each name. If `total_only=true`, the `items` list will
      be empty.
