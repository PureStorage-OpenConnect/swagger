allOf:
- $ref: ../../responses/FB1.0/page-info.yaml
- $ref: ../../responses/FB1.8/file-system-snapshot-response.yaml
- properties:
    total:
      description: >-
        If `total_only` query param is `true`, then this field will be a total of
        all records after filtering and no items will be returned.
        If `total_only` is `false`, then it will be a total of all records
        on this page.
      allOf:
      - $ref: ../../models/FB1.8/file-system-snapshot.yaml
