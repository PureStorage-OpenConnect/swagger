allOf:
- $ref: ../../responses/FB2.0/page-info.yaml
- type: object
  properties:
    items:
      type: array
      items:
        $ref: ../../models/FB2.3/hardware-connector-performance.yaml
      description: A list of hardware connector performance objects.
    total:
      type: array
      items:
        $ref: ../../models/FB2.3/hardware-connector-performance.yaml
      description: Total of all records after filtering. If `total_only` query param
        is `true`, then no individual items will be returned.
