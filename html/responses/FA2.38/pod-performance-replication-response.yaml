type: object
properties:
  items:
    type: array
    items:
      $ref: ../../models/FA2.38/pod-performance-replication.yaml
    description: A list of pod performance replication objects.
  total:
    type: array
    items:
      $ref: ../../models/FA2.38/pod-performance-replication.yaml
    description: The aggregate value of all items after filtering. For real-time performance,
      the values are aggregated for the latest timestamp. For historical performance,
      the values are aggregated for each timestamp from `start_time` to `end_time`.
      When applicable, the average value is displayed instead. The values are displayed
      for each field if meaningful.
