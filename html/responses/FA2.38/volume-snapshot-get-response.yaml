allOf:
- $ref: ../../responses/FA2.1/page-info.yaml
- $ref: ../../responses/FA2.38/volume-snapshot-response.yaml
- $ref: ../../responses/FA2.38/_error-context-response.yaml
- properties:
    total:
      type: array
      items:
        $ref: ../../models/FA2.38/volume-snapshot.yaml
      description: The aggregate value of all items after filtering. Where meaningful,
        the average value is displayed instead. If applicable, the values are displayed
        for each field.
