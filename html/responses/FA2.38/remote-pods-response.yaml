allOf:
- $ref: ../../responses/FA2.1/page-info.yaml
- $ref: ../../responses/FA2.38/_error-context-response.yaml
- type: object
  properties:
    items:
      type: array
      items:
        $ref: ../../models/FA2.38/remote-pod.yaml
      description: Displays a list of all items after filtering. The values are displayed
        for each name where meaningful.
