allOf:
- $ref: ../../responses/FB2.0/page-info.yaml
- properties:
    items:
      items:
        $ref: ../../models/FB2.0/resource-performance-replication.yaml
      type: array
    total:
      description: Total of all records after filtering. If `total_only` query param
        is `true`, then no items will be returned.
      items:
        $ref: ../../models/FB2.0/resource-performance-replication.yaml
      type: array
