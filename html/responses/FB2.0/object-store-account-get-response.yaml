allOf:
- $ref: ../../responses/FB2.0/page-info.yaml
- $ref: ../../responses/FB2.0/object-store-account-response.yaml
- properties:
    total:
      allOf:
      - $ref: ../../models/FB2.0/object-store-account.yaml
      description: If `total_only` query param is `true`, then this field will be
        a total of all records after filtering and no items will be returned. If `total_only`
        is `false`, then it will be a total of all records on this page.
