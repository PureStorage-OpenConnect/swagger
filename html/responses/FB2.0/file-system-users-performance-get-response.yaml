properties:
  items:
    description: A list of file system user performance objects.
    items:
      $ref: ../../models/FB2.0/file-system-user-performance.yaml
    type: array
  total:
    description: Total of all records after filtering. If `total_only` query param
      is `true`, then no items will be returned.
    items:
      $ref: ../../models/FB2.0/file-system-user-performance.yaml
    type: array
type: object
