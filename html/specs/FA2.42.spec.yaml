swagger: '2.0'
info:
  version: '2.42'
  title: FlashArray REST API
  description: "## Pure Swagger UI for FlashArray\nA light weight tool to explore
    the API for FlashArray, community supported. \nFor  help with this tool, use [Pure1
    Community](https://community.purestorage.com/) or the [code-purestorage Slack](https://community.purestorage.com/discussion/9561/join-the-new-pure-code-slack-team-for-api-and-extensibility-discussions)\n\n##
    Authenticate using API Token\nTo get an API Key, you can do POST /auth/apitoken
    with your user/pass using the 1.x API, then use your API key with POST /login.
    \ The array returns \na header  with the name \"x-auth-token\".  This swagger
    UI tool, translates that into a cookie so the webbrowser will send on subsequent
    calls.  This Swagger UI tool then\ntranslates that cookie back into a header when
    making the API request to the array.\n\n## Authenticate using OAuth\n### Manually
    generate an id_token \n\n### \n\nIntro to how OAuth2 works with the 2.0 version:
    [Intro](https://support.purestorage.com/FlashArray/PurityFA/Purity_FA_REST_API/Reference/REST_API_2.0_Authentication_Guide)"
securityDefinitions:
  OAuth2-Bearer-Token:
    type: apiKey
    in: header
    name: Authorization
  X-Auth-Token:
    type: apiKey
    in: header
    name: x-auth-token
security:
- OAuth2-Bearer-Token: []
- X-Auth-Token: []
consumes:
- application/x-www-form-urlencoded
- application/json
produces:
- application/json
paths:
  /api/2.42/protection-groups/tags:
    get:
      tags:
      - Protection Groups
      summary: List tags
      description: Displays the list of tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Resource_destroyed'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagGetResponse'
    delete:
      tags:
      - Protection Groups
      summary: Delete tags
      description: Deletes specified tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Keys'
      - $ref: '#/parameters/Namespaces_delete'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
  /api/2.42/vchost-connections:
    get:
      tags:
      - Vchost Connections
      summary: List the vchost-connections between protocol endpoint and vchost.
      description: 'Displays a list of vchost-connections between the protocol endpoint
        and vchost.


        '
      parameters:
      - $ref: '#/parameters/All_vchosts'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Protocol_endpoint_ids_for_vchost_connections'
      - $ref: '#/parameters/Protocol_endpoint_names_for_vchost_connections'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Vchost_ids_for_vchost_connections'
      - $ref: '#/parameters/Vchost_names_for_vchost_connections'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VchostConnectionGetResponse'
    post:
      tags:
      - Vchost Connections
      summary: Create a vchost-connection between protocol endpoint and vchost.
      description: 'Creates a vchost-connection between protocol endpoint and vchost.

        Each vchost is associated with a vCenter. Each protocol endpoint

        is associated with a storage container. A vchost-connection makes

        the storage container accessible to the vCenter when the vCenter

        attempts to mount the container. One of `protocol_endpoint_names`

        or `protocol_endpoint_ids` query parameters and one of `vchost_names`

        or `vchost_ids` query parameters are required. But if `all_vchosts` is

        set to `true`, `vchost_names` and `vchost_ids` should not be specified.

        '
      parameters:
      - $ref: '#/parameters/All_vchosts'
      - $ref: '#/parameters/Allow_stretched_multi_vchost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/Protocol_endpoint_names_for_vchost_connections'
      - $ref: '#/parameters/Protocol_endpoint_ids_for_vchost_connections'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Vchost_ids_for_vchost_connections'
      - $ref: '#/parameters/Vchost_names_for_vchost_connections'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VchostConnectionResponse'
    delete:
      tags:
      - Vchost Connections
      summary: Delete the vchost-connection between a protocol endpoint and its vchost
      description: 'Deletes the vchost-connection between a protocol endpoint and
        its vchost.

        The storage container represented by the protocol endpoint will no longer

        be visible to the vCenter represented by the vchost.

        One of the `protocol_endpoint_names` or `protocol_endpoint_ids` query parameters,

        and one of the `vchost_names` or `vchost_ids` query parameters are required.

        But if `all_vchosts` is set to `true`, `vchost_names` and `vchost_ids`

        should not be specified.


        '
      parameters:
      - $ref: '#/parameters/All_vchosts'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/Protocol_endpoint_ids_for_vchost_connections'
      - $ref: '#/parameters/Protocol_endpoint_names_for_vchost_connections'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Vchost_ids_for_vchost_connections'
      - $ref: '#/parameters/Vchost_names_for_vchost_connections'
      responses:
        '200':
          description: OK
  /api/2.42/workloads:
    get:
      tags:
      - Workloads
      summary: List workloads
      description: 'Displays a list of workloads.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/WorkloadGetResponse'
    post:
      tags:
      - Workloads
      summary: Create a workload
      description: 'Creates one workload.

        '
      parameters:
      - in: body
        name: workload
        required: true
        schema:
          $ref: '#/definitions/WorkloadPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names_single_required'
      - $ref: '#/parameters/Preset_names'
      - $ref: '#/parameters/Preset_ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/WorkloadResponse'
    delete:
      tags:
      - Workloads
      summary: Delete a workload
      description: 'Deletes a workload that has been destroyed and is pending eradication.

        Eradicated workloads cannot be recovered.

        Workloads are destroyed using the `PATCH` method.

        The `ids` or `names` parameter is required, but they cannot be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids_single'
      - $ref: '#/parameters/Names_single'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Workloads
      summary: Modify a workload
      description: 'Modifies a workload by renaming or destroying it.

        To rename a workload, set `name` to the new name. To destroy a workload, set
        `destroyed=true`.

        To recover a workload that has been destroyed and is pending eradication,
        set `destroyed=false`.

        The `ids` or `names` parameter is required, but they cannot be set together.

        '
      parameters:
      - in: body
        name: workload
        required: true
        schema:
          $ref: '#/definitions/WorkloadPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids_single'
      - $ref: '#/parameters/Names_single'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/WorkloadResponse'
  /api/2.42/log-targets/syslog/settings:
    get:
      tags:
      - Log Targets
      summary: List syslog settings
      description: 'Displays syslog settings. Values include `continuation_token`,
        `items`,

        `more_items_remaining`, and `total_item_count`.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SyslogServerSettingsGetResponse'
    patch:
      tags:
      - Log Targets
      summary: Modify syslog settings
      description: 'Modifies syslog settings. Values include `continuation_token`,
        `items`,

        `more_items_remaining`, and `total_item_count`.

        '
      parameters:
      - in: body
        name: syslog-server-settings
        required: true
        schema:
          $ref: '#/definitions/SyslogServerSettings'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SyslogServerSettingsResponse'
  /api/2.42/policies/autodir/members:
    get:
      tags:
      - Policies
      summary: List auto managed directories policy members
      description: 'Displays a list of auto managed directory policy members.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberGetResponse'
    post:
      tags:
      - Policies
      summary: Create auto managed directory policies
      description: 'Creates a membership between one or more resources with an auto
        managed

        directory policy. Applicable resources are directories.

        The `policy_ids` or `policy_names` parameter is required, but they cannot
        be set together.


        '
      parameters:
      - in: body
        name: members
        required: true
        schema:
          $ref: '#/definitions/PolicyMemberPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberResponse'
    delete:
      tags:
      - Policies
      summary: Delete auto managed directory policies
      description: 'Deletes one or more auto managed directory policies from resources.

        The `policy_ids` or `policy_names` parameter is required, but they cannot
        be set together.

        The `member_ids` or `member_names` parameter is required, but they cannot
        be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/policies/smb/client-rules:
    get:
      tags:
      - Policies
      summary: List SMB client policy rules
      description: Displays a list of SMB client policy rules.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleSmbClientGetResponse'
    post:
      tags:
      - Policies
      summary: Create SMB client policy rules
      description: 'Creates one or more SMB client policy rules.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.

        '
      parameters:
      - in: body
        name: rules
        required: true
        schema:
          $ref: '#/definitions/PolicyRuleSmbClientPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleSmbClientResponse'
    delete:
      tags:
      - Policies
      summary: Delete SMB client policy rules.
      description: 'Deletes one or more SMB client policy rules.

        The `policy_ids` or `policy_names` parameter is required, but they cannot
        be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/pods/space:
    get:
      tags:
      - Pods
      summary: List pod space information
      description: 'Displays provisioned size and physical storage consumption data
        for each pod on the

        local array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourcePodSpaceGetResponse'
    parameters:
    - $ref: '#/parameters/Names'
  /api/2.42/pods/members:
    get:
      tags:
      - Pods
      summary: List pods and their members
      description: 'Displays a list of pods and the local and remote members over
        which the pods are stretched.

        Currently either array or realm is supported as member.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Pod_names'
      - $ref: '#/parameters/Pod_ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodMemberGetResponse'
    post:
      tags:
      - Pods
      summary: Creates a pod to be stretched to a member
      description: 'Creates a pod to be stretched to a member.

        When a pod is stretched to an array,

        the data in the arrays over which the pod is stretched is synchronously replicated.

        The `member_names` or `member_ids` parameter represents the array over which
        the pod is

        to be stretched.

        Both pod and member must be specified by name or ID.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Pod_names'
      - $ref: '#/parameters/Pod_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodMemberResponse'
    delete:
      tags:
      - Pods
      summary: Delete a member from a pod
      description: 'Deletes a stretched pod from an array, collapsing the pod to a
        member array.

        Unstretch a pod from an array when the volumes within the stretched pod

        no longer need to be synchronously replicated between the two members.

        After a pod has been unstretched, synchronous replication stops.

        A destroyed version of the pod with ''restretch'' appended to the pod name

        is created on the member that no longer has the pod.

        The restretched pod represents a point-in-time snapshot of the pod,

        just before it was unstretched.

        The restretched pod enters an eradication pending period

        starting from the time that the pod was unstretched.

        A restretched pod can be cloned or destroyed, but it cannot be explicitly
        recovered.

        The `member_names` parameter represents the name of the array from which the
        pod is

        to be unstretched.

        Both pod and member must be specified by name or ID.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Pod_names'
      - $ref: '#/parameters/Pod_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/With_unknown'
      responses:
        '200':
          description: OK
  /api/2.42/logout:
    post:
      tags:
      - Authorization
      summary: POST logout (placeholder)
      description: 'Invalidate a session token.

        '
      parameters:
      - $ref: '#/parameters/XRequestId'
      - name: x-auth-token
        in: header
        description: Session token for a user.
        required: false
        type: string
        x-example: 3be3d489-55c6-4643-90ac-a476dbc98812
      responses:
        '200':
          description: OK
  /api/2.42/arrays/space:
    get:
      tags:
      - Arrays
      summary: List array space information
      description: 'Displays real-time and historical array space information including
        unique

        volume and snapshot space, shared space, data reduction, provisioned

        capacity, usable capacity, and parity.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArraySpaceGetResponse'
  /api/2.42/protection-groups/volumes:
    get:
      tags:
      - Protection Groups
      summary: List protection groups with volume members
      description: 'Displays a list of protection groups that have volume members.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Pgroup_group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_destroyed'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Include_remote'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupsVolumesGetResponse'
    post:
      tags:
      - Protection Groups
      summary: Create a volume to add it to a protection group
      description: 'Creates a volume member and adds it to a protection group.

        Members that are already in the protection group are not affected.

        For asynchronous replication, only members of the same type can belong to
        a protection group.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` and `member_ids` parameters represent the names or IDs
        of the volume.

        The `group_names` parameter, and either the `member_names` or `member_ids`

        parameters are required and must be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupsVolumesResponse'
    delete:
      tags:
      - Protection Groups
      summary: Delete a volume from a protection group
      description: 'Deletes a volume member from a protection group.

        After the member has been deleted, it is no longer protected by the group.

        Any protection group snapshots that were taken before the member was deleted
        are not affected.

        Deleting a member from a protection group does not delete the member from
        the array,

        and the member can be added back to the protection group at any time.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` and `member_ids` parameters represent the names or IDs
        of the volume.

        The `group_names` parameter, and either the `member_names` or `member_ids`

        parameters are required and must be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_ids'
      responses:
        '200':
          description: OK
  /api/2.42/support/test:
    get:
      tags:
      - Support
      summary: List Pure Storage Support connection data
      description: Displays information about whether the array can connect to Pure
        Storage Support by establishing a secure shell or secure HTTP connection and
        verifies that messages can be exchanged.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Test_type'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultGetResponse'
  /api/2.42/certificate-groups/certificates:
    get:
      tags:
      - Certificate Groups
      summary: List  certificate-groups and certificates
      description: 'Lists membership associations between groups and certificates
        on the array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Certificate_group_ids'
      - $ref: '#/parameters/Certificate_group_names'
      - $ref: '#/parameters/Certificate_ids'
      - $ref: '#/parameters/Certificate_names'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CertificateGroupCertificateGetResponse'
    post:
      tags:
      - Certificate Groups
      summary: Create certificate-groups/certificates
      description: 'Creates one or more certificates to one or more certificate groups
        on the array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Certificate_group_ids'
      - $ref: '#/parameters/Certificate_group_names'
      - $ref: '#/parameters/Certificate_ids'
      - $ref: '#/parameters/Certificate_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CertificateGroupCertificateResponse'
    delete:
      tags:
      - Certificate Groups
      summary: Delete certificate-groups or certificates
      description: 'Deletes one or more certificate groups.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Certificate_group_ids'
      - $ref: '#/parameters/Certificate_group_names'
      - $ref: '#/parameters/Certificate_ids'
      - $ref: '#/parameters/Certificate_names'
      responses:
        '200':
          description: OK
  /api/2.42/directories/policies/autodir:
    get:
      tags:
      - Directories
      summary: List auto managed directory policies attached to a directory
      description: 'Displays a list of auto managed directory policies that are attached
        to directories.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberGetResponse'
    post:
      tags:
      - Directories
      summary: Create a membership between a directory with one or more auto managed
        directory policies
      description: 'Creates a membership between a directory with one or more auto
        managed directory policies.

        The `member_ids` or `member_names` parameter is required, but they cannot
        be set together.


        '
      parameters:
      - in: body
        name: policies
        required: true
        schema:
          $ref: '#/definitions/DirectoryPolicyPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberResponse'
    delete:
      tags:
      - Directories
      summary: Delete a membership between a directory and one or more auto managed
        directory policies
      description: 'Deletes a membership between a directory with one or more auto
        managed directory policies.

        The `policy_ids` or `policy_names` parameter is required, but they cannot
        be set together.

        The `member_ids` or `member_names` parameter is required, but they cannot
        be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/support/remote-assist-sessions/settings:
    get:
      tags:
      - Support
      summary: List remote assist session global settings
      description: 'Displays global settings for remote assist sessions.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SupportRemoteAssistSessionsSettingsGetResponse'
    patch:
      tags:
      - Support
      summary: Update remote assist session global settings
      description: 'Updates global settings for remote assist sessions.

        '
      parameters:
      - in: body
        name: support
        required: true
        schema:
          $ref: '#/definitions/SupportRemoteAssistSessionsSettings'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SupportRemoteAssistSessionsSettingsResponse'
  /api/2.42/alert-watchers:
    get:
      tags:
      - Alert Watchers
      summary: List alert watchers
      description: Displays alert watcher email addressess and indicates whether they
        are `enabled`.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AlertWatcherGetResponse'
    post:
      tags:
      - Alert Watchers
      summary: Create alert watcher
      description: Creates one or more alert watcher email addresses, adding them
        to the list of alert watchers.
      parameters:
      - in: body
        name: alert-watcher
        required: false
        schema:
          $ref: '#/definitions/AlertWatcherPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AlertWatcherResponse'
    delete:
      tags:
      - Alert Watchers
      summary: Delete alert watcher
      description: Delete alert watcher email address from the list of alert watchers.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Alert Watchers
      summary: Modify alert watcher
      description: Modify alert watcher email address by enabling or disabling it.
      parameters:
      - in: body
        name: alert-watcher
        required: true
        schema:
          $ref: '#/definitions/AlertWatcherPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AlertWatcherResponse'
  /api/2.42/arrays/cloud-provider-tags:
    get:
      tags:
      - Arrays
      summary: List user tags on the cloud.
      description: Displays the list of user tags on deployed cloud resources of a
        CBS array.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CloudProviderTagGetResponse'
    delete:
      tags:
      - Arrays
      summary: Delete user tags from the cloud.
      description: Deletes user tags from deployed cloud resources of a CBS array.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Keys'
      responses:
        '200':
          description: OK
  /api/2.42/snmp-managers:
    get:
      tags:
      - SNMP Managers
      summary: List SNMP managers
      description: Displays designated SNMP managers and their communication and security
        attributes.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SnmpManagerGetResponse'
    post:
      tags:
      - SNMP Managers
      summary: Create SNMP manager
      description: 'Creates a Purity SNMP manager object that identifies a host (SNMP
        manager) and specifies the

        protocol attributes for communicating with it. Once a manager object is created,
        the

        transmission of SNMP traps is immediately enabled.

        '
      parameters:
      - in: body
        name: snmp-manager
        required: true
        schema:
          $ref: '#/definitions/SnmpManagerPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: Returns the newly created snmp manager object.
          schema:
            $ref: '#/definitions/SnmpManagerResponse'
    delete:
      tags:
      - SNMP Managers
      summary: Delete SNMP manager
      description: 'Deletes the SNMP manager object and stops communication with specified
        managers.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - SNMP Managers
      summary: Modify SNMP manager
      description: Modifies the name or the protocol attributes of the specified SNMP
        manager.
      parameters:
      - in: body
        name: snmp-manager
        required: true
        schema:
          $ref: '#/definitions/SnmpManagerPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: Returns the newly updated snmp manager object.
          schema:
            $ref: '#/definitions/SnmpManagerResponse'
  /api/2.42/admins/cache:
    get:
      tags:
      - Administrators
      summary: List administrator cache entries
      description: 'Displays entries in the administrator cache.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AdminCacheGetResponse'
    put:
      tags:
      - Administrators
      summary: Update or refresh entries in the administrator cache
      description: 'Updates entries in the cache for administrators who currently
        do not have a cache entry,

        otherwise refreshes the existing entry.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AdminCacheResponse'
    delete:
      tags:
      - Administrators
      summary: Delete cache entries
      description: 'Deletes all entries from the administrator cache.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Remove_all_entries'
      responses:
        '200':
          description: OK
  /api/2.42/volume-groups/tags:
    get:
      tags:
      - Volume Groups
      summary: List tags
      description: Displays the list of tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Resource_destroyed'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagGetResponse'
    delete:
      tags:
      - Volume Groups
      summary: Delete tags
      description: Deletes specified tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Keys'
      - $ref: '#/parameters/Namespaces_delete'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
  /api/2.42/protection-group-snapshots/transfer:
    get:
      tags:
      - Protection Group Snapshots
      summary: List protection group snapshots with transfer statistics
      description: 'Displays a list of protection group snapshots and corresponding
        transfer statistics.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupSnapshotTransferGetResponse'
  /api/2.42/syslog-servers/test:
    get:
      tags:
      - Syslog
      summary: List syslog server test results
      description: Displays syslog server test results, which indicate whether the
        syslog is working and configured correctly.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceGetResponse'
  /api/2.42/host-groups/protection-groups:
    get:
      tags:
      - Host Groups
      summary: List host groups that are members of protection groups
      description: 'Displays a list of host group members that belong to one or more
        protection groups.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdMemberGetResponse'
    post:
      tags:
      - Host Groups
      summary: Create a host group
      description: 'Creates a host group member and assigns to a protection group.

        Members that are already in the protection group are not affected.

        For asynchronous replication, only members of the same type can belong to
        a protection group.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` parameter represents the name of the host group.

        The `group_names` and `member_names` parameters are required and must be set
        together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdMemberResponse'
    delete:
      tags:
      - Host Groups
      summary: Delete a host group from a protection group
      description: 'Deletes a host group member from a protection group.

        After the member has been removed, it is no longer protected by the group.

        Any protection group snapshots that were taken before the member was removed
        are not affected.

        Removing a member from a protection group does not delete the member from
        the array,

        and the member can be added back to the protection group at any time.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` parameter represents the name of the host group.

        The `group_names` and `member_names` parameters are required and must be set
        together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
  /api/2.42/policies/smb/members:
    get:
      tags:
      - Policies
      summary: List SMB policy members
      description: 'Displays a list of SMB policy members.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberExportGetResponse'
    post:
      tags:
      - Policies
      summary: Create SMB policies
      description: 'Creates a membership between one or more resources and an SMB
        policy.

        Applicable resources are directories.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - in: body
        name: members
        required: true
        schema:
          $ref: '#/definitions/PolicyMemberExportPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberExportResponse'
    delete:
      tags:
      - Policies
      summary: Delete SMB policies
      description: 'Deletes one or more SMB policies from resources.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.

        The `member_ids` or `member_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/certificate-groups/uses:
    get:
      tags:
      - Certificate Groups
      summary: List  certificate-groups/uses
      description: 'Lists how certificate groups are being used and by what.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CertificateGroupUseGetResponse'
  /api/2.42/directory-exports:
    get:
      tags:
      - Directory Exports
      summary: List directory exports
      description: 'Displays a list of directory exports.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Directory_ids'
      - $ref: '#/parameters/Directory_names'
      - $ref: '#/parameters/Export_names'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryExportGetResponse'
    post:
      tags:
      - Directory Exports
      summary: Create directory exports
      description: 'Creates an export of a managed directory.

        The `directory_ids` or `directory_names` parameter is required, but cannot
        be set together.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - in: body
        name: exports
        required: true
        schema:
          $ref: '#/definitions/DirectoryExportPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Directory_ids'
      - $ref: '#/parameters/Directory_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryExportResponse'
    delete:
      tags:
      - Directory Exports
      summary: Delete directory exports
      description: 'Deletes one or more directory exports.

        If any of the `export_names` is not unique across the system,

        `policy_ids` or `policy_names` must be specified to determine the exports.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Directory_ids'
      - $ref: '#/parameters/Directory_names'
      - $ref: '#/parameters/Export_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Directory Exports
      summary: Modify directory exports
      description: 'Modifies a directory export.

        Used for manually renaming, enabling, and disabling directory exports.

        To rename a directory export, set ''export_name'' to the new name.


        '
      parameters:
      - in: body
        name: export
        required: true
        schema:
          $ref: '#/definitions/DirectoryExportPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Directory_ids'
      - $ref: '#/parameters/Directory_names'
      - $ref: '#/parameters/Export_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryExportResponse'
  /api/2.42/software-bundle:
    get:
      tags:
      - Software
      summary: List software-bundle
      description: 'Displays a list of software bundles.

        '
      parameters:
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SoftwareBundleGetResponse'
    post:
      tags:
      - Software
      summary: Create software-bundle
      description: 'Creates and initiates a software bundle download.

        '
      parameters:
      - in: body
        name: source
        required: true
        schema:
          $ref: '#/definitions/SoftwareBundlePost'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: 'The software bundle has started downloading successfully.

            '
          schema:
            $ref: '#/definitions/SoftwareBundleResponse'
  /api/2.42/realms/performance:
    get:
      tags:
      - Realms
      summary: List realm performance data
      description: 'Displays real-time and historical performance data, real-time
        latency data,

        and average I/O sizes across all realms,

        displayed both by realm and as a total across all realms.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ContainerPerformanceGetResponse'
  /api/2.42/volumes/diff:
    get:
      tags:
      - Volumes
      summary: List volume diffs
      description: Displays block differences for the specified volumes.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Base_name'
      - $ref: '#/parameters/Base_id'
      - $ref: '#/parameters/Segment_offset'
      - $ref: '#/parameters/Segment_length'
      - $ref: '#/parameters/Block_size'
      - $ref: '#/parameters/Next_allocated_block_hint'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumesDiffGetResponse'
  /api/2.42/pod-replica-links/mappings/policies:
    get:
      tags:
      - Pod Replica Links
      summary: List policy mappings
      description: 'Displays a list of policy mappings.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Local_pod_ids'
      - $ref: '#/parameters/Local_pod_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Pod_replica_link_ids'
      - $ref: '#/parameters/Remote_ids'
      - $ref: '#/parameters/Remote_names'
      - $ref: '#/parameters/Remote_pod_ids'
      - $ref: '#/parameters/Remote_pod_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodReplicaLinkMappingPolicyGetResponse'
    patch:
      tags:
      - Pod Replica Links
      summary: Modify policy mappings
      description: 'Modifies policy mappings of a replica link.

        Valid `mapping` values are `connected` and `disconnected`. `connected` indicates
        that the

        source policy and its attachments will be mirrored on the target pod.

        `disconnected` indicates that the associated policy and its attachments are
        independent

        from any policy on the remote. This operation can only be performed on

        the target side of a pod replica link.

        '
      parameters:
      - in: body
        name: mapping
        required: true
        schema:
          $ref: '#/definitions/MappingPolicyPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Local_pod_ids'
      - $ref: '#/parameters/Local_pod_names'
      - $ref: '#/parameters/Pod_replica_link_ids'
      - $ref: '#/parameters/Remote_ids'
      - $ref: '#/parameters/Remote_names'
      - $ref: '#/parameters/Remote_pod_ids'
      - $ref: '#/parameters/Remote_pod_names'
      - $ref: '#/parameters/Remote_policy_ids'
      - $ref: '#/parameters/Remote_policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodReplicaLinkMappingPolicyResponse'
  /api/2.42/volumes/protection-groups:
    get:
      tags:
      - Volumes
      summary: List volumes that are members of protection groups
      description: 'Displays a list of volume members that belong to one or more protection
        groups.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_destroyed'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupsVolumesGetResponse'
    post:
      tags:
      - Volumes
      summary: Create a volume and add it to a protection group
      description: 'Creates a volume member and adds it to a protection group.

        Members that are already in the protection group are not affected.

        For asynchronous replication, only members of the same type can belong to
        a protection group.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` and `member_ids` parameters represent the names or IDs
        of the volume.

        The `group_names` and only one of `member_names` or `member_ids`

        parameters are required and must be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupsVolumesResponse'
    delete:
      tags:
      - Volumes
      summary: Delete a volume from a protection group
      description: 'Deletes a volume member from a protection group.

        After the member has been deleted, it is no longer protected by the group.

        Any protection group snapshots that were taken before the member was deleted

        are not affected.

        Deleting a member from a protection group does not delete the member from
        the array,

        and the member can be added back to the protection group at any time.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` and `member_ids` parameters represent the names or IDs
        of the volume.

        The `group_names` and only one of `member_names` or `member_ids`

        parameters are required and must be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_ids'
      responses:
        '200':
          description: OK
  /api/2.42/policies/management-access/members:
    get:
      tags:
      - Policies
      summary: List management access policy members
      description: Displays a list of management access policy members.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_idsUnrestricted'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_idsUnrestricted'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberCleanGetResponse'
    post:
      tags:
      - Policies
      summary: Create a membership between an object and a management access policy
      description: Creates a membership between one or more objects and a management
        access policy.
      parameters:
      - in: body
        name: members
        required: true
        schema:
          $ref: '#/definitions/PolicyMemberPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_idsUnrestricted'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberCleanResponse'
    delete:
      tags:
      - Policies
      summary: Delete membership between management access policies and attached object
      description: 'Deletes a membership between one or more management access policies
        and attached object.

        One of `policy_ids` or `policy_names` is required, as well as one of `member_ids`

        or `member_names`. `member_types` is required.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_idsUnrestricted'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/files:
    post:
      tags:
      - Files
      summary: Create a file copy
      description: 'Creates a file copy from one path to another path.

        The `directory_ids`, `directory_names` or `paths` value must be specified.

        If the `directory_ids` or `directory_names` value is not specified, the file
        is copied to the

        source directory specified in the body params.

        The `paths` value refers to the path of the target file relative to the target
        directory.

        If `paths` value is not specified, the file will be copied to the relative
        path specified

        in `source_path` under the target directory.

        The `source_path` value refers to the path of the source file relative to
        the source directory.

        To overwrite an existing file, set the `overwrite` flag to `true`.

        '
      parameters:
      - in: body
        name: source-file
        required: true
        schema:
          $ref: '#/definitions/FilePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Directory_ids'
      - $ref: '#/parameters/Directory_names'
      - $ref: '#/parameters/Paths'
      - $ref: '#/parameters/Overwrite'
      responses:
        '200':
          description: OK
  /api/2.42/realms/tags:
    get:
      tags:
      - Realms
      summary: List tags
      description: 'Displays the list of tags.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Resource_destroyed'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagGetResponse'
    delete:
      tags:
      - Realms
      summary: Delete tags
      description: 'Deletes specified tags.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Keys'
      - $ref: '#/parameters/Namespaces_delete'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
  /api/2.42/protection-groups/space:
    get:
      tags:
      - Protection Groups
      summary: List protection group space information
      description: 'Displays provisioned size and physical storage consumption data
        for each protection group.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourceSpaceGetResponse'
  /api/2.42/directory-services/local/users/members:
    get:
      tags:
      - Directory Services
      summary: List local user memberships
      description: Displays a list of local user memberships.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Local_group_names'
      - $ref: '#/parameters/Group_sids'
      - $ref: '#/parameters/Group_gids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_sids'
      - $ref: '#/parameters/Local_member_ids'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LocalMembershipGetResponse'
    post:
      tags:
      - Directory Services
      summary: Create local user membership
      description: 'Creates a local user membership with a group.

        The `member_names` or `member_sids` or `member_ids` parameter is required,

        but cannot be set together.

        '
      parameters:
      - in: body
        name: local-membership
        description: 'The `group_names`, `group_sids`, or `group_gids` parameter is
          required, but cannot be set

          together.

          '
        required: true
        schema:
          $ref: '#/definitions/LocalUserMembershipPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_sids'
      - $ref: '#/parameters/Local_member_ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LocalMembershipResponse'
    delete:
      tags:
      - Directory Services
      summary: Delete local user membership
      description: 'Deletes one or more local user memberships.

        The `member_names`, `member_sids`, or `member_ids` parameter is required,
        but cannot be set

        together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Local_group_names'
      - $ref: '#/parameters/Group_sids'
      - $ref: '#/parameters/Group_gids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_sids'
      - $ref: '#/parameters/Local_member_ids'
      - $ref: '#/parameters/Member_types'
      responses:
        '200':
          description: OK
  /api/2.42/log-targets:
    get:
      tags:
      - Log Targets
      summary: List log targets
      description: Displays a list of configured log targets.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LogTargetsGetResponse'
  /api/2.42/protection-groups/performance/replication:
    get:
      tags:
      - Protection Groups
      summary: List protection group replication performance data
      description: 'Displays the total number of bytes of replication data transmitted
        and received per second.

        The data is grouped by protection group.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupPerformanceResponse'
  /api/2.42/volumes/space:
    get:
      tags:
      - Volumes
      summary: List volume space information
      description: 'Displays the provisioned size and physical storage consumption
        data for each volume.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumeSpaceGetResponse'
    parameters:
    - $ref: '#/parameters/Names'
  /api/2.42/api-clients:
    get:
      tags:
      - API Clients
      summary: List API clients
      description: 'Displays a list of API clients.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ApiClientGetResponse'
    post:
      tags:
      - API Clients
      summary: Create an API client
      description: 'Creates an API client. Newly created API clients are disabled
        by default.

        Enable an API client through the `PATCH` method.

        The `names`, `issuer`, and `public_key` parameters are required.

        The `access_policies` or `max_role` parameter is required, but they cannot
        be set together.

        '
      parameters:
      - in: body
        name: API clients
        required: true
        schema:
          $ref: '#/definitions/ApiClientPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ApiClientResponse'
    delete:
      tags:
      - API Clients
      summary: Delete an API client
      description: 'Deletes an API client.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - API Clients
      summary: Manage an API client
      description: 'Enables or disables an API client.

        The `ids` or `names` parameter is required, but they cannot be set together.

        '
      parameters:
      - in: body
        name: API clients
        required: true
        schema:
          $ref: '#/definitions/ApiClientPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ApiClientResponse'
  /api/2.42/software-installation-steps:
    get:
      tags:
      - Software
      summary: List software upgrade steps
      description: Displays a list of currently running and completed software upgrade
        steps.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Software_installation_ids'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SoftwareInstallationStepsGetResponse'
  /api/2.42/hosts/tags:
    get:
      tags:
      - Hosts
      summary: List tags
      description: 'Displays the list of tags on host objects.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      - $ref: '#/parameters/Resource_destroyed'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagGetResponse'
    delete:
      tags:
      - Hosts
      summary: Delete tags
      description: 'Deletes specified tags on host objects.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Keys'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
  /api/2.42/protection-groups/targets:
    get:
      tags:
      - Protection Groups
      summary: List protection groups with targets
      description: 'Displays a list of protection groups that have target arrays,
        pods, or offload targets.



        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupTargetGetResponse'
    post:
      tags:
      - Protection Groups
      summary: Create an action to add a target to a protection group
      description: 'Creates an action to add an array, pod, or offload target to a
        protection group.

        The `group_names` parameter represents the name of the protection group.

        The `member_names` parameter represents the name of the array, pod, or offload
        target

        that is being added to the protection group.

        The `group_names` and `member_names` parameters are required and must be set
        together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupTargetResponse'
    delete:
      tags:
      - Protection Groups
      summary: Delete a target from a protection group
      description: 'Deletes an array, pod, or offload target from a protection group.

        The `group_names` parameter represents the name of the protection group.

        The `member_names` parameter represents the name of the array, pod, or offload
        target

        that is being removed from the protection group.

        The `group_names` and `member_names` parameters are required and must be set
        together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Protection Groups
      summary: Modify a protection group target
      description: 'Modifies the source array to replicate protection group data to
        the target,

        or disallows the source array from replicating protection group data to the
        target.

        The `allowed` parameter must be set from the target array.

        The `group_names` parameter represents the name of the protection group.

        The `allowed` and `group_names` parameters are required and must be set together.

        Offload targets do not support the `allowed` parameter.


        '
      parameters:
      - in: body
        name: target
        required: true
        schema:
          $ref: '#/definitions/TargetProtectionGroupPostPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupTargetResponse'
  /api/2.42/protection-groups/host-groups:
    get:
      tags:
      - Protection Groups
      summary: List protection groups with host group members
      description: Displays a list of protection groups that have host group members.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdMemberGetResponse'
    post:
      tags:
      - Protection Groups
      summary: Creates an action to add a host group to a protection group
      description: 'Creates an action to add a host group member to a protection group.

        Members that are already in the protection group are not affected.

        For asynchronous replication, only members of the same type can belong to
        a protection group.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` parameter represents the name of the host group.

        The `group_names` and `member_names` parameters are required and must be set
        together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdMemberResponse'
    delete:
      tags:
      - Protection Groups
      summary: Delete a host group from a protection group
      description: 'Deletes a host group member from a protection group.

        After the member has been removed, it is no longer protected by the group.

        Protection group snapshots taken before the member was removed will not be
        affected.

        Removing a member from a protection group does not delete the member from
        the array,

        and the member can be added back to the protection group at any time.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` parameter represents the name of the host group.

        The `group_names` and `member_names` parameters are required and must be set
        together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
  /api/2.42/directory-services/roles:
    get:
      tags:
      - Directory Services
      summary: List group to management access policy mappings
      description: 'Displays access control settings for remote groups.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_mapping_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Role_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryServiceRoleGetResponse'
    post:
      tags:
      - Directory Services
      summary: Create a group in management access policy mappings
      description: 'Creates one or more groups in management access policy mappings.

        '
      parameters:
      - in: body
        name: directory-service-roles
        required: true
        schema:
          $ref: '#/definitions/DirectoryServiceRolePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_mapping_names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryServiceRoleResponse'
    delete:
      tags:
      - Directory Services
      summary: Delete group to management access policy mappings
      description: 'Deletes one or more group to management access policy mappings.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_mapping_names_required'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Directory Services
      summary: Modify group to management access policy mappings
      description: 'Modifies properties of one or more group to management access
        policy mappings.

        Modifying management access policies is done at

        `/directory-services/roles/policies/management-access` instead.

        '
      parameters:
      - in: body
        name: directory-service-roles
        required: true
        schema:
          $ref: '#/definitions/DirectoryServiceRole'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Role_names'
      - $ref: '#/parameters/Group_mapping_names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryServiceRoleResponse'
  /api/2.42/file-systems:
    get:
      tags:
      - File Systems
      summary: List file systems
      description: Displays a list of file systems, including those pending eradication.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/FileSystemGetResponse'
    post:
      tags:
      - File Systems
      summary: Create file system
      description: 'Creates one or more file systems.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/FileSystemResponse'
    delete:
      tags:
      - File Systems
      summary: Delete file system
      description: 'Deletes a file system that has been destroyed and is pending eradication.

        Eradicated file systems cannot be recovered. File systems are destroyed using
        the PATCH method.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - File Systems
      summary: Modify a file system
      description: 'Modifies a file system. You can rename, destroy, move, or recover
        a file system.

        To rename a file system, set `name` to the new name.

        To destroy a file system, set `destroyed=true`.

        To move a file system, set ''pod'' to the destination pod reference.

        To recover a file system that has been destroyed and is pending eradication,

        set `destroyed=false`.

        '
      parameters:
      - in: body
        name: file-system
        required: true
        schema:
          $ref: '#/definitions/FileSystemPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/FileSystemResponse'
  /api/2.42/protection-groups/performance/replication/by-array:
    get:
      tags:
      - Protection Groups
      summary: List protection group replication performance data with array details
      description: 'Displays the total number of bytes of replication data transmitted
        and received per second.

        The data is grouped by protection group and includes the names of the source
        array and targets

        for each protection group.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupPerformanceArrayResponse'
  /api/2.42/subscriptions:
    get:
      tags:
      - Subscriptions
      summary: List subscriptions
      description: 'Displays information about subscriptions.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SubscriptionGetResponse'
  /api/2.42/network-interfaces/neighbors:
    get:
      tags:
      - Network Interfaces
      summary: List network interface neighbors
      description: Displays all neighbors for all network interfaces on the array.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Local_port_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/NetworkInterfaceNeighborGetResponse'
  /api/2.42/connections:
    get:
      tags:
      - Connections
      summary: List volume connections
      description: 'Displays a list of connections between a volume and its hosts
        and host groups, as well as

        the logical unit numbers (LUNs) or NVMe Namespace IDs (NSIDs) used by the
        associated hosts

        to address these volumes.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Host_group_names'
      - $ref: '#/parameters/Host_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Protocol_endpoint_ids'
      - $ref: '#/parameters/Protocol_endpoint_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Volume_names'
      - $ref: '#/parameters/Volume_ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ConnectionGetResponse'
    post:
      tags:
      - Connections
      summary: Create a connection between a volume and host or host group
      description: 'Creates a connection between a volume and a host or host group.

        One of `volume_names` or `volume_ids` and one of `host_names`

        or `host_group_names` query parameters are required.


        '
      parameters:
      - in: body
        name: connection
        required: false
        schema:
          $ref: '#/definitions/ConnectionPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Host_group_names'
      - $ref: '#/parameters/Host_names'
      - $ref: '#/parameters/Volume_names'
      - $ref: '#/parameters/Volume_ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ConnectionResponse'
    delete:
      tags:
      - Connections
      summary: Delete a connection between a volume and its host or host group
      description: 'Deletes the connection between a volume and its associated host
        or host group.

        One of `volume_names` or `volume_ids` and one of `host_names`

        or `host_group_names` query parameters are required.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Host_group_names'
      - $ref: '#/parameters/Host_names'
      - $ref: '#/parameters/Volume_names'
      - $ref: '#/parameters/Volume_ids'
      responses:
        '200':
          description: OK
  /api/2.42/sso/saml2/idps:
    get:
      tags:
      - SAML2 SSO
      summary: List SAML2 SSO configurations
      description: 'Displays the SAML2 SSO service provider and identity provider
        configuration

        settings in the array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/Saml2SsoGetResponse'
    post:
      tags:
      - SAML2 SSO
      summary: Create SAML2 SSO configurations
      description: 'Creates SAML2 SSO configurations.

        '
      parameters:
      - in: body
        name: idp
        required: true
        schema:
          $ref: '#/definitions/Saml2SsoPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/Saml2SsoResponse'
    delete:
      tags:
      - SAML2 SSO
      summary: Delete SAML2 SSO configurations
      description: 'Deletes SAML2 SSO configurations.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - SAML2 SSO
      summary: Modify SAML2 SSO configurations
      description: 'Modifies one or more attributes of SAML2 SSO configurations.

        '
      parameters:
      - in: body
        name: idp
        required: true
        schema:
          $ref: '#/definitions/Saml2SsoPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/Saml2SsoResponse'
  /api/2.42/volume-snapshots/transfer:
    get:
      tags:
      - Volume Snapshots
      summary: List volume snapshots with transfer statistics
      description: 'Displays a list of volume snapshots and their transfer statistics.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumeSnapshotTransferGetResponse'
    parameters:
    - $ref: '#/parameters/Names'
  /api/2.42/volumes/performance/by-array:
    get:
      tags:
      - Volumes
      summary: List volume performance data by array
      description: 'Displays real-time and historical performance data, real-time
        latency data,

        and average I/O size data.

        The data returned is for each volume on the current array and for each volume

        on any remote arrays that are visible to the current array.

        The data is grouped by individual volumes

        and as a total across all volumes on each array.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourcePerformanceByArrayGetResponse'
  /api/2.42/offloads:
    get:
      tags:
      - Offloads
      summary: List offload targets
      description: 'Displays a list of offload targets that are connected to the array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Protocol'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/OffloadGetResponse'
    post:
      tags:
      - Offloads
      summary: Create offload target
      description: 'Creates an offload target, connecting it to an array.

        Before you can connect to, manage, and replicate to an offload target, the
        Purity Run app must

        be installed.

        '
      parameters:
      - in: body
        name: offload
        required: true
        schema:
          $ref: '#/definitions/OffloadPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Initialize'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/OffloadResponse'
    delete:
      tags:
      - Offloads
      summary: Delete offload target
      description: 'Deletes an offload target.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
  /api/2.42/pod-replica-links/performance/replication:
    get:
      tags:
      - Pod Replica Links
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Local_pod_ids'
      - $ref: '#/parameters/Local_pod_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Remote_ids'
      - $ref: '#/parameters/Remote_names'
      - $ref: '#/parameters/Remote_pod_ids'
      - $ref: '#/parameters/Remote_pod_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Historical_total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodReplicaLinkPerformanceReplicationGetResponse'
  /api/2.42/protection-group-snapshots/replica:
    post:
      tags:
      - Protection Group Snapshots
      summary: Create an action to send protection group snapshots
      description: 'Creates an action to send protection group snapshots to protection
        pgroup targets.

        When the `on` option is used, only specified targets will recieve the

        protection group snapshots.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Convert_source_to_baseline'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/On_as_target'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupSnapshotReplicaResponse'
  /api/2.42/volume-groups/space:
    get:
      tags:
      - Volume Groups
      summary: List volume group space information
      description: 'Displays the provisioned size and physical storage consumption
        data

        for each volume group.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourceSpaceGetResponse'
    parameters:
    - $ref: '#/parameters/Names'
  /api/2.42/host-groups/performance/by-array:
    get:
      tags:
      - Host Groups
      summary: List host group performance data by array
      description: 'Displays real-time and historical performance data, real-time
        latency data, and average I/O size

        data. The data is displayed by total size across all host groups on each array
        and by individual

        host group on each array. The displayed data represents the volumes that are
        connected to a host

        group on the current array and the volumes that are connected to a host group
        on any remote

        arrays that are visible to the current array.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourcePerformanceNoIdByArrayGetResponse'
  /api/2.42/arrays/cloud-provider-tags/batch:
    put:
      tags:
      - Arrays
      summary: Update user tags on the cloud.
      description: Updates users tags to deployed cloud resources of a CBS array.
      parameters:
      - in: body
        name: tag
        description: A list of tags to be created or, if already existing, updated.
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/CloudProviderTag'
          uniqueItems: true
          minItems: 1
          maxItems: 50
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CloudProviderTagResponse'
  /api/2.42/smi-s:
    get:
      tags:
      - SMI-S
      summary: List SMI-S settings
      description: 'Displays the SMI-S settings, including whether SLP and WBEM-HTTPS
        are enabled.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SmisGetResponse'
    patch:
      tags:
      - SMI-S
      summary: Modify SLP and SMI-S
      description: 'Modifies the Service Location Protocol (SLP) and the SMI-S provider,
        enabling or

        disabling them.

        '
      parameters:
      - in: body
        name: smi-s
        required: true
        schema:
          $ref: '#/definitions/Smis'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SmisResponse'
  /api/2.42/controllers:
    get:
      tags:
      - Controllers
      summary: List controller information and status
      description: Displays the name, mode, FlashArray model, Purity//FA software
        version, and status of each controller in the array.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ControllerGetResponse'
  /api/2.42/array-connections:
    get:
      tags:
      - Array Connections
      summary: List the connected arrays
      description: 'Displays a list of connected arrays.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Remote_ids'
      - $ref: '#/parameters/Remote_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayConnectionGetResponse'
    post:
      tags:
      - Array Connections
      summary: Create an array connection
      description: 'Creates a connection between this array and the specified array.

        '
      parameters:
      - in: body
        name: array-connection
        required: true
        schema:
          $ref: '#/definitions/ArrayConnectionPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayConnectionResponse'
    delete:
      tags:
      - Array Connections
      summary: Delete an array connection
      description: 'Deletes the connection between this array and the specified array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Array Connections
      summary: Modify an array connection
      description: 'Modifies attributes for an array connection.

        '
      parameters:
      - in: body
        name: array-connection
        required: false
        schema:
          $ref: '#/definitions/ArrayConnectionPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Refresh'
      - $ref: '#/parameters/Renew_encryption_key'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayConnectionResponse'
  /api/2.42/pods/performance:
    get:
      tags:
      - Pods
      summary: List pod performance data
      description: 'Displays real-time and historical performance data, real-time
        latency data,

        and average I/O sizes across all pods, displayed both by pod and as a total
        across all pods.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Perf_protocol_type'
      - $ref: '#/parameters/Perf_protocol_group'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ContainerPerformanceGetResponse'
  /api/2.42/pods/performance/replication:
    get:
      tags:
      - Pods
      summary: List pod replication performance data
      description: 'Displays pod replication performance data.

        The data returned is the real-time and historical performance data for each
        replication type

        at the pod level. Values include `continuous`, `periodic`, `resync`, and `sync`.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Historical_total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodPerformanceReplicationGetResponse'
  /api/2.42/apps/nodes:
    get:
      tags:
      - Apps
      summary: List app nodes
      description: Displays a list of installed apps and their nodes.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/App_names'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AppNodeGetResponse'
  /api/2.42/directory-services/local/users:
    get:
      tags:
      - Directory Services
      summary: List local users
      description: Displays a list of local users.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Uids'
      - $ref: '#/parameters/Sids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LocalUserGetResponse'
    post:
      tags:
      - Directory Services
      summary: Create local user
      description: Creates a local user.
      parameters:
      - in: body
        name: local-user
        required: false
        schema:
          $ref: '#/definitions/LocalUserPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LocalUserResponse'
    delete:
      tags:
      - Directory Services
      summary: Delete local users
      description: 'Deletes one or more local users.

        The `uids`, `names`, or `sids` parameter is required, but cannot be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Sids'
      - $ref: '#/parameters/Uids'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Directory Services
      summary: Modify local user
      description: Modifies a local user.
      parameters:
      - in: body
        name: local-user
        required: true
        schema:
          $ref: '#/definitions/LocalUserPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/KeepSessionsOpen'
      - $ref: '#/parameters/Sids'
      - $ref: '#/parameters/Uids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LocalUserResponse'
  /api/2.42/policies/alert-watcher/members:
    get:
      tags:
      - Policies
      summary: List alert-watcher policy members
      description: Displays a list of alert-watcher policy members.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberGetResponse'
    post:
      tags:
      - Policies
      summary: Create a membership between an object and an alert-watcher policy
      description: Creates a membership between one or more objects and an alert-watcher
        policy. The `policy_ids` or `policy_names` parameter is required, but cannot
        be set together.
      parameters:
      - in: body
        name: members
        required: true
        schema:
          $ref: '#/definitions/PolicyMemberPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberResponse'
    delete:
      tags:
      - Policies
      summary: Delete membership between alert-watcher policies and attached object
      description: Deletes a membership between one or more alert-watcher policies
        and attached object. The `policy_ids` or `policy_names` parameter is required,
        but cannot be set together. The `member_ids` or `member_names` parameter is
        required, but cannot be set together.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/alerts/rules:
    get:
      tags:
      - Alerts
      summary: List custom alert rules
      description: 'Displays a list of custom alert rules.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Alert_rules_code_list'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AlertRulesGetResponse'
    post:
      tags:
      - Alerts
      summary: Create a custom alert rule
      description: 'Creates an alert rule with a custom value.

        '
      parameters:
      - in: body
        name: alert-rule
        required: true
        schema:
          $ref: '#/definitions/AlertRules'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Alert_rules_code'
      - $ref: '#/parameters/Alert_rules_parameter'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AlertRulesResponse'
    delete:
      tags:
      - Alerts
      summary: Delete a custom alert rule
      description: 'Deletes a custom alert rule.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Alert_rules_code'
      - $ref: '#/parameters/Alert_rules_parameter'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Alerts
      summary: Modify a custom alert rule
      description: 'Modifies a custom alert rule to a new value.

        '
      parameters:
      - in: body
        name: alert-rule
        required: true
        schema:
          $ref: '#/definitions/AlertRules'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Alert_rules_code'
      - $ref: '#/parameters/Alert_rules_parameter'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AlertRulesResponse'
  /api/2.42/pods/test:
    post:
      tags:
      - Pods
      summary: Create an attempt to clone a pod
      description: 'Creates an attempt to clone a pod on the local array without actually
        cloning it

        to test whether the pod can be successfully cloned. This action does not suppport
        pod creation.

        '
      parameters:
      - in: body
        name: pod
        required: true
        schema:
          $ref: '#/definitions/PodPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_throttle_operation'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceWithIdPostResponse'
  /api/2.42/workloads/tags:
    get:
      tags:
      - Workloads
      summary: List tags
      description: Displays the list of tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagGetResponse'
    delete:
      tags:
      - Workloads
      summary: Delete tags
      description: Deletes specified tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Keys'
      - $ref: '#/parameters/Namespaces_delete'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
  /api/2.42/vchosts/endpoints:
    get:
      tags:
      - Vchosts
      summary: List vchost endpoints
      description: 'Displays a list of vchost endpoints with their associated vchosts
        and certificates.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Vchost_names'
      - $ref: '#/parameters/Vchost_ids'
      - $ref: '#/parameters/Endpoints'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VchostEndpointGetResponse'
    post:
      tags:
      - Vchosts
      summary: Create a vchost endpoint
      description: 'Creates a vchost endpoint.

        Either the `vchost_names` or `vchost_ids` query parameter is required.


        '
      parameters:
      - in: body
        name: endpoint
        required: true
        schema:
          $ref: '#/definitions/VchostEndpointPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Vchost_names'
      - $ref: '#/parameters/Vchost_ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VchostEndpointResponse'
    delete:
      tags:
      - Vchosts
      summary: Delete a vchost endpoint
      description: 'Deletes an existing vchost endpoint

        The `ids` query parameter alone, or the `endpoints` query parameter together
        with

        one of `vchost_names` or `vchost_ids`, is required.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Vchost_names'
      - $ref: '#/parameters/Vchost_ids'
      - $ref: '#/parameters/Endpoints'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Vchosts
      summary: Modify a vchost endpoint
      description: 'Modifies an existing vchost endpoint.

        The `ids` query parameter alone, or the `endpoints` query parameter together
        with

        one of `vchost_names` or `vchost_ids`, is required.


        '
      parameters:
      - in: body
        name: endpoint
        required: true
        schema:
          $ref: '#/definitions/VchostEndpointPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Vchost_names'
      - $ref: '#/parameters/Vchost_ids'
      - $ref: '#/parameters/Endpoints'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VchostEndpointResponse'
  /api/2.42/virtual-machines:
    get:
      tags:
      - Virtual Machines
      summary: List Virtual Machines
      description: 'Displays a list of virtual machines, including those pending eradication.

        If `vm_type` is `vvol`, the `recovery_context` in the response will represent

        the most recent snapshot of the config vVol. This must be used as the `source`
        in

        the `PATCH` method when recovering a virtual machine from the destroyed state.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - name: vm_type
        in: query
        description: 'The type of virtual machine.

          The only valid value is `vvol`.

          '
        required: false
        type: string
      - name: vm_ids
        in: query
        description: 'Performs the operation on the unique virtual machine IDs specified.

          Enter multiple virtual machine IDs in a comma-separated format.

          '
        required: false
        type: array
        items:
          type: string
        collectionFormat: csv
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VirtualMachineGetResponse'
    post:
      tags:
      - Virtual Machines
      summary: Create a virtual machine
      description: 'Creates one or more virtual machines from a protection group snapshot.

        If the virtual machine is of type `vvol`, the `source` property of the request
        body must

        be a `recovery_context` value retrieved from the `GET /virtual-machine-snapshots`
        endpoint.

        If `overwrite` is specified, an existing virtual machine will have its volumes
        overwritten by

        the snapshot. Otherwise, a new virtual machine will be created from the snapshot.

        If creating the new virtual machine will cause a conflict with an existing
        virtual machine, the

        operation will fail.


        '
      parameters:
      - in: body
        name: virtual-machine
        required: true
        schema:
          $ref: '#/definitions/VirtualMachinePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Overwrite'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VirtualMachineResponse'
    patch:
      tags:
      - Virtual Machines
      summary: Update a virtual machine
      description: 'Updates a virtual machine, recovering it from the destroyed state.

        If the virtual machine is of type `vvol`, the `source` property of the request
        body must

        be a `recovery_context` value retrieved from the `GET /virtual-machines` endpoint.

        If recovering the virtual machine will cause a conflict with an existing virtual
        machine, the

        operation will fail.


        '
      parameters:
      - in: body
        name: virtual-machine
        required: true
        schema:
          $ref: '#/definitions/VirtualMachinePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VirtualMachineResponse'
  /api/2.42/snmp-managers/test:
    get:
      tags:
      - SNMP Managers
      summary: List SNMP manager test results
      description: Displays SNMP manager test results (traps or informs).
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceGetResponse'
  /api/2.42/alerts/events:
    get:
      tags:
      - Alerts
      summary: List alert events
      description: Displays a list of alert events.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Flagged'
      - $ref: '#/parameters/IdsSemiRequired'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/NamesSemiRequired'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AlertEventGetResponse'
  /api/2.42/hosts/tags/batch:
    put:
      tags:
      - Hosts
      summary: Update tags
      description: 'Updates tags.

        '
      parameters:
      - in: body
        name: tag
        description: A list of tags to be created or, if one already exists, updated.
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/NonCopyableTag'
          uniqueItems: true
          minItems: 1
          maxItems: 30
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagResponse'
  /api/2.42/active-directory:
    get:
      tags:
      - Active Directory
      summary: List Active Directory accounts
      description: 'Displays configured Active Directory accounts.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ActiveDirectoryGetResponse'
    post:
      tags:
      - Active Directory
      summary: Create Active Directory account
      description: 'Creates one or more Active Directory accounts.

        The `user` and `password` provided are used

        to join the array to the specified `domain`.

        '
      parameters:
      - in: body
        name: active-directory
        required: true
        schema:
          $ref: '#/definitions/ActiveDirectoryPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      - $ref: '#/parameters/Join_existing_acct_ad'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ActiveDirectoryResponse'
    delete:
      tags:
      - Active Directory
      summary: Delete Active Directory account
      description: 'Deletes one or more specified Active Directory accounts.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Local_only_ad'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Active Directory
      summary: Modify Active Directory account
      description: 'Modifies specified Active Directory account.

        '
      parameters:
      - in: body
        name: active-directory
        required: true
        schema:
          $ref: '#/definitions/ActiveDirectoryPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ActiveDirectoryResponse'
  /api/2.42/directories/space:
    get:
      tags:
      - Directories
      summary: List directory space information
      description: 'Displays physical storage consumption data for each directory.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourceDirectorySpaceGetResponse'
  /api/2.42/host-groups/tags/batch:
    put:
      tags:
      - Host Groups
      summary: Update tags
      description: Updates tags.
      parameters:
      - in: body
        name: tag
        description: A list of tags to be created or, if one already exists, updated.
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/NonCopyableTag'
          uniqueItems: true
          minItems: 1
          maxItems: 30
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagResponse'
  /api/2.42/volume-snapshots:
    get:
      tags:
      - Volume Snapshots
      summary: List volume snapshots
      description: 'Displays a list of volume snapshots, including those pending eradication.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumeSnapshotGetResponse'
    post:
      tags:
      - Volume Snapshots
      summary: Create a volume snapshot
      description: 'Creates a point-in-time snapshot of the contents of a volume.

        The `source_ids` or `source_names` parameter is required, but they cannot
        be set together.


        '
      parameters:
      - in: body
        name: volume-snapshot
        required: true
        schema:
          $ref: '#/definitions/VolumeSnapshotPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/OnSingle'
      - $ref: '#/parameters/Allow_throttle_snapshot'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumeSnapshotResponse'
    delete:
      tags:
      - Volume Snapshots
      summary: Delete a volume snapshot
      description: 'Deletes a volume snapshot that has been destroyed and is pending
        eradication.

        Eradicated volume snapshots cannot be recovered.

        Volume snapshots are destroyed by using the `PATCH` method.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Replication_snapshot'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Volume Snapshots
      summary: Modify a volume snapshot
      description: 'Modifies a volume snapshot by renaming, destroying, or recovering
        it.

        To rename the suffix of a volume snapshot, set `name` to the new suffix name.

        To recover a volume snapshot that has been destroyed and is pending eradication,

        set `destroyed=true`.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - in: body
        name: volume-snapshot
        required: true
        schema:
          $ref: '#/definitions/VolumeSnapshotPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Replication_snapshot'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumeSnapshotResponse'
  /api/2.42/policies/nfs/client-rules:
    get:
      tags:
      - Policies
      summary: List NFS client policy rules
      description: 'Displays a list of NFS client policy rules.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleNfsClientGetResponse'
    post:
      tags:
      - Policies
      summary: Create NFS client policy rules
      description: 'Creates one or more NFS client policy rules.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.

        '
      parameters:
      - in: body
        name: rules
        required: true
        schema:
          $ref: '#/definitions/PolicyRuleNfsClientPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleNfsClientResponse'
    delete:
      tags:
      - Policies
      summary: Delete NFS client policy rules.
      description: 'Deletes one or more NFS client policy rules.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/policies/alert-watcher/rules/test:
    get:
      tags:
      - Policies
      summary: List rules of alert-watcher policy rule test
      description: Displays alert-watcher policy rule email test results.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceGetResponse'
  /api/2.42/software-installations:
    get:
      tags:
      - Software
      summary: List software upgrades
      description: 'Displays a list of software upgrades, including currently running
        and past upgrades.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Software_ids'
      - $ref: '#/parameters/Software_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SoftwareInstallationsGetResponse'
    post:
      tags:
      - Software
      summary: Create a software upgrade
      description: 'Creates and initiates a software upgrade.

        '
      parameters:
      - in: body
        name: software-installations
        required: true
        schema:
          $ref: '#/definitions/SoftwareInstallationPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Software_idsRequired'
      responses:
        '200':
          description: 'The software upgrade record was created successfully.

            '
          schema:
            $ref: '#/definitions/SoftwareInstallationsResponse'
    patch:
      tags:
      - Software
      summary: Modify software upgrade
      description: 'Modifies a software upgrade by continuing, retrying, or aborting
        it.

        All `override_checks` are updated before the command is issued if

        `add_override_checks` is present.

        The `override_checks` parameter is valid when `command` is set to `continue`
        or `retry`.


        '
      parameters:
      - in: body
        name: software-installations
        required: false
        schema:
          $ref: '#/definitions/SoftwareInstallationPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Software_installation_command_required'
      - $ref: '#/parameters/Software_installation_current_step_idRequired'
      responses:
        '200':
          description: 'The `command` was issued and/or `override_checks` was updated.

            '
          schema:
            $ref: '#/definitions/SoftwareInstallationsResponse'
  /api/2.42/policies/snapshot:
    get:
      tags:
      - Policies
      summary: List snapshot policies
      description: 'Displays a list of snapshot policies.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyGetResponse'
    post:
      tags:
      - Policies
      summary: Create snapshot policies
      description: 'Creates one or more snapshot policies.

        To copy a policy, set one of either `source_names` or `source_ids`.

        Each policy can only have one entry.

        To create a policy from scratch, policy body is required.



        '
      parameters:
      - in: body
        name: policy
        required: false
        schema:
          $ref: '#/definitions/PolicyPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyResponse'
    delete:
      tags:
      - Policies
      summary: Delete snapshot policies
      description: 'Deletes one or more snapshot policies.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Policies
      summary: Modify snapshot policies
      description: 'Modifies one or more snapshot policies. To enable a policy, set
        `enabled=true`.

        To disable a policy, set `enabled=true`.

        To rename a policy, set `name` to the new name.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - in: body
        name: policy
        required: true
        schema:
          $ref: '#/definitions/PolicyPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyResponse'
  /api/2.42/hardware:
    get:
      tags:
      - Hardware
      summary: List hardware component information
      description: Displays a list of hardware slots and bays and status of installed
        components.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/HardwareGetResponse'
    patch:
      tags:
      - Hardware
      summary: Modify visual identification
      description: Modifies the visual identification of a specified hardware component,
        and causing the ID LED to turn on or off.
      parameters:
      - in: body
        name: hardware
        required: true
        schema:
          $ref: '#/definitions/HardwarePatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/HardwareResponse'
  /api/2.42/remote-volume-snapshots/transfer:
    get:
      tags:
      - Remote Volume Snapshots
      summary: List remote volume snapshots with transfer statistics
      description: 'Displays a list of remote volume snapshots and their transfer
        statistics.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/On_'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemoteVolumeSnapshotTransferGetResponse'
    parameters:
    - $ref: '#/parameters/Names'
  /api/2.42/directories/policies/quota:
    get:
      tags:
      - Directories
      summary: List quota policies attached to a directory
      description: Displays a list of quota policies that are attached to directories.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberGetResponse'
    post:
      tags:
      - Directories
      summary: Create a membership between a directory and one or more quota policies
      description: 'Creates a membership between a directory and one or more quota
        policies.

        The `member_ids` or `member_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - in: body
        name: policies
        required: true
        schema:
          $ref: '#/definitions/DirectoryPolicyPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Ignore_usage'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberResponse'
    delete:
      tags:
      - Directories
      summary: Delete a membership between a directory and one or more quota policies
      description: 'Deletes a membership between a directory and one or more quota
        policies.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.

        The `member_ids` or `member_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/policies/autodir:
    get:
      tags:
      - Policies
      summary: List auto managed directory policies
      description: 'Displays a list of auto managed directory policies.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyGetResponse'
    post:
      tags:
      - Policies
      summary: Create auto managed directory policies
      description: 'Creates one or more auto managed directory policies.

        To copy a policy, set one of either `source_names` or `source_ids`.

        Each policy can only have one entry.

        To create a policy from scratch, policy body is required.



        '
      parameters:
      - in: body
        name: policy
        required: false
        schema:
          $ref: '#/definitions/PolicyPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyResponse'
    delete:
      tags:
      - Policies
      summary: Delete auto managed directory policies
      description: 'Deletes one or more auto managed directory policies.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Policies
      summary: Modify auto managed directory policies
      description: 'Modifies one or more auto managed directory policies. To enable
        a policy,

        set `enabled=true`.

        To disable a policy, set `enabled=true`. To rename a policy,

        set `name` to the new name.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - in: body
        name: policy
        required: true
        schema:
          $ref: '#/definitions/PolicyPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyResponse'
  /api/2.42/remote-volume-snapshots:
    get:
      tags:
      - Remote Volume Snapshots
      summary: List remote volume snapshots
      description: Displays a list of remote volume snapshots.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/On_'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemoteVolumeSnapshotGetResponse'
    post:
      tags:
      - Remote Volume Snapshots
      summary: Create a volume snapshot on a connected remote target or offload target
      description: Creates a volume snapshot on the specified connected remote target
        or offload target.
      parameters:
      - in: body
        name: remote-volume-snapshot
        required: true
        schema:
          $ref: '#/definitions/RemoteVolumeSnapshotPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/OnSingle'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemoteVolumeSnapshotResponse'
    delete:
      tags:
      - Remote Volume Snapshots
      summary: Delete a remote volume snapshot
      description: 'Deletes a remote volume snapshot

        that has been destroyed and is pending eradication.

        Eradicated remote volume snapshots cannot be recovered.

        Remote volume snapshots are destroyed through the `PATCH` method.

        The `names` parameter represents the name of the volume snapshot.

        The `on` parameter represents the name of the offload target.

        The `names` and `on` parameters are required and must be used together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/OnSingle'
      - $ref: '#/parameters/Replication_snapshot'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Remote Volume Snapshots
      summary: Modify a remote volume snapshot
      description: 'Modifies a remote volume snapshot by destroying or recovering
        it from the offload target.

        The `on` parameter represents the name of the offload target.

        The `names` parameter and the `on` parameter are required and must be used
        together.

        '
      parameters:
      - in: body
        name: remote-volume-snapshot
        required: true
        schema:
          $ref: '#/definitions/_destroyedPatchPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/OnSingle'
      - $ref: '#/parameters/Replication_snapshot'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemoteVolumeSnapshotResponse'
  /api/2.42/volumes/test:
    post:
      tags:
      - Volumes
      summary: Create a dry run attempt to copy a volume
      description: 'Creates a dry run attempt to see if copying a volume would

        succeed. No virtual volumes are actually copied or created in this process.

        '
      parameters:
      - in: body
        name: volume
        required: true
        schema:
          $ref: '#/definitions/VolumePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Add_to_protection_group_ids'
      - $ref: '#/parameters/Add_to_protection_group_names'
      - $ref: '#/parameters/Allow_throttle_operation'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Overwrite'
      - $ref: '#/parameters/With_default_protection'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceWithIdPostResponse'
  /api/2.42/ports/initiators:
    get:
      tags:
      - Ports
      summary: List port initiators
      description: Displays host iSCSI Qualified Names (IQNs), NVMe Qualified Names
        (NQNs), and Fibre Channel World Wide Names (WWNs), including those that were
        discovered by Purity//FA and those that have been manually assigned by system
        administrators, along with the array ports (targets) on which they are eligible
        to communicate.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PortInitiatorsGetResponse'
  /api/2.42/host-groups:
    get:
      tags:
      - Host Groups
      summary: List host groups
      description: 'Displays a list of host groups.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/HostGroupGetResponse'
    post:
      tags:
      - Host Groups
      summary: Create a host group and upsert tags
      description: 'Creates a host group in which the `names` query parameter is required.


        '
      parameters:
      - in: body
        name: host-group
        required: false
        schema:
          $ref: '#/definitions/HostGroupPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/HostGroupResponse'
    delete:
      tags:
      - Host Groups
      summary: Delete a host group
      description: 'Deletes a host group in which the `names` query parameter is required.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Host Groups
      summary: Modify a host group
      description: 'Modifies a host group in which the `names` query parameter is
        required.


        '
      parameters:
      - in: body
        name: host-group
        required: true
        schema:
          $ref: '#/definitions/HostGroupPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/From_member_ids'
      - $ref: '#/parameters/From_member_names'
      - $ref: '#/parameters/To_member_ids'
      - $ref: '#/parameters/To_member_names'
      - $ref: '#/parameters/Modify_resource_access_on_move'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/HostGroupResponse'
  /api/2.42/policies/nfs:
    get:
      tags:
      - Policies
      summary: List NFS policies
      description: 'Displays a list of NFS policies.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyNfsGetResponse'
    post:
      tags:
      - Policies
      summary: Create NFS policies
      description: 'Creates one or more NFS policies or copies an existing policy.

        To copy a policy, the `source_names` or `source_ids` parameter is required,

        but they cannot be set together.

        To create a policy, policy body is required.


        '
      parameters:
      - in: body
        name: policy
        required: false
        schema:
          $ref: '#/definitions/PolicyNfsPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyNfsResponse'
    delete:
      tags:
      - Policies
      summary: Delete NFS policies
      description: 'Deletes one or more NFS policies.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Policies
      summary: Modify NFS policies
      description: 'Modifies one or more NFS policies. To enable a policy, set `enabled=true`.

        To disable a policy, set `enabled=false`. To rename a policy, set `name` to
        the new name.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - in: body
        name: policy
        required: true
        schema:
          $ref: '#/definitions/PolicyNfsPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyNfsResponse'
  /api/2.42/policies/quota:
    get:
      tags:
      - Policies
      summary: List quota policies
      description: 'Displays a list of quota policies.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyGetResponse'
    post:
      tags:
      - Policies
      summary: Create quota policies
      description: 'Creates one or more quota policies.

        To copy a policy, the `source_names` or `source_ids` parameter is required,

        but they cannot be set together.

        To create a policy from scratch, policy body is required.


        '
      parameters:
      - in: body
        name: policy
        required: false
        schema:
          $ref: '#/definitions/PolicyPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyResponse'
    delete:
      tags:
      - Policies
      summary: Delete quota policies
      description: 'Deletes one or more quota policies.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Policies
      summary: Modify quota policies
      description: 'Modifies one or more quota policies. To enable a policy, set `enabled=true`.

        To disable a policy, set `enabled=false`.

        To rename a policy, set `name` to the new name.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - in: body
        name: policy
        required: true
        schema:
          $ref: '#/definitions/PolicyPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Ignore_usage'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyResponse'
  /api/2.42/kmip:
    get:
      tags:
      - KMIP
      summary: List KMIP server objects
      description: 'Displays the list of KMIP server objects.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/KmipGetResponse'
    post:
      tags:
      - KMIP
      summary: Create KMIP server object
      description: 'Creates KMIP server objects.

        '
      parameters:
      - in: body
        name: kmip
        required: true
        schema:
          $ref: '#/definitions/KmipPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: Returns the newly created KMIP server object.
          schema:
            $ref: '#/definitions/KmipResponse'
    delete:
      tags:
      - KMIP
      summary: Delete KMIP server object
      description: Deletes KMIP server objects.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - KMIP
      summary: Modify KMIP attributes
      description: 'Modifies one or more attributes of KMIP server objects.

        '
      parameters:
      - in: body
        name: kmip
        required: true
        schema:
          $ref: '#/definitions/KmipPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/KmipResponse'
  /api/2.42/directories/policies/audit-file:
    get:
      tags:
      - Directories
      summary: List audit policies attached to a directory
      description: 'Displays a list of audit policies that are attached to directories.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberGetResponse'
    post:
      tags:
      - Directories
      summary: Create a membership between a directory and one or more audit policies
      description: 'Creates a membership between a directory and one or more audit
        policies.

        Either the `member_ids` or `member_names` parameter is required, but they
        cannot

        be set together.


        '
      parameters:
      - in: body
        name: policies
        required: true
        schema:
          $ref: '#/definitions/DirectoryPolicyPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberResponse'
    delete:
      tags:
      - Directories
      summary: Delete a membership between a directory and one or more audit policies
      description: 'Deletes a membership between a directory and one or more audit
        policies.

        Either the `policy_ids` or `policy_names` parameter is required, but they
        cannot be

        set together.

        Either the `member_ids` or `member_names` parameter is required, but they
        cannot be

        set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/presets/workload:
    get:
      tags:
      - Presets
      summary: List workload presets
      description: 'Displays a list of workload presets.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PresetWorkloadGetResponse'
    post:
      tags:
      - Presets
      summary: Create a workload preset
      description: 'Creates one workload preset.

        '
      parameters:
      - in: body
        name: preset
        required: true
        schema:
          $ref: '#/definitions/PresetWorkloadPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names_single_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PresetWorkloadResponse'
    put:
      tags:
      - Presets
      summary: Update a workload preset
      description: 'Updates a workload preset by replacing it with the new contents
        in `preset`.

        The `ids` or `names` parameter is required, but they cannot be set together.

        '
      parameters:
      - in: body
        name: preset
        required: true
        schema:
          $ref: '#/definitions/PresetWorkload'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids_single'
      - $ref: '#/parameters/Names_single'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PresetWorkloadResponse'
    delete:
      tags:
      - Presets
      summary: Delete a workload preset
      description: 'Deletes a workload preset permanently.

        The `ids` or `names` parameter is required, but they cannot be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids_single'
      - $ref: '#/parameters/Names_single'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Presets
      summary: Modify a workload preset
      description: 'Modifies a workload preset by overriding the fields specified
        in `preset`.

        To rename a workload preset, set name to the new name.

        '
      parameters:
      - in: body
        name: preset
        required: true
        schema:
          $ref: '#/definitions/PresetWorkloadPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids_single'
      - $ref: '#/parameters/Names_single'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PresetWorkloadResponse'
  /api/2.42/certificates/certificate-signing-requests:
    post:
      tags:
      - Certificates
      summary: Create certificate-signing-requests
      description: 'Creates a certificate signing request using a specified certificate
        and parameters.

        '
      parameters:
      - in: body
        name: certificate
        required: true
        schema:
          $ref: '#/definitions/CertificateSigningRequestPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: Returns the newly created certificate object.
          schema:
            $ref: '#/definitions/CertificateSigningRequestResponse'
  /api/2.42/host-groups/tags:
    get:
      tags:
      - Host Groups
      summary: List tags
      description: 'Displays the list of tags on host group objects.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      - $ref: '#/parameters/Resource_destroyed'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagGetResponse'
    delete:
      tags:
      - Host Groups
      summary: Delete tags
      description: 'Deletes specified tags on host group objects.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Keys'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
  /api/2.42/vchosts/certificates:
    get:
      tags:
      - Vchosts
      summary: List vchost certificates
      description: 'Displays certificates that are attached to configured vchosts
        on at least one endpoint.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Vchost_names'
      - $ref: '#/parameters/Vchost_ids'
      - $ref: '#/parameters/Certificate_names'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VchostCertificateGetResponse'
    post:
      tags:
      - Vchosts
      summary: Create a vchost certificate
      description: 'Creates an association between a certificate and vchost on one
        or more endpoints.

        The `vchost_names` or `vchost_ids` query parameter is required.


        '
      parameters:
      - in: body
        name: certificate
        required: true
        schema:
          $ref: '#/definitions/VchostCertificatePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Vchost_names'
      - $ref: '#/parameters/Vchost_ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VchostCertificateResponse'
    delete:
      tags:
      - Vchosts
      summary: Delete a vchost certificate
      description: 'Deletes an existing vchost certificate.

        The `ids` query parameter alone, or the `certificate_names`

        query parameter together with one of `vchost_names` or `vchost_ids`, is required.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Vchost_names'
      - $ref: '#/parameters/Vchost_ids'
      - $ref: '#/parameters/Certificate_names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Vchosts
      summary: Modify a vchost certificate
      description: 'Modifies an existing vchost certificate.

        The `ids` query parameter alone, or the `certificate_names` query parameter
        together

        with one of `vchost_names` or `vchost_ids`, is required.


        '
      parameters:
      - in: body
        name: certificate
        required: true
        schema:
          $ref: '#/definitions/VchostCertificatePatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Vchost_names'
      - $ref: '#/parameters/Vchost_ids'
      - $ref: '#/parameters/Certificate_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VchostCertificateResponse'
  /api/2.42/subscription-assets:
    get:
      tags:
      - Subscription Assets
      summary: List subscription assets
      description: 'Displays information about subscription assets.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SubscriptionAssetGetResponse'
  /api/2.42/hosts:
    get:
      tags:
      - Hosts
      summary: List hosts
      description: 'Displays a list of hosts.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/HostGetResponse'
    post:
      tags:
      - Hosts
      summary: Create a host and upsert tags
      description: 'Creates a host for which the `names` query parameter is required.


        '
      parameters:
      - in: body
        name: host
        required: true
        schema:
          $ref: '#/definitions/HostPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/HostResponse'
    delete:
      tags:
      - Hosts
      summary: Delete a host
      description: 'Deletes an existing host. All volumes that are connected to the
        host,

        either through private or shared connections,

        must be disconnected from the host before the host can be deleted.

        The `names` query parameter is required.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Hosts
      summary: Modify a host
      description: 'Modifies an existing host, including its storage network addresses,
        CHAP, host personality, and

        preferred arrays, or associate a host to a host group.

        The `names` query parameter is required.


        '
      parameters:
      - in: body
        name: host
        required: true
        schema:
          $ref: '#/definitions/HostPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/From_member_ids'
      - $ref: '#/parameters/From_member_names'
      - $ref: '#/parameters/To_member_ids'
      - $ref: '#/parameters/To_member_names'
      - $ref: '#/parameters/Modify_resource_access_on_move'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/HostResponse'
  /api/2.42/protection-groups/tags/batch:
    put:
      tags:
      - Protection Groups
      summary: Update tags
      description: Updates tags for all specified objects.
      parameters:
      - in: body
        name: tag
        description: A list of tags to be created or, if one already exists, updated.
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/TagBatch'
          uniqueItems: true
          minItems: 1
          maxItems: 30
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagResponse'
  /api/2.42/software-patches:
    get:
      tags:
      - Software
      summary: List software patches
      description: 'Displays a list of software patches.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SoftwarePatchesGetResponse'
    post:
      tags:
      - Software
      summary: Create a software patch
      description: 'Creates software patch download and installation.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - name: name
        in: query
        description: Name of software patch to install
        required: true
        type: string
      - name: allow_ha_reduction
        in: query
        description: 'If Software Patch has ha_reduction_required set to true, `allow_ha_reduction`
          must be

          set to `true` for Software Patch installation.


          '
        required: false
        type: boolean
      responses:
        '200':
          description: 'The software patch download was initiated. The download and
            installation will start soon.

            '
          schema:
            $ref: '#/definitions/SoftwarePatchesResponse'
  /api/2.42/arrays/tags/batch:
    put:
      tags:
      - Arrays
      summary: Update tags
      description: 'Updates tags.

        '
      parameters:
      - in: body
        name: tag
        description: A list of tags to be created or, if they already exist, updated.
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/NonCopyableTag'
          uniqueItems: true
          minItems: 1
          maxItems: 30
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagResponse'
  /api/2.42/arrays/performance:
    get:
      tags:
      - Arrays
      summary: List array front-end performance data
      description: 'Displays real-time and historical front-end performance data at
        the array

        level including latency, bandwidth, IOPS, average I/O size, and queue depth.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Perf_protocol_type'
      - $ref: '#/parameters/Perf_protocol_group'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayPerformanceGetResponse'
  /api/2.42/realms/space:
    get:
      tags:
      - Realms
      summary: List realm space information
      description: 'Displays provisioned size and storage consumption data for each
        realm on the

        local array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RealmSpaceGetResponse'
  /api/2.42/fleets/members:
    get:
      tags:
      - Fleets
      summary: List fleet members
      description: 'Displays the members in the current fleet.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Fleet_ids'
      - $ref: '#/parameters/Fleet_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/FleetMemberGetResponse'
    post:
      tags:
      - Fleets
      summary: Add members to a fleet
      description: 'Adds arrays to an existing fleet. This API needs to be run from
        the array that''s joining the

        fleet.


        To add the current array to the fleet, specify its own array `id` or `name`,

        as well as the `fleet key` generated on any array already in the fleet.

        `resource_type` is `remote-arrays`.

        The array''s `ID` and `name` can be retrieved with a call to `GET /arrays`
        on the array.


        '
      parameters:
      - in: body
        name: members
        description: 'Info about the members being added to fleet.

          '
        required: false
        schema:
          $ref: '#/definitions/FleetMemberPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Fleet_ids'
      - $ref: '#/parameters/Fleet_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/FleetMemberResponse'
    delete:
      tags:
      - Fleets
      summary: Delete fleet members
      description: 'Deletes the specified array(s) from the fleet.

        Note that this will only succeed if the array(s) to be removed are not making
        use of any fleet

        resources.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Unreachable'
      responses:
        '200':
          description: OK
  /api/2.42/directories/policies/nfs:
    get:
      tags:
      - Directories
      summary: List NFS policies attached to a directory
      description: 'Displays a list of NFS policies that are attached to directories.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberExportGetResponse'
    post:
      tags:
      - Directories
      summary: Create a membership between a directory and one or more NFS policies
      description: 'Creates a membership between a directory and one or more NFS policies.

        The `member_ids` or `member_names` parameter is required but cannot be set
        together.


        '
      parameters:
      - in: body
        name: policies
        required: true
        schema:
          $ref: '#/definitions/DirectoryPolicyExportPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberExportResponse'
    delete:
      tags:
      - Directories
      summary: Delete a membership between a directory and one or more NFS policies
      description: 'Deletes a membership between a directory and one or more NFS policies.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.

        The `member_ids` or `member_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/admins/settings:
    get:
      tags:
      - Administrators
      summary: List administrator settings
      description: 'Displays the global administrator settings for the array.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AdminSettingsGetResponse'
    patch:
      tags:
      - Administrators
      summary: Modify administrator settings
      description: 'Modifies the global administrator settings for the array.


        '
      parameters:
      - in: body
        name: admin-settings
        required: true
        schema:
          $ref: '#/definitions/AdminSettings'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AdminSettingsResponse'
  /api/2.42/array-connections/connection-key:
    get:
      tags:
      - Array Connections
      summary: List connection key
      description: Displays the connection key for the array.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Encrypted'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayConnectionKeyGetResponse'
  /api/2.42/fleets:
    get:
      tags:
      - Fleets
      summary: List fleets
      description: 'Displays information about the fleets known to the array.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names_single'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/FleetGetResponse'
    post:
      tags:
      - Fleets
      summary: Create a fleet
      description: 'Creates a fleet and adds the current array as its first member.

        Additional arrays can be added to the fleet with `POST /fleets/members`.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_single'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/FleetResponse'
    delete:
      tags:
      - Fleets
      summary: Delete a fleet
      description: 'Deletes a fleet, permanently.

        A fleet can only be deleted if the current array is the only array in the
        fleet.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids_single'
      - $ref: '#/parameters/Names_single'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Fleets
      summary: Modify a fleet
      description: 'Modifies a fleet, changing its name by passing the old name in
        `name` query parameter, and the

        new name in the `name` parameter in the request body.


        '
      parameters:
      - in: body
        name: fleet
        required: true
        schema:
          $ref: '#/definitions/FleetPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids_single'
      - $ref: '#/parameters/Names_single'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/FleetResponse'
  /api/2.42/directories/policies/snapshot:
    get:
      tags:
      - Directories
      summary: List snapshot policies attached to a directory
      description: Displays a list of snapshot policies that are attached to directories.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberGetResponse'
    post:
      tags:
      - Directories
      summary: Create a membership between a directory with one or more snapshot policies
      description: 'Creates a membership between a directory with one or more snapshot
        policies.

        The `member_ids` or `member_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - in: body
        name: policies
        required: true
        schema:
          $ref: '#/definitions/DirectoryPolicyPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberResponse'
    delete:
      tags:
      - Directories
      summary: Delete a membership between a directory and one or more snapshot policies
      description: 'Deletes a membership between a directory and one or more snapshot
        policies.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.

        The `member_ids` or `member_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/workloads/tags/batch:
    put:
      tags:
      - Workloads
      summary: Update tags
      description: Updates or creates tags for all specified objects.
      parameters:
      - in: body
        name: tag
        description: A list of tags to be created or, if they already exist, updated.
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/TagBatch'
          uniqueItems: true
          minItems: 1
          maxItems: 30
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagResponse'
  /api/2.42/arrays/tags:
    get:
      tags:
      - Arrays
      summary: List tags
      description: 'Displays the list of tags on array objects.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagGetResponse'
    delete:
      tags:
      - Arrays
      summary: Delete tags
      description: 'Deletes specified tags on array objects.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Keys'
      - $ref: '#/parameters/Namespaces'
      responses:
        '200':
          description: OK
  /api/2.42/arrays/cloud-capacity/supported-steps:
    get:
      tags:
      - Arrays
      summary: List CBS array capacity steps
      description: 'Displays the list of supported raw capacity sizes in bytes the
        CBS array can be

        upgraded to. Requests for non-CBS arrays will receive an error response.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CloudCapacityStepsGetResponse'
  /api/2.42/volumes/tags:
    get:
      tags:
      - Volumes
      summary: List tags
      description: Displays the list of tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Resource_destroyed'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagGetResponse'
    delete:
      tags:
      - Volumes
      summary: Delete tags
      description: Deletes specified tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Keys'
      - $ref: '#/parameters/Namespaces_delete'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
  /api/2.42/volumes:
    get:
      tags:
      - Volumes
      summary: List volumes
      description: 'Displays a list of volumes, including those pending eradication.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumeGetResponse'
    post:
      tags:
      - Volumes
      summary: Create or copy a volume and upsert tags
      description: 'Creates one or more virtual storage volumes of the specified size.

        If `provisioned` is not specified, the size of the new volume defaults to
        1 MB.

        The `names` query parameter is required.

        The `add_to_protection_group_names` query parameter specifies a list of protection
        group

        names that will compose the initial protection for the volume.

        The `with_default_protection` query parameter specifies whether to use the
        container

        default protection configuration for the volume.

        The `add_to_protection_group_names` and `with_default_protection` query parameters

        cannot be provided when `overwrite` is `true`.


        '
      parameters:
      - in: body
        name: volume
        required: true
        schema:
          $ref: '#/definitions/VolumePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Add_to_protection_group_ids'
      - $ref: '#/parameters/Add_to_protection_group_names'
      - $ref: '#/parameters/Allow_throttle_operation'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Overwrite'
      - $ref: '#/parameters/With_default_protection'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumeResponse'
    delete:
      tags:
      - Volumes
      summary: Delete a volume
      description: 'Deletes a volume that has been destroyed and is pending eradication.

        Eradicated volumes cannot be recovered.

        Volumes are destroyed using the `PATCH` method.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Volumes
      summary: Modify a volume
      description: 'Modifies a volume by renaming, destroying, or resizing it.

        To rename a volume, set `name` to the new name. To destroy a volume, set `destroyed=true`.

        To recover a volume that has been destroyed and is pending eradication, set
        `destroyed=false`.

        Set the bandwidth and IOPS limits of a volume through the respective `bandwidth_limit`
        and

        `iops_limit` parameter. This moves the volume into a pod or volume group through
        the respective

        `pod` or `volume_group` parameter.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - in: body
        name: volume
        required: true
        schema:
          $ref: '#/definitions/VolumePatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Add_to_protection_group_ids'
      - $ref: '#/parameters/Add_to_protection_group_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Remove_from_protection_group_ids'
      - $ref: '#/parameters/Remove_from_protection_group_names'
      - $ref: '#/parameters/Truncate'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumeResponse'
  /api/2.42/pods/performance/replication/by-array:
    get:
      tags:
      - Pods
      summary: List pod replication performance data by array
      description: 'Displays pod replication performance data, organized by array.

        The data returned is the real-time and historical performance data for each
        replication type

        at the pod level. Values include `continuous`, `periodic`, `resync`, and `sync`.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodPerformanceReplicationByArrayGetResponse'
  /api/2.42/support-diagnostics/settings:
    get:
      tags:
      - Support
      summary: List diagnostics tools version and last updated date
      description: 'Displays diagnostics tools version and last updated date.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SupportDiagnosticsGetResponse'
    patch:
      tags:
      - Support
      summary: Update diagnostics tools
      description: 'Update diagnostics tools from downloaded bundle.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/PurityDefault'
      responses:
        '200':
          description: 'The diagnostics tools were updated successfully.

            '
          schema:
            $ref: '#/definitions/SupportDiagnosticsResponse'
  /api/2.42/login:
    post:
      tags:
      - Authorization
      summary: POST login (placeholder)
      description: 'Exchange an API token for a session token.

        '
      parameters:
      - $ref: '#/parameters/XRequestId'
      - name: api-token
        in: header
        description: 'API token for a user.

          '
        required: false
        type: string
        x-example: 0f2e2884-9486-c6c2-438c-f50418f2aac3
      responses:
        '200':
          description: OK
          headers:
            x-auth-token:
              type: string
              example: 3be3d489-55c6-4643-90ac-a476dbc98812
          schema:
            $ref: '#/definitions/UsernameResponse'
  /api/2.42/hosts/performance/balance:
    get:
      tags:
      - Hosts
      summary: List host performance balance
      description: 'Displays the I/O balance statistics for host paths.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/HostPerformanceBalanceGetResponse'
  /api/2.42/volume-groups:
    get:
      tags:
      - Volume Groups
      summary: List volume groups
      description: 'Displays a list of volume groups, including those pending eradication.



        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumeGroupGetResponse'
    post:
      tags:
      - Volume Groups
      summary: Create a volume group and upsert tags.
      description: 'Creates a volume group. The volume group itself does not contain
        any meaningful

        content; instead, it acts as a container that is used to organize volumes.

        Once a volume group has been created, volumes can be created inside the volume
        group or moved

        into and out of the volume group.

        '
      parameters:
      - in: body
        name: volume-group
        required: true
        schema:
          $ref: '#/definitions/VolumeGroupPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumeGroupResponse'
    delete:
      tags:
      - Volume Groups
      summary: Delete a volume group
      description: 'Deletes a volume group that has been destroyed and is pending
        eradication.

        Eradicated volume groups cannot be recovered.

        Volume groups are destroyed through the PATCH method.

        The `ids` or `names` parameter is required, but they cannot be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Eradicate_contents'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Volume Groups
      summary: Modify a volume group
      description: 'Modifies a volume group. You can rename, destroy, recover, or
        set QoS limits for a volume group.

        To rename a volume group, set `name` to the new name.

        To destroy a volume group, set `destroyed=true`.

        To recover a volume group that has been destroyed and is pending eradication,
        set

        `destroyed=false`.

        Sets the bandwidth and IOPS limits of a volume group through the respective
        `bandwidth_limit`

        and `iops_limit` parameter. The `ids` or `names` parameter is required, but
        they cannot be set

        together.

        Sets the priority adjustment for a volume group using the `priority_adjustment_operator`
        and

        `priority_adjustment_value` fields.

        '
      parameters:
      - in: body
        name: volume-group
        required: true
        schema:
          $ref: '#/definitions/VolumeGroupPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Destroy_contents'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumeGroupResponse'
  /api/2.42/policies/members:
    get:
      tags:
      - Policies
      summary: List policy members
      description: Displays a list of policy members.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberGetResponse'
  /api/2.42/policies/password:
    get:
      tags:
      - Policies
      summary: List password policies
      description: 'Displays a list of password policies.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyPasswordGetResponse'
    patch:
      tags:
      - Policies
      summary: Modify password policies
      description: 'Modifies one or more password policies. To enable a policy, set
        `enabled=true`.

        To disable a policy, set `enabled=false`.


        '
      parameters:
      - in: body
        name: policy
        required: true
        schema:
          $ref: '#/definitions/PolicyPassword'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyPasswordResponse'
  /api/2.42/drives:
    get:
      tags:
      - Drives
      summary: List flash, NVRAM, and cache modules
      description: Displays a list of flash, NVRAM, and cache modules that are installed
        in the array along with their attributes and status.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DriveGetResponse'
    patch:
      tags:
      - Drives
      summary: Modify flash and NVRAM modules
      description: Modifies flash and NVRAM modules that have been added or connected
        but not yet admitted to the array.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Admit'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DriveResponse'
  /api/2.42/arrays:
    get:
      tags:
      - Arrays
      summary: List arrays
      description: 'Displays general array properties including the array name, login
        banner,

        idle timeout for management sessions, and NTP servers.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayGetResponse'
    delete:
      tags:
      - Arrays
      summary: Delete an array
      description: 'Deletes an array. For physical appliances, deleting an array restores
        the hardware to factory

        settings. This entails deleting all data, metadata, configuration, and logs.
        The array returns

        to the state it was in prior to any configuration changes being made. If the
        hardware is reused,

        it must be as a different array with a new ID.

        For virtual appliances, deleting an array puts it into an unusable state.

        Virtual resources (e.g., virtual machines) can later be freed, which deletes
        any remaining data,

        metadata, configuration, and logs. Prior to factory reset, an array must be
        manually prepared

        (e.g., all volumes and snapshots must be eradicated) and a factory reset token
        must be created.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Eradicate_all_data'
      - $ref: '#/parameters/Factory_reset_token'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Arrays
      summary: Modify an array
      description: 'Modifies general array properties such as the array name, login
        banner, idle timeout for

        management sessions, and NTP servers.

        '
      parameters:
      - in: body
        name: array
        required: true
        schema:
          $ref: '#/definitions/Arrays'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayResponse'
  /api/2.42/arrays/ntp-test:
    get:
      tags:
      - Arrays
      summary: List NTP test results
      description: Displays test results for configured NTP servers.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultGetResponse'
  /api/2.42/volume-groups/performance:
    get:
      tags:
      - Volume Groups
      summary: List volume group performance data
      description: 'Displays real-time and historical performance data, real-time
        latency data, and average I/O

        sizes for each volume group and and as a total of all volume groups across
        the entire array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourcePerformanceGetResponse'
  /api/2.42/directories/policies:
    get:
      tags:
      - Directories
      summary: List policies
      description: Displays a list of policies that are attached to directories.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberGetResponse'
  /api/2.42/log-targets/file:
    get:
      tags:
      - Log Targets
      summary: List file log targets
      description: Displays a list of configured file log targets.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LogTargetFileGetResponse'
    post:
      tags:
      - Log Targets
      summary: Create file log target
      description: 'Creates a new file log target.

        '
      parameters:
      - in: body
        name: log-target-file
        required: true
        schema:
          $ref: '#/definitions/LogTargetFile'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: Displays the newly configured file log target.
          schema:
            $ref: '#/definitions/LogTargetFileResponse'
    delete:
      tags:
      - Log Targets
      summary: Delete file log target
      description: Deletes a configured file log target.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Log Targets
      summary: Modify file log target
      description: Modifies the URI of a configured file log target.
      parameters:
      - in: body
        name: log-target-file
        required: true
        schema:
          $ref: '#/definitions/LogTargetFile'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: Displays the newly updated file log target.
          schema:
            $ref: '#/definitions/LogTargetFileResponse'
  /api/2.42/remote-protection-groups:
    get:
      tags:
      - Remote Protection Groups
      summary: List remote protection groups
      description: 'Returns a list of remote protection groups.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/On_'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemoteProtectionGroupGetResponse'
    delete:
      tags:
      - Remote Protection Groups
      summary: Delete a remote protection group
      description: 'Deletes a remote protection group that has been destroyed and
        is pending eradication.

        Eradicated remote protection groups cannot be recovered.

        Remote protection groups are destroyed through the `PATCH` method.

        The `on` parameter represents the name of the offload target.

        The `ids` or `names` parameter and the `on` parameter are required and must
        be used together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/OnSingle'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Remote Protection Groups
      summary: Modify a remote protection group
      description: 'Modifies the snapshot retention schedule of a remote protection
        group.

        Also destroys a remote protection group from the offload target.

        Before the remote protection group can be destroyed,

        the offload target must first be removed from the protection group via the
        source array.

        The `on` parameter represents the name of the offload target.

        The `ids` or `names` parameter and the `on` parameter are required and must
        be used together.

        '
      parameters:
      - in: body
        name: remote-protection-group
        required: true
        schema:
          $ref: '#/definitions/RemoteProtectionGroup'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/OnSingle'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemoteProtectionGroupResponse'
  /api/2.42/pods/tags:
    get:
      tags:
      - Pods
      summary: List tags
      description: Displays the list of tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Resource_destroyed'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagGetResponse'
    delete:
      tags:
      - Pods
      summary: Delete tags
      description: Deletes specified tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Keys'
      - $ref: '#/parameters/Namespaces_delete'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
  /api/2.42/resource-accesses/batch:
    post:
      tags:
      - Resource Accesses
      summary: Create a resource access configuration
      description: 'Creates resource access objects.

        '
      parameters:
      - in: body
        name: items
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/ResourceAccess'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: Ok
          schema:
            $ref: '#/definitions/ResourceAccessesResponse'
  /api/2.42/vchosts:
    get:
      tags:
      - Vchosts
      summary: List vchosts
      description: 'Displays a list of vchosts.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VchostGetResponse'
    post:
      tags:
      - Vchosts
      summary: Create a vchost
      description: 'Creates a vchost.

        The `names` query parameter is required.


        '
      parameters:
      - in: body
        name: vchost
        required: true
        schema:
          $ref: '#/definitions/VchostPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VchostResponse'
    delete:
      tags:
      - Vchosts
      summary: Delete a vchost
      description: 'Deletes an existing vchost.

        The `names` or `ids` query parameter is required.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Ids'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Vchosts
      summary: Modify a vchost
      description: 'Modifies an existing vchost.

        The `names` or `ids` query parameter is required.


        '
      parameters:
      - in: body
        name: vchost
        required: true
        schema:
          $ref: '#/definitions/VchostPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VchostResponse'
  /api/2.42/policies/management-access:
    get:
      tags:
      - Policies
      summary: List management access policies
      description: 'Displays a list of management access policies.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyManagementAccessGetResponse'
    post:
      tags:
      - Policies
      summary: Create management access policies
      description: 'Creates one or more management access policies.

        To copy a policy, one of `source_names` or `source_ids` is required.

        To create a policy from scratch, policy body is required.


        '
      parameters:
      - in: body
        name: policy
        required: false
        schema:
          $ref: '#/definitions/PolicyManagementAccessPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names_required'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyManagementAccessResponse'
    delete:
      tags:
      - Policies
      summary: Delete management access policies
      description: 'Deletes one or more management access policies.

        One of `ids` or `names` is required.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Policies
      summary: Modify management access policies
      description: 'Modifies one or more management access policies.

        One of `ids` or `names` is required.

        For renames, can only specify one old policy in `ids` or `names`.


        '
      parameters:
      - in: body
        name: policy
        required: true
        schema:
          $ref: '#/definitions/PolicyManagementAccessPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyManagementAccessResponse'
  /api/2.42/remote-protection-group-snapshots/transfer:
    get:
      tags:
      - Remote Protection Group Snapshots
      summary: List remote protection groups with transfer statistics
      description: 'Displays a list of remote protection groups and their transfer
        statistics.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/On_'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemoteProtectionGroupSnapshotTransferGetResponse'
  /api/2.42/volume-groups/tags/batch:
    put:
      tags:
      - Volume Groups
      summary: Update tags
      description: Updates tags for all specified objects.
      parameters:
      - in: body
        name: tag
        description: A list of tags to be created or, if one already exists, updated.
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/TagBatch'
          uniqueItems: true
          minItems: 1
          maxItems: 30
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagResponse'
  /api/2.42/support/remote-assist-sessions:
    get:
      tags:
      - Support
      summary: List remote assist session
      description: 'Displays settings for remote assist sessions.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SupportRemoteAssistSessionsGetResponse'
    patch:
      tags:
      - Support
      summary: Modify remote assist session
      description: 'Modifies settings for remote assist sessions.

        '
      parameters:
      - in: body
        name: support
        required: true
        schema:
          $ref: '#/definitions/SupportRemoteAssistSessionsPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SupportRemoteAssistSessionsResponse'
  /api/2.42/remote-protection-group-snapshots:
    get:
      tags:
      - Remote Protection Group Snapshots
      summary: List remote protection group snapshots
      description: 'Displays a list of remote protection group snapshots.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/On_'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemoteProtectionGroupSnapshotGetResponse'
    post:
      tags:
      - Remote Protection Group Snapshots
      summary: Create remote protection group snapshot
      description: 'Creates remote protection group snapshots.

        '
      parameters:
      - in: body
        name: remote-protection-group-snapshot
        required: false
        schema:
          $ref: '#/definitions/RemoteProtectionGroupSnapshotPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Apply_retention'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Convert_source_to_baseline'
      - $ref: '#/parameters/For_replication'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Replicate'
      - $ref: '#/parameters/Replicate_now'
      - $ref: '#/parameters/Allow_throttle_snapshot'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/OnSingle'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemoteProtectionGroupSnapshotResponse'
    delete:
      tags:
      - Remote Protection Group Snapshots
      summary: Delete a remote protection group snapshot
      description: 'Deletes a remote protection group snapshot that has been destroyed
        and is pending eradication.

        Eradicated remote protection group snapshots cannot be recovered.

        Remote protection group snapshots are destroyed using the `PATCH` method.

        The `names` parameter represents the name of the protection group snapshot.

        The `on` parameter represents the name of the offload target.

        The `names` and `on` parameters are required and must be used together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/OnSingle'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Remote Protection Group Snapshots
      summary: Modify a remote protection group snapshot
      description: 'Modifies a remote protection group snapshot, removing it from
        the offload target and

        destroying the snapshot.

        The `on` parameter represents the name of the offload target.

        The `ids` or `names` parameter and the `on` parameter are required and must
        be used together.

        '
      parameters:
      - in: body
        name: remote-protection-group-snapshot
        required: true
        schema:
          $ref: '#/definitions/_destroyedPatchPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/OnSingle'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemoteProtectionGroupSnapshotResponse'
  /api/2.42/container-default-protections:
    get:
      tags:
      - Container Default Protections
      summary: List container default protections
      description: 'Displays a list of containers with its default protections.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Container_ids'
      - $ref: '#/parameters/Container_names'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ContainerDefaultProtectionGetResponse'
    patch:
      tags:
      - Container Default Protections
      summary: Modify a container's default protections
      description: 'Modifies a container''s default protections.

        To modify, set `default_protections` to the new list.

        '
      parameters:
      - in: body
        name: container-default-protection
        required: true
        schema:
          $ref: '#/definitions/ContainerDefaultProtection'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Container_ids'
      - $ref: '#/parameters/Container_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ContainerDefaultProtectionResponse'
  /oauth2/1.0/token:
    post:
      tags:
      - Authorization
      summary: Get access token
      description: 'Exchanges an ID Token for an OAuth 2.0 access token.

        '
      parameters:
      - $ref: '#/parameters/OauthSubjectTokenType'
      - $ref: '#/parameters/OauthGrantType'
      - $ref: '#/parameters/OauthSubjectToken'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          $ref: '#/responses/OauthToken200'
        '400':
          $ref: '#/responses/OauthToken400'
        '401':
          $ref: '#/responses/OauthToken401'
  /api/2.42/network-interfaces/port-details:
    get:
      tags:
      - Network Interfaces
      summary: List SFP port details
      description: 'Displays Ethernet and Fibre Channel SFP details.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/NetworkInterfacesPortDetailsGetResponse'
  /api/2.42/audits:
    get:
      tags:
      - Audits
      summary: List audits
      description: Displays a list of audits.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AuditGetResponse'
  /api/2.42/certificates/certificate-groups:
    get:
      tags:
      - Certificates
      summary: List certificates and certificate-groups
      description: 'Lists membership associations between groups and certificates.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Certificate_group_ids'
      - $ref: '#/parameters/Certificate_group_names'
      - $ref: '#/parameters/Certificate_ids'
      - $ref: '#/parameters/Certificate_names'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CertificateCertificateGroupGetResponse'
    post:
      tags:
      - Certificates
      summary: Create certificates orcertificate-groups
      description: 'Creates one or more certificates to one or more certificate groups.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Certificate_group_ids'
      - $ref: '#/parameters/Certificate_group_names'
      - $ref: '#/parameters/Certificate_ids'
      - $ref: '#/parameters/Certificate_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CertificateCertificateGroupResponse'
    delete:
      tags:
      - Certificates
      summary: Deletes certificates/certificate-groups
      description: 'Deletes one or more certificates from one or more certificate
        groups.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Certificate_group_ids'
      - $ref: '#/parameters/Certificate_group_names'
      - $ref: '#/parameters/Certificate_ids'
      - $ref: '#/parameters/Certificate_names'
      responses:
        '200':
          description: OK
  /api/2.42/network-interfaces:
    get:
      tags:
      - Network Interfaces
      summary: List network interfaces
      description: Displays all network interfaces for all controllers on the array.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/NetworkInterfaceGetResponse'
    post:
      tags:
      - Network Interfaces
      summary: Create network interface
      description: Creates a network interface on a controller on the array.
      parameters:
      - in: body
        name: network
        required: true
        schema:
          $ref: '#/definitions/NetworkInterfacePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/NetworkInterfaceResponse'
    delete:
      tags:
      - Network Interfaces
      summary: Delete network interface
      description: Deletes a network interface on a controller.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Network Interfaces
      summary: Modify network interface
      description: Modifies a network interface on a controller.
      parameters:
      - in: body
        name: network
        required: true
        schema:
          $ref: '#/definitions/NetworkInterfacePatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/NetworkInterfaceResponse'
  /api/2.42/hosts/space:
    get:
      tags:
      - Hosts
      summary: List host space information
      description: 'Displays provisioned size and physical storage consumption data
        for each host.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourceSpaceNoIdGetResponse'
  /api/2.42/certificate-groups:
    get:
      tags:
      - Certificate Groups
      summary: List certificate-groups
      description: 'Display all array certificate groups.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CertificateGroupGetResponse'
    post:
      tags:
      - Certificate Groups
      summary: Create certificate-groups
      description: 'Creates one or more certificate groups on the array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CertificateGroupResponse'
    delete:
      tags:
      - Certificate Groups
      summary: Delete certificate-groups
      description: 'Deletes one or more certificate groups from the array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
  /api/2.42/directory-services/local/groups/members:
    get:
      tags:
      - Directory Services
      summary: List local group memberships
      description: Displays a list of local group memberships.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Local_group_names'
      - $ref: '#/parameters/Group_sids'
      - $ref: '#/parameters/Group_gids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_sids'
      - $ref: '#/parameters/Local_member_ids'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LocalMembershipGetResponse'
    post:
      tags:
      - Directory Services
      summary: Create local group membership
      description: 'Creates a local group membership with a group.

        The `group_names`, `group_sids`, or `group_ids` parameter is required, but
        cannot be set

        together.

        '
      parameters:
      - in: body
        name: local-membership
        description: 'The `member_names`, `member_sids`, or `member_gids` parameter
          is required, but cannot be set

          together.

          '
        required: true
        schema:
          $ref: '#/definitions/LocalGroupMembershipPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Local_group_names'
      - $ref: '#/parameters/Group_sids'
      - $ref: '#/parameters/Group_gids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LocalMembershipResponse'
    delete:
      tags:
      - Directory Services
      summary: Delete local group membership
      description: 'Deletes one or more local group memberships.

        The `group_names`, `group_sids`, or `group_ids` parameter is required, but
        cannot be set

        together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Local_group_names'
      - $ref: '#/parameters/Group_sids'
      - $ref: '#/parameters/Group_gids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_sids'
      - $ref: '#/parameters/Local_member_ids'
      - $ref: '#/parameters/Member_types'
      responses:
        '200':
          description: OK
  /api/2.42/arrays/factory-reset-token:
    get:
      tags:
      - Arrays
      summary: List factory reset tokens
      description: Displays a list of tokens used to perform a factory reset on the
        array.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayFactoryResetTokenGetResponse'
    post:
      tags:
      - Arrays
      summary: Create a factory reset token
      description: 'Creates a token that can be used to perform a factory reset on
        the array.

        Factory reset tokens can only be created after the array has been prepared

        for reset (e.g., all volumes and snapshots must first be eradicated).

        After a token has been created, operations that would take the array out of

        the prepared state are disabled (e.g., creating volumes) until all

        tokens have been deleted.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayFactoryResetTokenResponse'
    delete:
      tags:
      - Arrays
      summary: Delete a factory reset token
      description: Deletes an existing token that could be used to perform a factory
        reset on the array.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
  /api/2.42/support:
    get:
      tags:
      - Support
      summary: List connection paths
      description: 'Displays connection paths between the current array and each connected
        array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SupportGetResponse'
    patch:
      tags:
      - Support
      summary: Create connection path
      description: 'Creates a connection path from the array to another array.

        '
      parameters:
      - in: body
        name: support
        required: true
        schema:
          $ref: '#/definitions/SupportPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SupportResponse'
  /api/2.42/arrays/eula:
    get:
      tags:
      - Arrays
      summary: List End User Agreement and signature
      description: Displays the End User Agreement and signature.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/EulaGetResponse'
    patch:
      tags:
      - Arrays
      summary: Modify signature on the End User Agreement
      description: Modifies the signature on the End User Agreement.
      parameters:
      - in: body
        name: eula
        required: false
        schema:
          $ref: '#/definitions/Eula'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/EulaResponse'
  /api/2.42/policies/snapshot/members:
    get:
      tags:
      - Policies
      summary: List snapshot policy members
      description: Displays a list of snapshot policy members.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberGetResponse'
    post:
      tags:
      - Policies
      summary: Create snapshot policies
      description: Creates a membership between one or more resources and a snapshot
        policy. Applicable resources are directories. The `policy_ids` or `policy_names`
        parameter is required, but cannot be set together.
      parameters:
      - in: body
        name: members
        required: true
        schema:
          $ref: '#/definitions/PolicyMemberPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberResponse'
    delete:
      tags:
      - Policies
      summary: Delete snapshot policies
      description: Deletes one or more snapshot policies from resources. The `policy_ids`
        or `policy_names` parameter is required, but cannot be set together. The `member_ids`
        or `member_names` parameter is required, but cannot be set together.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/software-versions:
    get:
      tags:
      - Software
      summary: List software versions
      description: 'Displays a list of software versions.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SoftwareVersionsGetResponse'
  /api/2.42/apps:
    get:
      tags:
      - Apps
      summary: List apps
      description: Displays a list of installed apps.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AppGetResponse'
    patch:
      tags:
      - Apps
      summary: Modify app
      description: 'Modifies an installed app or modifies Virtual Network Computing
        (VNC) access for apps that are

        being patched.

        '
      parameters:
      - in: body
        name: app
        required: true
        schema:
          $ref: '#/definitions/App'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AppResponse'
  /api/2.42/kmip/test:
    get:
      tags:
      - KMIP
      summary: Lists KMIP connection tests
      description: Displays communication data between a FlashArray and KMIP server.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/KmipTestResultGetResponse'
  /api/2.42/log-targets/syslog/test:
    get:
      tags:
      - Log Targets
      summary: List syslog server test results
      description: Displays syslog server test results, which indicate whether the
        syslog is working and configured correctly.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceGetResponse'
  /api/2.42/remote-pods:
    get:
      tags:
      - Remote Pods
      summary: List remote pods
      description: 'Displays a list of pods that that are on connected arrays but
        not stretched to this array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/On_'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemotePodsResponse'
  /api/2.42/policies/nfs/members:
    get:
      tags:
      - Policies
      summary: List NFS policy members
      description: 'Displays a list of NFS policy members.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberExportGetResponse'
    post:
      tags:
      - Policies
      summary: Create NFS policies
      description: 'Creates a membership between one or more resources and an NFS
        policy.

        Applicable resources are directories.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - in: body
        name: members
        required: true
        schema:
          $ref: '#/definitions/PolicyMemberExportPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberExportResponse'
    delete:
      tags:
      - Policies
      summary: Delete NFS policies
      description: 'Deletes one or more NFS policies from resources.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.

        The `member_ids` or `member_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/protection-groups/hosts:
    get:
      tags:
      - Protection Groups
      summary: List protection groups with host members
      description: Displays a list of protection groups that have host members.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdMemberGetResponse'
    post:
      tags:
      - Protection Groups
      summary: Create an action to add a host to a protection group
      description: 'Creates an action to add a host member to a protection group.

        Members that are already in the protection group are not affected.

        For asynchronous replication, only members of the same type can belong to
        a protection group.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` parameter represents the name of the host.

        The `group_names` and `member_names` parameters are required and must be set
        together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdMemberResponse'
    delete:
      tags:
      - Protection Groups
      summary: Delete a host from a protection group
      description: 'Deletes a host member from a protection group.

        After the member has been removed, it is no longer protected by the group.

        Any protection group snapshots that were taken before the member was removed

        will not be affected.

        Removing a member from a protection group does not delete the member from
        the array,

        and the member can be added back to the protection group at any time.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` parameter represents the name of the host.

        The `group_names` and `member_names` parameters are required and must be set
        together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
  /api/2.42/directory-services/roles/policies/management-access:
    get:
      tags:
      - Directory Services
      summary: List management access policies attached to a group to management access
        policy mapping
      description: 'Displays a list of management access policies that are attached
        to a group to management access

        policy mappings.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberCleanGetResponse'
    post:
      tags:
      - Directory Services
      summary: Create a membership between a group to management access policy mapping
        with one or more management access policies
      description: 'Creates a membership between a group to management access policy
        mapping with one or more

        management access policies. One of `member_ids` or `member_names` parameter
        is required.


        '
      parameters:
      - in: body
        name: policies
        required: true
        schema:
          $ref: '#/definitions/PolicyAssignmentPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberCleanResponse'
    delete:
      tags:
      - Directory Services
      summary: Delete a membership between a group to management access policy mapping
        and one or more management access policies
      description: 'Deletes a membership between a group to management access policy
        mapping with one or more

        management access policies. One of `policy_ids` or `policy_names` is required,
        and one of

        `member_ids` or `member_names` is required.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/policies/smb:
    get:
      tags:
      - Policies
      summary: List SMB policies
      description: 'Displays a list of SMB policies.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicySmbGetResponse'
    post:
      tags:
      - Policies
      summary: Create SMB policies
      description: 'Creates one or more SMB policies.

        To copy a policy, set one of either `source_names` or `source_ids`.

        Each policy can only have one entry.

        To create a policy from scratch, policy body is required.

        To create an SMB policy with access based enumeration enabled,

        set `access_based_enumeration_enabled=true`.


        '
      parameters:
      - in: body
        name: policy
        required: false
        schema:
          $ref: '#/definitions/PolicySmbPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicySmbResponse'
    delete:
      tags:
      - Policies
      summary: Delete SMB policies
      description: 'Deletes one or more SMB policies.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Policies
      summary: Modify SMB policies
      description: 'Modifies one or more SMB policies.

        To enable a policy, set `enabled=true`. To disable a policy, set `enabled=false`.

        To enable access based enumeration, set `access_based_enumeration_enabled=true`.

        To disable access based enumeration, set `access_based_enumeration_enabled=false`.

        To rename a policy, set `name` to the new name.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - in: body
        name: policy
        required: true
        schema:
          $ref: '#/definitions/PolicySmbPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicySmbResponse'
  /api/2.42/software-patches/catalog:
    get:
      tags:
      - Software
      summary: List available software patches
      description: 'Displays a list of available software patches.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SoftwarePatchesGetResponse'
  /api/2.42/hosts/performance/by-array:
    get:
      tags:
      - Hosts
      summary: List host performance data by array
      description: 'Displays real-time and historical performance data, real-time
        latency data, and average I/O size

        data. The data is displayed by total size across all hosts on each array and
        by individual host

        on each array. The displayed data represents the volumes that are connected
        to a host on the

        current array and the volumes that are connected to a host on any remote arrays
        that are visible

        to the current array.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourcePerformanceNoIdByArrayGetResponse'
  /api/2.42/arrays/cloud-config/supported-configs:
    get:
      tags:
      - Arrays
      summary: List CBS array supported upgrade target configs
      description: 'Displays the list of supported configurations to which the CBS
        array can be upgraded.

        Requests for non-CBS arrays will receive an error response.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CloudConfigSupportedConfigsGetResponse'
  /api/2.42/snmp-agents:
    get:
      tags:
      - SNMP Agents
      summary: List SNMP agent
      description: Displays the SNMP agent name and protocol attributes.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SnmpAgentGetResponse'
    patch:
      tags:
      - SNMP Agents
      summary: Modify SNMP agent
      description: Modifies the name or the protocol attributes of the SNMP agent.
      parameters:
      - in: body
        name: snmp-agent
        required: true
        schema:
          $ref: '#/definitions/SnmpAgentPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: Returns the newly updated SNMP agent object.
          schema:
            $ref: '#/definitions/SnmpAgentResponse'
  /api/2.42/host-groups/hosts:
    get:
      tags:
      - Host Groups
      summary: List host groups that are associated with hosts
      description: 'Displays a list of host groups that are associated with hosts.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdAllGetResponse'
    post:
      tags:
      - Host Groups
      summary: Create a membership to a host group
      description: 'Create a membership between a host and a host group. Adding a
        membership to a host group

        automatically connects the host to all volumes associated with the group.
        Multiple hosts can be

        belong to a host group, but a host can only belong to one host group. Hosts
        can be added to host

        groups at any time. The `group_names` and `member_names` parameters are required
        and must be set

        together, and only one host group can be specified at a time.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdAllResponse'
    delete:
      tags:
      - Host Groups
      summary: Delete a membership from a host group
      description: 'Deletes a membership from a host group. Removing a membership
        from a host group automatically

        disconnects the host from all volumes associated with the group.

        Hosts can be removed from host groups at any time.

        The `group_names` and `member_names` parameters are required

        and must be set together, and only one host group can be specified at a time.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
  /api/2.42/smtp-servers:
    get:
      tags:
      - SMTP
      summary: List SMTP server attributes
      description: Displays SMTP server attributes. Values include `user_name`, `password`,
        `relay_host`, `sender_domain`, `sender_username`, `subject_prefix`, `body_prefix`,
        `encryption_mode`, and `name`.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SmtpServerGetResponse'
    patch:
      tags:
      - SMTP
      summary: Modify SMTP server attributes
      description: Modifies SMTP server attributes. Values include `user_name`, `password`,
        `relay_host`, `sender_domain`, `sender_username`, `subject_prefix`, `body_prefix`,
        `encryption_mode`, and `name`.
      parameters:
      - in: body
        name: smtp
        required: true
        schema:
          $ref: '#/definitions/SmtpServer'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SmtpServerResponse'
  /api/2.42/maintenance-windows:
    get:
      tags:
      - Maintenance Windows
      summary: List maintenance window details
      description: Displays maintenance window details, including start time, end
        time, and maintenance type.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MaintenanceWindowsGetResponse'
    post:
      tags:
      - Maintenance Windows
      summary: Create a maintenance window
      description: 'Creates a maintenance window that suppresses alerts for a specified
        period of time.

        A maintenance window can be manually closed at any time.

        The `names` and `timeout` parameters are required. Set the `names` parameter
        to `environment`.

        '
      parameters:
      - in: body
        name: maintenance-window
        required: true
        schema:
          $ref: '#/definitions/MaintenanceWindowPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MaintenanceWindowsResponse'
    delete:
      tags:
      - Maintenance Windows
      summary: Delete maintenance window
      description: 'Deletes an open maintenance window before its scheduled end (`expire`)
        time.

        The `names` parameter is required and must be set to `environment`.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
  /api/2.42/pods/performance/by-array:
    get:
      tags:
      - Pods
      summary: List pod performance data by array
      description: 'Displays real-time and historical performance data, real-time
        latency data,

        and average I/O size data.

        The data is displayed as a total across all pods on the local array and by
        individual pod.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Perf_protocol_type'
      - $ref: '#/parameters/Perf_protocol_group'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodPerformanceByArrayGetResponse'
  /api/2.42/dns:
    get:
      tags:
      - DNS
      summary: List DNS parameters
      description: 'Displays the current DNS configurations and their parameters

        including domain suffix, the list of DNS name server IP addresses,

        and the list of services that DNS parameters apply to.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DnsGetResponse'
    post:
      tags:
      - DNS
      summary: Create DNS configuration
      description: 'Creates new DNS configuration with parameters including the domain
        suffix,

        the list of DNS name server IP addresses, and the list of services that DNS
        parameters

        apply to.

        '
      parameters:
      - in: body
        name: dns
        required: true
        schema:
          $ref: '#/definitions/DnsPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DnsResponse'
    delete:
      tags:
      - DNS
      summary: Delete DNS configuration
      description: 'Deletes DNS configuration identified by configuration name.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - DNS
      summary: Modify DNS parameters
      description: 'Modifies the DNS parameters of an array, including the domain
        suffix, the list of

        DNS name server IP addresses, and the list of services that DNS parameters
        apply to.

        If there is no DNS configuration beforehand new DNS configuration with ''default''

        name is created. If more than one DNS configuration exists, `name` has to
        be specified

        to identify the DNS configuration to be modified.

        '
      parameters:
      - in: body
        name: dns
        required: true
        schema:
          $ref: '#/definitions/DnsPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DnsResponse'
  /api/2.42/syslog-servers:
    get:
      tags:
      - Syslog
      summary: List syslog servers
      description: 'Displays a list of configured syslog servers.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SyslogServerGetResponse'
    post:
      tags:
      - Syslog
      summary: Create syslog server
      description: 'Creates a new syslog server.

        '
      parameters:
      - in: body
        name: syslog-server
        required: true
        schema:
          $ref: '#/definitions/SyslogServer'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: Displays the newly configured syslog server.
          schema:
            $ref: '#/definitions/SyslogServerResponse'
    delete:
      tags:
      - Syslog
      summary: Delete syslog server
      description: 'Deletes a configured syslog server and stops forwarding syslog
        messages.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Syslog
      summary: Modify syslog server
      description: 'Modifies the URI and services of a configured syslog server.

        '
      parameters:
      - in: body
        name: syslog-server
        required: true
        schema:
          $ref: '#/definitions/SyslogServer'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: Displays the newly updated syslog server.
          schema:
            $ref: '#/definitions/SyslogServerResponse'
  /api/2.42/support-diagnostics/details:
    get:
      tags:
      - Support
      summary: List diagnostics details
      description: 'Displays the diagnostics tests details.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SupportDiagnosticsDetailsGetResponse'
  /api/2.42/volume-snapshots/tags:
    get:
      tags:
      - Volume Snapshots
      summary: List tags
      description: Displays the list of tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Namespaces'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Resource_destroyed'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagGetResponse'
    delete:
      tags:
      - Volume Snapshots
      summary: Delete tags
      description: Deletes specified tags.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Keys'
      - $ref: '#/parameters/Namespaces_delete'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
  /api/2.42/directories/policies/smb:
    get:
      tags:
      - Directories
      summary: List SMB policies attached to a directory
      description: Displays a list of SMB policies that are attached to directories.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberExportGetResponse'
    post:
      tags:
      - Directories
      summary: Create a membership between a directory and one or more SMB policies
      description: 'Creates a membership between a directory and one or more SMB policies.

        The `member_ids` or `member_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - in: body
        name: policies
        required: true
        schema:
          $ref: '#/definitions/DirectoryPolicyExportPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberExportResponse'
    delete:
      tags:
      - Directories
      summary: Delete a membership between a directory and one or more SMB policies
      description: 'Deletes a membership between a directory and one or more SMB policies.

        The `policy_ids` or `policy_names` parameter is required, but cannot be set
        together.

        The `member_ids` or `member_names` parameter is required, but cannot be set
        together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/pods/tags/batch:
    put:
      tags:
      - Pods
      summary: Update tags
      description: Updates tags for all specified objects.
      parameters:
      - in: body
        name: tag
        description: A list of tags to be created or, if already existing, updated.
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/Tag'
          uniqueItems: true
          minItems: 1
          maxItems: 30
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagResponse'
  /api/2.42/pod-replica-links:
    get:
      tags:
      - Pod Replica Links
      summary: List pod replica links
      description: Displays the list of pod replica links that are configured between
        arrays.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Local_pod_ids'
      - $ref: '#/parameters/Local_pod_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Remote_ids'
      - $ref: '#/parameters/Remote_names'
      - $ref: '#/parameters/Remote_pod_ids'
      - $ref: '#/parameters/Remote_pod_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodReplicaLinkGetResponse'
    post:
      tags:
      - Pod Replica Links
      summary: Create pod replica links
      description: Creates pod replica links between two arrays. The `local_pod_names`
        and `remote_pod_names` are required. Valid values are `replicating`, `baselining`,
        `paused`, `unhealthy`, `quiescing`, and `quiesced`. A status of `replicating`
        indicates that the source array is replicating to the target array. A status
        of `baselining` indicates that the the initial version of the dataset is being
        sent. During this phase, you cannot promote the target pod. In addition, changing
        the link direction might trigger the `baselining` status to recur. A status
        of `paused ` indicates that data transfer between objects has stopped. A status
        of `unhealthy` indicates that the link is currently unhealthy and customers
        must perform some health checks to determine the cause. A status of `quiescing`
        indicates that the source pod is not accepting new write requests but the
        most recent changes to the source have not arrived on the target. A status
        of `quiesced` indicates that the source pod has been demoted and all changes
        have been replicated to the target pod.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Local_pod_ids'
      - $ref: '#/parameters/Local_pod_names'
      - $ref: '#/parameters/Remote_ids'
      - $ref: '#/parameters/Remote_names'
      - $ref: '#/parameters/Remote_pod_ids'
      - $ref: '#/parameters/Remote_pod_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodReplicaLinkResponse'
    delete:
      tags:
      - Pod Replica Links
      summary: Delete pod replica links
      description: Deletes pod replica links. The `local_pod_names` and `remote_pod_names`
        are required. Valid values are `replicating`, `baselining`, `paused`, `unhealthy`,
        `quiescing`, and `quiesced`. A status of `replicating` indicates that the
        source array is replicating to the target array. A status of `baselining`
        indicates that the the initial version of the dataset is being sent. During
        this phase, you cannot promote the target pod. In addition, changing the link
        direction might trigger the `baselining` status to recur. A status of `paused
        ` indicates that data transfer between objects has stopped. A status of `unhealthy`
        indicates that the link is currently unhealthy and customers must perform
        some health checks to determine the cause. A status of `quiescing` indicates
        that the source pod is not accepting new write requests but the most recent
        changes to the source have not arrived on the target. A status of `quiesced`
        indicates that the source pod has been demoted and all changes have been replicated
        to the target pod.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Local_pod_ids'
      - $ref: '#/parameters/Local_pod_names'
      - $ref: '#/parameters/Remote_pod_ids'
      - $ref: '#/parameters/Remote_pod_names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Pod Replica Links
      summary: Modify pod replica links
      description: Modifies pod replica links. The `local_pod_names` and `remote_pod_names`
        are required. Valid values are `replicating`, `baselining`, `paused`, `unhealthy`,
        `quiescing`, and `quiesced`. A status of `replicating` indicates that the
        source array is replicating to the target array. A status of `baselining`
        indicates that the the initial version of the dataset is being sent. During
        this phase, you cannot promote the target pod. In addition, changing the link
        direction might trigger the `baselining` status to recur. A status of `paused
        ` indicates that data transfer between objects has stopped. A status of `unhealthy`
        indicates that the link is currently unhealthy and customers must perform
        some health checks to determine the cause. A status of `quiescing` indicates
        that the source pod is not accepting new write requests but the most recent
        changes to the source have not arrived on the target. A status of `quiesced`
        indicates that the source pod has been demoted and all changes have been replicated
        to the target pod.
      parameters:
      - in: body
        name: pod-replica-link
        required: true
        schema:
          $ref: '#/definitions/PodReplicaLinkPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Local_pod_ids'
      - $ref: '#/parameters/Local_pod_names'
      - $ref: '#/parameters/Remote_ids'
      - $ref: '#/parameters/Remote_names'
      - $ref: '#/parameters/Remote_pod_ids'
      - $ref: '#/parameters/Remote_pod_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodReplicaLinkResponse'
  /api/2.42/policies/snapshot/rules:
    get:
      tags:
      - Policies
      summary: List snapshot policy rules
      description: Displays a list of snapshot policy rules.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleSnapshotGetResponse'
    post:
      tags:
      - Policies
      summary: Create snapshot policy rules
      description: Creates one or more snapshot policy rules. The `policy_ids` or
        `policy_names` parameter is required, but cannot be set together.
      parameters:
      - in: body
        name: rules
        required: true
        schema:
          $ref: '#/definitions/PolicyRuleSnapshotPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleSnapshotResponse'
    delete:
      tags:
      - Policies
      summary: Delete snapshot policy rules
      description: Deletes one or more snapshot policy rules. The `policy_ids` or
        `policy_names` parameter is required, but cannot be set together.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/pods:
    get:
      tags:
      - Pods
      summary: List pods
      description: 'Displays a list of pods that are stretched to this array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodGetResponse'
    post:
      tags:
      - Pods
      summary: Create a pod
      description: 'Creates a pod on the local array.

        Each pod must be given a unique name across the arrays to which they are stretched.

        A pod cannot be stretched to an array that already contains a pod with the
        same name.

        After a pod has been created, add volumes and protection groups,

        and then stretch the pod to another connected array.

        '
      parameters:
      - in: body
        name: pod
        required: true
        schema:
          $ref: '#/definitions/PodPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Allow_throttle_operation'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodResponse'
    delete:
      tags:
      - Pods
      summary: Delete a pod
      description: 'Deletes a pod that has been destroyed and is pending eradication.

        Eradicated pods cannot be recovered. Pods are destroyed using the PATCH method.

        The `ids` or `names` parameter is required, but they cannot be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Eradicate_contents'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Pods
      summary: Modify a pod
      description: 'Modifies pod details.

        '
      parameters:
      - in: body
        name: pod
        required: true
        schema:
          $ref: '#/definitions/PodPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Abort_quiesce'
      - $ref: '#/parameters/Quiesce'
      - $ref: '#/parameters/Skip_quiesce'
      - $ref: '#/parameters/Promote_from'
      - $ref: '#/parameters/Destroy_contents'
      - $ref: '#/parameters/From_member_ids'
      - $ref: '#/parameters/From_member_names'
      - $ref: '#/parameters/To_member_ids'
      - $ref: '#/parameters/To_member_names'
      - $ref: '#/parameters/Move_with_host_names'
      - $ref: '#/parameters/Move_with_host_group_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodResponse'
  /api/2.42/directory-services/test:
    get:
      tags:
      - Directory Services
      summary: List directory services test results
      description: Displays the directory services test and displays the results.
        The test verifies that URIs can be resolved and that the array can bind and
        query the tree using the bind user credentials. The test also verifies that
        the array can find all configured groups to ensure the common names and group
        base are correctly configured.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names_required'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceResponse'
  /api/2.42/fleets/fleet-key:
    get:
      tags:
      - Fleets
      summary: Get fleet key information
      description: 'Displays the time remaining on the fleet key.

        Note, fleet key itself is not displayed, since that is only available at time
        of fleet key

        creation.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/FleetKeyGetResponse'
    post:
      tags:
      - Fleets
      summary: Create a fleet key
      description: 'Creates a fleet key allowing an array to be added to a fleet.

        This is the only time the fleet key is displayed, it cannot be retrieved subsequently.

        Note, creation of a subsequent key invalidates all keys created earlier.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/FleetKeyResponse'
  /api/2.42/directory-snapshots:
    get:
      tags:
      - Directory Snapshots
      summary: List directory snapshots
      description: 'Displays a list of directory snapshots, including those pending
        eradication.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectorySnapshotGetResponse'
    post:
      tags:
      - Directory Snapshots
      summary: Create directory snapshot
      description: 'Creates a snapshot of the contents of a directory.

        The `source_ids` or `source_names` parameter is required, but they cannot
        be set together.


        '
      parameters:
      - in: body
        name: directory-snapshot
        required: true
        schema:
          $ref: '#/definitions/DirectorySnapshotPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectorySnapshotResponse'
    delete:
      tags:
      - Directory Snapshots
      summary: Delete directory snapshot
      description: 'Deletes a directory snapshot that has been destroyed and is pending
        eradication.

        Eradicated directory snapshots cannot be recovered.

        Directory snapshots are destroyed by using the PATCH method.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Directory Snapshots
      summary: Modify directory snapshot
      description: 'Modifies a directory snapshot. You can destroy, recover, or update
        the policy or time remaining

        of a directory snapshot.

        To destroy a directory snapshot, set `destroyed=true`.

        To recover a directory snapshot that has been destroyed and is pending eradication,

        set `destroyed=false`.

        To rename a directory snapshot, set `name` to the new name.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - in: body
        name: directory-snapshot
        required: true
        schema:
          $ref: '#/definitions/DirectorySnapshotPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectorySnapshotResponse'
  /api/2.42/policies/quota/members:
    get:
      tags:
      - Policies
      summary: List quota policy members
      description: Displays a list of quota policy members.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberGetResponse'
    post:
      tags:
      - Policies
      summary: Create a membership between a managed directory and a quota policy
      description: Creates a membership between one or more managed directories and
        a quota policy. The `policy_ids` or `policy_names` parameter is required,
        but cannot be set together.
      parameters:
      - in: body
        name: members
        required: true
        schema:
          $ref: '#/definitions/PolicyMemberPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Ignore_usage'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberResponse'
    delete:
      tags:
      - Policies
      summary: Delete membership between quota policies and managed directories
      description: Deletes a membership between one or more quota policies and managed
        directories. The `policy_ids` or `policy_names` parameter is required, but
        cannot be set together. The `member_ids` or `member_names` parameter is required,
        but cannot be set together.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/policies/audit-file:
    get:
      tags:
      - Policies
      summary: List audit policies
      description: 'Displays a list of audit policies.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyAuditFileGetResponse'
    post:
      tags:
      - Policies
      summary: Create audit policies
      description: 'Creates one or more audit policies.

        To copy a policy, the `source_names` or `source_ids` parameter is required,

        but they cannot be set together.

        To create a policy from scratch, policy body is required.


        '
      parameters:
      - in: body
        name: policy
        required: false
        schema:
          $ref: '#/definitions/PolicyAuditFilePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyAuditFileResponse'
    delete:
      tags:
      - Policies
      summary: Delete audit policies
      description: 'Deletes one or more audit policies.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Policies
      summary: Modify audit policies
      description: 'Modifies one or more audit policies. To enable a policy, set `enabled=true`.

        To disable a policy, set `enabled=false`.

        To rename a policy, set `name` to the new name.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - in: body
        name: policy
        required: true
        schema:
          $ref: '#/definitions/PolicyAuditFilePatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyAuditFileResponse'
  /api/2.42/volumes/performance:
    get:
      tags:
      - Volumes
      summary: List volume performance data
      description: 'Displays real-time and historical performance data, real-time
        latency data, and average I/O

        sizes for each volume and and as a total of all volumes across the entire
        array.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourcePerformanceGetResponse'
  /api/2.42/volumes/tags/batch:
    put:
      tags:
      - Volumes
      summary: Update tags
      description: Updates tags.
      parameters:
      - in: body
        name: tag
        description: A list of tags to be created or, if one already exists, updated.
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/Tag'
          uniqueItems: true
          minItems: 1
          maxItems: 30
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagResponse'
  /api/api_version:
    get:
      tags:
      - Authorization
      summary: List available API versions
      description: 'Returns a list of available API versions. No authentication is
        required to access this endpoint.

        '
      parameters:
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/Api_versionResponse'
  /api/2.42/arrays/erasures:
    get:
      tags:
      - Arrays
      summary: List factory reset details
      description: 'Displays a list of factory reset processes.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayErasureGetResponse'
    post:
      tags:
      - Arrays
      summary: Create a process for factory reset
      description: 'Creates the initiation of the factory reset process


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Eradicate_all_data_required'
      - $ref: '#/parameters/Preserve_configuration_data_required'
      - $ref: '#/parameters/Skip_phonehome_check'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayErasureResponse'
    delete:
      tags:
      - Arrays
      summary: Delete factory reset
      description: 'Deletes the factory reset process when it is `failed`.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Arrays
      summary: Update factory reset
      description: 'Modifies the factory reset process to finalize.


        '
      parameters:
      - in: body
        name: erasure_patch
        required: false
        schema:
          $ref: '#/definitions/ArrayErasurePatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Delete_sanitization_certificate_required'
      - $ref: '#/parameters/Eradicate_all_data_required'
      - $ref: '#/parameters/Finalize_array_erasure_required'
      - $ref: '#/parameters/Reinstall_image_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayErasureResponse'
  /api/2.42/sessions:
    get:
      tags:
      - Sessions
      summary: List session data
      description: Displays session data for user login events performed in the Purity//FA
        GUI, CLI, and REST API.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SessionGetResponse'
  /api/2.42/syslog-servers/settings:
    get:
      tags:
      - Syslog
      summary: List syslog settings
      description: Displays syslog settings. Values include `continuation_token`,
        `items`, `more_items_remaining`, and `total_item_count`.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SyslogServerSettingsGetResponse'
    patch:
      tags:
      - Syslog
      summary: Modify syslog settings
      description: Modifies syslog settings. Values include `continuation_token`,
        `items`, `more_items_remaining`, and `total_item_count`.
      parameters:
      - in: body
        name: syslog-server-settings
        required: true
        schema:
          $ref: '#/definitions/SyslogServerSettings'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SyslogServerSettingsResponse'
  /api/2.42/certificates/uses:
    get:
      tags:
      - Certificates
      summary: List certificates uses
      description: 'Displays the usage and associations of certificates.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CertificateUseGetResponse'
  /api/2.42/volumes/batch:
    post:
      tags:
      - Volumes
      summary: Create or copy volumes and upsert tags
      description: 'Creates or copies a list of virtual storage volumes. Volumes of
        different

        sizes, vgroups, and tag upsertions can be copied this way.

        '
      parameters:
      - in: body
        name: volume
        description: 'A list of volumes to be created or copied.

          '
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/VolumeBatchPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Allow_throttle_operation'
      - $ref: '#/parameters/Overwrite'
      - $ref: '#/parameters/With_default_protection'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VolumesBatchResponse'
  /api/2.42/software-check:
    get:
      tags:
      - Software
      summary: List software check tasks
      description: Displays a list of software check tasks.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Software_versions'
      - $ref: '#/parameters/Software_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SoftwareChecksGetResponse'
    post:
      tags:
      - Software
      summary: Create a software check task
      description: 'Creates a software check task. If there are no previously queued
        or running

        software check tasks, the new task immediately starts. Otherwise, it is queued
        and starts once

        all previously queued or running software check tasks finish. To create a
        task, use a software

        name and version.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Software_versions'
      - $ref: '#/parameters/Software_names'
      responses:
        '200':
          description: 'The software check record was created successfully.

            '
          schema:
            $ref: '#/definitions/SoftwareChecksResponse'
    delete:
      tags:
      - Software
      summary: Delete a software check task
      description: 'Deletes a software check task specified by software name and version.

        The check task must be queued. If the check task is running or passed/failed

        then the task cannot be deleted.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Software_versions'
      - $ref: '#/parameters/Software_names'
      responses:
        '200':
          description: OK
  /api/2.42/admins/policies/management-access:
    get:
      tags:
      - Administrators
      summary: List management access policies attached to administrators
      description: 'Displays a list of management access policies that are attached
        to administrators.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberCleanGetResponse'
    post:
      tags:
      - Administrators
      summary: Create a membership between an administrator with one or more management
        access policies.
      description: 'Creates a membership between an administrator with one or more
        management access policies.

        One of `member_ids` or `member_names` parameter is required.


        '
      parameters:
      - in: body
        name: policies
        required: true
        schema:
          $ref: '#/definitions/PolicyAssignmentPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberCleanResponse'
    delete:
      tags:
      - Administrators
      summary: Delete a membership between an administrator and one or more management
        access policies
      description: 'Deletes a membership between an administrator with one or more
        management access policies.

        One of `policy_ids` or `policy_names` is required, and one of `member_ids`
        or `member_names`

        is required.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/volumes/volume-groups:
    get:
      tags:
      - Volumes
      summary: List volumes that are in volume groups
      description: 'Displays a list of volumes that are in a volume group.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberGetResponse'
    parameters:
    - $ref: '#/parameters/Group_names'
    - $ref: '#/parameters/Member_names'
  /api/2.42/hosts/protection-groups:
    get:
      tags:
      - Hosts
      summary: List hosts that are members of protection groups
      description: 'Displays a list of host members that belong to one or more protection
        groups.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdMemberGetResponse'
    post:
      tags:
      - Hosts
      summary: Create a host
      description: 'Creates a host member and adds it to a protection group.

        Members that are already in the protection group are not affected.

        For asynchronous replication, only members of the same type can belong to
        a protection group.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` parameter represents the name of the host.

        The `group_names` and `member_names` parameters are required and must be set
        together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdMemberResponse'
    delete:
      tags:
      - Hosts
      summary: Delete a host from a protection group
      description: 'Deletes a host member from a protection group.

        After the member has been removed, it is no longer protected by the group.

        Any protection group snapshots that were taken before the member was removed
        are not affected.

        Removing a member from a protection group does not delete the member from
        the array,

        and the member can be added back to the protection group at any time.

        The `group_names` parameter represents the name of the protection group, and

        the `member_names` parameter represents the name of the host.

        The `group_names` and `member_names` parameters are required and must be set
        together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
  /api/2.42/volumes/batch/test:
    post:
      tags:
      - Volumes
      summary: Create an attempt to copy volumes and upsert tags
      description: 'Creates an attempt to copy virtual storage volumes of different
        sizes in different vgroups and

        also upserts tags, without actually creating them, to test if they can be
        successfully created.

        Only available for creating volumes via a copy, not direct volume creation.


        '
      parameters:
      - in: body
        name: volume
        description: 'A list of volumes to be copied.

          '
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/VolumeBatchPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Allow_throttle_operation'
      - $ref: '#/parameters/Overwrite'
      - $ref: '#/parameters/With_default_protection'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceWithIdPostResponse'
  /api/2.42/directories/performance:
    get:
      tags:
      - Directories
      summary: List directory performance data
      description: 'Displays real-time and historical performance data, real-time
        latency data, and average I/O

        sizes for each directory and as a total of all directories across the entire
        array.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Perf_protocol_type'
      - $ref: '#/parameters/Perf_protocol_group'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryPerformanceGetResponse'
  /api/2.42/snmp-agents/mib:
    get:
      tags:
      - SNMP Agents
      summary: List SNMP agent MIB text
      description: Displays the SNMP MIB text.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SnmpAgentMibGetResponse'
  /api/2.42/remote-arrays:
    get:
      tags:
      - Remote Arrays
      summary: List remote arrays
      description: 'Dispalys arrays in the current fleet, as well as arrays that the
        current array has existing

        replication connections to.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - name: current_fleet_only
        in: query
        description: 'If current_fleet_only is specified, then results will only show
          arrays from the same fleet to

          which the current array is a member of.

          '
        required: false
        type: boolean
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RemoteArraysResponse'
  /api/2.42/directory-services/local/groups:
    get:
      tags:
      - Directory Services
      summary: List local groups
      description: Displays a list of local groups.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Gids'
      - $ref: '#/parameters/Sids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LocalGroupGetResponse'
    post:
      tags:
      - Directory Services
      summary: Create local group
      description: Creates a local group.
      parameters:
      - in: body
        name: local-group
        required: false
        schema:
          $ref: '#/definitions/LocalGroupPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LocalGroupResponse'
    delete:
      tags:
      - Directory Services
      summary: Delete local groups
      description: 'Deletes one or more local groups.

        The `gids`, `names`, or `sids` parameter is required, but cannot be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Sids'
      - $ref: '#/parameters/Gids'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Directory Services
      summary: Modify local groups
      description: Modifies local groups.
      parameters:
      - in: body
        name: local-group
        required: true
        schema:
          $ref: '#/definitions/LocalGroup'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Sids'
      - $ref: '#/parameters/Gids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/LocalGroupResponse'
  /api/2.42/arrays/cloud-config:
    get:
      tags:
      - Arrays
      summary: List CBS array configuration status
      description: 'Displays the configuration status of the CBS array, including
        the progress of the

        configuration upgrade. Requests for non-CBS arrays will receive an error response.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CloudConfigGetResponse'
    patch:
      tags:
      - Arrays
      summary: Modify CBS array configuration
      description: 'Modifies the configuration of the CBS array. This should trigger
        a backend

        process that will upgrade the CBS array configuration. The array can be

        upgraded only to models defined by the supported configurations endpoint.
        When an

        existing array upgrade request is in progress, any new request to upgrade

        the array will receive an error response. For non-CBS arrays, this

        feature is not supported and requests will receive an error response.

        '
      parameters:
      - in: body
        name: config
        description: The requested configuration of the CBS array hardware.
        required: true
        schema:
          $ref: '#/definitions/CloudConfigPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CloudConfigPatchResponse'
  /api/2.42/realms:
    get:
      tags:
      - Realms
      summary: List realms
      description: 'Displays a list of realms.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RealmGetResponse'
    post:
      tags:
      - Realms
      summary: Create realms
      description: 'Creates realms on the local array.

        Each realm must be given a name that is unique across the connected arrays.

        '
      parameters:
      - in: body
        name: realm
        required: false
        schema:
          $ref: '#/definitions/RealmPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RealmResponse'
    delete:
      tags:
      - Realms
      summary: Delete realms
      description: 'Deletes realms that have been destroyed and are pending eradication.

        Eradicated realms cannot be recovered. Realms are destroyed using the PATCH
        method.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Eradicate_contents'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Realms
      summary: Modify realms
      description: 'Modifies realm details.

        '
      parameters:
      - in: body
        name: realm
        required: true
        schema:
          $ref: '#/definitions/RealmPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Destroy_contents'
      - $ref: '#/parameters/Container_ignore_usage'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/RealmResponse'
  /api/2.42/policies:
    get:
      tags:
      - Policies
      summary: List policies
      description: 'Displays a list of policies.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyGetResponse'
  /api/2.42/virtual-machine-snapshots:
    get:
      tags:
      - Virtual Machines
      summary: List Virtual Machine Snapshots
      description: 'Displays a list of virtual machine snapshots, including those
        pending eradication.

        If `vm_type` is `vvol`, the `recovery_context` in the response will represent

        a protection group snapshot containing the virtual machine. This must be used
        as

        the `source` in the `POST /virtual-machine` method when recovering or overwriting
        a

        virtual machine from a snapshot.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - name: vm_type
        in: query
        description: 'The type of virtual machine.

          The only valid value is `vvol`.

          '
        required: false
        type: string
      - name: vm_ids
        in: query
        description: 'Performs the operation on the unique virtual machine IDs specified.

          Enter multiple resource IDs in a comma-separated format.

          '
        required: false
        type: array
        items:
          type: string
        collectionFormat: csv
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VirtualMachineGetResponse'
  /api/2.42/alerts/rules/catalog:
    get:
      tags:
      - Alerts
      summary: List available customizable alert codes
      description: 'Displays a list of available customizable alert codes.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Alert_rules_code_list'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AlertRulesCatalogGetResponse'
  /api/2.42/log-targets/syslog:
    get:
      tags:
      - Log Targets
      summary: List syslog servers
      description: 'Displays a list of configured syslog servers.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SyslogServerGetResponse'
    post:
      tags:
      - Log Targets
      summary: Create syslog server
      description: 'Creates a new syslog server.

        '
      parameters:
      - in: body
        name: syslog-server
        required: true
        schema:
          $ref: '#/definitions/SyslogServer'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: Displays the newly configured syslog server.
          schema:
            $ref: '#/definitions/SyslogServerResponse'
    delete:
      tags:
      - Log Targets
      summary: Delete syslog server
      description: 'Deletes a configured syslog server and stop forwarding syslog
        messages.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Log Targets
      summary: Modify syslog server
      description: 'Modifies the URI and services of a configured syslog server.

        '
      parameters:
      - in: body
        name: syslog-server
        required: true
        schema:
          $ref: '#/definitions/SyslogServer'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: Displays the newly updated syslog server.
          schema:
            $ref: '#/definitions/SyslogServerResponse'
  /api/2.42/array-connections/path:
    get:
      tags:
      - Array Connections
      summary: List connection path
      description: 'Displays the connection path from the array to which the connection
        was made.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Remote_ids'
      - $ref: '#/parameters/Remote_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayConnectionPathGetResponse'
  /api/2.42/certificates:
    get:
      tags:
      - Certificates
      summary: List certificate attributes
      description: 'Displays certificate attributes.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CertificateGetResponse'
    post:
      tags:
      - Certificates
      summary: Create certificate
      description: 'Creates a certificate object and specifies the valid time period
        and organization

        details of the certificate. A certificate can be imported or manually configured.

        '
      parameters:
      - in: body
        name: certificate
        required: true
        schema:
          $ref: '#/definitions/CertificatePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: Returns the newly created certificate object.
          schema:
            $ref: '#/definitions/CertificateResponse'
    delete:
      tags:
      - Certificates
      summary: Delete certificate
      description: 'Deletes a specific certificate object.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Certificates
      summary: Modify certificates
      description: 'Modifies certificate attributes. Modifying self-signed certificate
        attributes

        replaces the existing certificate with a new one, generated by Purity//FA,

        using the specified attributes.

        '
      parameters:
      - in: body
        name: certificate
        required: true
        schema:
          $ref: '#/definitions/CertificatePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Generate_new_key'
      responses:
        '200':
          description: Returns the newly updated certificate object.
          schema:
            $ref: '#/definitions/CertificateResponse'
  /api/2.42/network-interfaces/performance:
    get:
      tags:
      - Network Interfaces
      summary: List network performance statistics
      description: 'Displays network statistics, historical bandwidth, and error reporting
        for all

        specified network interfaces.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/NetworkInterfacePerformanceGetResponse'
  /api/2.42/directory-services:
    get:
      tags:
      - Directory Services
      summary: List directory services configuration
      description: 'Displays the directory services configuration settings for manageability.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryServiceGetResponse'
    patch:
      tags:
      - Directory Services
      summary: Modify directory services configuration
      description: 'Modifies the directory service configuration.

        '
      parameters:
      - in: body
        name: directory-service
        required: true
        schema:
          $ref: '#/definitions/DirectoryService'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryServiceResponse'
  /api/2.42/remote-protection-group-snapshots/test:
    post:
      tags:
      - Remote Protection Group Snapshots
      summary: Create an attempt to take remote protection group snapshot
      description: 'Creates an attempt to take a remote protection group snapshot
        without

        actually taking it, to test if the snapshot can be successfully taken.

        '
      parameters:
      - in: body
        name: remote-protection-group-snapshot
        required: false
        schema:
          $ref: '#/definitions/RemoteProtectionGroupSnapshotPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Apply_retention'
      - $ref: '#/parameters/Convert_source_to_baseline'
      - $ref: '#/parameters/For_replication'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Replicate'
      - $ref: '#/parameters/Replicate_now'
      - $ref: '#/parameters/Allow_throttle_snapshot'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/OnSingle'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceWithIdPostResponse'
  /api/2.42/sso/saml2/idps/test:
    get:
      tags:
      - SAML2 SSO
      summary: List existing SAML2 SSO configurations
      description: 'Displays the existing SAML2 SSO configurations in the array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceResponse'
    patch:
      tags:
      - SAML2 SSO
      summary: Modify provided SAML2 SSO configurations
      description: 'Modifies the provided SAML2 SSO configurations. If the configurations
        with the

        specified `ids` or `names` exist, the provided configurations will overwrite

        the existing configurations, but will not be persisted in the array.

        '
      parameters:
      - in: body
        name: idp
        required: true
        schema:
          $ref: '#/definitions/Saml2SsoPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourcePatchResponse'
  /api/2.42/policies/alert-watcher:
    get:
      tags:
      - Policies
      summary: List alert-watcher policies
      description: 'Displays a list of alert-watcher policies.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyGetResponse'
    post:
      tags:
      - Policies
      summary: Create alert-watcher policies
      description: 'Creates one or more alert-watcher policies.

        To copy a policy, the `source_names` or `source_ids` parameter is required,

        but they cannot be set together.

        To create a policy from scratch, policy body is required.


        '
      parameters:
      - in: body
        name: policy
        required: false
        schema:
          $ref: '#/definitions/PolicyPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyResponse'
    delete:
      tags:
      - Policies
      summary: Delete alert-watcher policies
      description: 'Deletes one or more alert-watcher policies.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Policies
      summary: Modify alert-watcher policies
      description: 'Modifies one or more alert-watcher policies. To enable a policy,
        set `enabled=true`.

        To disable a policy, set `enabled=false`.

        To rename a policy, set `name` to the new name.

        The `ids` or `names` parameter is required, but they cannot be set together.


        '
      parameters:
      - in: body
        name: policy
        required: true
        schema:
          $ref: '#/definitions/PolicyPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyResponse'
  /api/2.42/volume-groups/volumes:
    get:
      tags:
      - Volume Groups
      summary: List volume groups with volumes
      description: Displays a list of volume groups that contain volumes.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberGetResponse'
    parameters:
    - $ref: '#/parameters/Group_names'
    - $ref: '#/parameters/Member_names'
  /api/2.42/realms/tags/batch:
    put:
      tags:
      - Realms
      summary: Update tags
      description: 'Updates or creates tags for all specified objects.

        '
      parameters:
      - in: body
        name: tag
        description: A list of tags to be created or, if already existing, updated.
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/NonCopyableTag'
          uniqueItems: true
          minItems: 1
          maxItems: 30
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagResponse'
  /api/2.42/arrays/cloud-capacity:
    get:
      tags:
      - Arrays
      summary: List CBS array capacity status
      description: 'Displays the capacity status of the CBS array including the progress
        of the

        capacity update. Requests for non-CBS arrays will receive an error response.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CloudCapacityGetResponse'
    patch:
      tags:
      - Arrays
      summary: Modify CBS array capacity
      description: 'Modifies the raw capacity of the CBS array. This should trigger
        a backend

        process that will update the CBS array capacity. The array capacity can be

        updated only to values defined by the capacity steps endpoint.

        When an existing capacity update request is in progress, any new request to
        update

        of the capacity will receive an error response.

        For non-CBS arrays, this feature is not supported and requests will receive
        an error response.

        '
      parameters:
      - in: body
        name: capacity
        description: The requested capacity of the CBS array.
        required: true
        schema:
          $ref: '#/definitions/CloudCapacityStatus'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/CloudCapacityPatchResponse'
  /api/2.42/directories:
    get:
      tags:
      - Directories
      summary: List directories
      description: 'Displays a list of directories, including those pending eradication.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/File_system_ids'
      - $ref: '#/parameters/File_system_names'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryGetResponse'
    post:
      tags:
      - Directories
      summary: Create directory
      description: 'Creates a managed directory at the specified path.

        The managed directory name must consist of a file system name prefix and a
        managed directory

        name suffix (separated with ''&#58;''). The suffix must be between 1 and 63
        characters

        (alphanumeric and ''-'') in length and begin and end with a letter or number.

        The suffix must include at least one letter or ''-''.

        Set `names` to create a managed directory with the specified full managed
        directory name,

        or set `file_system_names` or `file_system_ids` in the query parameters and
        `suffix` in

        the body parameters to create a managed directory in the specified file system
        with the

        specified suffix. These two options are exclusive.

        '
      parameters:
      - in: body
        name: directory
        required: true
        schema:
          $ref: '#/definitions/DirectoryPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/File_system_names'
      - $ref: '#/parameters/File_system_ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryResponse'
    delete:
      tags:
      - Directories
      summary: Delete managed directories
      description: 'Deletes one or more managed directories.

        To be deleted, a managed directory must be empty and not attached to any enabled
        export

        policies. Deleted managed directories cannot be recovered.

        The `ids` or `names` parameter is required, but cannot be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Directories
      summary: Modify a managed directory
      description: 'Modifies a managed directory.

        To rename a managed directory, set `name` to the new name.

        The `ids` or `names` parameter is required, but cannot be set together.

        The `name` or `directory_name` parameter is required.


        '
      parameters:
      - in: body
        name: directory
        required: true
        schema:
          $ref: '#/definitions/DirectoryPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryResponse'
  /api/2.42/host-groups/performance:
    get:
      tags:
      - Host Groups
      summary: List host group performance data
      description: 'Displays real-time and historical performance data, real-time
        latency data, and average I/O

        sizes of all host groups, displayed both by host group and by total size across
        all host

        groups. This data represents volumes that are connected to the host groups
        on the local

        array and stretched volumes connected to the host groups on arrays that are
        connected by

        synchronous replication.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourcePerformanceNoIdGetResponse'
  /api/2.42/protection-group-snapshots:
    get:
      tags:
      - Protection Group Snapshots
      summary: List protection group snapshots
      description: 'Displays a list of protection group snapshots, including those
        pending eradication.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Source_names'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupSnapshotGetResponse'
    post:
      tags:
      - Protection Group Snapshots
      summary: Create a protection group snapshot
      description: 'Creates a point-in-time snapshot of the contents of a protection
        group.

        The `source_ids` or `source_names` parameter is required, but they cannot
        be set together.

        '
      parameters:
      - in: body
        name: protection-group-snapshot
        required: false
        schema:
          $ref: '#/definitions/ProtectionGroupSnapshotPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Apply_retention'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/For_replication'
      - $ref: '#/parameters/Replicate'
      - $ref: '#/parameters/Replicate_now'
      - $ref: '#/parameters/Allow_throttle_snapshot'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupSnapshotResponse'
    delete:
      tags:
      - Protection Group Snapshots
      summary: Delete a protection group snapshot
      description: 'Deletes a protection group snapshot that has been destroyed and
        is pending eradication.

        Eradicating a protection group snapshot eradicates all of its protection group
        snapshots

        and cannot be recovered.

        Protection group snapshots are destroyed through the `PATCH` method.

        The `names` or `ids` parameter is required, but they cannot be set together.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Protection Group Snapshots
      summary: Modify a protection group snapshot
      description: 'Modifies a protection group snapshot so that it can be destroyed.

        To destroy a volume, set `destroyed=true`.

        To recover a volume that has been destroyed and is pending eradication, set
        `destroyed=false`.

        The `names` or `ids` parameter is required, but they cannot be set together.

        '
      parameters:
      - in: body
        name: protection-group-snapshot
        required: true
        schema:
          $ref: '#/definitions/ProtectionGroupSnapshotPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupSnapshotResponse'
  /api/2.42/alert-watchers/test:
    get:
      tags:
      - Alert Watchers
      summary: List alert watcher test
      description: Displays alert watcher email test results.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceGetResponse'
  /api/2.42/protection-group-snapshots/test:
    post:
      tags:
      - Protection Group Snapshots
      summary: Create an attempt to take protection group snapshot
      description: 'Creates an attempt to take the protection group snapshot, without

        actually taking it, to test if the snapshot can be successfully taken.

        '
      parameters:
      - in: body
        name: protection-group-snapshot
        required: false
        schema:
          $ref: '#/definitions/ProtectionGroupSnapshotPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Apply_retention'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/For_replication'
      - $ref: '#/parameters/Replicate'
      - $ref: '#/parameters/Replicate_now'
      - $ref: '#/parameters/Allow_throttle_snapshot'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourceWithIdPostResponse'
  /api/2.42/admins/api-tokens:
    get:
      tags:
      - Administrators
      summary: List API tokens
      description: Displays API tokens for the specified administrators.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Expose_api_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AdminApiTokenGetResponse'
    post:
      tags:
      - Administrators
      summary: Create API tokens
      description: Creates API tokens for the specified administrators.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Timeout'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AdminApiTokenResponse'
    delete:
      tags:
      - Administrators
      summary: Delete API tokens
      description: Deletes the API tokens of the specified administrators.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
  /api/2.42/policies/alert-watcher/rules:
    get:
      tags:
      - Policies
      summary: List alert-watcher policy rules
      description: Displays a list of alert-watcher policy rules.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleAlertWatcherGetResponse'
    post:
      tags:
      - Policies
      summary: Create alert-watcher policy rules
      description: Creates one or more alert-watcher policy rules. Either the 'policy_ids'
        or 'policy_names' parameter is required, but both parameters cannot be set
        together.
      parameters:
      - in: body
        name: rules
        required: true
        schema:
          $ref: '#/definitions/PolicyRuleAlertWatcherPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleAlertWatcherResponse'
    delete:
      tags:
      - Policies
      summary: Delete alert-watcher policy rules
      description: Deletes one or more alert-watcher policy rules. Either the 'policy_ids'
        or 'policy_names' parameter is required, but both parameters cannot be set
        together.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Policies
      summary: Modify alert-watcher policy rules
      description: Modifies alert-watcher policy rules.
      parameters:
      - in: body
        name: rules
        required: true
        schema:
          $ref: '#/definitions/PolicyRuleAlertWatcherPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleAlertWatcherResponse'
  /api/2.42/volume-snapshots/test:
    post:
      tags:
      - Volume Snapshots
      summary: Create the volume snapshot path
      description: 'Creates the volume snapshot path without actually taking a volume
        snapshot.

        '
      parameters:
      - in: body
        name: volume-snapshot
        required: true
        schema:
          $ref: '#/definitions/VolumeSnapshotPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/OnSingle'
      - $ref: '#/parameters/Allow_throttle_snapshot'
      - $ref: '#/parameters/Source_ids'
      - $ref: '#/parameters/Source_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TestResultWithResourcePostResponse'
  /api/2.42/volume-snapshots/tags/batch:
    put:
      tags:
      - Volume Snapshots
      summary: Update tags
      description: Updates tags.
      parameters:
      - in: body
        name: tag
        description: A list of tags to be created or modified.
        required: true
        schema:
          type: array
          items:
            $ref: '#/definitions/Tag'
          uniqueItems: true
          minItems: 1
          maxItems: 30
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Resource_ids'
      - $ref: '#/parameters/Resource_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/TagResponse'
  /api/2.42/virtual-machine-volume-snapshots:
    get:
      tags:
      - Virtual Machines
      summary: List Virtual Machine Volume Snapshots
      description: 'Displays a list of virtual machine volume snapshots, including
        those pending eradication.

        If `vm_type` is `vvol`, the `recover_context` in the response will represent

        a protection group snapshot containing the virtual machine volume.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - name: vm_type
        in: query
        description: 'The type of virtual machine.

          The only valid value is `vvol`.

          '
        required: false
        type: string
      - name: vm_ids
        in: query
        description: 'Performs the operation on the unique virtual machine IDs specified.

          Enter multiple resource IDs in a comma-separated format.

          '
        required: true
        type: array
        items:
          type: string
        collectionFormat: csv
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/VirtualMachineVolumeSnapshotResponse'
  /api/2.42/workloads/placement-recommendations:
    get:
      tags:
      - Workloads
      summary: List workload placement recommendations
      description: Displays a list of placement recommendations for workloads.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Allow_errors'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/WorkloadPlacementRecommendationGetResponse'
    post:
      tags:
      - Workloads
      summary: Create a request for a workload placement recommendation.
      description: 'Creates a recommendation for the placement of the specified workload.

        The computation might take a few minutes.

        '
      parameters:
      - in: body
        name: inputs
        required: true
        schema:
          $ref: '#/definitions/WorkloadPlacementRecommendation'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Placement_names'
      - $ref: '#/parameters/Context_names'
      - name: preset_names
        in: query
        description: 'Creates the workload placement recommendations using the presets

          specified by names. Only one preset can be specified. One of the

          `preset_ids` or `preset_names` parameters are required, but they

          cannot be set together.

          '
        required: false
        type: array
        items:
          type: string
        collectionFormat: csv
      - name: preset_ids
        in: query
        description: 'Creates the workload placement recommendations using the presets

          specified by ids. Only one preset can be specified. One of the

          `preset_ids` or `preset_names` parameters are required, but they

          cannot be set together.

          '
        required: false
        type: array
        items:
          type: string
        collectionFormat: csv
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/WorkloadPlacementRecommendationResponse'
  /api/2.42/directories/locks/nlm-reclamations:
    post:
      tags:
      - Directories
      summary: Create NLM reclamation
      description: 'Creates an NLM reclamation, which is a system-wide operation that
        affects all clients,

        so only one may be in progress at a time. Attempting to initiate reclamation
        while one is in

        progress will result in an error. When NLM reclamation is initiated, all NLM
        locks are deleted

        and client applications are notified that they can reacquire their locks within
        a grace period.

        '
      parameters:
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryLockNlmReclamationResponse'
  /api/2.42/protection-groups:
    get:
      tags:
      - Protection Groups
      summary: List protection groups
      description: 'Displays a list of protection groups,

        including their associated source arrays, replication targets,

        hosts, host groups, and volumes.

        The list includes protection groups that were created on the

        local array to replicate snapshot data to other arrays or

        offload targets, created on a remote array and replicated

        asynchronously to this array, or created inside a pod

        on a remote array and stretched to the local array.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupGetResponse'
    post:
      tags:
      - Protection Groups
      summary: Create a protection group and upsert tags
      description: 'Creates a protection group on the local array for asynchronous
        replication.

        '
      parameters:
      - in: body
        name: protection-group
        required: false
        schema:
          $ref: '#/definitions/ProtectionGroupPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Protection_group_source_ids'
      - $ref: '#/parameters/Protection_group_source_names'
      - $ref: '#/parameters/Overwrite'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupResponse'
    delete:
      tags:
      - Protection Groups
      summary: Delete a protection group
      description: 'Deletes a protection group that has been destroyed and is pending
        eradication.

        Eradicated protection groups cannot be recovered.

        Protection groups are destroyed through the `PATCH` method.

        The `names` parameter is required.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Protection Groups
      summary: Modify a protection group
      description: 'Modifies the protection group schedules to generate and replicate

        snapshots to another array or to an external storage system.

        Renames or destroys a protection group.

        '
      parameters:
      - in: body
        name: protection-group
        required: true
        schema:
          $ref: '#/definitions/ProtectionGroup'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ProtectionGroupResponse'
  /api/2.42/arrays/performance/by-link:
    get:
      tags:
      - Arrays
      summary: List array front-end IO performance data by link
      description: 'Displays real-time and historical front-end performance data at
        the array

        level including latency, bandwidth, IOPS, average I/O size, and queue depth.

        Group output is shown by link. A link represents a set of arrays that an I/O
        operation

        depends on.

        For local-only I/Os, this is a local array. For mirrored I/Os, this is a set
        of synchronous

        replication peer arrays.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ArrayPerformanceByLinkGetResponse'
  /api/2.42/host-groups/space:
    get:
      tags:
      - Host Groups
      summary: List host group space information
      description: 'Displays provisioned size and physical storage consumption data
        for each host group.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourceSpaceNoIdGetResponse'
  /api/2.42/hosts/host-groups:
    get:
      tags:
      - Hosts
      summary: List hosts that are associated with host groups
      description: 'Displays a list of hosts that are associated with host groups.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdAllGetResponse'
    post:
      tags:
      - Hosts
      summary: Create a membership to a host group
      description: 'Creates a membership to a host group.

        Creating a membership to a host group automatically

        connects the host to all volumes associated with the group.

        Multiple hosts can be belong to a host group,

        but a host can only belong to one host group.

        Hosts can be added to host groups at any time.

        The `group_names` and `member_names` parameters are required

        and must be set together, and only one host group can be specified at a time.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberNoIdAllResponse'
    delete:
      tags:
      - Hosts
      summary: Delete a membership from a host group
      description: 'Deletes a membership from a host group.

        Deleting a membership from a host group automatically

        disconnects the host from all volumes associated with the group.

        Hosts can be deleted from host groups at any time.

        The `group_names` and `member_names` parameters are required

        and must be set together, and only one host group can be specified at a time.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Member_names'
      responses:
        '200':
          description: OK
  /api/2.42/ports:
    get:
      tags:
      - Ports
      summary: List ports
      description: Displays host name, iSCSI Qualified Names (IQNs), NVMe Qualified
        Names (NQNs), IPv4 address of the portal, Fibre Channel World Wide Names (WWNs),
        and failover ports, including those that were discovered by Purity//FA and
        those that have been manually assigned by system administrators.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PortGetResponse'
  /api/2.42/software:
    get:
      tags:
      - Software
      summary: List software packages
      description: 'Displays a list of available software packages.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Versions'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SoftwareGetResponse'
    post:
      tags:
      - Software
      summary: Create a software package
      description: 'Creates a software package download.

        '
      parameters:
      - in: body
        name: software
        required: true
        schema:
          $ref: '#/definitions/SoftwarePost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      responses:
        '200':
          description: 'The software package download was initiated. The download
            will start soon.

            '
          schema:
            $ref: '#/definitions/SoftwareResponse'
    delete:
      tags:
      - Software
      summary: Delete a software package
      description: 'Deletes a software package specified by software name and version.

        A software package being used for an installation can be deleted if the installation
        is

        `downloading`, `downloaded`, `aborted`, or `finished`. If the software package
        is currently

        `downloading`, the download will be cancelled.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Software_versions'
      responses:
        '200':
          description: OK
  /api/2.42/hosts/performance:
    get:
      tags:
      - Hosts
      summary: List host performance data
      description: 'Displays real-time and historical performance data, real-time
        latency data, and average I/O

        sizes across all hosts, displayed by host and by total size across all hosts.
        This data

        represents volumes that are connected to the hosts on the local array and
        stretched volumes

        connected to the hosts on any arrays that are connected by synchronous replication.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      - $ref: '#/parameters/Total_only'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourcePerformanceNoIdGetResponse'
  /api/2.42/subnets:
    get:
      tags:
      - Subnets
      summary: List subnets
      description: Displays a list of subnets with statuses and attributes.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/SubnetGetResponse'
    post:
      tags:
      - Subnets
      summary: Create subnet
      description: Creates a subnet with the specified parameters.
      parameters:
      - in: body
        name: subnet
        required: true
        schema:
          $ref: '#/definitions/SubnetPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names_required'
      responses:
        '200':
          description: Displays the newly created subnet.
          schema:
            $ref: '#/definitions/SubnetResponse'
    delete:
      tags:
      - Subnets
      summary: Delete subnet
      description: 'Deletes subnets. A subnet can only be deleted if it has no interfaces.
        Interfaces must be

        removed by using the `network-interfaces` endpoint.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Subnets
      summary: Modify subnet
      description: Modifies one or more specified subnet properties.
      parameters:
      - in: body
        name: subnet
        required: true
        schema:
          $ref: '#/definitions/SubnetPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: Displays the updated subnet.
          schema:
            $ref: '#/definitions/SubnetResponse'
  /api/2.42/pod-replica-links/lag:
    get:
      tags:
      - Pod Replica Links
      summary: List pod replica link lag
      description: Displays the lag in milliseconds that the replication target is
        behind the source. This is the time difference between the current time and
        the recovery point.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/History_end_time'
      - $ref: '#/parameters/History_resolution'
      - $ref: '#/parameters/History_start_time'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Local_pod_ids'
      - $ref: '#/parameters/Local_pod_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Remote_ids'
      - $ref: '#/parameters/Remote_names'
      - $ref: '#/parameters/Remote_pod_ids'
      - $ref: '#/parameters/Remote_pod_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PodReplicaLinkLagGetResponse'
  /api/2.42/policies/quota/rules:
    get:
      tags:
      - Policies
      summary: List quota policy rules
      description: Displays a list of quota policy rules.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleQuotaGetResponse'
    post:
      tags:
      - Policies
      summary: Create quota policy rules
      description: Creates one or more quota policy rules. The `policy_ids` or `policy_names`
        parameter is required, but cannot be set together.
      parameters:
      - in: body
        name: rules
        required: true
        schema:
          $ref: '#/definitions/PolicyRuleQuotaPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Ignore_usage'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleQuotaResponse'
    delete:
      tags:
      - Policies
      summary: Delete quota policy rules
      description: Deletes one or more quota policy rules. The `policy_ids` or `policy_names`
        parameter is required, but they cannot be set together.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Policies
      summary: Modify quota policy rules
      description: Modifies quota policy rules.
      parameters:
      - in: body
        name: rules
        required: true
        schema:
          $ref: '#/definitions/PolicyRuleQuotaPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Ignore_usage'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyRuleQuotaResponse'
  /api/2.42/alerts:
    get:
      tags:
      - Alerts
      summary: List alerts
      description: Displays a list of alerts.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Flagged'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AlertGetResponse'
    patch:
      tags:
      - Alerts
      summary: Modify flagged state
      description: Modifies one or more alerts and display updated information about
        these alerts.
      parameters:
      - in: body
        name: alert
        required: true
        schema:
          $ref: '#/definitions/Alert'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AlertResponse'
  /api/2.42/resource-accesses:
    get:
      tags:
      - Resource Accesses
      summary: List resource access configurations
      description: 'Displays a list of configured resource accesses.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Ids_without_names'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourceAccessesGetResponse'
    delete:
      tags:
      - Resource Accesses
      summary: Delete a resource access configuration
      description: 'Deletes a resource access object.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Ids_without_names'
      responses:
        '200':
          description: OK
  /api/2.42/directory-quotas:
    get:
      tags:
      - Directory Quotas
      summary: List directories with attached quota policies
      description: Displays a list of directories and the quota policies attached
        to them. Directories with multiple policies are listed repeatedly (once per
        policy). The directories without a policy attached are not listed.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Directory_ids'
      - $ref: '#/parameters/Directory_names'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/DirectoryQuotasGetResponse'
  /api/2.42/admins:
    get:
      tags:
      - Administrators
      summary: List administrators
      description: 'Displays a list of administrators.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Expose_api_token'
      - $ref: '#/parameters/Expose_public_key'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AdminGetResponse'
    post:
      tags:
      - Administrators
      summary: Create an administrator
      description: 'Creates an administrator.


        '
      parameters:
      - in: body
        name: admin
        required: true
        schema:
          $ref: '#/definitions/AdminPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AdminResponse'
    delete:
      tags:
      - Administrators
      summary: Delete an administrator
      description: 'Deletes the specified administrator.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
    patch:
      tags:
      - Administrators
      summary: Modify an administrator
      description: 'Modifies properties for the specified administrator.


        '
      parameters:
      - in: body
        name: admin
        required: true
        schema:
          $ref: '#/definitions/AdminPatch'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/AdminResponse'
  /api/2.42/pods/arrays:
    get:
      tags:
      - Pods
      summary: List pods and their the array members
      description: 'Displays a list of pods and the local and remote arrays over which
        the pods are stretched.

        Pods with realm members will not be returned.

        The optional `group_names` parameter represents the name of the pod.

        The optional `member_names` parameter

        represents the name of the array over which the pod is stretched.

        (Deprecated) Use pods/members instead.)

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberGetResponse'
    post:
      tags:
      - Pods
      summary: Creates a pod to be stretched to an array
      description: 'Creates a pod to be stretched to an array.

        When a pod is stretched to an array,

        the data in the arrays over which the pod is stretched is synchronously replicated.

        The `group_names` parameter represents the name of the pod to be stretched.

        The `member_names` parameter represents the name of the array over which the
        pod is

        to be stretched.

        The `group_names` and `member_names` parameters are required and must be set
        together.

        (Deprecated) Use pods/members instead.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_ids'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/MemberResponse'
    delete:
      tags:
      - Pods
      summary: Delete a pod that was stretched to an array
      description: 'Deletes a pod that was stretchd to an array, collapsing the pod
        to a single array.

        Unstretch a pod from an array when the volumes within the stretched pod

        no longer need to be synchronously replicated between the two arrays.

        After a pod has been unstretched, synchronous replication stops.

        A destroyed version of the pod with ''restretch'' appended to the pod name

        is created on the array that no longer has the pod.

        The restretched pod represents a point-in-time snapshot of the pod,

        just before it was unstretched.

        The restretch pod enters an eradication pending period

        starting from the time that the pod was unstretched.

        A restretched pod can be cloned or destroyed, but it cannot be explicitly
        recovered.

        The `group_names` parameter represents the name of the pod to be unstretched.

        The `member_names` parameter represents the name of the array from which the
        pod is

        to be unstretched.

        The `group_names` and `member_names` parameters are required and must be set
        together.

        (Deprecated) Use pods/members instead.

        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Group_names'
      - $ref: '#/parameters/Group_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/With_unknown'
      responses:
        '200':
          description: OK
  /api/2.42/policies/audit-file/members:
    get:
      tags:
      - Policies
      summary: List audit policy members
      description: Displays a list of audit policy members.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Allow_errors'
      - $ref: '#/parameters/Context_names_get'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Destroyed'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberGetResponse'
    post:
      tags:
      - Policies
      summary: Create a membership between a managed directory and a audit policy
      description: Creates a membership between one or more managed directories and
        a audit policy. The `policy_ids` or `policy_names` parameter is required,
        but cannot be set together.
      parameters:
      - in: body
        name: members
        required: true
        schema:
          $ref: '#/definitions/PolicyMemberPost'
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/PolicyMemberResponse'
    delete:
      tags:
      - Policies
      summary: Delete membership between audit policies and managed directories
      description: Deletes a membership between one or more audit policies and managed
        directories. The `policy_ids` or `policy_names` parameter is required, but
        cannot be set together. The `member_ids` or `member_names` parameter is required,
        but cannot be set together.
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Context_names'
      - $ref: '#/parameters/Member_ids'
      - $ref: '#/parameters/Member_names'
      - $ref: '#/parameters/Member_types'
      - $ref: '#/parameters/Policy_ids'
      - $ref: '#/parameters/Policy_names'
      responses:
        '200':
          description: OK
  /api/2.42/resource-accesses/status:
    get:
      tags:
      - Resource Accesses
      summary: List status of resource accesses as specified by resource access configuration
      description: 'Displays a list of effective resource accesses.


        '
      parameters:
      - $ref: '#/parameters/Authorization'
      - $ref: '#/parameters/XRequestId'
      - $ref: '#/parameters/Continuation_token'
      - $ref: '#/parameters/Filter'
      - $ref: '#/parameters/Limit'
      - $ref: '#/parameters/Offset'
      - $ref: '#/parameters/Sort'
      - $ref: '#/parameters/Total_item_count'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/ResourceAccessesStatusGetResponse'
  /oauth2_pub_priv_key_pair:
    get:
      description: "Returns a public and private key pair, This is not part of the
        FlashArray API, \nit's a helper endpoint making it easier to generate an oauth2
        token_id in this Swagger UI. \nThey keys are generated in the Docker container
        running on this machine.\n\nRecommend to NOT use this API but to use OpenSSL
        instead for production use cases:\n  You can generate a public and private
        RSA key pair like this:\n\n    openssl genrsa -des3 -out private.pem 2048\n
        \   \n    That generates a 2048-bit RSA key pair, encrypts them with a password
        you provide and writes them to a file. \n    You need to next extract the
        public key file. You will use this, for instance, on your web server to encrypt
        \n    content so that it can only be read with the private key.\n    \n    Export
        the RSA Public Key to a File\n    This is a command that is\n    \n    openssl
        rsa -in private.pem -outform PEM -pubout -out public.pem"
      responses:
        200:
          description: Token Generated
          schema:
            type: object
            properties:
              private_key:
                type: string
                description: Private key in PEM format.
              public_key:
                type: string
                description: Public key in PEM format.
      tags:
      - Auth Helpers
  /oauth2_token_from_private_key:
    post:
      consumes:
      - text/plain
      produces:
      - application/json
      description: "Authenticates to Flasharray, This returns a token_id (think session
        id), from an existing API Client, \nThis is not part of the FlashArray API,
        it's a helper endpoint making it easy to generate an \noauth2 token_id in
        this Swagger UI.  The private key is being passed into the Docker container
        in\norder to sign the JWT token.  Take a look at the Docker container output
        to see more details \nof what is happening.\n\nCopy the output value, Authorization
        header, and then click on the green Authorize button at the \ntop of the page.
        \ Paste in the value into "
      responses:
        200:
          description: Token Generated
          schema:
            type: object
            properties:
              Authorization:
                type: string
                description: An OAuth bearer token to be used in the Authorization
                  header for authorization to API endpoints.
              description:
                type: string
                desciption: How to use token
            example:
              username: pureuser
      tags:
      - Auth Helpers
      parameters:
      - name: issuer_name
        in: header
        type: string
        description: Issuer value displayed in the FlashArray API Registration page
          or pureapiclient list ) by default it's the same name as API Client
      - name: client_id
        in: header
        type: string
        description: Client Id value displayed in the FlashArray API Registration
          page or pureapiclient list
      - name: key_id
        in: header
        type: string
        description: Key Id value displayed in the FlashArray API Registration page
          or pureapiclient list
      - name: username
        in: header
        type: string
        description: The name of a valid user on the FlashArray that will be authenticated
          to.
      - name: private_key
        required: true
        in: body
        schema:
          type: string
        description: Private key in PEM format that was used to create the Public
          key for this API Client specified as the issuer in this call.
definitions:
  _pod_array_status:
    allOf:
    - $ref: '#/definitions/_reference'
    - type: object
      properties:
        pre_elected:
          type: boolean
          description: 'If set to `true`, the array has been pre-elected to remain
            online

            in the rare event that the mediator is inaccessible on both arrays within
            the stretched pod,

            and then later, the arrays within the stretched pod become disconnected
            from each other.

            If set to `false`, either the array has been pre-elected to remain offline

            while its peer array remains online, or pre-election is not in effect.

            One and only one array within each pod is pre-elected at a given point
            in time,

            so while a pre-elected array is keeping the pod online,

            the pod on its non-elected peer array remains offline during the

            communication failure. Users cannot pre-elect arrays.

            '
          readOnly: true
        member:
          type: object
          description: 'A reference to the member associated with the pod''s array
            status.

            '
          title: ReferenceWithType
          allOf:
          - $ref: '#/definitions/_referenceWithType'
        mediator_status:
          type: string
          description: 'The status of the mediator,

            which determines whether it is available to mediate a high availability
            event.

            Valid values are `flummoxed`, `online`, `unknown`, and `unreachable`.

            Only mediators in the `online` status can mediate high availability events.

            If set to `flummoxed`, the array can reach a mediator, but it is talking
            to the wrong one.

            Verify that the DNS in the environment is properly configured.

            This status might also appear if the pod has been offline on one array

            for an extended period of time and the peer array is unreachable.

            If set to `online`, the array is successfully communicating with the mediator,

            and the mediator is available to mediate a high availability event.

            If set to `unreachable`, the array cannot reach the mediator,

            either due to network issues or because the mediator is down.

            When a mediator is unreachable, synchronous replication continues to function

            provided all arrays are healthy and communicating,

            but a high availability event without mediator access can result in an
            outage.

            '
          readOnly: true
        progress:
          type: number
          format: float
          example: 0.66
          description: 'The percentage progress of the pod resyncing process for this
            array.

            The percentage is displayed as a decimal value, starting at 0.00 and ending
            at 1.00.

            '
          readOnly: true
        frozen_at:
          type: integer
          format: int64
          example: 1492662000
          description: 'The timestamp of when the data on the pod was frozen when
            the array went offline.

            Measured in milliseconds since the UNIX epoch.

            Also known as the recovery point.

            If the pod is in sync, a value of `null` will be returned.

            '
          readOnly: true
        status:
          type: string
          example: resyncing
          description: 'The status of the array within the stretched pod.

            Valid values are `offline`, `online`, `resyncing`, `suspended`, and `unknown`.

            If set to `offline`, the array is experiencing problems

            and may not have the latest pod data.

            The array cannot handle I/O to the pod and cannot take over

            during a high availability event.

            If set to `online`, the array is online and has the latest pod data.

            The array can handle I/O to the pod and take over during a high availability
            event.

            If set to `suspended`, the array is experiencing a short connection glitch
            of the pod.

            This is a temporary status and would transition to either `online` or
            `offline` soon.

            `suspended` is a new status that is exposed, starting from version 6.6.0.

            If set to `resyncing`, the array is actively getting the latest pod data

            so that it becomes fully synchronized with its peer array.

            During the resyncing process, the array cannot handle I/O to the pod.

            Once the arrays are fully synchronized, the array changes to `online`
            status.

            If set to `unknown`, the status of the peer array is unknown

            because this array is offline and cannot determine the state of the pod
            on the peer array.

            Only the peer array can ever be in unknown status; this unknown status

            is unique to the local array and will differ when viewed from its peer
            array.

            '
          readOnly: true
  PolicyRuleNfsClientPost:
    type: object
    properties:
      rules:
        type: array
        description: A list of NFS policy rules to create.
        items:
          $ref: '#/definitions/_policyrulenfsclientpostRules'
  KmipPost:
    type: object
    properties:
      ca_certificate:
        type: string
        example: '-----BEGIN CERTIFICATE-----...-----END CERTIFICATE-----'
        description: The text of the CA certificate for the KMIP server.
        maxLength: 3000
      uris:
        type: array
        description: List of URIs for the configured KMIP servers.
        items:
          type: string
      certificate:
        type: object
        description: 'The certificate used to verify FlashArray authenticity to the
          KMIP servers.

          '
        title: ReferenceNoId
        allOf:
        - $ref: '#/definitions/_referenceNoId'
  PodResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Pod'
  ResourcePodSpaceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            When applicable, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/ResourcePodSpace'
        items:
          type: array
          description: 'Displays a list of all items after filtering.

            If applicable, the values are displayed for each name.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/ResourcePodSpace'
  _policyrulenfsclientpostRules:
    type: object
    properties:
      security:
        type: array
        description: 'The security flavors to use for accessing files on this mount
          point.

          Values include `auth_sys`, `krb5`, `krb5i`, and `krb5p`.

          If the server does not support the requested flavor, the mount operation
          fails.

          This operation updates all rules of the specified policy.

          If `auth_sys`, the client is trusted to specify the identity of the user.

          If `krb5`, cryptographic proof of the identity of the user is provided in
          each RPC

          request. This provides strong verification of the identity of users accessing
          data

          on the server. Note that additional configuration besides adding this mount
          option

          is required to enable Kerberos security. If `krb5i`, integrity checking
          is added to

          krb5. This ensures the data has not been tampered with. If `krb5p`, integrity

          checking and encryption is added to krb5. This is the most secure setting,
          but it

          also involves the most performance overhead.

          '
        items:
          type: string
          example: auth_sys
      access:
        type: string
        description: 'Specifies access control for the export.

          Valid values include `root-squash`, `all-squash`, and `no-root-squash`.

          The value `root-squash` prevents client users and groups with root privilege

          from mapping their root privilege to a file system.

          All users with UID 0 will have their UID mapped to `anonuid`.

          All users with GID 0 will have their GID mapped to `anongid`.

          The value `all-squash` maps all UIDs (including root) to `anonuid`

          and all GIDs (including root) to `anongid`.

          The value `no-root-squash` allows users and groups to access the file system

          with their UIDs and GIDs.

          If not specified, the default value is `root-squash`.

          '
      anonuid:
        type: string
        example: '65530'
        description: 'Any user whose UID is affected by an `access` of `root_squash`
          or `all_squash` will have

          their UID mapped to `anonuid`. The default `anonuid` is null, which means
          65534.

          Use "" to clear.

          '
      anongid:
        type: string
        example: '65530'
        description: 'Any user whose GID is affected by an `access` of `root_squash`
          or `all_squash`

          will have their GID mapped to `anongid`.

          The default `anongid` is null, which means 65534. Use "" to clear.

          This value is ignored when user mapping is enabled.

          '
      client:
        type: string
        description: 'Specifies which clients are given access.

          Valid values include `IP`, `IP mask`, or `hostname`.

          The default is `*` if not specified.

          '
      permission:
        type: string
        description: 'Specifies which read-write client access permissions are allowed
          for the export.

          Values include `rw` and `ro`. The default value is `rw` if not specified.

          '
      nfs_version:
        type: array
        description: 'NFS protocol version allowed for the export. Valid values are
          `nfsv3` and `nfsv4`.

          If not specified, defaults to `nfsv3`.

          '
        items:
          type: string
          example: nfsv3
  ResourceAccessesResponse:
    type: object
    properties:
      items:
        type: array
        description: A list of resource access objects.
        items:
          $ref: '#/definitions/ResourceAccess'
  FleetMemberGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/FleetMemberResponse'
  SoftwarePost:
    type: object
    properties:
      name:
        type: string
        example: Purity//FA
        description: 'The name of the software.

          '
      version:
        type: string
        example: 5.2.3
        description: 'The version of the software.

          '
    description: Software to be installed on the array.
  ArrayConnectionGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ArrayConnectionResponse'
    - $ref: '#/definitions/_errorContextResponse'
  MemberNoIdAll:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        member:
          type: object
          description: The member of the resource.
          title: ReferenceNoId
          allOf:
          - $ref: '#/definitions/_referenceNoId'
        group:
          type: object
          description: The resource in which the host, volume, or other item in the
            environment is a member.
          title: ReferenceNoId
          allOf:
          - $ref: '#/definitions/_referenceNoId'
  ArrayConnectionPathGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ArrayConnectionPathResponse'
  _referenceWithType:
    allOf:
    - $ref: '#/definitions/_reference'
    - type: object
      properties:
        resource_type:
          type: string
          description: 'Type of the object (full name of the endpoint).

            Valid values are `hosts`, `host-groups`, `network-interfaces`, `pods`,

            `ports`, `pod-replica-links`, `subnets`, `volumes`, `volume-snapshots`,

            `volume-groups`, `directories`, `policies/nfs`, `policies/smb`, and

            `policies/snapshot`, etc.

            '
  PolicyRuleAlertWatcherGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyRuleAlertWatcherResponse'
    - $ref: '#/definitions/_errorContextResponse'
  VolumeGroupPatch:
    allOf:
    - $ref: '#/definitions/_resource'
    - type: object
      properties:
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the volume group has been destroyed
            and is pending eradication.

            Before the `time_remaining` period has elapsed, the destroyed volume group
            can be recovered

            by setting `destroyed=false`.

            After the `time_remaining` period has elapsed, the volume group is permanently
            eradicated

            and cannot be recovered.

            '
        qos:
          $ref: '#/definitions/_qos'
        priority_adjustment:
          $ref: '#/definitions/_priorityAdjustment'
        space:
          type: object
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
      description: 'Volume groups organize FlashArray volumes into logical groupings.

        '
  PodPerformanceByArray:
    allOf:
    - $ref: '#/definitions/PodPerformance'
    - type: object
      properties:
        array:
          type: object
          description: 'The array on which the performance metrics were recorded.

            '
          title: Resource
          allOf:
          - $ref: '#/definitions/_resource'
  RemotePod:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        arrays:
          type: array
          items:
            $ref: '#/definitions/_resource'
          minItems: 1
      description: 'Remote pod that''s not directly stretched to this array.

        '
  Arrays:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        os:
          type: string
          example: Purity//FA
          description: 'Specifies the operating system.

            Valid values are `Purity`, `Purity//FA`, and `Purity//FB`.

            '
          readOnly: true
        scsi_timeout:
          type: integer
          format: int32
          example: 60000
          description: 'The SCSI timeout. If not specified, defaults to `60s`.

            If the user does not have sufficient access, this field will return `null`.

            '
        parity:
          type: number
          format: float
          example: 0.99
          description: 'A representation of data redundancy on the array.

            Data redundancy is rebuilt automatically by the system whenever parity
            is less than `1.0`.

            If the user does not have sufficient access, this field will return `null`.

            '
          readOnly: true
        banner:
          type: string
          example: Restricted area. Authorized personnel only.
        console_lock_enabled:
          type: boolean
          example: false
          description: 'If `true`, console lock is enabled for the array.

            If the user does not have sufficient access, this field will return `null`.

            '
        eradication_config:
          $ref: '#/definitions/Eradication_config'
        ntp_servers:
          type: array
          description: 'NTP Servers.

            If the user does not have sufficient access, this field will return `null`.

            '
          items:
            type: string
            example: time.acme.com
        _as_of:
          type: integer
          format: int64
          example: 1684403432765
          description: 'The time in milliseconds since UNIX epoch.

            '
          readOnly: true
        time_zone:
          type: string
          example: America/Los_Angeles
          description: 'The time zone of the array.

            '
          readOnly: true
        version:
          type: string
          example: 5.0.0
          readOnly: true
        space:
          type: object
          title: Space
          allOf:
          - $ref: '#/definitions/Space'
        capacity:
          type: integer
          format: int64
          example: 10995116277760
          description: 'The usable capacity in bytes.

            If the user does not have sufficient access, this field will return `null`.

            '
          readOnly: true
        encryption:
          type: object
          title: ArrayEncryption
          allOf:
          - $ref: '#/definitions/ArrayEncryption'
        ntp_symmetric_key:
          type: string
          description: 'The text of ntp symmetric authentication key. Supported formats
            include a hex-encoded

            string no longer than 64 characters, or an ASCII string no longer than
            20 characters,

            excluding "#". Any configured key will be masked as "****" on return.

            If the user does not have sufficient access, this field will return `null`.

            '
        idle_timeout:
          type: integer
          format: int32
          example: 300000
          description: 'The idle timeout in milliseconds. Valid values include `0`
            and any multiple of `60000` in

            the range of `300000` and `10800000`. Any other values are rounded down
            to the nearest

            multiple of `60000`.

            '
  _offload_azure:
    type: object
    properties:
      container_name:
        type: string
        description: 'The name of the container in the Azure Blob storage account

          to where the data will be offloaded.

          The name must be a valid DNS name. If not specified, defaults to `offload`.

          '
      account_name:
        type: string
        description: 'The name of the existing Azure Blob storage account.

          '
      profile:
        type: string
        description: 'The offload target profile that will be selected for this target.
          This option

          allows more granular configuration for the target on top of the `protocol`

          parameter. Values include `azure`.

          '
      secret_access_key:
        type: string
        description: 'The secret access key that goes with the account name (`account_name`)
          of the Azure Blob

          storage account.

          The secret access key is only accepted

          when creating the connection between the array and the Azure Blob storage
          account.

          The `account_name` and `container_name`, and `secret_access_key` parameters
          must be set

          together.

          '
  CloudProviderTag:
    type: object
    properties:
      details:
        type: string
        example: partially applied
        description: 'Details about the status of the tag if not healthy.

          '
        readOnly: true
      value:
        type: string
        example: staging
        description: 'Value of the tag. Supports UTF-8 Unicode characters.

          Please refer to the offical document from cloud providers for limitations.

          '
      key:
        type: string
        example: environment
        description: 'Key of the tag. Supports UTF-8 Unicode characters.

          Please refer to the offical document from cloud providers for limitations.

          '
  AppNode:
    type: object
    properties:
      app:
        type: object
        description: A reference to the app of which this node is a part.
        title: FixedReference
        allOf:
        - $ref: '#/definitions/_fixedReference'
      index:
        type: integer
        format: int32
        example: 0
        description: An index that represents this node.
        readOnly: true
      vnc:
        type: array
        description: 'The VNC information used to access this node if VNC is enabled.

          '
        readOnly: true
        items:
          type: string
          example: 10.1.1.10:5900
      version:
        type: string
        example: 1.0.0
        description: The node version.
        readOnly: true
      status:
        type: string
        example: healthy
        description: 'The node status. Possible values are `starting`, `healthy`,
          `unhealthy`, `critical`,

          `stopping`, and `disabled`.

          '
        readOnly: true
    description: 'An individual node of a Purity app. Nodes are indexed starting at
      `0`.

      '
  _errorcontextresponseErrors:
    type: object
    properties:
      context:
        type: string
        example: <object_name>
        description: 'Contains information relating to the cause of this error,

          or the name of the object that was being processed when the error was encountered.

          This may be `null` for more general errors.

          '
      location_context:
        type: object
        description: 'Contains information relating to the context in which the request
          was executing

          when the error occurred.

          For example, this may be the name of an array in the same fleet.

          This may be `null` for more general errors, or if no explicit `context`
          parameter

          was provided with the request.

          '
        title: FixedReferenceWithType
        allOf:
        - $ref: '#/definitions/_fixedReferenceWithType'
      message:
        type: string
        example: Object does not exist.
        description: A description of the error which occurred.
  PodPatch:
    allOf:
    - $ref: '#/definitions/_resource'
    - type: object
      properties:
        quota_limit:
          type: integer
          format: int64
          description: 'The logical quota limit of the pod, measured in bytes. Must
            be a multiple of 512.

            '
          minimum: 1048576
          maximum: 4503599627370496
        destroyed:
          type: boolean
          description: 'If set to `true`, the pod has been destroyed and is pending
            eradication.

            The `time_remaining` value displays the amount of time left

            until the destroyed pod is permanently eradicated.

            A pod can only be destroyed if it is empty, so before destroying a pod,

            ensure all volumes and protection groups inside the pod

            have been either moved out of the pod or destroyed.

            A stretched pod cannot be destroyed unless you unstretch it first.

            Before the `time_remaining` period has elapsed, the destroyed pod can
            be recovered

            by setting `destroyed=false`.

            Once the `time_remaining` period has elapsed,

            the pod is permanently eradicated and can no longer be recovered.

            '
        failover_preferences:
          type: array
          description: 'Determines which array within a stretched pod should be given
            priority to stay online

            should the arrays ever lose contact with each other. The current array
            and

            any peer arrays that are connected to the current array for synchronous
            replication can be

            added to a pod for failover preference.

            By default, `failover_preferences=null`,

            meaning no arrays have been configured for failover preference.

            Enter multiple arrays in comma-separated format.

            '
          items:
            $ref: '#/definitions/_reference'
        requested_promotion_state:
          type: string
          description: 'Patch `requested_promotion_state` to `demoted` to demote the
            pod so that it can be used as

            a link target for continuous replication between pods.

            Demoted pods do not accept write requests, and a destroyed version of
            the pod with

            `undo-demote` appended to the pod name is created on the array with the
            state of the pod

            when it was in the promoted state.

            Patch `requested_promotion_state` to `promoted` to start the process of
            promoting the pod.

            The `promotion_status` indicates when the pod has been successfully promoted.

            Promoted pods stop incorporating replicated data from the source pod and
            start accepting

            write requests.

            The replication process does not stop when the source pod continues replicating
            data to the

            pod.

            The space consumed by the unique replicated data is tracked by the `space.journal`
            field

            of the pod.

            '
        mediator:
          type: string
          example: https://mediator.cloud-support.purestorage.com
          description: 'Sets the URL of the mediator for this pod, replacing the URL
            of the current mediator.

            By default, the Pure1 Cloud Mediator (`purestorage`) serves as the mediator.

            '
        ignore_usage:
          type: boolean
          description: 'Set to `true` to set a `quota_limit` that is lower than the
            existing usage.

            This ensures that no new volumes can be created until the existing usage

            drops below the `quota_limit`.

            If not specified, defaults to `false`.

            '
      description: 'A pod is an administrative domain, a data container, and a namespace
        for

        volumes and protection groups.

        '
  ConnectionResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Connection'
  PodReplicaLinkLag:
    allOf:
    - $ref: '#/definitions/_builtInRelationship'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        lag:
          type: object
          description: 'Duration in milliseconds that indicates how far behind the
            replication target is from the

            source.

            '
          title: ReplicaLinkLag
          allOf:
          - $ref: '#/definitions/_replicaLinkLag'
        recovery_point:
          type: integer
          format: int64
          example: 1565034592494
          description: 'Time when the last piece of data was replicated, in milliseconds
            since the UNIX epoch, and

            the recovery point if the pod is promoted. If the pod is baselining then
            the value is

            `null`.

            '
          readOnly: true
        remotes:
          type: array
          description: Reference to a remote array.
          items:
            $ref: '#/definitions/_fixedReference'
        remote_pod:
          type: object
          description: Reference to a remote pod.
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        time:
          type: integer
          format: int64
          description: Sample time in milliseconds since the UNIX epoch.
        local_pod:
          type: object
          description: Reference to a local pod.
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        direction:
          type: string
          description: 'The direction of replication. Valid values include `inbound`
            and `outbound`.

            '
          readOnly: true
        status:
          type: string
          description: 'Status of the replica-link. Valid values include `replicating`,
            `baselining`,

            `paused`, `quiescing`, `quiesced`, `idle`, and `unhealthy`.

            '
          readOnly: true
  TargetProtectionGroup:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        allowed:
          type: boolean
          example: true
          description: 'If set to `true`, the target array or pod allows the source
            array

            to replicate protection group data to the target.

            If set to `false`, the target array or pod does not allow the source array

            to replicate protection group data to the target.

            '
        member:
          $ref: '#/definitions/_referenceWithType'
        group:
          $ref: '#/definitions/_reference'
        status:
          type: string
          example: suspended
          description: 'The replication status of the target.

            Valid values are `replicating`, `suspended`, and `disallowed`.

            If `allowed` is `true` and protection group data is replicating to the

            target, `status` will display `replicating`.

            If `allowed` is `true`, but replication is suspended due to the target

            being demoted, linked, stretched, etc., `status` will display `suspended`.

            Replication will be automatically resumed if the target becomes promoted

            or local only again.

            If `allowed` is `false`, `status` will display `disallowed`.

            '
  PodReplicaLinkLagGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PodReplicaLinkLagResponse'
    - $ref: '#/definitions/_errorContextResponse'
  ArrayErasurePatch:
    type: object
    properties:
      image_version:
        type: string
        example: 6.6.6
        description: 'Version of the image to download. `image_version` is optional,

          and the default value is the current running Purity version.

          If `image_source` is `auto`, the image with the specified version

          will be downloaded from Pure1 cloud.

          If `image_source` is a URL, `image_version` will be used to compare

          to the downloaded image version for the purpose of validation.

          '
      image_source:
        type: string
        example: auto
        description: 'Source of the ISO image to download. Valid values include `auto`
          and URLs.

          `auto` means download the image from Pure1 cloud,

          and a URL means download the image from the specified URL.

          Default value is `auto`.

          '
  TestResultWithResourceWithId:
    allOf:
    - $ref: '#/definitions/TestResult'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        resource:
          type: object
          description: 'A reference to the object being tested.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
  PresetWorkloadPost:
    allOf:
    - $ref: '#/definitions/_presetWorkloadBase'
    - type: object
  _presetWorkloadSnapshotConfiguration:
    type: object
    required:
    - name
    - rules
    properties:
      name:
        type: string
        description: 'The name of the snapshot configuration, by which other configuration
          objects in the preset

          can reference it. Name must be unique across all configuration objects in
          the preset.

          '
      rules:
        type: array
        description: Rules describe the frequency and retention of snapshots taken
          by the configuration.
        items:
          $ref: '#/definitions/_presetWorkloadSnapshotRule'
        maxItems: 2
        minItems: 1
  FilePost:
    type: object
    properties:
      source:
        type: object
        description: 'The source information of a file copy.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      source_path:
        type: string
        description: 'The source file path relative to the source directory.

          '
  Directory:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left, measured in milliseconds until the
            destroyed managed directory is

            permanently eradicated.

            '
          readOnly: true
        path:
          type: string
          description: 'Absolute path of the managed directory in the file system.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the managed directory has been
            destroyed and is pending

            eradication.

            The `time_remaining` value displays the amount of time left

            until the destroyed managed directory is permanently eradicated.

            Once the `time_remaining` period has elapsed,

            the managed directory is permanently eradicated and can no longer be recovered.

            '
        file_system:
          type: object
          description: 'The file system that this managed directory is in.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        created:
          type: integer
          format: int64
          description: 'The managed directory creation time, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
        limited_by:
          type: object
          description: 'The quota policy that is limiting usage on this managed directory.

            This policy defines the total amount of space provisioned to this managed
            directory

            and its descendants. The returned value contains two parts&#58; the name
            of the policy

            and the managed directory to which the policy is attached.

            '
          title: LimitedBy
          allOf:
          - $ref: '#/definitions/_limitedBy'
        directory_name:
          type: string
          description: 'The managed directory name without the file system name prefix.

            A full managed directory name is constructed in the form of `FILE_SYSTEM:DIR`
            where

            `FILE_SYSTEM` is the file system name and `DIR` is the value of this field.

            '
          readOnly: true
        space:
          type: object
          description: 'Displays size and space consumption details.

            '
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
  SoftwareInstallationStepsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SoftwareInstallationStepsResponse'
  _networkInterfacePortDetailsRx_power:
    type: object
    properties:
      channel:
        type: integer
        format: int32
        example: 0
        description: 'The channel number if the transceiver is a multilane transceiver.

          If not specified, defaults to `null`.

          '
      measurement:
        type: number
        format: double
        description: 'Rx input power in mW.

          '
      status:
        type: string
        example: ok
        description: 'Indicates whether the measurement is outside the high alarm,
          low alarm, high warning,

          or low warning thresholds for Rx input power.

          Valid values are `alarm low`, `alarm high`, `warn low`, `warn high`, and
          `ok`.

          '
  AppNodeGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/AppNodeResponse'
  DirectoryPatch:
    allOf:
    - $ref: '#/definitions/_newName'
    - type: object
      properties:
        directory_name:
          type: string
          description: 'The managed directory name without the file system name prefix.

            A full managed directory name is constructed in the form of `FILE_SYSTEM:DIR`
            where

            `FILE_SYSTEM` is the file system name and `DIR` is the value of this field.

            '
  _saml2SsoSpCredential:
    type: object
    properties:
      decryption_credential:
        type: object
        description: 'The credential used by the service provider to decrypt encrypted
          SAML

          assertions from the identity provider. The credential is managed by

          the `certificates` endpoint and `purecert` CLI commands.

          '
        title: ReferenceNoId
        allOf:
        - $ref: '#/definitions/_referenceNoId'
      signing_credential:
        type: object
        description: 'The credential used by the service provider to sign SAML requests.

          The credential is managed by the `certificates` endpoint and

          `purecert` CLI commands.

          '
        title: ReferenceNoId
        allOf:
        - $ref: '#/definitions/_referenceNoId'
  _errorContextResponse:
    type: object
    properties:
      errors:
        type: array
        description: The list of errors encountered when attempting to perform an
          operation.
        readOnly: true
        items:
          $ref: '#/definitions/_errorcontextresponseErrors'
  ProtectionGroupsVolumes:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        member:
          $ref: '#/definitions/_protectionGroupsVolumesMember'
        group:
          $ref: '#/definitions/_reference'
  _replicaLinkPerformanceReplication:
    type: object
    properties:
      bytes_per_sec_total:
        type: integer
        format: int64
        description: Total bytes transmitted and received per second.
        readOnly: true
        minimum: 0
      bytes_per_sec_from_remote:
        type: integer
        format: int64
        description: The number of bytes received per second from a remote array.
        readOnly: true
        minimum: 0
      remotes:
        type: array
        description: Reference to a remote array.
        readOnly: true
        items:
          $ref: '#/definitions/_fixedReference'
      bytes_per_sec_to_remote:
        type: integer
        format: int64
        description: The number of bytes transmitted per second to a remote array.
        readOnly: true
        minimum: 0
      remote_pod:
        type: object
        description: Reference to a remote pod.
        title: FixedReference
        allOf:
        - $ref: '#/definitions/_fixedReference'
      time:
        type: integer
        format: int64
        description: Sample time in milliseconds since the UNIX epoch.
        readOnly: true
      local_pod:
        type: object
        description: Reference to a local pod.
        title: FixedReference
        allOf:
        - $ref: '#/definitions/_fixedReference'
      direction:
        type: string
        description: The direction of replication. Valid values are `inbound` and
          `outbound`.
        readOnly: true
  Audit:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        subcommand:
          type: string
          example: create
          description: 'The `command` and `subcommand` combination determines which
            action the user attempted to

            perform.

            '
          readOnly: true
        origin:
          type: object
          description: The array from which the command originated.
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        user_interface:
          type: string
          example: CLI
          description: 'The user interface through which the user session event was
            performed.

            Valid values are `CLI`, `GUI`, and `REST`.

            '
          readOnly: true
        arguments:
          type: string
          example: --size 1G vol2
          description: The arguments provided to the command.
          readOnly: true
        time:
          type: integer
          format: int64
          example: 1577402821354
          description: The time at which the command was run in milliseconds since
            the UNIX epoch.
          readOnly: true
        user:
          type: string
          example: pureuser
          description: The user who ran the command.
          readOnly: true
        command:
          type: string
          example: purevol
          description: The top level command that starts with the string "pure" as
            a convention.
          readOnly: true
  DirectorySnapshotPatch:
    allOf:
    - $ref: '#/definitions/_destroyedPatchPost'
    - type: object
      properties:
        name:
          type: string
          description: 'The new name of a directory snapshot.

            The name of a directory snapshot managed by a snapshot policy is not changeable.

            '
        keep_for:
          type: integer
          format: int64
          description: 'The amount of time to keep the snapshots, in milliseconds.

            Can only be set on snapshots that are not managed by any snapshot policy.

            Set to `""` to clear the keep_for value.

            '
        suffix:
          type: string
          description: 'The suffix portion of the client-visible snapshot name.

            A full snapshot name is constructed in the form of `DIR.CLIENT_NAME.SUFFIX`
            where

            `DIR` is the managed directory name, `CLIENT_NAME` is the client name,
            and

            `SUFFIX` is the value of this field.

            The client-visible snapshot name is `CLIENT_NAME.SUFFIX`.

            The suffix of a directory snapshot managed by a snapshot policy is not
            changeable.

            If the `name` and `suffix` parameters are both specified, `suffix` must
            match

            the suffix portion of `name`.

            '
        client_name:
          type: string
          description: 'The client name portion of the client-visible snapshot name.

            A full snapshot name is constructed in the form of `DIR.CLIENT_NAME.SUFFIX`
            where

            `DIR` is the managed directory name, `CLIENT_NAME` is the value of this
            field, and

            `SUFFIX` is the suffix. The client-visible snapshot name is `CLIENT_NAME.SUFFIX`.

            The client name of a directory snapshot managed by a snapshot policy is
            not

            changeable.

            If the `name` and `client_name` parameters are both specified, `client_name`

            must match the client name portion of `name`.

            '
        policy:
          type: object
          description: 'The snapshot policy that manages this snapshot.

            Set to `name` or `id` to `""` to clear the policy.

            '
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
  FleetMemberResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Returns a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/FleetMember'
  SyslogServerSettings:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        ca_certificate:
          type: string
          description: 'The certificate of the certificate authority (CA) that signed
            the directory servers''

            certificate(s), which is used to validate the authenticity of the configured
            servers.

            '
        tls_audit_enabled:
          type: boolean
          description: 'Returns a value of `true` if messages that are necessary in
            order to

            audit TLS negotiations performed by the array are forwarded to the

            configured syslog servers.

            '
        logging_severity:
          type: string
          description: 'Returns the configured logging severity threshold for which
            events will

            be forwarded to the configured syslog servers. Default configuration is

            info level severity.

            Valid values are `debug`, `info`, and `notice`.

            '
    description: Global settings for syslog servers.
  _workloadPlacementRecommendationTargetLoadProjections:
    type: object
    properties:
      projection_blended_max:
        type: object
        description: 'The projected blended maximum load on the target.

          '
        title: WorkloadPlacementRecommendationProjection
        allOf:
        - $ref: '#/definitions/_workloadPlacementRecommendationProjection'
      projection_avg:
        type: object
        description: 'The projected average load on the target.

          '
        title: WorkloadPlacementRecommendationProjection
        allOf:
        - $ref: '#/definitions/_workloadPlacementRecommendationProjection'
    description: 'Load depicts the storage system utilization as a percentage from
      0,

      when the system is completely idle, to 100, when the system is completely

      saturated.

      '
  PodPerformance:
    allOf:
    - $ref: '#/definitions/_performance'
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        others_per_sec:
          type: integer
          format: int64
          description: The number of other requests processed per second.
          minimum: 0
        usec_per_other_op:
          type: integer
          format: int64
          description: 'The average time it takes the pod to process an I/O other
            request, measured in

            microseconds.

            '
          minimum: 0
  ArrayEncryption:
    type: object
    properties:
      data_at_rest:
        type: object
        title: ArrayencryptionDataAtRest
        allOf:
        - $ref: '#/definitions/_arrayencryptionDataAtRest'
      module_version:
        type: string
        example: FA-1.3
        description: 'The version of the Purity encryption module installed on the
          array.

          Security certifications are carried out on a per-version basis.

          On non-encrypt builds, an encryption module may be installed without

          being enabled.

          Values include `FA-1.0`, `FA-1.1`, `FA-1.2`, `FA-1.3`, and so on.

          '
        readOnly: true
    description: If the user does not have sufficient access, all fields will return
      `null`.
  FleetKey:
    type: object
    properties:
      expires:
        type: integer
        format: int64
        example: 1706854577
        description: 'Expiration time in milliseconds since UNIX epoch.

          '
        readOnly: true
      created:
        type: integer
        format: int64
        example: 1706850977
        description: 'Creation time in milliseconds since UNIX epoch.

          '
        readOnly: true
      fleet_key:
        type: string
        example: CAUSJDljMGM3MGY3LWFhZDEtNDRkZC05ZDQ2LTZlOTA3YzY3Yzg1YxokOWMwYzcwZjctYWFkMS00NGRkLTlkND
        description: 'Fleet key, used for fleet connections.

          After creation, listing will only show ****.

          '
        readOnly: true
  LocalUserResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/LocalUser'
  PodPerformanceReplication:
    allOf:
    - $ref: '#/definitions/_podPerformanceReplication'
  SoftwareResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Software'
  AlertWatcher:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        enabled:
          type: boolean
          description: 'If set to `true`, email notifications will be sent to this
            watcher for alerts.

            If set to `false`, email notification is disabled.

            '
  ResourcePerformance:
    allOf:
    - $ref: '#/definitions/_performance'
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
  RemoteProtectionGroup:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        target_retention:
          type: object
          description: The retention policy for the remote protection group.
          title: RetentionPolicy
          allOf:
          - $ref: '#/definitions/_retentionPolicy'
        time_remaining:
          type: integer
          format: int64
          description: 'Milliseconds remaining until eradication, if remote protection
            group

            has been destroyed.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the remote protection group has
            been destroyed

            and is pending eradication.

            The `time_remaining` value displays the amount of time left

            until the destroyed remote protection group is permanently eradicated.

            Before the `time_remaining` period has elapsed,

            the destroyed remote protection group can be recovered by setting `destroyed=false`.

            Once the `time_remaining` period has elapsed,

            the remote protection group is permanently eradicated and can no longer
            be recovered.

            '
        source:
          type: object
          description: 'The array where the remote protection group is replicated
            from.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        is_local:
          type: boolean
          description: 'If set to `true`, the location reference is to the local array.

            If set to `false`, the location reference is to a remote location,

            such as a remote array or offload target.

            '
          readOnly: true
        remote:
          type: object
          description: 'The offload target that the remote protection group is on.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
      description: 'A remote protection group represents a protection group that is
        replicated

        to an offload target.

        '
  PolicyRuleSmbClient:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left, in milliseconds, until the destroyed
            SMB policy rule is permanently

            eradicated.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the pod containing the SMB policy
            rule has been destroyed and

            is pending eradication.

            The `time_remaining` value displays the amount of time left until the
            destroyed policy is

            permanently eradicated.

            Once the `time_remaining` period has elapsed, the SMB policy rule is permanently
            eradicated

            and can no longer be recovered.

            '
        anonymous_access_allowed:
          type: boolean
          description: 'Specifies whether access to information is allowed for anonymous
            users.

            Returns a value of `false` if not specified.

            '
          readOnly: true
        name:
          type: string
          description: 'Name of this rule. The name is automatically generated by
            the system.

            '
          readOnly: true
        client:
          type: string
          description: 'Specifies which clients are given access to the export.

            Accepted notation includes IP, IP mask, or hostname.

            The default is `*` if not specified.

            '
          readOnly: true
        smb_encryption_required:
          type: boolean
          description: 'Specifies whether the remote client is required to use SMB
            encryption.

            If not specified, defaults to `false`.

            '
          readOnly: true
        policy:
          type: object
          description: The policy to which this rule belongs.
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
  FileSystem:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left, measured in milliseconds until the
            destroyed file system is

            permanently eradicated.

            Before the `time_remaining` period has elapsed, the destroyed file system
            can be recovered

            by setting `destroyed=false`.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the file system has been destroyed
            and is pending eradication.

            The `time_remaining` value displays the amount of time left until the
            destroyed file system

            is permanently eradicated.

            Before the `time_remaining` period has elapsed, the destroyed file system
            can be recovered

            by setting `destroyed=false`.

            Once the `time_remaining` period has elapsed, the file system is permanently
            eradicated and

            can no longer be recovered.

            '
        pod:
          type: object
          description: 'The reference to the pod this file systems belongs to, or
            `null`

            if it is not in a pod.

            '
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        created:
          type: integer
          format: int64
          description: 'The file system creation time, measured in milliseconds since
            the UNIX epoch.

            '
          readOnly: true
  TestResultGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/TestResultResponse'
  SubscriptionResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Subscription'
  WorkloadPlacementRecommendationGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/WorkloadPlacementRecommendationResponse'
  SyslogServer:
    allOf:
    - $ref: '#/definitions/LogTarget'
    - type: object
      properties:
        services:
          type: array
          description: 'Valid values are `data-audit` and `management`.

            If not specified, defaults to `management`.

            '
          items:
            type: string
        uri:
          type: string
          example: tcp://my.syslogserver.com
          description: 'The URI of the syslog server in the format `PROTOCOL://HOSTNAME:PORT`.

            '
      description: A syslog server
  TestResultResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/TestResult'
  PolicyManagementAccessPatch:
    allOf:
    - $ref: '#/definitions/_newName'
    - type: object
      properties:
        rules:
          type: array
          description: 'List the rules that defines the scoped roles.

            '
          items:
            $ref: '#/definitions/_policyrulemanagementaccessRules'
        aggregation_strategy:
          type: string
          description: 'When this is set to `least-common-permissions`, any users
            to whom this policy applies can

            receive no access rights exceeding those defined in this policy''s capability
            and resource.

            When this is set to `all-permissions`, any users to whom this policy applies
            are capable of

            receiving additional access rights from other policies that apply to them.

            '
        enabled:
          type: boolean
          description: 'Sets whether the policy is enabled and grants the permissions
            specified.

            '
  _volumeSpaceCommon:
    allOf:
    - $ref: '#/definitions/_space'
    - type: object
      properties:
        snapshots_effective:
          type: integer
          format: int64
          description: 'This field has been deprecated.

            The effective space contributed by data unique to one or more snapshots,

            measured in bytes.

            Use the `snapshots` field in the future, as it contains the same information

            for Evergreen//One arrays.

            '
          readOnly: true
          minimum: 0
        unique_effective:
          type: integer
          format: int64
          description: 'This field has been deprecated.

            The effective space contributed by unique customer data.

            Unique data does not include shared space, snapshots, and internal array
            metadata, measured

            in bytes.

            Use the `unique` field in the future, as it contains the same information

            for Evergreen//One arrays.

            '
          readOnly: true
          minimum: 0
        total_effective:
          type: integer
          format: int64
          description: 'This field has been deprecated.

            The total effective space contributed by customer data, measured in bytes.

            Use the `total_physical` field in the future, as it contains the same
            information

            for Evergreen//One arrays.

            '
          readOnly: true
          minimum: 0
  _certificateBase:
    type: object
    properties:
      country:
        type: string
        example: CA
        description: 'Two-letter country (ISO) code listed in the certificate.

          '
      issued_by:
        type: string
        example: Symantec, Inc.
        description: 'The party that issued the certificate.

          '
        readOnly: true
      issued_to:
        type: string
        example: Pure Storage, Inc.
        description: 'The party to whom the certificate is issued.

          '
        readOnly: true
      subject_alternative_names:
        type: array
        description: 'The alternative names that are secured by this certificate.

          Alternative names may be IP addresses, DNS names, or URIs.

          '
        items:
          type: string
          example: otherdnsname.mydomain.com
      certificate:
        type: string
        description: 'The text of the certificate.

          '
      locality:
        type: string
        example: Toronto
        description: 'The locality field listed in the certificate.

          '
      valid_from:
        type: integer
        format: int64
        example: 1491070899
        description: 'The date when the certificate starts being valid.

          '
        readOnly: true
      key_algorithm:
        type: string
        example: rsa
        description: 'The key algorithm used to generate the certificate.

          This field can only be specified when creating a new self-signed certificate.

          Defaults to rsa if not specified. Valid values when creating a new self-signed

          certificate only include `rsa`, `ec`, `ed448` and `ed25519`.

          '
      intermediate_certificate:
        type: string
        description: 'The text of the intermediate certificate chains.

          '
      organization:
        type: string
        example: Veridian Dynamics
        description: 'The organization field listed in the certificate.

          '
      valid_to:
        type: integer
        format: int64
        example: 1524248499
        description: 'The date when the certificate stops being valid.

          '
        readOnly: true
      state:
        type: string
        example: Ontario
        description: 'The state/province field listed in the certificate.

          '
      certificate_type:
        type: string
        description: 'The type of certificate. Values include `appliance` and `external`.

          Certificates of type `appliance` are used by the array to verify

          its identity to clients. Certificates of type `external` are used

          by the array to identify external servers to which it is configured

          to communicate.

          '
        readOnly: true
      common_name:
        type: string
        example: Pure Storage Inc.
        description: 'The common name field listed in the certificate.

          '
      email:
        type: string
        example: tcrisp@veridiandynamics.com
        description: 'The email field listed in the certificate.

          '
      organizational_unit:
        type: string
        example: Research & Development
        description: 'The organizational unit field listed in the certificate.

          '
      key_size:
        type: integer
        format: int32
        example: 2048
        description: 'The size (in bits) of the private key for the certificate. Default
          is 2048 bits

          for `rsa` key_algorithm, 256 for `ec` and `ed25519`. The `ed448` default
          key

          size is 456. This field can only be specified when creating a new self-signed

          certificate.

          '
      status:
        type: string
        description: 'The type of certificate. Valid values are `self-signed` and
          `imported`.

          '
        readOnly: true
    description: SSL certificate managed by Purity.
  CertificateGroupUseGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          description: A list of certificate group use objects.
          items:
            $ref: '#/definitions/CertificateGroupUse'
  HostPatch:
    allOf:
    - $ref: '#/definitions/_newName'
    - type: object
      properties:
        remove_wwns:
          type: array
          description: 'Disassociates the specified Fibre Channel World Wide Names
            (WWNs) from the specified host.

            '
          items:
            type: string
            example: 52:4A:93:70:4E:E5:68:00
            description: WWNs to remove.
        host_group:
          type: object
          description: The host group to which the host should be associated.
          title: ReferenceNoId
          allOf:
          - $ref: '#/definitions/_referenceNoId'
        add_nqns:
          type: array
          description: 'Adds the specified NVMe Qualified Names (NQNs)

            to those already associated with the specified host.

            '
          items:
            type: string
            example: nqn.2010-06.com.purestorage:flasharray.512f7bb185978505
            description: NQNs to add.
        wwns:
          type: array
          description: The Fibre Channel World Wide Name (WWN) associated with the
            host.
          items:
            type: string
            example: 52:4A:93:70:4E:E5:68:00
        remove_iqns:
          type: array
          description: 'Disassociates the specified iSCSI Qualified Names (IQNs) from
            the specified host.

            '
          items:
            type: string
            example: iqn.2010-06.com.purestorage:linux.393d930077be2977
            description: IQNs to remove.
        personality:
          type: string
          description: 'Determines how the system tunes the array to ensure that it
            works optimally with the host.

            Set `personality` to the name of the host operating system or virtual
            memory system.

            Valid values are `aix`, `esxi`, `hitachi-vsp`, `hpux`,

            `oracle-vm-server`, `solaris`, and `vms`.

            If your system is not listed as one of the valid host personalities, do
            not set the option.

            By default, the personality is not set.

            '
        vlan:
          type: string
          example: '1'
          description: 'The VLAN ID that the host is associated with.

            If not set, there is no change in VLAN.

            If set to `any`, the host can access any VLAN.

            If set to `untagged`, the host can only access untagged VLANs.

            If set to a number between `1` and `4094`,

            the host can only access the specified VLAN with that number.

            '
        add_iqns:
          type: array
          description: 'Adds the specified iSCSI Qualified Names (IQNs)

            to those already associated with the specified host.

            '
          items:
            type: string
            example: iqn.2010-06.com.purestorage:linux.393d930077be2977
            description: IQNs to add.
        chap:
          $ref: '#/definitions/_chap'
        add_wwns:
          type: array
          description: 'Adds the specified Fibre Channel World Wide Names (WWNs)

            to those already associated with the specified host.

            '
          items:
            type: string
            example: 52:4A:93:70:4E:E5:68:00
            description: WWNs to add.
        nqns:
          type: array
          description: The NVMe Qualified Name (NQN) associated with the host.
          items:
            type: string
            example: nqn.2010-06.com.purestorage:flasharray.512f7bb185978505
        iqns:
          type: array
          description: 'The iSCSI qualified name (IQN) associated with the host.

            '
          items:
            type: string
            example: iqn.2010-06.com.purestorage:linux.393d930077be2977
        preferred_arrays:
          type: array
          description: 'For synchronous replication configurations, sets a host''s
            preferred array to specify which

            array exposes active/optimized paths to that host.

            Enter multiple preferred arrays in comma-separated format.

            If a preferred array is set for a host,

            then the other arrays in the same pod will expose active/non-optimized
            paths to that host.

            If the host is in a host group, `preferred_arrays` cannot be set because
            host groups

            have their own preferred arrays.

            On a preferred array of a certain host, all the paths on all the ports

            (for both the primary and secondary controllers) are set up as A/O (active/optimized)
            paths,

            while on a non-preferred array, all the paths are A/N (Active/Non-optimized)
            paths.

            '
          items:
            $ref: '#/definitions/_reference'
        remove_nqns:
          type: array
          description: 'Disassociates the specified NVMe Qualified Names (NQNs) from
            the specified host.

            '
          items:
            type: string
            example: nqn.2010-06.com.purestorage:flasharray.512f7bb185978505
            description: NQNs to remove.
  FleetMember:
    type: object
    properties:
      fleet:
        $ref: '#/definitions/_fixedReferenceWithTypeAndLocation'
      member:
        $ref: '#/definitions/_fixedReferenceWithTypeAndLocation'
      status_details:
        type: string
        example: cannot reach array
        description: Describes the error, if any.
      status:
        type: string
        description: 'Current fleet membership status

          Valid values are one of: `joining`, `joined`, or `removing`

          A status of `joining` indicates that the member is attempting to join the
          fleet.

          A status of `joined` indicates that the member has joined the fleet.

          A status of `removing` indicates that the member is being removed from the
          fleet.

          '
  SoftwarePatch:
    allOf:
    - $ref: '#/definitions/_resourceFixedNonUniqueName'
    - type: object
      properties:
        description:
          type: string
          description: The detailed description of the patch.
        progress:
          type: number
          format: float
          description: The progress of the software patch installation, displayed
            in decimal format.
        details:
          type: string
          description: The detailed reason for the `status`.
        alert_code:
          type: integer
          format: int64
          example: 5200
          description: 'Alert code for the Software Patch.

            This code is used to uniquely identify the Software Patch.

            '
        ha_reduction_required:
          type: boolean
          example: false
          description: 'If set to `true`, indicates reduction in high availability
            is required for

            Software Patch installation.

            If set to `false`, indicates such a reduction is not required.

            '
        status:
          type: string
          example: installed
          description: 'The status of the software patch installation.

            Valid values are `available`, `downloaded`, `downloading`, `download_failed`,

            `installing`, `installed`, `failed`, and `not_applicable`.

            A status of `available` indicates that the patch is available for download.

            A status of `downloaded` indicates that the patch has been downloaded
            and will be installed.

            A status of `downloading` indicates that the patch is currently downloading.

            A status of `download_failed` indicates that the download of the patch
            failed.

            A status of `installing` indicates that the patch is currently being installed.

            A status of `installed` indicates that the patch has been installed.

            A status of `failed` indicates that the patch failed to be installed.

            A status of `not_applicable` indicates the patch does not need to be installed
            on the

            array.

            '
      description: Software patch to be installed on the array.
  Api_versionResponse:
    type: object
    properties:
      version:
        type: array
        description: A list of supported API versions.
        items:
          type: string
  VolumePost:
    allOf:
    - $ref: '#/definitions/_volumePostBase'
    - type: object
      properties:
        workload:
          type: object
          description: 'The workload to which the volume is added.

            Set one of `id` or `name`, and `configuration` to add the volume to the
            workload

            and configure it based on the `configuration` from the preset the workload
            was

            originally provisioned from.

            '
          title: WorkloadConfigurationReference
          allOf:
          - $ref: '#/definitions/_workloadConfigurationReference'
  PolicyMemberClean:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        member:
          type: object
          description: 'Reference to the resource that the policy is applied to.

            '
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
        policy:
          type: object
          description: 'Reference to the policy.

            '
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
  AdminCacheResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/AdminCache'
  ActiveDirectory:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        kerberos_servers:
          type: array
          description: 'A list of key distribution servers to use for Kerberos protocol.

            Servers must be specified in FQDN format.

            All specified servers must be registered to the domain appropriately in
            the configured DNS

            of the array.

            If this field is `null`, the servers are resolved for the domain in DNS.

            '
          readOnly: true
          items:
            type: string
            example: kdc.my-corporation.com
        computer_name:
          type: string
          example: FLASHARRAY01
          description: 'The name of the computer account in the Active Directory domain.

            '
          readOnly: true
        directory_servers:
          type: array
          description: 'A list of directory servers used for lookups related to user
            authorization.

            Servers must be specified in FQDN format.

            All specified servers must be registered to the domain appropriately in
            the configured DNS

            of the array and are only communicated with over the secure LDAP (LDAPS)
            protocol.

            If this field is `null`, the servers are resolved for the domain in DNS.

            '
          readOnly: true
          items:
            type: string
            example: ldap.my-corporation.com
        domain:
          type: string
          example: my-corporation.com
          description: The Active Directory domain joined.
          readOnly: true
        tls:
          type: string
          example: required
          description: 'TLS mode for communication with domain controllers.

            Valid values are `required` and `optional`.

            `required` forces TLS communication with a domain controller.

            `optional` allows the use of non-TLS communication,

            TLS will still be preferred, if available.

            If not specified, defaults to `required`.

            '
  PolicyRuleQuotaPatch:
    type: object
    properties:
      rules:
        type: array
        description: 'Updates rules in a policy and accepts a single-rule update object.
          The list can contain

          only one item that will update all the specified rules.

          '
        items:
          $ref: '#/definitions/_policyrulequotapatchRules'
        maxItems: 1
  VolumesDiffResponse:
    type: object
    properties:
      items:
        type: array
        description: A list of different chunks between specified volumes.
        items:
          $ref: '#/definitions/VolumeDiff'
  _networkInterfacePortDetailsStatic:
    type: object
    properties:
      vendor_serial_number:
        type: string
        example: AB1234C56DE
        description: The serial number provided by the SFP vendor.
        maxLength: 16
      voltage_thresholds:
        $ref: '#/definitions/_networkInterfacePortDetailsStaticVoltageThresholds'
      signaling_rate:
        type: string
        example: 14000 MBd
        description: 'The nominal signaling rate in MBd, rounded off to the nearest
          100 MBd, or if greater

          than 25400 MBd, rounded off to the nearest 250 MBd. The value can be unspecified.

          '
      fc_link_lengths:
        type: array
        description: 'The Fibre Channel distance capabilities supported by the transceiver.

          '
        items:
          type: string
          example: short distance (S)
      specifications:
        type: array
        description: 'The Ethernet, 10G Ethernet, ESCON, Infiniband, SONET, and other
          specifications

          supported by the transceiver.

          '
        items:
          type: string
          example: 1000BASE-T
      tx_bias_thresholds:
        $ref: '#/definitions/_networkInterfacePortDetailsStaticTxBiasThresholds'
      tx_power_thresholds:
        $ref: '#/definitions/_networkInterfacePortDetailsStaticTxPowerThresholds'
      fc_speeds:
        type: string
        example: 16|32|64 Gb/s
        description: The Fibre Channel speeds supported by the transceiver. Speeds
          are in units of 100 MBytes/sec, which correspond to GFC (Gb/s).
      link_length:
        type: string
        example: 'OM2: 40 m, OM1: 20 m, OM3: 100 m'
        description: 'Link length and cable attenuation (for active or copper cables)
          for the specified

          transceiver.

          Values are comma-separated lists of fields and values, where each field
          is separated

          from its corresponding value by a colon. Valid fields include

          `Copper Cable Attenuation (12.9 GHz)`, `Copper Cable Attenuation (25.78
          GHz)`,

          `Copper Cable`, `SMF`, `OM2`, `OM1`, `OM4`, and `OM3`. The unit for attenuation
          is dB,

          and the units for length are meters or kilometers. Unspecified fields are
          omitted.

          '
      cable_technology:
        type: array
        description: 'The SFP+ cable technology supported by the transceiver.

          '
        items:
          type: string
          example: Active Cable
      connector_type:
        type: string
        example: LC
        description: 'The media connector type of the transceiver.

          '
      signaling_rate_min:
        type: string
        example: 1%
        description: 'The lower signaling rate limit at which the transceiver still
          meets its

          specifications, specified as a percentage below the nominal signaling rate.
          The value

          can be unspecified.

          '
      vendor_part_number:
        type: string
        example: ABCD-12E3FG-ELX
        description: The part number provided by the SFP vendor.
        maxLength: 16
      temperature_thresholds:
        $ref: '#/definitions/_networkInterfacePortDetailsStaticTemperatureThresholds'
      wavelength:
        type: string
        example: 850 nm
        description: 'Laser wavelength (for optical variants) at room temperature,
          in units of nm. For

          passive and active cable variants, the value is unspecified.

          '
      identifier:
        type: string
        example: SFP or SFP+
        description: 'The transceiver type.

          '
      fc_technology:
        type: array
        description: 'The Fibre Channel technologies supported by the transceiver.

          '
        items:
          type: string
          example: Electrical intra-enclosure (EL)
      rx_power_thresholds:
        $ref: '#/definitions/_networkInterfacePortDetailsStaticRxPowerThresholds'
      extended_identifier:
        type: string
        example: GBIC/SFP function is defined by 2-wire interface ID only
        description: 'The extended identifier of the transceiver type.

          '
      vendor_name:
        type: string
        example: EMULEX
        description: The SFP vendor name.
        maxLength: 16
      signaling_rate_max:
        type: string
        example: 1%
        description: 'The upper signaling rate limit at which the transceiver still
          meets its

          specifications, specified as a percentage above the nominal signaling rate.
          The value

          can be unspecified.

          '
      vendor_oui:
        type: string
        example: 00-17-6A
        description: The SFP vendor IEEE company ID.
        maxLength: 8
      encoding:
        type: string
        example: 64B/66B
        description: 'The serial encoding algorithm of the transceiver.

          '
      vendor_date_code:
        type: string
        example: '190221'
        description: 'The SFP vendor''s manufacturing date code. The first six characters
          are the date in

          YYMMDD format, and the last two characters are the vendor specific lot code.

          The last two characters are optional.

          '
        maxLength: 8
      rate_identifier:
        type: string
        example: FC-PI-5 (16/8/4G Independent Rx, Tx Rate_select)
        description: 'The type of rate select functionality of the transceiver.

          '
      fc_transmission_media:
        type: array
        description: 'The Fibre Channel transmission media supported by the transceiver.

          '
        items:
          type: string
          example: Multimode, 62.5um (M6)
      vendor_revision:
        type: string
        description: The revision level for the part number provided by the SFP vendor.
        maxLength: 4
    description: 'Static module identity and capabilities information for the specified
      network interface or

      Fibre Channel port.

      '
  AdminPost:
    type: object
    properties:
      management_access_policies:
        type: array
        description: 'List of management access policies to associate with the administrator.

          Policies can be specified by the `name` or `ID` parameter, but they cannot
          be set together.

          '
        items:
          $ref: '#/definitions/_referenceWithType'
      password:
        type: string
        description: 'Password associated with the account.

          '
      role:
        $ref: '#/definitions/_adminRole'
  SoftwareBundleResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SoftwareBundle'
  DirectoryExport:
    allOf:
    - $ref: '#/definitions/ExportPolicyAttachmentMixin'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        path:
          type: string
          description: The path of the exported managed directory.
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the managed directory of the
            export has been destroyed and is

            pending eradication.

            The export can be recovered by recovering the destroyed managed directory.

            '
          readOnly: true
        export_name:
          type: string
          description: The export name for accessing this export.
          readOnly: true
        directory:
          type: object
          description: The managed directory of the export.
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
        enabled:
          type: boolean
          description: 'Returns a value of `true` if the export policy that manages
            this export is enabled.

            '
          readOnly: true
        policy:
          type: object
          description: 'The export policy that manages this export.

            An export can be managed by at most one export policy.

            '
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
  MaintenanceWindowsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/MaintenanceWindowsResponse'
  PolicyPost:
    type: object
    properties:
      enabled:
        type: boolean
        description: If set to `true`, enables the policy. If set to `false`, disables
          the policy.
  Hardware:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        identify_enabled:
          type: boolean
          example: true
          description: 'If `true`, the ID LED is lit to visually identify the component.

            '
          readOnly: true
        serial:
          type: string
          example: PSMFL1717009B
          description: 'Serial number of the hardware component.

            '
          readOnly: true
        temperature:
          type: integer
          format: int32
          example: 25
          description: 'Temperature (in degrees Celsius) reported by the component.

            '
          readOnly: true
        index:
          type: integer
          format: int32
          example: 1
          description: 'Number that identifies the relative position of a hardware
            component within the array.

            '
          readOnly: true
        details:
          type: string
          example: Temperature too high
          description: 'Details about the status of the component if not healthy.

            '
          readOnly: true
        model:
          type: string
          example: DFSC1
          description: 'Model number of the hardware component.

            '
          readOnly: true
        slot:
          type: integer
          format: int32
          example: 16
          description: 'Slot number occupied by the PCI Express card that hosts the
            component.

            '
          readOnly: true
        type:
          type: string
          example: ct
          description: 'Type of hardware component.

            Values include `bay`, `ct`, `ch`, `eth`, `fan`, `fb`, `fc`, `fm`, `ib`,
            `iom`, `nvb`,

            `pwr`, `sas`, `sh`, and `tmp`.

            '
          readOnly: true
        speed:
          type: integer
          format: int64
          example: 24000000000
          description: 'Speed (in bytes per second) at which the component is operating.

            '
          readOnly: true
        status:
          type: string
          example: critical
          description: 'Component status.

            Values include `critical`, `healthy`, `identifying`, `unhealthy`, `unknown`,
            and `unused`.

            '
          readOnly: true
        voltage:
          type: integer
          format: int32
          example: 206
          description: 'Voltage (in Volts) reported by the component.

            '
          readOnly: true
  VolumePerformance:
    allOf:
    - $ref: '#/definitions/_performance'
    - $ref: '#/definitions/_resource'
  _host_port_connectivity:
    type: object
    properties:
      details:
        type: string
        example: Redundant
        description: 'The host connection status.

          Values include `Redundant`, `N/A`,

          `Redundant-Failover`, `Uneven`, `Unused Port`,

          `Single Controller`, `Single Controller-Failover`, and `None`.

          '
        readOnly: true
      status:
        type: string
        example: healthy
        description: 'The host connection health status.

          Values include `healthy`, `unhealthy`, and `critical`.

          '
        readOnly: true
    description: 'The connectivity status between the host and the ports on each controller.

      '
  PresetWorkloadGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfoWithoutTotalItemCountResponse'
    - $ref: '#/definitions/PresetWorkloadResponse'
    - $ref: '#/definitions/_errorContextResponse'
  ExportPolicyAttachmentMixin:
    type: object
    properties:
      export_name:
        type: string
        description: 'The export name for accessing this export.

          '
      enabled:
        type: boolean
        description: 'Returns a value of `true` if and only if the export policy that
          manages this

          export is enabled AND this export is enabled.

          '
        readOnly: true
      status:
        type: string
        description: 'Explains why `enabled` is `false`, or whether there is a name
          conflict with

          another export. Valid values include `policy_disabled`, `export_disabled`,
          and

          `name_conflict`.

          '
        readOnly: true
  PolicyRuleSmbClientGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyRuleSmbClientResponse'
    - $ref: '#/definitions/_errorContextResponse'
  RemoteVolumeSnapshotTransfer:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_transfer'
    - $ref: '#/definitions/_context'
    - type: object
      description: 'The transfer statistics for snapshot replication data.

        '
  ProtectionGroupsVolumesGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ProtectionGroupsVolumesResponse'
    - $ref: '#/definitions/_errorContextResponse'
  PodReplicaLink:
    allOf:
    - $ref: '#/definitions/_builtInRelationship'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        paused:
          type: boolean
          description: 'Returns a value of `true` if the replica link is in a `paused`
            state.

            Returns a value of `false` if the replica link is not in a `paused` state.

            '
        lag:
          type: integer
          format: int64
          description: 'Duration in milliseconds that represents how far behind the
            replication target is from the

            source. This is the time difference between current time and `recovery_point`.

            '
          readOnly: true
        recovery_point:
          type: integer
          format: int64
          example: 1565034592494
          description: 'Time when the last piece of data was replicated, in milliseconds
            since the UNIX epoch,

            and the recovery point if the target pod is promoted.

            If the pod is currently baselining, then the value is `null`.

            '
          readOnly: true
        remotes:
          type: array
          description: A list of remote arrays that share this pod.
          items:
            $ref: '#/definitions/_fixedReference'
        remote_pod:
          type: object
          description: Reference to a remote pod.
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        local_pod:
          type: object
          description: Reference to a local pod.
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        direction:
          type: string
          description: 'The direction of replication. Valid values include `inbound`
            and `outbound`.

            '
          readOnly: true
        status:
          type: string
          description: 'Status of the replica-link. Valid values include `replicating`,
            `baselining`,

            `paused`, `quiescing`, `quiesced`, `idle`, and `unhealthy`.

            '
          readOnly: true
  SmisGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SmisResponse'
  Admin:
    allOf:
    - $ref: '#/definitions/_fixedNameResourceNoId'
    - type: object
      properties:
        management_access_policies:
          type: array
          description: 'List of management access policies associated with the administrator.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_referenceWithType'
        public_key:
          type: string
          description: 'Public key for SSH access.

            Multiple public keys can be specified, separated by newlines.

            '
        password:
          type: string
          description: 'Password associated with the account.

            '
        role:
          $ref: '#/definitions/_adminRole'
        api_token:
          $ref: '#/definitions/_apiToken'
        lockout_remaining:
          type: integer
          format: int64
          description: 'The remaining lockout period if the user is locked out, in
            milliseconds.

            This field is only visible to `array_admin` roles.

            For all other users, the value is always `null`.

            '
          readOnly: true
        is_local:
          type: boolean
          example: true
          description: 'Returns a value of `true` if the user is local to the machine.

            '
          readOnly: true
        locked:
          type: boolean
          example: false
          description: 'Returns a value of `true` if the user is currently locked
            out. otherwise `false`.

            Change to `false` to unlock a user. This field is only visible to `array_admin`
            roles.

            For all other users, the value is always `null`.

            '
      description: Administrator account.
  _saml2SsoIdp:
    type: object
    properties:
      verification_certificate:
        type: string
        description: 'The X509 certificate that the service provider uses to verify
          the SAML response

          signature from the identity provider.

          '
      sign_request_enabled:
        type: boolean
        description: 'If set to `true`, SAML requests will be signed by the service
          provider.

          '
      metadata_url:
        type: string
        example: https://myidp.mycompany.com/federationmetadata/2007-06/federationmetadata.xml
        description: 'The URL of the identity provider metadata.

          '
      encrypt_assertion_enabled:
        type: boolean
        description: 'If set to `true`, SAML assertions will be encrypted by the identity
          provider.

          '
      entity_id:
        type: string
        example: http://myidp.mycompany.com/adfs/services/trust
        description: 'A globally unique name for the identity provider.

          '
      url:
        type: string
        example: https://myidp.mycompany.com/adfs/ls
        description: 'The URL of the identity provider.

          '
    description: 'Properties specific to the identity provider.

      '
  SupportResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Support'
  _networkInterfacePortDetailsStaticTxPowerThresholds:
    type: object
    properties:
      warn_low:
        type: number
        format: double
        description: The warn low threshold for Tx output power in mW.
      warn_high:
        type: number
        format: double
        description: The warn high threshold for Tx output power in mW.
      alarm_high:
        type: number
        format: double
        description: The alarm high threshold for Tx output power in mW.
      alarm_low:
        type: number
        format: double
        description: The alarm low threshold for Tx output power in mW.
    description: 'The alarm low, warn low, warn high, alarm high thresholds for Tx
      output power in mW.

      '
  PageInfo:
    type: object
    properties:
      continuation_token:
        type: string
        description: 'Continuation token that can be provided in the `continuation_token`

          query param to get the next page of data.

          If you use the continuation token to page through data you

          are guaranteed to get all items exactly once regardless of

          how items are modified. If an item is added or deleted during

          the pagination then it may or may not be returned.

          The continuation token is generated if the limit is less

          than the remaining number of items, and the default sort is used

          (no sort is specified).

          '
      more_items_remaining:
        type: boolean
        example: false
        description: 'Returns a value of `true` if subsequent items can be retrieved.

          '
      total_item_count:
        type: integer
        format: int32
        description: 'The total number of records after applying all filter query
          parameters.

          The `total_item_count` will be calculated if and only if the corresponding

          query parameter `total_item_count` is set to `true`. If this query parameter

          is not set or set to `false`, a value of `null` will be returned.

          '
  DirectoryPerformanceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            When it makes sense, the average value is displayed instead.

            The values are displayed for each field if meaningful.

            '
          items:
            $ref: '#/definitions/DirectoryPerformance'
        items:
          type: array
          description: 'Performance data.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/DirectoryPerformance'
  HardwareResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Hardware'
  Username:
    type: object
    properties:
      username:
        type: string
        example: pureuser
        description: The username of the user.
  _transfer:
    type: object
    properties:
      physical_bytes_written:
        type: integer
        format: int64
        description: 'The amount of physical/logical data written to the target due
          to replication.

          Measured in bytes.

          '
        readOnly: true
      data_transferred:
        type: integer
        format: int64
        description: 'The number of bytes transferred from the source to the target

          as part of the replication process.

          Measured in bytes.

          '
        readOnly: true
      destroyed:
        type: boolean
        description: 'Returns a value of `true` if the snapshot has been destroyed
          and is pending eradication.

          The destroyed snapshot can be recovered by setting `destroyed=false`.

          Once the eradication pending period has elapsed,

          the snapshot is permanently eradicated and can no longer be recovered.

          '
      progress:
        type: number
        format: float
        example: 0.66
        description: 'The percentage progress of the snapshot transfer from the source
          array to the target.

          Displayed in decimal format.

          '
        readOnly: true
      started:
        type: integer
        format: int64
        description: 'The timestamp of when the snapshot replication process started.

          Measured in milliseconds since the UNIX epoch.

          '
        readOnly: true
      completed:
        type: integer
        format: int64
        description: 'The timestamp of when the snapshot replication process completed.

          Measured in milliseconds since the UNIX epoch.

          '
        readOnly: true
  _networkInterfacePortDetailsRx_los:
    type: object
    properties:
      flag:
        type: boolean
        description: A value of `true` indicates Rx Loss-of-Signal.
      channel:
        type: integer
        format: int32
        example: 0
        description: 'The channel number if the transceiver is a multilane transceiver.

          If not specified, defaults to `null`.

          '
  ArrayPerformanceByLinkGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ArrayPerformanceByLinkResponse'
  PolicyMemberPost:
    type: object
    properties:
      members:
        type: array
        description: 'A list of resources to which the policy may be applied.

          '
        items:
          $ref: '#/definitions/_policymemberpostMembers'
  SnmpAgentMib:
    type: object
    properties:
      mib:
        type: string
        description: MIB text.
        readOnly: true
  _protocol_endpoint:
    type: object
    properties:
      container_version:
        type: string
        example: '1'
        description: 'Defines vCenter and EXSi host compatibility of the protocol
          endpoint

          and its associated container.

          Valid values include: `1`, `2`, `3`.

          When `container_version` is set to `1`, it''s compatible with vSphere version
          7.0.1 or higher.

          When `container_version` is set to `2`, it''s compatible with vSphere version
          8.0.0 or higher.

          When `container_version` is set to `3`, it''s compatible with vSphere version
          8.0.1 or higher.

          The default `container_version` is `1`.

          '
  Smis:
    type: object
    properties:
      wbem_https_enabled:
        type: boolean
        description: 'If set to `true`, the SMI-S provider and its port, TCP 5989
          is enabled.

          '
      slp_enabled:
        type: boolean
        description: 'If set to `true`, the Service Location Protocol (SLP) and its
          ports,

          TCP 427 and UDP 427, are enabled.

          '
  VchostPost:
    type: object
    properties:
      vcuuid:
        type: string
        example: ddf43502-4e00-405e-b341-e87e7eaa13f6
        description: 'The vCenter Server uuid of the vchost.

          '
  VolumeResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          If `total_only=true`, the `items` list is empty.

          '
        items:
          $ref: '#/definitions/Volume'
  PolicyPasswordGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyPasswordResponse'
    - $ref: '#/definitions/_errorContextResponse'
  PolicyManagementAccessResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/PolicyManagementAccess'
  ResourceAccessStatus:
    type: object
    properties:
      resource:
        type: object
        description: Resource to which the scope has access.
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      scope:
        type: object
        description: Scope which has access to the resource.
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      created_by:
        type: array
        description: 'Reference the IDs of resource accesses that created this status.

          '
        readOnly: true
        items:
          $ref: '#/definitions/ResourceAccessStatusCreatedBy'
    description: 'A resource access status realizes the configuration of resource
      accesses. For instance, if there

      is a resource access for a host group to a realm, resource access status would
      include

      the explicit host group resource access as well as an implicit resource access
      for each host

      in that host group.

      '
  ProtectionGroupGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ProtectionGroupResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            When applicable, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/ProtectionGroup'
  CertificateUseGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          description: A list of certificate use objects.
          items:
            $ref: '#/definitions/CertificateUse'
  ResourceAccessesStatusResponse:
    type: object
    properties:
      items:
        type: array
        description: A list of resource access status objects.
        items:
          $ref: '#/definitions/ResourceAccessStatus'
  ArrayErasureGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ArrayErasureResponse'
  RemoteVolumeSnapshotTransferResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/RemoteVolumeSnapshotTransfer'
  _workloadConfigurationReference:
    allOf:
    - $ref: '#/definitions/_referenceWithFixedType'
    - type: object
      properties:
        configuration:
          type: string
          description: 'The name of the preset configuration object.

            '
  SessionGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/Session'
  SnmpManagerPost:
    type: object
    properties:
      notification:
        type: string
        example: trap
        description: 'The type of notification the agent will send.

          Valid values are `inform` and `trap`.

          '
      v2c:
        $ref: '#/definitions/_snmp_v2c'
      host:
        type: string
        example: snmp.purestorage.com
        description: 'DNS hostname or IP address of a computer that hosts an SNMP
          manager

          to which Purity//FA is to send trap messages when it generates alerts.

          '
      v3:
        $ref: '#/definitions/_snmp_v3_post'
      version:
        type: string
        example: v3
        description: 'Version of the SNMP protocol to be used by Purity//FA to communicate
          with the specified

          manager. Valid values are `v2c` and `v3`.

          '
  _networkInterfacePortDetailsStaticTxBiasThresholds:
    type: object
    properties:
      warn_low:
        type: number
        format: double
        description: The warn low threshold for Tx bias current in mA.
      warn_high:
        type: number
        format: double
        description: The warn high threshold for Tx bias current in mA.
      alarm_high:
        type: number
        format: double
        description: The alarm high threshold for Tx bias current in mA.
      alarm_low:
        type: number
        format: double
        description: The alarm low threshold for Tx bias current in mA.
    description: 'The alarm low, warn low, warn high, alarm high thresholds for Tx
      bias current in mA.

      '
  DirectorySnapshotPost:
    type: object
    properties:
      keep_for:
        type: integer
        format: int64
        description: 'The time to keep the snapshots for, in milliseconds.

          '
      suffix:
        type: string
        description: 'The suffix portion of the client visible snapshot name.

          A full snapshot name is constructed in the form of `DIR.CLIENT_NAME.SUFFIX`
          where

          `DIR` is the managed directory name, `CLIENT_NAME` is the client name, and

          `SUFFIX` is the value of this field.

          The client visible snapshot name is `CLIENT_NAME.SUFFIX`.

          If not specified, defaults to a monotonically increasing number generated
          by the system.

          '
      client_name:
        type: string
        description: 'The client name portion of the client visible snapshot name.

          A full snapshot name is constructed in the form of `DIR.CLIENT_NAME.SUFFIX`
          where

          `DIR` is the managed directory name, `CLIENT_NAME` is the value of this
          field, and

          `SUFFIX` is the suffix. The client visible snapshot name is `CLIENT_NAME.SUFFIX`.

          '
  VolumeSnapshotPatch:
    allOf:
    - $ref: '#/definitions/_destroyedPatchPost'
    - $ref: '#/definitions/_newName'
  Fleet:
    allOf:
    - $ref: '#/definitions/_resource'
    - type: object
      properties:
        is_local:
          type: boolean
          description: 'If set to `true`, the current array is a member of the fleet.

            '
  PodMemberResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name where meaningful.

          If `total_only=true`, the `items` list will be empty.

          '
        items:
          $ref: '#/definitions/PodMember'
  CertificateGroupCertificateResponse:
    type: object
    properties:
      items:
        type: array
        description: 'A list of certificate and certificate group relationships.

          '
        items:
          $ref: '#/definitions/_memberWithType'
  PolicyRuleNfsClientGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyRuleNfsClientResponse'
    - $ref: '#/definitions/_errorContextResponse'
  VolumeBatchPost:
    allOf:
    - $ref: '#/definitions/_volumePostBase'
    - type: object
      properties:
        add_to_protection_groups:
          type: array
          description: 'Specifies a list of protection groups that will compose

            the initial protection for the volume.

            '
          items:
            $ref: '#/definitions/_reference'
        name:
          type: string
          description: 'Specifies the name of the volume.

            '
  PolicySmbGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicySmbResponse'
    - $ref: '#/definitions/_errorContextResponse'
  _context:
    type: object
    properties:
      context:
        type: object
        description: 'The context in which the operation was performed.

          Valid values include a reference to any array which is a member of the same
          fleet.

          If the array is not a member of a fleet, `context` will always implicitly
          be set

          to the array that received the request.


          Other parameters provided with the request, such as names of volumes or
          snapshots,

          are resolved relative to the provided `context`.

          '
        title: FixedReference
        allOf:
        - $ref: '#/definitions/_fixedReference'
  SupportPatch:
    type: object
    properties:
      proxy:
        type: string
        example: http://proxy.example.com:8080
        description: 'The value of the current proxy, which is used for connecting
          to cloud services such as

          phonehome, remote assist, etc.

          Specify the server name, including the scheme and proxy port number.

          '
      phonehome_enabled:
        type: boolean
        description: 'The status of phonehome. If set to `true`, enable phonehome.

          If set to `false`, disable phonehome.

          '
      remote_assist_duration:
        type: integer
        format: int64
        example: 86400000
        description: 'Specifies the duration of the remote assist session in milliseconds.

          This parameter should only be provided when establishing a new session.

          This parameter determines the length of time the session will remain active

          after it is initiated.

          '
      remote_assist_active:
        type: boolean
        description: 'The status of a remote assist session. If set to `true`, enable
          the remote assist session.

          If set to `false`, disable the remote assist session.

          '
  _networkInterfacePortDetailsTemperature:
    type: object
    properties:
      channel:
        type: integer
        format: int32
        example: 0
        description: 'The channel number if the diagnostic has an associated channel
          number.

          If not specified, defaults to `null`.

          '
      measurement:
        type: number
        format: double
        description: 'Temperature in degrees Celsius.

          '
      status:
        type: string
        example: ok
        description: 'Indicates whether the measurement is outside the high alarm,
          low alarm, high warning,

          or low warning thresholds for temperature.

          Valid values are `alarm low`, `alarm high`, `warn low`, `warn high`, and
          `ok`.

          '
  _snapshot:
    type: object
    properties:
      time_remaining:
        type: integer
        format: int64
        description: 'The amount of time left until the destroyed snapshot is permanently
          eradicated.

          Measured in milliseconds.

          Before the `time_remaining` period has elapsed, the destroyed snapshot can
          be recovered

          by setting `destroyed=false`.

          '
        readOnly: true
      destroyed:
        type: boolean
        description: 'Returns a value of `true` if the snapshot has been destroyed
          and is pending eradication.

          The `time_remaining` value displays the amount of time left

          until the destroyed volume snapshot is permanently eradicated.

          Before the `time_remaining` period has elapsed, the destroyed volume snapshot
          can be recovered

          by setting `destroyed=false`.

          Once the `time_remaining` period has elapsed,

          the volume snapshot is permanently eradicated and can no longer be recovered.

          '
      pod:
        type: object
        description: 'A reference to the pod.

          '
        title: FixedReference
        allOf:
        - $ref: '#/definitions/_fixedReference'
      provisioned:
        type: integer
        format: int64
        description: 'The provisioned space of the snapshot. Measured in bytes. The
          minimum size is 1048576 (1MB),

          the maximum size is 4503599627370496 (4PB)

          '
        readOnly: true
      created:
        type: integer
        format: int64
        description: 'The snapshot creation time. Measured in milliseconds since the
          UNIX epoch.

          '
        readOnly: true
      source:
        type: object
        description: 'The volume from which this snapshot was taken.

          For a replicated snapshot being viewed on the target side, the

          `source` is the replica volume.

          '
        title: FixedReference
        allOf:
        - $ref: '#/definitions/_fixedReference'
      suffix:
        type: string
        description: 'The suffix that is appended to the `source_name` value to generate
          the full

          volume snapshot name in the form `VOL.SUFFIX`.

          If the suffix is not specified, the system constructs the snapshot name
          in the form `VOL.NNN`,

          where `VOL` is the volume name, and `NNN` is a monotonically increasing
          number.

          '
  PolicyMember:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left, in milliseconds, until the destroyed
            policy member is

            permanently eradicated.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the member is destroyed.

            '
          readOnly: true
        member:
          type: object
          description: Reference to the resource that the policy is applied to.
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
        enabled:
          type: boolean
          description: 'Returns a value of `true` if the policy is enabled.

            '
          readOnly: true
        policy:
          type: object
          description: Reference to the policy.
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
  _workloadPlacementRecommendationTarget:
    allOf:
    - $ref: '#/definitions/_referenceWithType'
    - type: object
      properties:
        warnings:
          type: array
          description: 'The warnings for the target.

            '
          items:
            $ref: '#/definitions/_workloadPlacementRecommendationWarning'
        model:
          type: string
          description: 'The model of the target.

            For example, `FA-X20R2`.

            '
        load_projections:
          $ref: '#/definitions/_workloadPlacementRecommendationTargetLoadProjections'
        capacity:
          type: integer
          format: int64
          example: 10995116277760
          description: 'The usable capacity of the target, measured in bytes.

            '
        capacity_used_projections:
          $ref: '#/definitions/_workloadPlacementRecommendationTargetCapacityUsedProjections'
    description: 'Data for a target in a workload placement recommendation.

      Target is a FlashArray, which may be used for workload placement.

      '
  PolicyMemberCleanGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyMemberCleanResponse'
    - $ref: '#/definitions/_errorContextResponse'
  ResourcePerformanceNoId:
    allOf:
    - $ref: '#/definitions/_performance'
    - $ref: '#/definitions/_resourceNoId'
    - $ref: '#/definitions/_context'
  _fixedReference:
    type: object
    properties:
      name:
        type: string
        description: 'The resource name,

          such as volume name, pod name, snapshot name, and so on.

          '
        readOnly: true
      id:
        type: string
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified.

          '
        readOnly: true
  _workloadPlacementRecommendationAdditionalConstraints:
    type: object
    properties:
      required_resource_references:
        type: array
        description: 'Defines a group of resources. At least one resource from each
          item

          should be present on this target for the target to be considered an

          acceptable candidate for workload placement

          '
        items:
          $ref: '#/definitions/_workloadPlacementRecommendationResourceReference'
      targets:
        type: array
        description: 'If specified, only consider these targets when computing placement

          recommendations. If not specified, the whole fleet is used to

          compute the recommendation.

          '
        items:
          $ref: '#/definitions/_referenceWithType'
    description: 'The list of targets considered for workload placement must

      satisfy the requirements imposed by the preset. It is

      also possible to specify additional constraints to further restrict the

      list of targets for consideration.

      '
  RemoteProtectionGroupSnapshotResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/RemoteProtectionGroupSnapshot'
  _throttle:
    type: object
    properties:
      window:
        type: object
        description: The time during which the `window_limit` threshold is in effect.
        title: TimeWindow
        allOf:
        - $ref: '#/definitions/_time_window'
      default_limit:
        type: integer
        format: int64
        example: 1073741824
        description: 'Default maximum bandwidth threshold for outbound traffic in
          bytes. Once

          exceeded, bandwidth throttling occurs.

          '
      window_limit:
        type: integer
        format: int64
        example: 2097152
        description: 'Maximum bandwidth threshold for outbound traffic during the
          specified

          `window_limit` time range in bytes. Once exceeded, bandwidth throttling

          occurs.

          '
    description: 'The bandwidth throttling for an array connection. Configurable on
      PATCH only.

      '
  ActiveDirectoryGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ActiveDirectoryResponse'
  _chap:
    type: object
    properties:
      host_password:
        type: string
        description: 'The host password for CHAP authentication.

          The password must be between 12 and 255 characters (inclusive)

          and cannot be the same as the target password.

          '
      target_password:
        type: string
        description: 'The target password for CHAP authentication.

          The password must be between 12 and 255 characters (inclusive)

          and cannot be the same as the host password.

          '
      target_user:
        type: string
        description: The target username for CHAP authentication.
      host_user:
        type: string
        description: The host username for CHAP authentication.
    description: Challenge-Handshake Authentication Protocol (CHAP).
  _arrayencryptionDataAtRest:
    type: object
    properties:
      enabled:
        type: boolean
        description: 'Returns a value of `true` if data at rest encryption is enabled
          on

          the array.

          '
      algorithm:
        type: string
        example: AES-256-CTR
        description: 'The algorithm used to encrypt data on the array.

          Values include `AES-256-CTR` and `null`.

          '
    description: 'Data at rest encryption properties.

      '
  DirectoryExportGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/DirectoryExportResponse'
    - $ref: '#/definitions/_errorContextResponse'
  SoftwareChecksGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SoftwareChecksResponse'
  ProtectionGroupSnapshotTransfer:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_transfer'
    - $ref: '#/definitions/_context'
    - type: object
      description: 'The transfer statistics for snapshot replication data.

        '
  ProtectionGroupsVolumesResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/ProtectionGroupsVolumes'
  AlertEvent:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        severity:
          type: string
          example: critical
          description: 'The severity level of the alert.

            Valid values include `info`, `warning`, `critical`, and `hidden`.

            '
          readOnly: true
        summary:
          type: string
          example: boot drive overutilization
          description: A summary of the alert.
          readOnly: true
        actual:
          type: string
          example: 95%
          description: Actual condition at the time the alert is created.
          readOnly: true
        code:
          type: integer
          format: int64
          example: 7
          description: The parent alert number.
          readOnly: true
        issue:
          type: string
          example: boot drive overutilization
          description: Information about the alert cause.
          readOnly: true
        knowledge_base_url:
          type: string
          example: https://support.purestorage.com/?cid=Alert_0007
          description: The knowledge base URL of the alert.
          readOnly: true
        created:
          type: integer
          format: int64
          example: 1576275532434
          description: The time the parent alert was created.
        component_name:
          type: string
          example: vm-tom
          description: The component type of the alert.
          readOnly: true
        expected:
          type: string
          example: 90%
          description: Expected state and threshold under normal conditions.
          readOnly: true
        component_type:
          type: string
          example: storage
          description: The component name of the alert.
          readOnly: true
        alert:
          $ref: '#/definitions/_fixedReference'
        state:
          type: string
          example: open
          description: 'The state of the alert.

            Valid values include `open`, `closing`, and `closed`.

            '
          readOnly: true
        time:
          type: integer
          format: int64
          example: 1578440491109
          description: The time the event occurred.
          readOnly: true
      description: Alert timeline event.
  LocalUserPost:
    type: object
    properties:
      uid:
        type: integer
        format: int32
        description: Optional field to set the UID of the local user.
      password:
        type: string
        description: 'The password of the local user. This field is only required
          if the `enabled` field is `true`.

          '
      primary_group:
        type: object
        description: 'Local group that would be assigned as the primary group of the
          local user.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      email:
        type: string
        description: Optional field to set the email of the local user.
      enabled:
        type: boolean
        description: 'If this field is `false`, the local user will be disabled on
          creation.

          Otherwise, the local user will be enabled and functional.

          '
  _policyrulemanagementaccessRules:
    type: object
    properties:
      role:
        type: object
        description: 'Role defines a set of permissions applicable in a scope. `viewer`
          grants users

          the ability to read a resource. `support` grants the ability to perform
          general

          support-related actions, as well as read resources. `storage` grants access
          to all

          storage operations on a resource. `admin` grants access to all operations.

          '
        title: ReferenceNoId
        allOf:
        - $ref: '#/definitions/_referenceNoId'
      scope:
        type: object
        description: 'Reference to the resource (e.g. arrays, realms) which specifies
          the scope that

          the role applies. The `id` or `name` parameter is required, but they cannot
          be set together.

          Also, `resource_type` must be set.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
  ProtectionGroupSnapshotReplica:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/ProtectionGroupSnapshot'
    - type: object
      description: 'Replication of specified protection snapshots.

        '
  VolumeGroupGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/VolumeGroupResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where meaningful, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/VolumeGroup'
  SupportRemoteAssistSessionsPatch:
    type: object
    properties:
      duration:
        type: integer
        format: int64
        example: 86400000
        description: 'Specifies the duration of the remote assist session in milliseconds.

          This parameter should only be provided when establishing a new session.

          This parameter determines the length of time the session will remain active

          after initiation.

          Defaults to 86400000 (24h) with a min of 14400000 (4h) and a max of 172800000
          (48h).

          '
      access_level_override:
        type: string
        description: 'Overrides the default access level for the remote assist session
          only.

          Values include `restricted` and `elevated`.

          If not provided, the remote assist session will use the default_access_level
          setting.

          '
      active:
        type: boolean
        description: 'The status of a remote assist session. If set to `true`, enables
          the remote assist session.

          If set to `false`, disables the remote assist session.

          '
  ResourceAccessStatusCreatedBy:
    type: object
    properties:
      implicit:
        type: boolean
        description: 'Set to `true` when this resource access status was not explicitly
          created, but instead, its

          existence is implied by another resource access. Usually, this happens when
          a scope has access

          to a group of resources (e.g., a host group). Then this scope has access
          to each member of

          the group, individual hosts. Set to `false` when a resource access status
          reflects explicitly

          created resource access.

          Note that if there is both implicit and explicit access of an object to
          a scope, there will be

          only one entry in the response for that object and scope pair. The `created_by`
          field will

          contain both implicit and explicit resource access references.


          '
      id:
        type: string
        description: ID of a Resource Access.
    description: 'Each Resource Access Status has a reference to the resource accesses
      that explain its existence.

      '
  SnmpAgentMibResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SnmpAgentMib'
  VchostCertificate:
    type: object
    properties:
      endpoints:
        type: array
        description: 'The IPv4 or IPv6 addresses of the endpoints to configure for
          the vchost, over which the

          configured certificate will be presented.

          '
        items:
          type: string
          example: 1.2.3.4
      certificate:
        type: object
        description: 'A reference to the certificate that will be presented to clients
          accessing the

          referenced `vchost` using any of the network addresses defined by `endpoints`.

          '
        title: ReferenceNoIdWithType
        allOf:
        - $ref: '#/definitions/_referenceNoIdWithType'
      vchost:
        type: object
        description: 'The vchost by which the certificate is to be presented over
          the configured endpoints.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      id:
        type: string
        example: uuid-of-association
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified.

          '
      is_local:
        type: boolean
        description: 'Returns true if the vchost certificate is local to the array,
          and false otherwise.

          '
  SoftwareBundleGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SoftwareBundleResponse'
  Vchost:
    allOf:
    - $ref: '#/definitions/_resource'
    - type: object
      properties:
        vcuuid:
          type: string
          example: ddf43502-4e00-405e-b341-e87e7eaa13f6
          description: 'The vCenter Server uuid of the vchost.

            '
  PortInitiatorsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/PortInitiator'
  SoftwareChecksResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SoftwareCheck'
  LocalGroupResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/LocalGroup'
  RealmPatch:
    allOf:
    - $ref: '#/definitions/_newName'
    - type: object
      properties:
        quota_limit:
          type: integer
          format: int64
          description: 'The logical quota limit of the realm, measured in bytes.

            '
          minimum: 1048576
          maximum: 4503599627370496
        destroyed:
          type: boolean
          description: 'If set to `true`, the realm will be destroyed and pending
            eradication.

            The `time_remaining` value displays the amount of time left

            until the destroyed realm is permanently eradicated.

            A realm can only be destroyed if it is empty or destroy_contents is set
            to true.

            Before the `time_remaining` period has elapsed, the destroyed realm can
            be recovered

            by setting `destroyed=false`.

            Once the `time_remaining` period has elapsed,

            the realm is permanently eradicated and can no longer be recovered.

            '
        qos:
          type: object
          description: 'Sets QoS limits.

            '
          title: ContainerQos
          allOf:
          - $ref: '#/definitions/_containerQos'
  _workloadPlacementRecommendationWarning:
    type: object
    properties:
      code:
        type: string
        description: 'The warning code.

          Possible values include `limit_volumes_count`, `limit_pods_count`, etc.

          '
      message:
        type: string
        description: 'The warning message. Possible values include

          `Adding the workload on the array would exceed the limit on volumes`,

          `Adding the workload on the array would exceed the limit on pods`, etc.

          '
  PolicyAuditFilePost:
    allOf:
    - $ref: '#/definitions/PolicyPost'
    - type: object
      properties:
        log_targets:
          type: array
          description: 'A list of targets to which audit logs will be sent.

            '
          items:
            $ref: '#/definitions/_referenceNoIdWithType'
  FleetPatch:
    allOf:
    - $ref: '#/definitions/_newName'
    - type: object
  DirectoryQuota:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        quota_limit:
          type: integer
          format: int64
          description: 'Effective quota limit imposed by the quota policy rule attached
            to the directory, measured in

            bytes.

            '
          readOnly: true
          minimum: 0
        enforced:
          type: boolean
          description: 'Defines whether the quota rule is enforced or unenforced.

            If the quota rule is enforced and logical space usage exceeds the quota
            limit,

            any modification operations that result in a need for more space are blocked.

            If the quota rule is unenforced and logical space usage exceeds the quota
            limit, notification

            emails are sent to targets that are specified using the `notification`
            parameter.

            No client operations are blocked when an unenforced limit is exceeded.

            If set to `true`, the limit is enforced. If set to `false`, notification
            targets are informed

            when the usage exceeds 80 percent of the limit.

            '
        path:
          type: string
          description: 'Absolute path of the directory in the file system.

            '
          readOnly: true
        rule_name:
          type: string
          description: 'Name of the rule that results in this quota and behavior being
            applied to this directory.

            '
          readOnly: true
        percentage_used:
          type: number
          format: float
          description: 'The percentage of the space used in the directory with respect
            to the quota limit.

            '
          readOnly: true
          minimum: 0
        usage:
          type: integer
          format: int64
          description: 'The amount of logically written data for the directory, measured
            in bytes.

            '
          readOnly: true
          minimum: 0
        directory:
          type: object
          description: The directory to which the quota applies.
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
        enabled:
          type: boolean
          description: 'Returns a value of `true` if the policy is enabled.

            '
          readOnly: true
        policy:
          type: object
          description: 'The effective quota policy that imposes the limit. This is
            the policy with the lowest limit.

            '
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
  MemberNoIdMember:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        member:
          type: object
          description: 'The member of the resource.

            '
          title: ReferenceNoId
          allOf:
          - $ref: '#/definitions/_referenceNoId'
        group:
          type: object
          description: 'The resource in which the host, volume, or other item in the
            environment is a member.

            '
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
  DirectoryServiceRole:
    allOf:
    - $ref: '#/definitions/_fixedNameResourceNoId'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        management_access_policies:
          type: array
          description: 'List of management access policies associated with the group.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_fixedReferenceWithType'
        role:
          type: object
          description: 'This field has been deprecated. Predecessor to management
            access policies.

            '
          title: ReferenceNoId
          allOf:
          - $ref: '#/definitions/_referenceNoId'
        group_base:
          type: string
          example: OU=PureGroups,OU=SANManagers
          description: 'Specifies where the configured group is located in the directory
            tree.

            '
        group:
          type: string
          example: groupOfUsers
          description: 'Group name that contains users with the abilities granted
            by the associated policies.

            '
  LogTarget:
    allOf:
    - $ref: '#/definitions/_fixedNameResourceNoId'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        target_type:
          type: string
          example: syslog
          description: 'The type of log target. Valid values include `file`, and `syslog`.

            '
          readOnly: true
  VirtualMachineVolumeSnapshot:
    type: object
    properties:
      time_remaining:
        type: integer
        format: int64
        description: 'Specifies the amount of time left until the destroyed volume
          snapshot is

          permanently eradicated, measured in milliseconds.

          Once the `time_remaining` period has elapsed,

          the volume snapshot is permanently eradicated and can no longer be recovered.

          '
        readOnly: true
      destroyed:
        type: boolean
        description: 'Returns a value of `true` if the virtual machine volume snapshot

          has been destroyed and is pending eradication.

          '
        readOnly: true
      vm_id:
        type: string
        description: 'The ID of the virtual machine, as assigned by the external system.

          '
        readOnly: true
      vm_type:
        type: string
        example: vvol
        description: 'The type of virtual machine.

          The only valid value is `vvol`.

          '
        readOnly: true
      created:
        type: integer
        format: int64
        description: 'The virtual machine volume snapshot creation time

          measured in milliseconds since the UNIX epoch.

          '
        readOnly: true
      name:
        type: string
        description: 'The name of the virtual machine volume snapshot.

          '
        readOnly: true
      recover_context:
        type: object
        description: 'A reference to any additional entities needed to recover this
          virtual machine.

          '
        title: FixedReference
        allOf:
        - $ref: '#/definitions/_fixedReference'
      vvol_type:
        type: string
        example: config
        description: 'The type of virtual machine volume. Values include `config`
          and `data`.

          '
        readOnly: true
      id:
        type: string
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified.

          '
        readOnly: true
      vvol_name:
        type: string
        description: 'The name of the virtual machine volume.

          '
        readOnly: true
  ApiClientGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ApiClientResponse'
  MappingPolicy:
    allOf:
    - $ref: '#/definitions/_builtInRelationship'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        mapping:
          type: string
          description: 'The mapping for this policy mapping.

            Valid values are `connected` and `disconnected`.

            '
        policy_type:
          type: string
          description: 'The type of policies involved in this policy mapping.

            '
        remote_policy:
          type: object
          description: 'Reference to a remote policy.

            '
          title: ReferenceWithType
          allOf:
          - $ref: '#/definitions/_referenceWithType'
        local_policy:
          type: object
          description: 'Reference to a local policy.

            '
          title: ReferenceWithType
          allOf:
          - $ref: '#/definitions/_referenceWithType'
        direction:
          type: string
          description: 'The direction of replication. Valid values include `inbound`
            and `outbound`.

            '
        pod_replica_link:
          type: object
          description: 'Reference to a pod replica link.

            '
          title: PodReplicaLinkReference
          allOf:
          - $ref: '#/definitions/_podReplicaLinkReference'
  AdminResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Admin'
  PolicyPatch:
    allOf:
    - $ref: '#/definitions/_newName'
    - type: object
      properties:
        enabled:
          type: boolean
          description: If set to `true`, enables the policy. If set to `false`, disables
            the policy.
  RemoteProtectionGroupGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/RemoteProtectionGroupResponse'
    - $ref: '#/definitions/_errorContextResponse'
  KmipTestResultGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/KmipTestResult'
  _volumeCommon:
    allOf:
    - $ref: '#/definitions/_resource'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left until the destroyed volume is permanently
            eradicated,

            measured in milliseconds.

            Before the `time_remaining` period has elapsed, the destroyed volume can
            be recovered

            by setting `destroyed=false`.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the volume has been destroyed
            and is pending eradication.

            The `time_remaining` value displays the amount of time left until the
            destroyed volume is

            permanently eradicated.

            Before the `time_remaining` period has elapsed, the destroyed volume can
            be recovered

            by setting `destroyed=false`.

            Once the `time_remaining` period has elapsed, the volume is permanently
            eradicated and can

            no longer be recovered.

            '
        host_encryption_key_status:
          type: string
          example: detected
          description: 'The host encryption key status for this volume.

            Values include `none`, `detected`, and `fetched`.

            '
          readOnly: true
        provisioned:
          type: integer
          format: int64
          description: 'The virtual size of the volume as a multiple of 512, measured
            in bytes.

            The maximum size is 4503599627370496 (4PB).

            '
        qos:
          type: object
          description: 'Displays QoS limit information.

            '
          title: Qos
          allOf:
          - $ref: '#/definitions/_qos'
        serial:
          type: string
          description: 'A globally unique serial number generated by the FlashArray
            when the volume is created.

            '
          readOnly: true
        created:
          type: integer
          format: int64
          description: 'The volume creation time, measured in milliseconds since the
            UNIX epoch.

            '
          readOnly: true
        connection_count:
          type: integer
          format: int64
          description: 'The total number of hosts and host groups connected to the
            volume.

            '
          readOnly: true
        priority_adjustment:
          type: object
          description: 'Priority adjustment operator and value.

            '
          title: PriorityAdjustment
          allOf:
          - $ref: '#/definitions/_priorityAdjustment'
        space:
          type: object
          description: 'Displays size and space consumption information.

            '
          title: VolumeSpaceCommon
          allOf:
          - $ref: '#/definitions/_volumeSpaceCommon'
      description: 'A random access virtual storage device that a FlashArray exports
        to hosts via a

        logical unit number (LUN). For a host, a volume contains a number of 512-byte
        sectors in which

        data can be written and from which it can be read.

        '
  CertificateUse:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - type: object
      properties:
        use:
          type: object
          description: A reference to an object using this certificate.
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
        group:
          type: object
          description: 'A reference to the certificate group that is being used, if
            any, where

            this certificate is a member of the certificate-group.

            Returns `null` if the referenced user object is not associated

            with a group and is directly using this certificate.

            '
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
  ProtectionGroupSnapshotGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ProtectionGroupSnapshotResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            If applicable, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/ProtectionGroupSnapshot'
  _policyrulequotapostRules:
    type: object
    properties:
      quota_limit:
        type: integer
        format: int64
        description: 'Logical space limit of the quota (in bytes) assigned by the
          rule.

          This value cannot be set to 0.

          '
      enforced:
        type: boolean
        description: 'If set to `true`, this rule describes an enforced quota. An
          out-of-space warning is issued if

          logical space usage exceeds the limit value described in this rule.

          If set to `false`, this rule describes an unenforced quota. Alerts and/or
          notifications are

          issued when logical space usage exceeds the limit value described in this
          rule.

          If not specified, defaults to `false`.

          '
      notifications:
        type: string
        description: 'Targets to notify when usage approaches the quota limit.

          The list of notification targets is a comma-separated string.

          Valid values are one or more of `user` and `group`. To notify no targets,
          use `none`.

          If not specified, defaults to `none`.

          '
  Saml2SsoGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/Saml2SsoResponse'
  _reference:
    type: object
    properties:
      name:
        type: string
        description: 'The resource name,

          such as volume name, pod name, snapshot name, and so on.

          '
      id:
        type: string
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified.

          '
  _workloadPlacementRecommendationResourceReference:
    type: object
    required:
    - allowed_values
    properties:
      allowed_values:
        type: array
        description: 'A list of allowed values for the resource reference.

          For hosts and host groups, each item does not define a specific object on

          some target, but defines a pattern which should be matched by the target.

          For example, { `type`: `host`, `name`: `host1` } will match all targets

          that have a host with name `host1`.

          '
        items:
          $ref: '#/definitions/_referenceWithType'
    description: 'Defines a group of resources. At least one of them should be present

      on this target to use it for computation of a recommendation.

      Currently only hosts or host groups could be used.

      '
  RemoteProtectionGroupSnapshotTransfer:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_transfer'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        source:
          type: object
          description: 'The original protection group from which this snapshot was
            taken.

            '
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
      description: 'The transfer statistics for snapshot replication.

        '
  ProtectionGroupPerformanceArray:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_aggregate_replication_performance'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        source:
          type: string
          description: 'The source array from where the data is replicated.

            '
          readOnly: true
        time:
          type: integer
          format: int64
          description: 'The time when the sample performance data was taken.

            Measured in milliseconds since the UNIX epoch.

            '
          readOnly: true
        target:
          type: string
          description: 'The target to where the data is replicated.

            '
          readOnly: true
  _workloadPlacementRecommendationProjection:
    type: object
    properties:
      start:
        type: number
        format: float
        example: 0.77
        description: 'The projection for the start of the projection period (i.e.,
          immediately

          after the creation of the workload on this target).

          The percentage is displayed as a decimal value, starting at 0.00 and ending
          at 1.00.

          '
      end:
        type: number
        format: float
        example: 0.77
        description: 'The projection for the end of the projection period. Length
          of the

          projection period is defined by ''projection_months''.

          The percentage is displayed as a decimal value, starting at 0.00 and ending
          at 1.00.

          '
  _presetWorkloadPlacementConfiguration:
    type: object
    required:
    - name
    - storage_class
    properties:
      storage_class:
        type: object
        description: 'The storage class to which the placement and its associated
          storage resources may be deployed.

          Supports parameterization.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      name:
        type: string
        description: 'The name of the placement configuration, by which other configuration
          objects in the preset

          can reference it. Name must be unique across all configuration objects in
          the preset.

          '
      qos_configurations:
        type: array
        description: 'The names of the QoS configurations to apply to the storage
          resources (such as volumes) in

          the placement. The limits defined in the QoS configurations will be shared
          across all storage

          resources in the placement.

          '
        items:
          type: string
        maxItems: 1
  _aggregate_replication_performance:
    type: object
    properties:
      bytes_per_sec:
        type: integer
        format: int64
        description: The total number of bytes of replication data transmitted and
          received per second.
        readOnly: true
        minimum: 0
  CloudConfigModelPatch:
    type: object
    properties:
      requested:
        type: string
        example: CBS-V20MP2R2
        description: A supported model for the CBS array.
      add_override_checks:
        type: array
        description: A list of upgrade checks to be overridden.
        items:
          type: string
  SubscriptionAssetResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SubscriptionAsset'
  _saml2SsoSp:
    allOf:
    - $ref: '#/definitions/_saml2SsoSpCredential'
    - type: object
      properties:
        metadata_url:
          type: string
          example: https://myarray.mycompany.com/saml2/service-provider-metadata/myidp
          description: 'The URL of the service provider metadata.

            '
        assertion_consumer_url:
          type: string
          example: https://myarray.mycompany.com/login/saml2/sso/myidp
          description: 'The URL where the identity provider will send its SAML response
            after authenticating a user.

            '
        entity_id:
          type: string
          example: https://myarray.mycompany.com/saml2/service-provider-metadata/myidp
          description: 'A globally unique name for the service provider.

            '
      description: 'Properties specific to the service provider.

        '
  PolicyRuleSnapshot:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left, in milliseconds, until the destroyed
            snapshot policy rule is

            permanently eradicated.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the pod containing the snapshot
            policy rule has been destroyed

            and is pending eradication.

            The `time_remaining` value displays the amount of time left until the
            destroyed policy is

            permanently eradicated.

            Once the `time_remaining` period has elapsed, the snapshot policy rule
            is permanently

            eradicated and can no longer be recovered.

            '
        at:
          type: integer
          format: int64
          description: 'Specifies the number of milliseconds since midnight at which
            to take a snapshot.

            The `at` value can only be set to an hour and must be between 0 and 82800000,
            inclusive.

            The `at` value can only be set on the rule with the smallest `every` value.

            The `at` value cannot be set if the `every` value is not measured in days.

            The `at` value can only be set for at most one rule in the same policy.

            '
          readOnly: true
        name:
          type: string
          description: 'Name of this rule. The name is automatically generated by
            the system.

            '
          readOnly: true
        keep_for:
          type: integer
          format: int64
          description: 'Specifies the period that snapshots are retained before they
            are eradicated, in

            milliseconds.

            The `keep_for` value cannot be less than the `every` value of the rule.

            The `keep_for` value must be unique for each rule in the same policy.

            The `keep_for` value must be between 5 minutes and 5 years.

            The `keep_for` value cannot be less than the `keep_for` value of any rule
            in the same

            policy with a smaller `every` value.

            '
          readOnly: true
        suffix:
          type: string
          description: 'The snapshot suffix name.

            A full snapshot name is constructed in the form of `DIR.CLIENT_NAME.SUFFIX`
            where

            `DIR` is the managed directory name, `CLIENT_NAME` is the snapshot client
            name,

            and `SUFFIX` is the snapshot suffix.

            The client-visible snapshot name is `CLIENT_NAME.SUFFIX`.

            The `suffix` value can only be set for one rule in the same policy.

            The `suffix` value can only be set on a rule with the same `keep_for`
            value

            and `every` value.

            The `suffix` value can only be set on the rule with the largest `keep_for`
            value.

            If not specified, defaults to a monotonically increasing number generated

            by the system.

            '
          readOnly: true
        client_name:
          type: string
          description: 'The snapshot client name.

            A full snapshot name is constructed in the form of `DIR.CLIENT_NAME.SUFFIX`
            where

            `DIR` is the managed directory name, `CLIENT_NAME` is the snapshot client
            name,

            and `SUFFIX` is the snapshot suffix.

            The client visible snapshot name is `CLIENT_NAME.SUFFIX`.

            '
          readOnly: true
        every:
          type: integer
          format: int64
          description: 'Specifies the interval between snapshots, in milliseconds.

            The `every` value for all rules must be multiples of one another.

            The `every` value must be unique for each rule in the same policy.

            The `every` value must be between 5 minutes and 1 year.

            '
          readOnly: true
        policy:
          type: object
          description: The policy to which this rule belongs.
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
  _networkInterfacePortDetailsTx_power:
    type: object
    properties:
      channel:
        type: integer
        format: int32
        example: 0
        description: 'The channel number if the transceiver is a multilane transceiver.

          If not specified, defaults to `null`.

          '
      measurement:
        type: number
        format: double
        description: 'Tx output power in mW.

          '
      status:
        type: string
        example: ok
        description: 'Indicates whether the measurement is outside the high alarm,
          low alarm, high warning,

          or low warning thresholds for Tx output power.

          Valid values are `alarm low`, `alarm high`, `warn low`, `warn high`, and
          `ok`.

          '
  _protectionGroupsVolumesMember:
    allOf:
    - $ref: '#/definitions/_reference'
    - type: object
      properties:
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the volume has been destroyed
            and is pending eradication.

            Through the `volumes` endpoint, the user can see `time_remaining` of the
            destroyed

            volume, recover, or eradicate the destroyed volume.

            '
  DirectoryServiceResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/DirectoryService'
  AdminCacheGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/AdminCacheResponse'
  VolumeSnapshotResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          If `total_only=true`, the `items` list will be empty.

          '
        items:
          $ref: '#/definitions/VolumeSnapshot'
  Host:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        space:
          type: object
          description: 'Displays provisioned (virtual) size and physical storage consumption

            information for the sum of all volumes connected to the specified host.

            '
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left until the destroyed host is permanently
            eradicated,

            measured in milliseconds.

            '
          readOnly: true
        host_group:
          type: object
          description: The host group to which the host should be associated.
          title: ReferenceNoId
          allOf:
          - $ref: '#/definitions/_referenceNoId'
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the host has been destroyed

            with its container realm and is pending eradication.

            The `time_remaining` value displays the amount of time left

            until the destroyed host is permanently eradicated.

            Before the `time_remaining` period has elapsed, the destroyed host

            will be recovered if its container realm is recovered.

            Once the `time_remaining` period has elapsed,

            the host is permanently eradicated and can no longer be recovered.

            '
          readOnly: true
        port_connectivity:
          $ref: '#/definitions/_host_port_connectivity'
        wwns:
          type: array
          description: The Fibre Channel World Wide Name (WWN) associated with the
            host.
          items:
            type: string
            example: 52:4A:93:70:4E:E5:68:00
        personality:
          type: string
          description: 'Determines how the system tunes the array to ensure that it
            works optimally with the host.

            Set `personality` to the name of the host operating system or virtual
            memory system.

            Valid values are `aix`, `esxi`, `hitachi-vsp`, `hpux`,

            `oracle-vm-server`, `solaris`, and `vms`.

            If your system is not listed as one of the valid host personalities, do
            not set the option.

            By default, the personality is not set.

            '
        vlan:
          type: string
          example: '1'
          description: 'The VLAN ID that the host is associated with.

            If set to `any`, the host can access any VLAN.

            If set to `untagged`, the host can only access untagged VLANs.

            If set to a number between `1` and `4094`,

            the host can only access the specified VLAN with that number.

            '
        connection_count:
          type: integer
          format: int64
          example: 3
          description: The number of volumes connected to the specified host.
          readOnly: true
        chap:
          $ref: '#/definitions/_chap'
        nqns:
          type: array
          description: The NVMe Qualified Name (NQN) associated with the host.
          items:
            type: string
            example: nqn.2010-06.com.purestorage:flasharray.512f7bb185978505
        iqns:
          type: array
          description: 'The iSCSI qualified name (IQN) associated with the host.

            '
          items:
            type: string
            example: iqn.2010-06.com.purestorage:linux.393d930077be2977
        is_local:
          type: boolean
          description: 'If set to `true`, the location reference is to the local array.

            If set to `false`, the location reference is to a remote location,

            such as a remote array or offload target.

            '
          readOnly: true
        preferred_arrays:
          type: array
          description: 'For synchronous replication configurations, sets a host''s
            preferred array to specify which

            array exposes active/optimized paths to that host.

            Enter multiple preferred arrays in comma-separated format.

            If a preferred array is set for a host,

            then the other arrays in the same pod will expose active/non-optimized
            paths to that host.

            If the host is in a host group, `preferred_arrays` cannot be set because
            host groups

            have their own preferred arrays.

            On a preferred array of a certain host, all the paths on all the ports

            (for both the primary and secondary controllers) are set up as A/O (active/optimized)
            paths,

            while on a non-preferred array, all the paths are A/N (Active/Non-optimized)
            paths.

            '
          items:
            $ref: '#/definitions/_reference'
  RealmGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/RealmResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering. When applicable,
            the average value is

            displayed instead. The values are displayed for each field, if meaningful.

            '
          items:
            $ref: '#/definitions/Realm'
  _networkinterfacepatchEth:
    type: object
    properties:
      add_subinterfaces:
        type: array
        description: 'Child devices to be added to the specified bond interface.

          '
        items:
          $ref: '#/definitions/_referenceNoId'
      subnet:
        type: object
        description: 'Subnet that is associated with the specified network interface.

          '
        title: ReferenceNoId
        allOf:
        - $ref: '#/definitions/_referenceNoId'
      address:
        type: string
        description: 'The IPv4 or IPv6 address to be associated with the specified
          network interface.

          '
      netmask:
        type: string
        description: 'Netmask of the specified network interface that, when combined
          with the

          address of the interface, determines the network address of the interface.

          '
      remove_subinterfaces:
        type: array
        description: 'Child devices to be removed from the specified bond interface.

          '
        items:
          $ref: '#/definitions/_referenceNoId'
      subinterfaces:
        type: array
        description: 'Child devices to be added to the specified bond interface.

          '
        items:
          $ref: '#/definitions/_referenceNoId'
      gateway:
        type: string
        description: 'The IPv4 or IPv6 address of the gateway through which the specified
          network interface

          is to communicate with the network.

          '
      mtu:
        type: integer
        format: int32
        description: 'Maximum message transfer unit (packet) size for the network
          interface in bytes.

          MTU setting cannot exceed the MTU of the corresponding physical interface.

          '
    description: Ethernet network interface properties.
  CertificateCertificateGroupResponse:
    type: object
    properties:
      items:
        type: array
        description: 'A list of certificate and certificate group relationships.

          '
        items:
          $ref: '#/definitions/_memberWithType'
  CertificateSigningRequestPost:
    type: object
    properties:
      country:
        type: string
        example: CA
        description: Two-letter country (ISO) code listed in the certificate.
      organization:
        type: string
        example: Veridian Dynamics
        description: The organization field listed in the certificate.
      subject_alternative_names:
        type: array
        description: 'The alternative names that are secured by this certificate.

          Alternative names include IP addresses, DNS names, or URIs.

          '
        items:
          type: string
          example: otherdnsname.mydomain.com
      certificate:
        $ref: '#/definitions/_referenceNoId'
      locality:
        type: string
        example: Toronto
        description: The locality field listed in the certificate.
      state:
        type: string
        example: Ontario
        description: The state/province field listed in the certificate.
      common_name:
        type: string
        example: Pure Storage Inc.
        description: The common name field listed in the certificate.
      email:
        type: string
        example: tcrisp@veridiandynamics.com
        description: The email field listed in the certificate.
      organizational_unit:
        type: string
        example: Research & Development
        description: The organizational unit field listed in the certificate.
    description: SSL Certificate managed by Purity//FA.
  CertificateGroupCertificateGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/CertificateGroupCertificateResponse'
  SupportRemoteAssistSessions:
    type: object
    properties:
      access_level:
        type: string
        description: 'The access level for this remote assist session.

          This is set to default_access_level unless access_level_override is provided.

          Values include `restricted` and `elevated`.

          '
      duration:
        type: integer
        format: int64
        example: 86400000
        description: 'Specifies the duration of the remote assist session in milliseconds.

          This parameter should only be provided when establishing a new session.

          This parameter determines the length of time the session will remain active

          after initiation.

          Defaults to 86400000 (24h) with a min of 14400000 (4h) and a max of 172800000
          (48h).

          '
      expires:
        type: integer
        format: int64
        example: 1574633337678
        description: 'The timestamp when the session expires, measured in milliseconds
          since the UNIX epoch.

          '
        readOnly: true
      paths:
        type: array
        readOnly: true
        items:
          $ref: '#/definitions/_supportRemoteAssistSessionsPaths'
      active:
        type: boolean
        description: 'The status of a remote assist session. If set to `true`, enables
          the remote assist session.

          If set to `false`, disables the remote assist session.

          '
      opened:
        type: integer
        format: int64
        example: 1574460534674
        description: 'The timestamp when the session opened, measured in milliseconds
          since the UNIX epoch.

          '
        readOnly: true
      status:
        type: string
        example: connected
        description: 'The status of the remote assist session.

          Values include `connected`, `connecting`, `disconnected`, and `session-active`.

          '
        readOnly: true
    description: 'Remote assist session settings.

      '
  VirtualMachineVolumeSnapshotResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          description: 'Displays a list of all items after filtering.

            The values are displayed for each name if meaningful.

            '
          items:
            $ref: '#/definitions/VirtualMachineVolumeSnapshot'
  WorkloadPatch:
    allOf:
    - $ref: '#/definitions/_destroyedPatchPost'
    - $ref: '#/definitions/_newName'
  App:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        vnc_enabled:
          type: boolean
          description: 'If set to `true`, VNC access is enabled. By default, VNC access
            is disabled.

            '
        description:
          type: string
          example: Snapshot offload to NFS or Amazon S3
          description: 'A description of the app.

            '
          readOnly: true
        details:
          type: string
          example: Insufficient resources to operate
          description: 'Details of the status of the app.

            '
          readOnly: true
        version:
          type: string
          example: 1.0.0
          description: 'The app version.

            For cluster apps, this represents the node version if all nodes are of
            the same version.

            If the node versions differ, a value of `null` is returned.

            '
          readOnly: true
        enabled:
          type: boolean
          description: 'If set to `true`, the app is enabled. By default, apps are
            disabled.

            '
        status:
          type: string
          example: healthy
          description: 'The status of the app. Values include `healthy` and `unhealthy`.

            For cluster apps, this represents the aggregate status of the app.

            The aggregate status is only `healthy` if all nodes are `healthy`&#59;

            otherwise, it is `unhealthy`.

            '
          readOnly: true
      description: 'An app that is installed on Purity to extend its functionality.

        The app is an add-on service that is integrated into the system to extend
        array functionality.

        '
  PodMember:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        pod:
          $ref: '#/definitions/_referenceWithType'
        member:
          $ref: '#/definitions/_referenceWithType'
  VolumeGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/VolumeResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            If applicable, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/Volume'
  _remoteArrayFleet:
    allOf:
    - $ref: '#/definitions/Fleet'
    - type: object
      properties:
        is_local:
          type: boolean
          example: false
          description: 'If set to `true`, the current array is a member of the fleet.

            '
  _presetWorkloadConstraintsInteger:
    type: object
    properties:
      allowed_values:
        type: array
        description: 'The valid values that can be supplied to the parameter.

          A parameter that collects the number of volumes to provision might, for
          example, limit

          the allowed values to a few fixed options. Supports up to five values.

          '
        items:
          type: integer
          format: int64
      default:
        type: integer
        format: int64
        description: 'The default value to use if no value is provided.

          Must be present in `allowed_values`, if set.

          Must comply with `minimum`, if set.

          Must comply with `maximum`, if set.

          '
      maximum:
        type: integer
        format: int64
        description: The maximum acceptable value, inclusive.
      minimum:
        type: integer
        format: int64
        description: The minimum acceptable value, inclusive.
    description: Constraints to apply to integer parameters.
  SupportDiagnosticsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SupportDiagnosticsSettings'
  _workloadParameterValue:
    type: object
    properties:
      boolean:
        type: boolean
        description: The value for a boolean parameter.
      string:
        type: string
        description: The value for a string parameter.
      integer:
        type: integer
        format: int64
        description: The value for an integer parameter.
      resource_reference:
        $ref: '#/definitions/_workloadParameterValueResourceReference'
    description: The value of the parameter.
  DnsPost:
    type: object
    properties:
      ca_certificate:
        type: object
        description: 'A reference to the `certificate` to use for validating nameservers

          with HTTPS connections.

          This property is mutually exclusive with `ca_certificate_group` property
          and may only

          be configured when nameservers begin with HTTPS.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      nameservers:
        type: array
        description: 'The list of DNS servers either in the form of IP addresses or
          HTTPS endpoints.

          Domain names in HTTPS endpoints are not supported. IP addresses must be
          used instead.

          If nameservers begin with `https://`, then DNS queries will be performed
          over HTTPS.

          Otherwise, unencrypted DNS queries will be performed.

          Using a combination of nameservers that begin with `https://` and that do
          not begin

          with `https://` is not supported.

          If servers are specified with `https://` one of `ca_certificate` and `ca_certificate_group`

          parameters must be set.

          '
        items:
          type: string
      domain:
        type: string
        example: example.com
        description: 'The domain suffix to be appended by the appliance when performing
          DNS lookups.

          '
      ca_certificate_group:
        type: object
        description: 'A reference to the `certificate group` to use for validating
          nameservers

          with HTTPS connections.

          This property is mutually exclusive with `ca_certificate` property and may
          only

          be configured when nameservers begin with HTTPS.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      services:
        type: array
        description: 'The list of services utilizing the DNS configuration.

          '
        items:
          type: string
          description: 'Valid values include `management` and `file`.

            '
      source:
        type: object
        description: 'The network interface used for communication with the DNS server.

          '
        title: ReferenceNoId
        allOf:
        - $ref: '#/definitions/_referenceNoId'
    description: 'Configuration information for the domain name servers.

      '
  WorkloadPlacementRecommendationResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/WorkloadPlacementRecommendation'
  _networkInterfaceNeighborNeighborPortId:
    type: object
    properties:
      type:
        type: string
        example: ifname
        description: 'The port ID subtype. Valid values are `ifname`, `ifalias`, `local`,
          `mac`, `ip`, and

          `unhandled`.

          '
      value:
        type: string
        example: Ethernet7
        description: The specific identifier for the particular port.
    description: 'An administratively assigned identifier of the particular neighboring
      port.

      '
  _referenceNoId:
    type: object
    properties:
      name:
        type: string
        description: 'The resource name, such as volume name, pod name, snapshot name,
          and so on.

          '
  Pod:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        quota_limit:
          type: integer
          format: int64
          description: 'The logical quota limit of the pod, measured in bytes.

            '
          readOnly: true
          maximum: 4503599627370496
        promotion_status:
          type: string
          description: 'Current promotion status of a pod.

            Values include `promoted`, `demoted`, and `promoting`.

            The `promoted` status indicates that the pod has been promoted. The pod
            takes writes from

            hosts instead of incorporating replicated data. This is the default mode
            for a pod when it

            is created.

            The `demoted` status indicates that the pod has been demoted. The pod
            does not accept write

            requests and is ready to be used as a link target.

            The `promoting` status indicates that the pod is in an intermediate status
            between

            `demoted` and `promoted` while the promotion process is taking place.

            '
        failover_preferences:
          type: array
          description: 'Determines which array within a stretched pod should be given
            priority to stay online

            should the arrays ever lose contact with each other. The current array
            and

            any peer arrays that are connected to the current array for synchronous
            replication can be

            added to a pod for failover preference.

            By default, `failover_preferences=null`,

            meaning no arrays have been configured for failover preference.

            '
          items:
            $ref: '#/definitions/_reference'
        eradication_config:
          type: object
          title: ContainerEradicationConfig
          allOf:
          - $ref: '#/definitions/ContainerEradicationConfig'
        source:
          type: object
          description: 'The source pod from where data is cloned to create the new
            pod.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        link_target_count:
          type: integer
          format: int32
          example: 1
          description: 'The number of target pods that link to the pod.

            '
        space:
          type: object
          description: 'Displays provisioned size and physical storage consumption

            information for the sum of all volumes connected to the specified host.

            '
          title: PodSpace
          allOf:
          - $ref: '#/definitions/_podSpace'
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left until the destroyed pod is permanently
            eradicated,

            measured in milliseconds.

            Before the `time_remaining` period has elapsed, the destroyed pod can
            be recovered

            by setting `destroyed=false`.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the pod has been destroyed and
            is pending eradication.

            The `time_remaining` value displays the amount of time left

            until the destroyed pod is permanently eradicated.

            Before the `time_remaining` period has elapsed, the destroyed pod can
            be recovered

            by setting `destroyed=false`.

            Once the `time_remaining` period has elapsed,

            the pod is permanently eradicated and can no longer be recovered.

            '
        footprint:
          type: integer
          format: int64
          description: 'This field has been deprecated.

            Use the `space.footprint` field in the future, as it contains the same
            information.

            The maximum amount of space the pod would take up on any array,

            ignoring any data shared outside the pod.

            Measured in bytes.

            The footprint metric is mostly used for capacity planning.

            '
          minimum: 0
        array_count:
          type: integer
          format: int32
          example: 1
          description: 'The number of arrays a pod connects to.

            '
        link_source_count:
          type: integer
          format: int32
          example: 1
          description: 'The number of source pods that link to the pod.

            '
        members:
          type: array
          description: 'A list of arrays or realms over which the pod is stretched.

            If there are two or more members in the stretched pod,

            all data in the pod is synchronously replicated between all of the members
            within the pod.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_referenceWithType'
          minItems: 1
        requested_promotion_state:
          type: string
          description: 'Values include `promoted` and `demoted`.

            Patch `requested_promotion_state` to `demoted` to demote the pod so that
            it can be used as

            a link target for continuous replication between pods.

            Demoted pods do not accept write requests, and a destroyed version of
            the pod with

            `undo-demote` appended to the pod name is created on the array with the
            state of the pod

            when it was in the promoted state.

            Patch `requested_promotion_state` to `promoted` to start the process of
            promoting the pod.

            The `promotion_status` indicates when the pod has been successfully promoted.

            Promoted pods stop incorporating replicated data from the source pod and
            start accepting

            write requests.

            The replication process does not stop as the source pod continues replicating
            data to the

            pod.

            The space consumed by the unique replicated data is tracked by the `space.journal`
            field of

            the pod.

            '
        arrays:
          type: array
          description: 'A list of arrays over which the pod is stretched.

            If there are two or more arrays in the stretched pod,

            all data in the pod is synchronously replicated between all of the arrays
            within the pod.

            '
          items:
            $ref: '#/definitions/_pod_array_status'
          minItems: 1
        mediator:
          type: string
          example: https://mediator.cloud-support.purestorage.com
          description: 'The URL of the mediator for the pod.

            By default, the Pure1 Cloud Mediator (`purestorage`) serves as the mediator.

            '
        mediator_version:
          type: string
          example: 1.0.0
          description: 'The mediator version.

            '
      description: 'A pod is an administrative domain, a data container, and a namespace
        for

        volumes and protection groups.

        '
  WorkloadGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfoWithoutTotalItemCountResponse'
    - $ref: '#/definitions/WorkloadResponse'
    - $ref: '#/definitions/_errorContextResponse'
  CertificateGroupResponse:
    type: object
    properties:
      items:
        type: array
        description: 'A list of certificate group objects.

          '
        items:
          $ref: '#/definitions/CertificateGroup'
  PodGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PodResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering. When applicable,
            the average value is

            displayed instead. The values are displayed for each field, if meaningful.

            '
          items:
            $ref: '#/definitions/Pod'
  RemoteProtectionGroupSnapshotTransferResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/RemoteProtectionGroupSnapshotTransfer'
  PodReplicaLinkResponse:
    type: object
    properties:
      items:
        type: array
        description: A list of pod replica link objects.
        items:
          $ref: '#/definitions/PodReplicaLink'
  _presetWorkloadConstraintsBoolean:
    type: object
    properties:
      default:
        type: boolean
        description: The default value to use if no value is provided.
    description: Constraints to apply to boolean parameters.
  SnmpManager:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - type: object
      properties:
        notification:
          type: string
          example: trap
          description: 'The type of notification the agent will send.

            Valid values are `inform` and `trap`.

            '
        v2c:
          $ref: '#/definitions/_snmp_v2c'
        host:
          type: string
          example: snmp.purestorage.com
          description: 'DNS hostname or IP address of a computer that hosts an SNMP
            manager

            to which Purity//FA is to send trap messages when it generates alerts.

            '
        v3:
          $ref: '#/definitions/_snmp_v3'
        version:
          type: string
          example: v3
          description: 'Version of the SNMP protocol to be used by Purity//FA to communicate
            with the specified

            manager. Valid values are `v2c` and `v3`.

            '
  _fixedReferenceWithTypeAndLocation:
    allOf:
    - $ref: '#/definitions/_fixedReference'
    - $ref: '#/definitions/_locationReference'
    - type: object
      properties:
        resource_type:
          type: string
          description: "Type of the object (full name of the endpoint).\n  Valid values
            are the unique part of the resource's REST endpoint.\n  For example, a
            reference to a fleet would have a\n  `resource_type` of `fleets`.\n"
  PolicyAuditFile:
    allOf:
    - $ref: '#/definitions/Policy'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        log_targets:
          type: array
          description: 'A list of targets to which audit logs will be sent.

            '
          items:
            $ref: '#/definitions/_referenceNoIdWithType'
  Volume:
    allOf:
    - $ref: '#/definitions/_volumeCommon'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        protocol_endpoint:
          type: object
          description: 'Properties that are specific to volumes of `subtype=protocol_endpoint`.

            '
          title: ProtocolEndpoint
          allOf:
          - $ref: '#/definitions/_protocol_endpoint'
        promotion_status:
          type: string
          description: 'Current promotion status of a volume. Values include `promoted`
            and `demoted`.

            A status of `promoted` indicates that the volume has been promoted and
            can accept write

            requests from hosts. This is the default status for a volume when it is
            created.

            A status of `demoted` indicates that the volume has been demoted and no
            longer accepts write

            requests.

            '
        pod:
          type: object
          description: 'A reference to the pod.

            '
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        subtype:
          type: string
          description: 'The type of volume. Values include `protocol_endpoint` and
            `regular`.

            '
          readOnly: true
        requested_promotion_state:
          type: string
          description: 'Values include `promoted` and `demoted`.

            Patch `requested_promotion_state` to `demoted` to demote the volume so
            that the volume

            stops accepting write requests.

            Patch `requested_promotion_state` to `promoted` to promote the volume
            so that the volume

            starts accepting write requests.

            '
        workload:
          type: object
          description: 'A reference to the workload that is managing this volume.

            The `name` may show as an empty string briefly after controller failover.

            '
          title: WorkloadConfigurationReference
          allOf:
          - $ref: '#/definitions/_workloadConfigurationReference'
        source:
          type: object
          description: 'A reference to the originating volume as a result of a volume
            copy.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        volume_group:
          type: object
          description: 'A reference to the volume group.

            '
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        priority:
          type: integer
          format: int32
          description: 'The current priority value. Priority is calculated by combining
            all applicable relative

            `priority_adjustment` values or taking the exact value if the volume has
            an absolute

            `priority_adjustment` (specified by an `=` `priority_adjustment_operator`).

            '
          readOnly: true
  AlertGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/AlertResponse'
    - $ref: '#/definitions/_errorContextResponse'
  PodMemberGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PodMemberResponse'
    - $ref: '#/definitions/_errorContextResponse'
  AdminApiToken:
    allOf:
    - $ref: '#/definitions/_fixedNameResourceNoId'
    - type: object
      properties:
        api_token:
          $ref: '#/definitions/_apiToken'
      description: Administrative account API token.
  SnmpAgentMibGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SnmpAgentMibResponse'
  SubnetResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Subnet'
  SmtpServer:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        sender_username:
          type: string
          description: 'The local-part of the email address used when sending alert
            email messages.

            '
        subject_prefix:
          type: string
          example: DC:45 RM:23 RACK:12
          description: 'Optional string added to the beginning of the subject when
            sending alert email messages.

            HTML tags are not allowed.

            '
          maxLength: 64
        password:
          type: string
          description: 'Password for the relay host, if needed.

            '
        relay_host:
          type: string
          description: 'Relay server used as a forwarding point for email sent from
            the array.

            Can be set as a hostname, IPv4 address, or IPv6 address, with optional
            port numbers.

            The expected format for IPv4 is `ddd.ddd.ddd.ddd:PORT`.

            The expected format for IPv6 is `xxxx:xxxx:xxxx:xxxx:xxxx:xxxx:xxxx:xxxx`
            or,

            if a port number is specified, `[xxxx:xxxx:xxxx:xxxx:xxxx:xxxx:xxxx:xxxx]:PORT`.

            '
        user_name:
          type: string
          description: 'User name for the relay host, if needed.

            '
        encryption_mode:
          type: string
          example: starttls
          description: 'Enforces an encryption mode when sending alert email messages.

            Valid values are `starttls`.

            Use "" to clear.

            '
        body_prefix:
          type: string
          example: Classifaction is UNCLASSIFIED
          description: 'Optional string added to the beginning of the email body when
            sending alert email messages.

            HTML tags are not allowed.

            '
          maxLength: 256
        sender_domain:
          type: string
          description: 'Domain name appended to alert email messages.

            '
  PodPerformanceReplicationGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PodPerformanceReplicationResponse'
    - $ref: '#/definitions/_errorContextResponse'
  ArrayConnectionKey:
    type: object
    properties:
      connection_key:
        type: string
        example: 6207d123-d123-0b5c-5fa1-95fabc5c7123
        description: 'Connection-key used on another array to connect to this array.

          '
        readOnly: true
  ProtectionGroup:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - $ref: '#/definitions/_workloadConfigurationFixedReference'
    - type: object
      properties:
        target_retention:
          type: object
          description: 'The retention policy for the target(s) of the protection group.

            '
          title: RetentionPolicy
          allOf:
          - $ref: '#/definitions/_retentionPolicy'
        pod:
          type: object
          description: The pod in which the protection group resides.
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        host_group_count:
          type: integer
          format: int64
          example: 3
          description: Number of host groups in this protection group.
          readOnly: true
        host_count:
          type: integer
          format: int64
          example: 3
          description: Number of hosts in this protection group.
          readOnly: true
        eradication_config:
          $ref: '#/definitions/ProtectionGroupEradicationConfig'
        source:
          type: object
          description: The array or pod on which the protection group was created.
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        volume_count:
          type: integer
          format: int64
          example: 3
          description: The number of volumes in the protection group.
          readOnly: true
        space:
          type: object
          description: 'Displays provisioned size and physical storage consumption
            data

            for each protection group.

            '
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left until the destroyed protection group
            is permanently eradicated.

            Measured in milliseconds.

            Before the `time_remaining` period has elapsed, the destroyed protection
            group

            can be recovered by setting `destroyed=false`.

            '
          readOnly: true
        target_count:
          type: integer
          format: int64
          example: 3
          description: The number of targets to where this protection group replicates.
          readOnly: true
        destroyed:
          type: boolean
          description: 'Has this protection group been destroyed?

            To destroy a protection group, patch to `true`.

            To recover a destroyed protection group, patch to `false`.

            If not specified, defaults to `false`.

            '
        source_retention:
          type: object
          description: 'The retention policy for the source array of the protection
            group.

            '
          title: RetentionPolicy
          allOf:
          - $ref: '#/definitions/_retentionPolicy'
        replication_schedule:
          type: object
          description: The schedule settings for asynchronous replication.
          title: ReplicationSchedule
          allOf:
          - $ref: '#/definitions/_replication_schedule'
        snapshot_schedule:
          type: object
          description: The schedule settings for protection group snapshots.
          title: SnapshotSchedule
          allOf:
          - $ref: '#/definitions/_snapshot_schedule'
        is_local:
          type: boolean
          description: 'If set to `true`, the protection group belongs to the local
            array.

            If set to `false`, the protection group belongs to the remote array.

            '
          readOnly: true
        retention_lock:
          type: string
          example: unlocked
          description: 'The valid values are `ratcheted` and `unlocked`.

            The default value for a newly created protection group is `unlocked`.

            Set `retention_lock` to `ratcheted` to enable SafeMode restrictions on
            the protection group.

            Contact Pure Technical Services to change `retention_lock` to `unlocked`.

            '
      description: 'A protection group defines a set of either volumes, hosts, or
        host groups

        (called members) that are protected together through snapshots with

        point-in-time consistency across the member volumes. The members within the

        protection group have common data protection requirements and share the

        same snapshot, replication, and retention schedules.

        '
  DirectoryExportPost:
    type: object
    properties:
      export_enabled:
        type: boolean
        example: false
        description: 'If set to `true`, the export is enabled.

          If not specified, defaults to `true`.

          '
      export_name:
        type: string
        description: 'The name of the export to create.

          Export names must be unique within the same protocol.

          '
  ApiClientPatch:
    type: object
    properties:
      enabled:
        type: boolean
        example: true
        description: 'Returns a value of `true` if the API client is permitted to
          exchange ID

          tokens for access tokens.

          API clients are disabled by default.

          '
  SupportRemoteAssistSessionsSettings:
    type: object
    properties:
      default_access_level:
        type: string
        description: 'The default access level that support will have when connecting
          to the array.

          Values include `restricted` and `elevated`.

          '
  VolumeSnapshotTransferResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name where meaningful.

          If `total_only=true`, the `items` list will be empty.

          '
        items:
          $ref: '#/definitions/VolumeSnapshotTransfer'
  Eula:
    type: object
    properties:
      agreement:
        type: string
        description: The End User Agreement text.
        readOnly: true
      signature:
        $ref: '#/definitions/_eula_signature'
  HardwarePatch:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        identify_enabled:
          type: boolean
          example: true
          description: 'State of an LED used to visually identify the component.

            '
        index:
          type: integer
          format: int32
          example: 1
          description: 'Number that identifies the relative position of a hardware
            component within the array.

            '
  Connection:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        protocol_endpoint:
          type: object
          description: A protocol endpoint (also known as a conglomerate volume) which
            acts as a proxy through which virtual volumes are created and then connected
            to VMware ESXi hosts or host groups. The protocol endpoint itself does
            not serve I/Os; instead, its job is to form connections between FlashArray
            volumes and ESXi hosts and host groups.
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        volume:
          type: object
          description: A container that manages the storage space on the array.
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        host_group:
          type: object
          description: A virtual collection of hosts with common connectivity to volumes.
          title: ReferenceNoId
          allOf:
          - $ref: '#/definitions/_referenceNoId'
        nsid:
          type: integer
          format: int64
          example: 3856
          description: 'Namespace identifier allocated for the NVMe namespace backing
            the volume.

            NSID is set for NVMe connections only.

            '
        lun:
          type: integer
          format: int64
          example: 7
          description: 'The logical unit number (LUN) by which the specified hosts
            are to

            address the specified volume. A LUN is set for SCSI connections only.

            A LUN can be in one of two formats: a simple LUN, or a LUN and Sublun

            with virtual volumes. The first format is simply the LUN. The second

            format is a single int64 combining both ((LUN << 32) + Sublun) or (LUN
            *

            4294967296 + Sublun).

            In the FA UI, a combined LUN and Sublun is represented as `LUN:Sublun`.

            The maximum int64 LUN:Sublun value is `17587891081215`.

            '
        host:
          type: object
          description: The host computer that sends and receives I/O requests to and
            from volumes on the FlashArray array.
          title: ReferenceNoId
          allOf:
          - $ref: '#/definitions/_referenceNoId'
  ArrayConnectionResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/ArrayConnection'
  _adminRole:
    allOf:
    - $ref: '#/definitions/_referenceNoId'
    - type: object
      description: 'This field has been deprecated. Predecessor to management access
        policies.

        '
  RemotePodsResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        items:
          type: array
          description: 'Displays a list of all items after filtering.

            The values are displayed for each name where meaningful.

            '
          items:
            $ref: '#/definitions/RemotePod'
  Workload:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left until the destroyed workload is permanently
            eradicated, measured in

            milliseconds.

            Before the `time_remaining` period has elapsed, the destroyed workload
            can be recovered

            by setting `destroyed=false`.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the workload has been destroyed
            and is pending eradication.

            The `time_remaining` displays the amount of time left

            until the destroyed workload is permanently eradicated.

            Before the `time_remaining` period has elapsed, the destroyed workload
            can be recovered

            by setting `destroyed=false`.

            Once the `time_remaining` period has elapsed,

            the workload is permanently eradicated and can no longer be recovered.

            '
          readOnly: true
        created:
          type: integer
          format: int64
          description: The workload creation time, measured in milliseconds since
            the UNIX epoch.
          readOnly: true
        preset:
          type: object
          description: 'A reference to the preset the workload was deployed from.

            If the preset has since been destroyed, `name` will be `null`.

            '
          title: WorkloadPreset
          allOf:
          - $ref: '#/definitions/_workloadPreset'
        status_details:
          type: array
          description: 'Additional information about the workload status. For example,
            if the workload status

            is `creating`, status details might describe what resources are currently
            being created.

            '
          readOnly: true
          items:
            type: string
        status:
          type: string
          example: ready
          description: 'The status of the workload. Supported values are `creating`,
            `ready`, `destroying`,

            `destroyed`, `eradicating` and `recovering`.

            '
          readOnly: true
      description: 'Workloads organize storage resources (such as volumes) and their
        related configuration

        and policy objects into logical groupings.

        '
  HostGroupGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/HostGroupResponse'
    - $ref: '#/definitions/_errorContextResponse'
  SoftwarePatchesResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SoftwarePatch'
  ResourcePerformanceNoIdByArrayGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where it makes more sense, the average value is displayed instead.

            The values are displayed for each field where meaningful.

            '
          items:
            $ref: '#/definitions/ResourcePerformanceNoIdByArray'
        items:
          type: array
          description: 'Performance data, broken down by array.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/ResourcePerformanceNoIdByArray'
  MemberNoIdMemberResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/MemberNoIdMember'
  TotalItemCountResponse:
    type: object
    properties:
      total_item_count:
        type: integer
        format: int32
        description: 'The total number of records after applying all filter query
          parameters.

          The `total_item_count` will be calculated if and only if the corresponding

          query parameter `total_item_count` is set to `true`. If this query parameter

          is not set or set to `false`, a value of `null` will be returned.

          '
  NetworkInterfacesPortDetailsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/NetworkInterfacesPortDetailsResponse'
  _portCommon:
    type: object
    properties:
      iqn:
        type: string
        example: iqn.2010-06.com.purestorage:flasharray.393d930077be2977
        description: The iSCSI Qualified Name (or `null` if target is not iSCSI).
        readOnly: true
      nqn:
        type: string
        example: nqn.2010-06.com.purestorage:flasharray.512f7bb185978505
        description: NVMe Qualified Name (or `null` if target is not NVMeoF).
        readOnly: true
      portal:
        type: string
        example: 10.16.200.27:3260
        description: IP and port number (or `null` if target is not iSCSI).
        readOnly: true
      wwn:
        type: string
        example: 52:4A:93:70:4E:E5:68:00
        description: 'Fibre Channel World Wide Name (or `null` if target is not Fibre
          Channel).

          '
        readOnly: true
  VchostEndpoint:
    type: object
    properties:
      endpoint:
        type: string
        example: 1.2.3.4
        description: 'The IPv4 or IPv6 address of the endpoint.

          '
      certificates:
        type: array
        description: 'The certificate to be presented by the vchost over the configured
          endpoints.

          '
        items:
          $ref: '#/definitions/_referenceNoIdWithType'
      vchost:
        type: object
        description: 'The vchost for which this endpoint is to be configured.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      id:
        type: string
        example: 3a668af2-f63a-0110-bf4a-39e911ac6f07
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified.

          '
      is_local:
        type: boolean
        description: Returns true if the vchost endpoint is local to the array, and
          false otherwise.
  _replication_schedule:
    allOf:
    - $ref: '#/definitions/_snapshot_schedule'
    - type: object
      properties:
        blackout:
          type: object
          description: 'The range of time when to suspend replication.

            To clear the blackout period, set to an empty string ("").

            '
          title: TimeWindow
          allOf:
          - $ref: '#/definitions/_time_window'
      description: 'The schedule of a replication action.

        '
  SyslogServerGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SyslogServerResponse'
    - $ref: '#/definitions/_errorContextResponse'
  KmipPatch:
    type: object
    properties:
      ca_certificate:
        type: string
        example: '-----BEGIN CERTIFICATE-----...-----END CERTIFICATE-----'
        description: The text of the CA certificate for the KMIP server.
        maxLength: 3000
      uris:
        type: array
        description: List of URIs for the configured KMIP servers.
        items:
          type: string
      certificate:
        type: object
        description: 'The certificate used to verify FlashArray authenticity to the
          KMIP servers.

          '
        title: ReferenceNoId
        allOf:
        - $ref: '#/definitions/_referenceNoId'
  NetworkInterfacePost:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        eth:
          $ref: '#/definitions/_networkinterfacepostEth'
        services:
          type: array
          description: 'The services provided by the specified network interface or
            Fibre Channel port.

            '
          items:
            type: string
            description: 'Valid values include `iscsi`, `management`, `nvme-fc`, `nvme-roce`,

              `offload`, `replication`, and `scsi-fc`.

              '
  VolumeGroup:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - $ref: '#/definitions/_workloadConfigurationFixedReference'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left until the destroyed volume group is
            permanently eradicated, measured

            in milliseconds.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the volume group has been destroyed
            and is pending eradication.

            Before the `time_remaining` period has elapsed, the destroyed volume group
            can be recovered

            by setting `destroyed=false`.

            After the `time_remaining` period has elapsed, the volume group is permanently
            eradicated

            and cannot be recovered.

            '
        pod:
          type: object
          description: 'A reference to the pod.

            '
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        qos:
          $ref: '#/definitions/_qos'
        volume_count:
          type: integer
          format: int64
          example: 3
          description: The number of volumes in the volume group.
          readOnly: true
        priority_adjustment:
          $ref: '#/definitions/_priorityAdjustment'
        space:
          type: object
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
      description: 'Volume groups organize FlashArray volumes into logical groupings.

        '
  _podPerformanceReplication:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        total_bytes_per_sec:
          type: integer
          format: int64
          description: 'Total bytes transmitted and received per second for all types
            of replication.

            '
          readOnly: true
          minimum: 0
        sync_bytes_per_sec:
          type: object
          description: 'Total bytes transmitted or received per second for synchronous
            replication.

            '
          title: ReplicationPerformanceWithTotal
          allOf:
          - $ref: '#/definitions/_replicationPerformanceWithTotal'
        periodic_bytes_per_sec:
          type: object
          description: 'Total bytes transmitted or received per second for periodic
            replication.

            '
          title: ReplicationPerformanceWithTotal
          allOf:
          - $ref: '#/definitions/_replicationPerformanceWithTotal'
        pod:
          type: object
          description: Reference to the pod that the performance data is associated
            with.
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        continuous_bytes_per_sec:
          type: object
          description: 'Total bytes transmitted or received per second for continuous
            replication.

            The continuous replication feature is used for disaster recovery on FlashArray
            and provides a

            recovery point objective (RPO) of significantly less than 30s.

            '
          title: ReplicationPerformanceWithTotal
          allOf:
          - $ref: '#/definitions/_replicationPerformanceWithTotal'
        resync_bytes_per_sec:
          type: object
          description: 'Total bytes transmitted or received per second during resync
            replication.

            Resync replication is the mechanism to bring two arrays into sync.

            This may occur during an initial pod stretch, or, in case of outage,

            when two arrays reestablish connection. After the connection is restored,

            the array that was online starts replicating pod data

            to its peer array until the pod is once again in sync.

            '
          title: ReplicationPerformanceWithTotal
          allOf:
          - $ref: '#/definitions/_replicationPerformanceWithTotal'
        time:
          type: integer
          format: int64
          description: Sample time in milliseconds since the UNIX epoch.
          readOnly: true
  _basePort:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - $ref: '#/definitions/_portCommon'
    - type: object
      properties:
        failover:
          type: string
          example: CT0.ETH2
          description: 'If the array port has failed over, returns the name of the
            port

            to which this port has failed over.

            '
      description: 'A target port on the array enabling access to block storage (Fibre
        Channel

        or iSCSI).

        '
  Drive:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        protocol:
          type: string
          example: SAS
          description: 'Storage protocol of the module. Valid values are `NVMe` and
            `SAS`.

            '
          readOnly: true
        capacity_installed:
          type: integer
          format: int64
          example: 42000000000000
          description: 'Raw capacity of individual drive in bytes.

            '
          readOnly: true
        details:
          type: string
          example: Cache module in wrong bay.
          description: 'Details about the status of the module if not healthy.

            '
          readOnly: true
        type:
          type: string
          example: SSD
          description: 'The type of the module. Valid values are `cache`, `NVRAM`,
            `SSD`, and `virtual`.

            '
          readOnly: true
        capacity:
          type: integer
          format: int64
          example: 10000000000000
          description: 'Entitled capacity of individual drive in bytes.

            '
          readOnly: true
        status:
          type: string
          example: healthy
          description: 'Current status of the module.

            Valid values are `empty`, `failed`, `healthy`, `identifying`,

            `missing`, `recovering`, `unadmitted`, `unhealthy`, `unrecognized`,

            and `updating`.

            '
          readOnly: true
      description: Information about a flash, NVRAM, or cache module.
  _eula_signature:
    type: object
    properties:
      name:
        type: string
        example: John Doe
        description: 'The name of the person who accepted the End User Agreement.

          This field is deprecated and the response will be `null`. Modification to
          this field

          will be ignored.

          '
      accepted:
        type: integer
        format: int64
        example: 1577880000000
        description: Accepted time in milliseconds since the UNIX epoch.
        readOnly: true
      company:
        type: string
        example: Example Inc.
        description: 'The company of the person who accepted the End User Agreement.

          This field is deprecated and the response will be `null`. Modification to
          this field

          will be ignored.

          '
      title:
        type: string
        example: Admin
        description: 'The title of the person who accepted the End User Agreement.

          This field is deprecated and the response will be `null`. Modification to
          this field

          will be ignored.

          '
  PolicyResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/Policy'
  AdminSettings:
    type: object
    properties:
      required_authentication_methods:
        $ref: '#/definitions/_adminSettingsRequiredAuthenticationMethods'
      lockout_duration:
        type: integer
        format: int64
        example: 3600000
        description: 'The lockout duration, in milliseconds, if a user is locked out
          after reaching the maximum

          number of login attempts. Ranges from 1 second to 90 days.

          '
        minimum: 1000
        maximum: 7776000000
      single_sign_on_enabled:
        type: boolean
        example: true
        description: 'If `true`, then single sign-on is enabled for the array.

          '
      min_password_length:
        type: integer
        format: int32
        example: 1
        description: 'Minimum password length. If not specified, defaults to 1.

          '
      max_login_attempts:
        type: integer
        format: int32
        example: 10
        description: 'Maximum number of failed login attempts allowed before the user
          is locked out.

          '
        minimum: 1
        maximum: 20
    description: Global settings for administrative accounts.
  LocalMembershipResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/LocalMember'
  DirectoryQuotasGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/DirectoryQuotasResponse'
    - $ref: '#/definitions/_errorContextResponse'
  PolicyNfsResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/PolicyNfs'
  NetworkInterfaceNeighbor:
    type: object
    properties:
      initial_ttl_in_sec:
        type: integer
        format: uint32
        example: 120
        description: 'The initial time to live in seconds from when the local port
          received notice that the

          neighbor information is regarded as valid. The time is not measured from
          when this endpoint

          was queried.

          '
        readOnly: true
      local_port:
        type: object
        description: 'A reference to the local network interface the neighbor is connected
          to.

          '
        title: FixedReferenceNoId
        allOf:
        - $ref: '#/definitions/_fixedReferenceNoId'
      neighbor_port:
        type: object
        title: NetworkInterfaceNeighborNeighborPort
        allOf:
        - $ref: '#/definitions/_networkInterfaceNeighborNeighborPort'
      neighbor_chassis:
        type: object
        title: NetworkInterfaceNeighborNeighborChassis
        allOf:
        - $ref: '#/definitions/_networkInterfaceNeighborNeighborChassis'
  RealmResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Realm'
  PolicyNfsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyNfsResponse'
    - $ref: '#/definitions/_errorContextResponse'
  RemoteProtectionGroupSnapshotPost:
    allOf:
    - $ref: '#/definitions/RemoteProtectionGroupSnapshot'
    - type: object
      properties:
        destroyed:
          type: boolean
          readOnly: true
        suffix:
          type: string
          description: 'Specifies a name suffix for the snapshots created. The snapshot
            is created on the FlashArray

            specified by the `on` parameter. The `on` parameter cannot refer to an
            offload target.

            Snapshots with suffixes specified have names in the form `PGROUP.SUFFIX`
            instead of the

            default `PGROUP.NNN` form. The names of all snapshots created by a single
            command that

            specifies this option have the same suffix.

            '
  NetworkInterfacePerformance:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - type: object
      properties:
        eth:
          type: object
          title: NetworkInterfacePerformanceEth
          allOf:
          - $ref: '#/definitions/_networkInterfacePerformanceEth'
        time:
          type: integer
          format: int64
          description: 'Sample time in milliseconds since UNIX epoch.

            '
          readOnly: true
        interface_type:
          type: string
          description: 'The interface type. Valid values are `eth` and `fc`.

            '
          readOnly: true
        fc:
          type: object
          title: NetworkInterfacePerformanceFc
          allOf:
          - $ref: '#/definitions/_networkInterfacePerformanceFc'
  _replicaLinkLag:
    type: object
    properties:
      avg:
        type: integer
        format: int64
        description: 'The average time difference between the current time and `recovery_point`
          for a period of

          time.

          '
        readOnly: true
        minimum: 0
      max:
        type: integer
        format: int64
        description: 'The maximum time difference between the current time and `recovery_point`
          for a period of

          time.

          '
        readOnly: true
        minimum: 0
  DirectoryExportPatch:
    type: object
    properties:
      export_enabled:
        type: boolean
        example: false
        description: 'If set to `true`, the export is enabled.

          If not specified, defaults to `true`.

          '
      export_name:
        type: string
        description: 'The new name of the export.

          Export names must be unique within the same protocol.

          '
  PresetWorkloadResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/PresetWorkload'
  PodPerformanceReplicationByArrayResponse:
    type: object
    properties:
      total:
        type: array
        description: 'The aggregate value of all items after filtering.

          When applicable, the average value is displayed instead.

          The values are displayed for each field if meaningful.

          '
        items:
          $ref: '#/definitions/PodPerformanceReplicationByArray'
      items:
        type: array
        description: A list of pod performance replication objects, arranged by array.
        items:
          $ref: '#/definitions/PodPerformanceReplicationByArray'
  CertificateGroup:
    allOf:
    - $ref: '#/definitions/_builtIn'
  HostGroup:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left until the destroyed host group is
            permanently eradicated,

            measured in milliseconds.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the host group has been destroyed

            with its container realm and is pending eradication.

            The `time_remaining` value displays the amount of time left

            until the destroyed host group is permanently eradicated.

            Before the `time_remaining` period has elapsed, the destroyed host group

            will be recovered if its container realm is recovered.

            Once the `time_remaining` period has elapsed,

            the host group is permanently eradicated and can no longer be recovered.

            '
          readOnly: true
        connection_count:
          type: integer
          format: int64
          example: 3
          description: The number of volumes connected to the host group.
          readOnly: true
        host_count:
          type: integer
          format: int64
          example: 3
          description: The number of hosts in the host group.
          readOnly: true
        is_local:
          type: boolean
          description: Returns a value of `true` if the host or host group belongs
            to the current array. Returns a value of `false` if the host or host group
            belongs to a remote array.
          readOnly: true
        space:
          type: object
          description: 'Displays provisioned (virtual) size and physical storage consumption

            information for the sum of all volumes connected to the specified host.

            '
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
  MaintenanceWindowPost:
    type: object
    properties:
      timeout:
        type: integer
        format: int32
        example: 3600000
        description: 'The specified length of time that alerts are suppressed during
          a maintenance window,

          measured in milliseconds.

          The maintenance window timeout value must be between `60000` (1 minute)

          and `86400000` (24 hours).

          The value entered is rounded down to the nearest minute.

          The `names` and `timeout` parameters must be set together, and the `names`
          parameter

          must be set to `environment`.

          '
        x-minimum: 0
        x-maximum: 86400000
  Saml2SsoPost:
    type: object
    properties:
      idp:
        $ref: '#/definitions/_saml2SsoIdp'
      sp:
        type: object
        description: 'Properties specific to the service provider.

          '
        title: Saml2SsoSpCredential
        allOf:
        - $ref: '#/definitions/_saml2SsoSpCredential'
      array_url:
        type: string
        example: https://myarray.mycompany.com
        description: 'The URL of the array.

          '
  PodPerformanceByArrayGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            When applicable, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/PodPerformanceByArray'
        items:
          type: array
          description: 'Performance data, broken down by array.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/PodPerformanceByArray'
  ProtectionGroupPerformanceResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        items:
          type: array
          description: 'A list of protection group performance objects.

            '
          items:
            $ref: '#/definitions/ProtectionGroupPerformance'
  HardwareGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/HardwareResponse'
  SyslogServerSettingsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SyslogServerSettings'
  SubnetGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SubnetResponse'
  _override_check:
    type: object
    properties:
      args:
        type: string
        example: host1,host2,host3
        description: 'The name of the specific check within the override check to
          ignore so that the system can

          continue with the software upgrade. The `name` parameter of the override
          check must be

          specified with the `args` parameter. For example, if the HostIOCheck check
          fails on

          hosts host01 and host02, the system displays a list of these host names
          in the failed

          check. To override the HostIOCheck checks for host01 and host02, set `name=HostIOCheck`,

          and set `args=host01,host02`. Enter multiple `args` in comma-separated format.
          Note that

          not all checks have `args` values.

          '
      name:
        type: string
        example: HostIOCheck
        description: 'The name of the upgrade check to be overridden so the software
          upgrade can continue if the

          check failed or is anticipated to fail during the upgrade process. Overriding
          the check

          forces the system to ignore the check failure and continue with the upgrade.
          If the

          check includes more specific checks that failed or are anticipated to fail,
          set them

          using the `args` parameter. For example, the HostIOCheck check may include
          a list of hosts

          that have failed or are anticipated to fail the upgrade check.

          '
      persistent:
        type: boolean
        description: 'If set to `true`, the system always ignores the failure of the
          specified upgrade

          check and continues with the upgrade process.


          If set to `false`, the system ignores the failure of the specified upgrade
          check until

          the upgrade check finishes and the upgrade process is continued. For example,
          the

          `continue` command is successfully issued in an `interactive` mode, or the
          first upgrade

          check step successfully finishes in a `one-click` mode.

          '
  _networkInterfacePortDetailsStaticTemperatureThresholds:
    type: object
    properties:
      warn_low:
        type: number
        format: double
        description: The warn low threshold for temperature in degrees Celsius.
      warn_high:
        type: number
        format: double
        description: The warn high threshold for temperature in degrees Celsius.
      alarm_high:
        type: number
        format: double
        description: The alarm high threshold for temperature in degrees Celsius.
      alarm_low:
        type: number
        format: double
        description: The alarm low threshold for temperature in degrees Celsius.
    description: 'The alarm low, warn low, warn high, alarm high thresholds for temperature
      in degrees

      Celsius.

      '
  _supportRemoteAssistPaths:
    type: object
    properties:
      component_name:
        type: string
        example: CT0
        description: 'The name of the local controller that is running the remote
          assist session.

          '
      status:
        type: string
        example: connected
        description: 'The status of the remote assist session on the local controller.

          Values include `connected`, `connecting`, `disconnected`, and `session-active`.

          '
  ArrayErasure:
    allOf:
    - $ref: '#/definitions/_resource'
    - type: object
      properties:
        sanitization_certificate:
          type: string
          description: 'The sanitization certificate of the factory reset,

            which complies with the standard described in NIST SP800-88R1 section
            4.8.

            '
        details:
          type: string
          description: 'The detailed reason of the `status`.

            '
        image_download_progress:
          type: number
          format: float
          example: 0.32
          description: 'The progress of the ISO image download, displayed in decimal
            format.

            '
        image_version:
          type: string
          example: 6.6.6
          description: 'Version of the image to download and install.

            '
        image_source:
          type: string
          example: auto
          description: 'Source of the ISO image to download. Valid values include
            `auto` and URLs.

            `auto` means download the image from Pure1 cloud,

            and a URL means download the image from the specified URL.

            '
        status:
          type: string
          example: resetting
          description: 'The status of the factory reset process.

            Valid values include `resetting`, `reset_failed`, `reimage_failed`,

            `waiting_for_finalize`, `downloading`, `downloaded`, and `download_failed`.

            A status of `resetting` indicates that the factory reset is running.

            A status of `reset_failed` indicates that the factory reset encountered
            a failure.

            A status of `reimage_failed` indicates that the factory reset failed to
            reimage the array.

            A status of `waiting_for_finalize` indicates that the factory reset has
            finished

            sanitizing drives, and is waiting to be finalized.

            A status of `downloading` indicates that the factory reset is downloading
            ISO image.

            A status of `downloaded` indicates that the factory reset completed ISO
            image download.

            A status of `download_failed` indicates that the factory reset failed
            to download ISO image.

            '
      description: 'Status and detailed information of the factory reset process.

        '
  AppResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/App'
  Saml2SsoResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Saml2Sso'
  PolicyMemberExport:
    allOf:
    - $ref: '#/definitions/PolicyMember'
    - $ref: '#/definitions/ExportPolicyAttachmentMixin'
    - $ref: '#/definitions/_context'
  _networkInterfaceFc:
    type: object
    properties:
      wwn:
        type: string
        description: World Wide Name of the specified Fibre Channel port.
        readOnly: true
    description: Fibre Channel port properties.
  SnmpAgentResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SnmpAgent'
  PolicyRuleAlertWatcher:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        name:
          type: string
          description: 'Name of this rule. The name is automatically generated by
            the system.

            '
          readOnly: true
        minimum_notification_severity:
          type: string
          description: 'The minimum severity that an alert must have in order for
            emails to

            be sent to the recipient.

            Possible values include `info`, `warning`, and `critical`.

            '
        included_codes:
          type: array
          description: 'An alert must have one of these codes in order for emails
            to be sent to

            the recipient.

            Cannot be specified with `exclude_codes`. If specified while

            `exclude_codes` is already set, `exclude_codes` will be cleared.

            Use "" to clear.

            If both `exclude_codes` and `include_codes` are cleared, defaults to an

            empty list for `exclude_codes` and emails for all alert codes will be
            sent.

            '
          items:
            type: integer
            format: int32
            example: 101
        email:
          type: string
          description: 'The email address that will receive the alert notification
            emails.

            '
        excluded_codes:
          type: array
          description: 'An alert with one of these codes will not have emails sent
            to the

            recipient.

            Cannot be specified with `include_codes`. If specified while

            `include_codes` is already set, `include_codes` will be cleared.

            Use "" to clear.

            If both `exclude_codes` and `include_codes` are cleared, defaults to an

            empty list for `exclude_codes` and emails for all alert codes will be
            sent.

            '
          items:
            type: integer
            format: int32
            example: 101
        policy:
          type: object
          description: The policy to which this rule belongs.
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
  LocalMembershipGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/LocalMembershipResponse'
  AdminSettingsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/AdminSettings'
  DnsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Dns'
  PresetWorkload:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - $ref: '#/definitions/_presetWorkloadBase'
  Alert:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        severity:
          type: string
          example: critical
          description: 'The severity level of the alert.

            Valid values include `info`, `warning`, `critical`, and `hidden`.

            '
          readOnly: true
        summary:
          type: string
          example: boot drive overutilization
          description: A summary of the alert.
          readOnly: true
        actual:
          type: string
          example: 95%
          description: Actual condition at the time the alert is created.
          readOnly: true
        code:
          type: integer
          format: int64
          example: 7
          description: The code number of the alert.
          readOnly: true
        issue:
          type: string
          example: boot drive overutilization
          description: Information about the alert cause.
          readOnly: true
        knowledge_base_url:
          type: string
          example: https://support.purestorage.com/?cid=Alert_0007
          description: The URL of the relevant knowledge base page.
          readOnly: true
        created:
          type: integer
          format: int64
          example: 1576275532434
          description: The time the alert was created in milliseconds since the UNIX
            epoch.
          readOnly: true
        notified:
          type: integer
          format: int64
          example: 1578440491109
          description: 'The time the most recent alert notification was sent in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
        component_name:
          type: string
          example: vm-tom
          description: The name of the component that generated the alert.
          readOnly: true
        expected:
          type: string
          example: 90%
          description: Expected state or threshold under normal conditions.
          readOnly: true
        description:
          type: string
          example: boot drive overutilization
          description: A short description of the alert.
          readOnly: true
        component_type:
          type: string
          example: storage
          description: The type of component that generated the alert.
          readOnly: true
        flagged:
          type: boolean
          example: true
          description: 'If set to `true`, the message is flagged.

            Important messages can can be flagged and listed separately.

            '
        closed:
          type: integer
          format: int64
          example: 1578440492342
          description: The time the alert was closed in milliseconds since the UNIX
            epoch.
          readOnly: true
        state:
          type: string
          example: open
          description: 'The current state of the alert.

            Valid values include `open`, `closing`, and `closed`.

            '
          readOnly: true
        category:
          type: string
          example: array
          description: 'The category of the alert.

            Valid values include `array`, `hardware` and `software`.

            '
          readOnly: true
        updated:
          type: integer
          format: int64
          example: 1578440491109
          description: The time the alert was last updated in milliseconds since the
            UNIX epoch.
          readOnly: true
      description: Alert generated by the array when an event occurs.
  WorkloadPost:
    type: object
    properties:
      parameters:
        type: array
        description: 'The parameter values to pass to the preset when creating the
          workload.

          Value must be supplied for all parameters that do not have a default defined
          in the preset.

          '
        items:
          $ref: '#/definitions/_workloadParameter'
  FleetResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Returns a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/Fleet'
  Support:
    allOf:
    - $ref: '#/definitions/SupportPatch'
    - type: object
      properties:
        proxy:
          type: string
          example: http://proxy.example.com:8080
          description: 'The value of the current proxy, which is used to connect to
            cloud services such as

            phonehome and remote assist.

            Specify the server name, including the scheme and proxy port number.

            '
        phonehome_enabled:
          type: boolean
          description: 'If set to `true`, enables phonehome.

            If set to `false`, disables phonehome.

            '
        remote_assist_paths:
          type: array
          readOnly: true
          items:
            $ref: '#/definitions/_supportRemoteAssistPaths'
        remote_assist_status:
          type: string
          example: connected
          description: 'The status of the remote assist session.

            Values include `connected`, `connecting`, `disconnected`, and `session-active`.

            '
          readOnly: true
        remote_assist_duration:
          type: integer
          format: int64
          example: 86400000
          description: 'Specifies the duration of the remote assist session in milliseconds.

            This parameter should only be provided when establishing a new session.

            It determines the length of time the session will remain active after
            it''s initiated.

            '
        remote_assist_opened:
          type: integer
          format: int64
          example: 1574460534674
          description: 'The timestamp when the session opened, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
        remote_assist_active:
          type: boolean
          description: 'If set to `true`, enables the remote assist session.

            If set to `false`, disables the remote assist session.

            '
        remote_assist_expires:
          type: integer
          format: int64
          example: 1574633337678
          description: 'The timestamp when the session expires, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
  PolicyRuleAlertWatcherPost:
    type: object
    properties:
      rules:
        type: array
        description: A list of alert-watcher policy rules to create.
        items:
          $ref: '#/definitions/_policyrulealertwatcherpostRules'
  SubscriptionAssetGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SubscriptionAssetResponse'
  _snmp_v3_post:
    type: object
    properties:
      auth_protocol:
        type: string
        example: MD5
        description: 'Hash algorithm used to validate the authentication passphrase.

          Valid values are `MD5` and `SHA`.

          '
      privacy_protocol:
        type: string
        example: DES
        description: 'Encryption protocol for SNMP messages.

          Valid values are `AES` and `DES`.

          '
      auth_passphrase:
        type: string
        example: '********'
        description: 'Passphrase used by Purity//FA to authenticate the array with
          the specified managers.

          '
        maxLength: 32
      privacy_passphrase:
        type: string
        example: '********'
        description: Passphrase used to encrypt SNMP messages.
        minLength: 8
        maxLength: 63
      user:
        type: string
        example: User1
        description: 'User ID recognized by the specified SNMP managers which Purity//FA

          is to use in communications with them.

          '
    description: 'The v3 configurations of SNMP.

      '
  ResourcePerformanceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where it makes more sense, the average value is displayed instead.

            The values are displayed for each field where meaningful.

            '
          items:
            $ref: '#/definitions/ResourcePerformance'
        items:
          type: array
          description: 'Performance data.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/ResourcePerformance'
  _presetWorkloadConstraintsString:
    type: object
    properties:
      allowed_values:
        type: array
        description: 'The valid values that can be supplied to the parameter.

          A parameter that collects the name of the environment to which a workload
          will deploy

          might, for example, limit the allowed values to `production`, `testing`
          and

          `development`. Supports up to five values, with up to 64 unicode characters
          per value.

          '
        items:
          type: string
      default:
        type: string
        description: 'The default value to use if no value is provided.

          Must be present in `allowed_values`, if `allowed_values` is set.

          Supports up to 64 unicode characters.

          '
    description: Constraints to apply to string parameters.
  Subscription:
    type: object
    properties:
      service:
        type: string
        example: Evergreen//One
        description: 'The service type of the subscription.

          '
      id:
        type: string
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified.

          '
  AlertRulesResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/AlertRules'
  ProtectionGroupResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          If `total_only=true`, the `items` list will be empty.

          '
        items:
          $ref: '#/definitions/ProtectionGroup'
  PolicyAuditFileResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/PolicyAuditFile'
  TestResultWithResourceResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/TestResultWithResource'
  ArrayPerformanceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/ArrayPerformance'
  VchostEndpointPatch:
    type: object
    properties:
      certificates:
        type: array
        description: 'The certificate to be presented by the vchost over the configured
          endpoints.

          '
        items:
          $ref: '#/definitions/_referenceNoIdWithType'
  DnsPatch:
    allOf:
    - $ref: '#/definitions/_newName'
    - $ref: '#/definitions/DnsPost'
  AdminGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/AdminResponse'
  SupportGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SupportResponse'
  NetworkInterfaceNeighborResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/NetworkInterfaceNeighbor'
  ResourcePerformanceNoIdGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where it makes more sense, the average value is displayed instead.

            The values are displayed for each field where meaningful.

            '
          items:
            $ref: '#/definitions/ResourcePerformanceNoId'
        items:
          type: array
          description: 'Performance data.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/ResourcePerformanceNoId'
  Subnet:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - $ref: '#/definitions/SubnetPost'
    - type: object
      properties:
        interfaces:
          type: array
          description: List of network interfaces associated with this subnet.
          readOnly: true
          items:
            $ref: '#/definitions/_fixedReferenceNoId'
        services:
          type: array
          description: The services provided by this subnet, as inherited from all
            of its interfaces.
          readOnly: true
          items:
            type: string
            description: 'Valid values are `iscsi`, `management`, `nvme/fc`, `nvme/roce`,
              and `replication`.

              '
  HostGroupResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Returns a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/HostGroup'
  SoftwareInstallationPatch:
    type: object
    properties:
      add_override_checks:
        type: array
        description: 'Adds the specified checks to the current list of override checks.

          '
        items:
          $ref: '#/definitions/_override_check'
  NetworkInterfaceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/NetworkInterfaceResponse'
  AlertWatcherPost:
    type: object
    properties:
      enabled:
        type: boolean
        description: 'If set to `true`, email notifications will be sent to this watcher
          for alerts.

          If set to `false`, email notifications are disabled.

          If not specified, defaults to `true`.

          '
  ContainerPerformanceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            When applicable, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/ContainerPerformance'
        items:
          type: array
          description: 'Performance data.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/ContainerPerformance'
  WorkloadPlacementRecommendation:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        expires:
          type: integer
          format: int64
          description: 'The time when recommendation expires, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
        more_results_available:
          type: boolean
          readOnly: true
        created:
          type: integer
          format: int64
          description: 'The time the recommendation was made, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
        preset:
          type: object
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        projection_months:
          type: integer
          format: int32
          description: 'The number of months to compute the projections.

            If not specified, defaults to 1 month.

            '
          x-minimum: 1
          x-maximum: 12
        additional_constraints:
          $ref: '#/definitions/_workloadPlacementRecommendationAdditionalConstraints'
        name:
          type: string
          description: 'A globally unique, system-generated name.

            The name cannot be modified and cannot refer to another resource.

            '
          readOnly: true
        progress:
          type: number
          format: float
          example: 0.77
          description: 'The percentage progress of the recommendation computation.

            The percentage is displayed as a decimal value, starting at 0.00 and ending
            at 1.00.

            '
          readOnly: true
        id:
          type: string
          description: 'A globally unique, system-generated ID.

            The ID cannot be modified and cannot refer to another resource.

            '
          readOnly: true
        parameters:
          type: array
          description: 'The parameter values to pass to the preset.

            Value must be supplied for all parameters that do not have a default

            defined in the preset.

            '
          items:
            $ref: '#/definitions/_workloadParameter'
        placement_names:
          type: array
          readOnly: true
          items:
            type: string
        results:
          type: array
          description: 'The results of the recommendation.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_workloadPlacementRecommendationResult'
        results_limit:
          type: integer
          format: int32
          description: 'The maximum number of results to return.

            If not specified, defaults to 10 results.

            '
        status:
          type: string
          description: 'The status of the recommendation.

            Possible values are `processing`, `completed`, and `failed`.

            '
          readOnly: true
      description: 'A workload placement recommendation.

        '
  VchostEndpointPost:
    type: object
    properties:
      endpoint:
        type: string
        example: 1.2.3.4
        description: 'The IPv4 or IPv6 address of the endpoint.

          '
      certificates:
        type: array
        description: 'The certificate to be presented by the vchost over the configured
          endpoints.

          '
        items:
          $ref: '#/definitions/_referenceNoIdWithType'
  PodReplicaLinkMappingPolicyGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PodReplicaLinkMappingPolicyResponse'
    - $ref: '#/definitions/_errorContextResponse'
  ResourceAccessesStatusGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ResourceAccessesStatusResponse'
  PolicyManagementAccessGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyManagementAccessResponse'
    - $ref: '#/definitions/_errorContextResponse'
  OffloadGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/OffloadResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where meaningful, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/Offload'
  PolicyAuditFileGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyAuditFileResponse'
    - $ref: '#/definitions/_errorContextResponse'
  Kmip_object:
    type: object
    properties:
      uid:
        type: string
        description: 'User ID of the KMIP object.

          '
      name:
        type: string
        description: 'Name of the KMIP object.

          '
  TestResultWithResource:
    allOf:
    - $ref: '#/definitions/TestResult'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        resource:
          type: object
          description: A reference to the object being tested.
          title: FixedReferenceNoId
          allOf:
          - $ref: '#/definitions/_fixedReferenceNoId'
  HostPost:
    type: object
    properties:
      wwns:
        type: array
        description: The Fibre Channel World Wide Name (WWN) associated with the host.
        items:
          type: string
          example: 52:4A:93:70:4E:E5:68:00
      personality:
        type: string
        description: 'Determines how the system tunes the array to ensure that it
          works optimally with the host.

          Set `personality` to the name of the host operating system or virtual memory
          system.

          Valid values are `aix`, `esxi`, `hitachi-vsp`, `hpux`,

          `oracle-vm-server`, `solaris`, and `vms`.

          If your system is not listed as one of the valid host personalities, do
          not set the option.

          By default, the personality is not set.

          '
      vlan:
        type: string
        example: '1'
        description: 'The VLAN ID that the host is associated with.

          If not set or set to `any`, the host can access any VLAN.

          If set to `untagged`, the host can only access untagged VLANs.

          If set to a number between `1` and `4094`,

          the host can only access the specified VLAN with that number.

          '
      chap:
        $ref: '#/definitions/_chap'
      nqns:
        type: array
        description: The NVMe Qualified Name (NQN) associated with the host.
        items:
          type: string
          example: nqn.2010-06.com.purestorage:flasharray.512f7bb185978505
      iqns:
        type: array
        description: 'The iSCSI qualified name (IQN) associated with the host.

          '
        items:
          type: string
          example: iqn.2010-06.com.purestorage:linux.393d930077be2977
      preferred_arrays:
        type: array
        description: 'For synchronous replication configurations, sets a host''s preferred
          array to specify which

          array exposes active/optimized paths to that host.

          Enter multiple preferred arrays in comma-separated format.

          If a preferred array is set for a host,

          then the other arrays in the same pod will expose active/non-optimized paths
          to that host.

          If the host is in a host group, `preferred_arrays` cannot be set because
          host groups

          have their own preferred arrays.

          On a preferred array of a certain host, all the paths on all the ports

          (for both the primary and secondary controllers) are set up as A/O (active/optimized)
          paths,

          while on a non-preferred array, all the paths are A/N (Active/Non-optimized)
          paths.

          '
        items:
          $ref: '#/definitions/_reference'
      tags:
        type: array
        description: 'The list of tags to be upserted with the object.

          '
        items:
          $ref: '#/definitions/NonCopyableTag'
  AlertRulesCatalogResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/AlertRulesCatalog'
  PolicySmb:
    allOf:
    - $ref: '#/definitions/Policy'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        policy_mapping:
          type: object
          description: 'Specifies the mapping of this policy across a pod replica
            link. If

            this policy is not inside a pod of a pod replica link, mapping is `null`.

            '
          title: PolicyNfsPolicyMapping
          allOf:
          - $ref: '#/definitions/PolicyNfsPolicyMapping'
        access_based_enumeration_enabled:
          type: boolean
          description: 'Returns a value of `true` if access based enumeration is enabled
            on the policy.

            When access based enumeration is enabled on a policy,

            files and folders within exports that are attached to the policy

            will be hidden from users who do not have permission to view them.

            '
  Controller:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        mode:
          type: string
          example: primary
          description: 'Mode of controller. Values include `not present`, `offline`,
            `primary`, and `secondary`.

            '
          readOnly: true
        mode_since:
          type: integer
          format: int64
          example: 1687086901996
          description: 'The time at which the current mode of the controller was last
            set.

            It is measured in milliseconds since the UNIX epoch.

            If the controller mode is `not present` or `offline`, this field will
            be null.

            '
          readOnly: true
        model:
          type: string
          example: FA-m50
          readOnly: true
        type:
          type: string
          description: 'Type of controller. Values include `array_controller` and
            `shelf_controller`.

            '
          readOnly: true
        version:
          type: string
          example: 5.0.0
          readOnly: true
        status:
          type: string
          example: ready
          description: 'Status of controller. Values include `not ready`, `ready`,
            `unknown`, and `updating`.

            '
          readOnly: true
  DirectorySnapshotResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/DirectorySnapshot'
  PolicyNfsPost:
    allOf:
    - $ref: '#/definitions/PolicyPost'
    - type: object
      properties:
        policy_mapping:
          $ref: '#/definitions/_policynfspostPolicyMapping'
        user_mapping_enabled:
          type: boolean
          description: 'If set to `true`, FlashArray queries the joined

            AD/OpenLDAP server to find the user

            corresponding to the incoming UID.

            If set to `false`, users are defined by UID/GID pair.

            If not specified, defaults to `true`.

            '
  SnmpManagerResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SnmpManager'
  ArrayConnectionPath:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        encryption:
          type: string
          example: encrypted
          description: 'If `encrypted`, all traffic over this array connection will
            be encrypted.

            If `unencrypted`, all traffic over this array connection will be unencrypted.

            '
        local_port:
          type: string
          example: ct0.eth0
          description: 'The local port of the path.

            '
        replication_transport:
          type: string
          description: 'The protocol used to transport data between the local array
            and the remote array.

            Valid values are `ip` and `fc`.

            '
        local_address:
          type: string
          example: 10.202.101.70
          description: 'IP address or WWN of the local port.

            '
        remote_port:
          type: string
          example: ct1.eth0
          description: 'The remote port of the path.

            '
        id:
          type: string
          description: 'A globally unique, system-generated ID of an array connection.

            '
          readOnly: true
        remote_address:
          type: string
          example: 10.202.101.78
          description: 'IP address or WWN of the remote port.

            '
        encryption_mode:
          type: string
          example: TLS-PSK-AES-256
          description: 'Cryptographic protocol, trust model, and encryption algorithm
            information.

            Will be `null` if `encrypted` is `false`.

            '
        remote:
          type: object
          description: 'The remote array.

            '
          title: ReferenceWithType
          allOf:
          - $ref: '#/definitions/_referenceWithType'
        type:
          type: string
          description: 'The type of path. Valid values include `async-replication`,
            `sync-replication`, and

            `fleet-management`.

            '
        status_details:
          type: string
          description: 'Additional information describing any issues encountered when

            connecting, or `null` if the `status` is `connected`.

            '
          readOnly: true
        status:
          type: string
          description: 'Status of the connection.

            Valid values are `connected`, `connecting`, and `quarantined`.

            A status of `connected` indicates that the arrays are communicating.

            A status of `connecting` indicates that the array is trying to establish
            a connection.

            A status of `quarantined` indicates that the path is unstable and has
            been temporarily

            embargoed for synchronous replication connections.

            '
          readOnly: true
      description: 'Path from the local array to a remote array. The `name` parameter
        indicates the name of the

        remote array.

        '
  ProtectionGroupPerformanceArrayResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        items:
          type: array
          description: 'Lists performance data, broken down by array.

            '
          items:
            $ref: '#/definitions/ProtectionGroupPerformanceArray'
  SubscriptionAsset:
    allOf:
    - $ref: '#/definitions/_resource'
    - type: object
      properties:
        subscription:
          $ref: '#/definitions/_subscriptionAssetSubscription'
  PolicyRuleAlertWatcherPatch:
    type: object
    properties:
      rules:
        type: array
        description: 'Updates an alert-watcher rule in a policy. Only one update object
          will be accepted.

          '
        items:
          $ref: '#/definitions/_policyrulealertwatcherpatchRules'
        maxItems: 1
  ActiveDirectoryResponse:
    type: object
    properties:
      items:
        type: array
        description: 'A list of Active Directory computer account configuration objects.

          '
        items:
          $ref: '#/definitions/ActiveDirectory'
  TargetProtectionGroupPostPatch:
    type: object
    properties:
      protection_group:
        $ref: '#/definitions/_fixedReference'
      allowed:
        type: boolean
        example: true
        description: 'If set to `true`, the target array has allowed the source array

          to replicate protection group data to the target array.

          If set to `false`, the target array has not allowed the source array

          to replicate protection group data to the target.

          '
      target:
        $ref: '#/definitions/_fixedReferenceNoId'
  PolicyGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyResponse'
    - $ref: '#/definitions/_errorContextResponse'
  MemberResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name where meaningful.

          If `total_only=true`, the `items` list will be empty.

          '
        items:
          $ref: '#/definitions/Member'
  PolicyPasswordResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Returns a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/PolicyPassword'
  ArrayConnectionPathResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/ArrayConnectionPath'
  KmipResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Kmip'
  PolicyMemberResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/PolicyMember'
  HostPerformanceBalanceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        items:
          type: array
          description: 'A list of entries indicating count and percentage of I/O operations

            across various data paths between the host and the array.

            '
          items:
            $ref: '#/definitions/HostPerformanceBalance'
  _presetWorkloadConstraints:
    type: object
    properties:
      boolean:
        $ref: '#/definitions/_presetWorkloadConstraintsBoolean'
      string:
        $ref: '#/definitions/_presetWorkloadConstraintsString'
      integer:
        $ref: '#/definitions/_presetWorkloadConstraintsInteger'
      resource_reference:
        $ref: '#/definitions/_presetWorkloadConstraintsResourceReference'
    description: Constraints to apply to the parameter.
  PolicyNfsPolicyMapping:
    type: object
    properties:
      mapping:
        type: string
        description: 'The mapping of this policy. Valid values are `connected` and
          `disconnected`.

          '
      remote_policy:
        type: string
        description: 'The policy on the remote pod of the pod replica link for this
          policy mapping.

          '
  _locationReference:
    allOf:
    - $ref: '#/definitions/_reference'
    - type: object
      properties:
        is_local:
          type: boolean
          description: 'If set to `true`, the location reference is to the local array.

            If set to `false`, the location reference is to a remote location,

            such as a remote array or offload target.

            '
          readOnly: true
  NetworkInterface:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        eth:
          $ref: '#/definitions/_networkInterfaceEth'
        services:
          type: array
          description: 'The services provided by the specified network interface or
            Fibre Channel port.

            '
          readOnly: true
          items:
            type: string
            description: 'Valid values include `iscsi`, `management`, `nvme-fc`, `nvme-roce`,

              `offload`, `replication`, `scsi-fc`, and `system`.

              '
        interface_type:
          type: string
          description: 'The interface type. Valid values are `eth` and `fc`.

            '
          readOnly: true
        fc:
          $ref: '#/definitions/_networkInterfaceFc'
        enabled:
          type: boolean
          description: 'Returns a value of `true` if the specified network interface
            or Fibre

            Channel port is enabled. Returns a value of `false` if the specified

            network interface or Fibre Channel port is disabled.

            '
          readOnly: true
        speed:
          type: integer
          format: int64
          description: 'Configured speed of the specified network interface or Fibre
            Channel

            port (in Gb/s). Typically this is the maximum speed of the port or bond

            represented by the network interface.

            '
          readOnly: true
  DirectoryServiceRolePost:
    type: object
    properties:
      management_access_policies:
        type: array
        description: 'List of management access policies to associate with the group.

          '
        items:
          $ref: '#/definitions/_referenceWithType'
      role:
        type: object
        description: 'This field has been deprecated.

          Predecessor to management access policies. Can be set to the

          premade policy of the same name. Cannot be set with management_access_policies.

          '
        title: ReferenceNoId
        allOf:
        - $ref: '#/definitions/_referenceNoId'
      group_base:
        type: string
        example: OU=PureGroups,OU=SANManagers
        description: 'Specifies where the configured group is located in the directory
          tree.

          '
      group:
        type: string
        example: groupOfUsers
        description: 'Group name that contains users with the abilities granted by
          the associated policies.

          '
  _networkInterfacePerformanceFc:
    type: object
    properties:
      received_crc_errors_per_sec:
        type: integer
        format: int64
        description: 'Fibre Channel frame CRC errors per second.

          '
        minimum: 0
      transmitted_frames_per_sec:
        type: integer
        format: int64
        description: 'Fibre Channel frames transmitted per second.

          '
        minimum: 0
      received_link_failures_per_sec:
        type: integer
        format: int64
        description: 'Loss of connectivity errors per second.

          '
        minimum: 0
      transmitted_invalid_words_per_sec:
        type: integer
        format: int64
        description: 'Bit errors in transmission word per second.

          '
        minimum: 0
      received_loss_of_signal_per_sec:
        type: integer
        format: int64
        description: 'Loss of signal errors on Fibre Channel port per second.

          '
        minimum: 0
      received_loss_of_sync_per_sec:
        type: integer
        format: int64
        description: 'Loss of sync errors on Fibre Channel port per second.

          '
        minimum: 0
      transmitted_bytes_per_sec:
        type: integer
        format: int64
        description: 'Bytes transmitted per second.

          '
        minimum: 0
      received_bytes_per_sec:
        type: integer
        format: int64
        description: 'Bytes received per second.

          '
        minimum: 0
      total_errors_per_sec:
        type: integer
        format: int64
        description: 'The sum of all reception and transmission errors per second.

          '
        minimum: 0
      received_frames_per_sec:
        type: integer
        format: int64
        description: 'Fibre Channel frames received per second.

          '
        minimum: 0
    description: 'Fibre Channel port statistics.

      '
  SoftwareInstallation:
    allOf:
    - $ref: '#/definitions/_resourceFixedNonUniqueName'
    - $ref: '#/definitions/_start_end_time'
    - type: object
      properties:
        mode:
          type: string
          example: interactive
          description: 'Mode that the upgrade is in. Valid values are `check-only`,
            `interactive`,

            `semi-interactive`, and `one-click`.

            The `check_only` mode is deprecated. Use `/software-checks`. In this mode,
            the upgrade only

            runs pre-upgrade checks and returns.

            In `interactive` mode, the upgrade pauses at several points, at which
            users must

            enter certain commands to proceed.

            In `semi-interactive` mode, the upgrade pauses if there are any upgrade
            check failures, and

            functions like `one-click` mode otherwise.

            In `one-click` mode, the upgrade proceeds automatically without pausing.

            '
        current_step_id:
          type: string
          description: The `id` of the current step or `null` if the upgrade is not
            active.
        override_checks:
          type: array
          description: 'A list of upgrade checks whose failure is overridden during
            the upgrade.

            If any optional `args` are provided, they are validated later when the
            corresponding

            check script runs.

            '
          items:
            $ref: '#/definitions/_override_check'
        software:
          type: object
          description: 'Referenced `software` to which the upgrade belongs.

            '
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        details:
          type: string
          description: The detailed reason for the `status`.
        upgrade_parameters:
          type: array
          description: 'A list of parameters to be sent to the upgrade process. These
            parameters can be used to

            change the behavior of the upgrade process in specific ways such as enabling
            or disabling

            features in the target version or changing the attributes of the appliance
            at upgrade time.

            When there is a need or option to set an upgrade parameter, Pure Storage
            will provide

            documentation for the specific parameter. If you have been provided documentation
            related

            to a specific upgrade parameter, follow the instructions in the documentation.

            Otherwise, do not set any upgrade parameters.

            '
          items:
            $ref: '#/definitions/_upgrade_parameters'
        status:
          type: string
          example: Installing
          description: 'Status of the upgrade. Valid values are `installing`, `paused`,
            `aborting`,

            `aborted`, and `finished`.

            A status of `installing` indicates that the upgrade is running.

            A status of `paused` indicates that the upgrade is paused and waiting
            for user input.

            A status of `aborting` indicates that the upgrade is being aborted.

            A status of `aborted` indicates that the upgrade has stopped due to an
            abort.

            A status of `finished` indicates that the upgrade has finished successfully.

            '
      description: 'Interactive software upgrade attempt that contains information
        about the current upgrade

        instance, including the current step, status of the upgrade attempt, etc.

        '
  CertificateCertificateGroupGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/CertificateCertificateGroupResponse'
  PortInitiatorTarget:
    allOf:
    - $ref: '#/definitions/_basePort'
  _policyrulequotapatchRules:
    type: object
    properties:
      quota_limit:
        type: integer
        format: int64
        description: 'Logical space limit of the quota (in bytes) assigned by the
          rule.

          This value cannot be set to 0.

          '
      enforced:
        type: boolean
        description: 'If set to `true`, this rule describes an enforced quota. An
          out-of-space warning is issued if

          logical space usage exceeds the limit value described in this rule.

          If set to `false`, this rule describes an unenforced quota. Alerts and/or
          notifications are

          issued when logical space usage exceeds the limit value described in this
          rule.

          '
      notifications:
        type: string
        description: 'Targets to notify when usage approaches the quota limit.

          The list of notification targets is a comma-separated string.

          Valid values are one or more of `user` and `group`. To notify no targets,
          use `none`.

          '
  SoftwareBundle:
    allOf:
    - $ref: '#/definitions/_builtInRelationship'
    - type: object
      properties:
        download_progress:
          type: number
          format: float
          description: 'The progress of the download. Displayed in decimal format.

            '
        created:
          type: number
          format: integer
          description: 'Creation time in milliseconds since the UNIX epoch.

            '
        details:
          type: string
          description: 'The detailed reason for the `status`.

            '
        source:
          type: string
          example: ftp://local/upgrade_bundle.sh
          description: 'The location of the upgrade bundle.

            '
        status:
          type: string
          example: downloading
          description: 'The status of the software bundle.

            Valid values are `downloading`, `failed`, `ready`, and `verifying`.

            A status of `downloading` indicates that the array is downloading the
            upgrade bundle.

            A status of `failed` indicates that the array has failed to download

            the upgrade bundle.

            A status of `ready` indicates that the upgrade bundle is ready to be installed.

            A status of `verifying` indicates that the array is verifying the upgrade
            bundle.


            '
      description: Software bundle to be installed on the array.
  NonCopyableTag:
    type: object
    properties:
      resource:
        $ref: '#/definitions/_fixedReference'
      namespace:
        type: string
        example: default
        description: 'Optional namespace of the tag.

          Namespace identifies the category of the tag. Omitting the namespace

          defaults to the namespace `default`. The `pure&#42;` namespaces are

          reserved for plugins and integration partners. It is recommended

          that customers avoid using reserved namespaces.

          '
      value:
        type: string
        example: staging
        description: 'Value of the tag. Supports up to 256 Unicode characters.

          '
      key:
        type: string
        example: environment
        description: 'Key of the tag. Supports up to 64 Unicode characters.

          '
  LocalGroupPost:
    type: object
    properties:
      gid:
        type: integer
        format: int32
        description: 'Optional field to set the GID of the local group.

          '
      email:
        type: string
        description: 'Optional field to set the email address of the local group.

          '
  _presetWorkloadParameter:
    type: object
    required:
    - name
    - type
    properties:
      metadata:
        $ref: '#/definitions/_presetWorkloadMetadata'
      name:
        type: string
        example: billing-id
        description: 'The name of the parameter, by which other fields in the preset
          can reference it.

          Name must be unique across all parameters in the preset.

          '
      type:
        type: string
        description: 'The type of the parameter. Valid values include `string`, `integer`,
          `boolean` and

          `resource_reference`.


          String parameters can be used to collect metadata about workloads deployed
          from the preset,

          such as the environment to which they are deployed (e.g., production, development,
          etc.) or

          the billing account to which they belong for charge back and show back purposes.


          Integer and boolean parameters can be used to configure specific fields
          in the preset, such as

          the number or size of volumes to provision.


          Resource reference parameters can be used to collect references to other
          objects, such as

          storage classes or remote arrays.'
      constraints:
        $ref: '#/definitions/_presetWorkloadConstraints'
  PolicyRuleSnapshotGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyRuleSnapshotResponse'
    - $ref: '#/definitions/_errorContextResponse'
  CloudConfigSupported:
    type: object
    properties:
      supported_models:
        type: array
        description: List of supported models for an upgrade.
        items:
          type: string
          example: CBS-V20MP2R2
          description: A supported model for the CBS array.
  _replicationPerformanceWithTotal:
    type: object
    properties:
      total_bytes_per_sec:
        type: integer
        format: int64
        description: 'Total bytes transmitted and received per second.

          The number will be zero if the arrays are unable to communicate.

          '
        readOnly: true
        minimum: 0
      to_remote_bytes_per_sec:
        type: integer
        format: int64
        description: 'The number of bytes transmitted per second to a remote array.

          The number will be zero if the arrays are unable to communicate.

          '
        readOnly: true
        minimum: 0
      from_remote_bytes_per_sec:
        type: integer
        format: int64
        description: 'The number of bytes received per second from a remote array.

          The number will be zero if the arrays are unable to communicate.

          '
        readOnly: true
        minimum: 0
  SupportRemoteAssistSessionsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SupportRemoteAssistSessionsResponse'
  _referenceWithFixedType:
    type: object
    properties:
      name:
        type: string
        description: 'The resource name,

          such as volume name, pod name, snapshot name, and so on.

          '
      resource_type:
        type: string
        description: 'Type of the object (full name of the endpoint).

          Valid values are `hosts`, `host-groups`, `network-interfaces`, `pods`,

          `ports`, `pod-replica-links`, `subnets`, `volumes`, `volume-snapshots`,

          `volume-groups`, `directories`, `policies/nfs`, `policies/smb`, and

          `policies/snapshot`, etc.

          '
        readOnly: true
      id:
        type: string
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified.

          '
  RemoteArray:
    allOf:
    - $ref: '#/definitions/_resource'
    - type: object
      properties:
        fleet:
          $ref: '#/definitions/_remoteArrayFleet'
        os:
          type: string
          example: Purity//FA
          description: 'Specifies the operating system. Valid values are `Purity`,
            `Purity//FA`, and `Purity//FB`.

            '
        model:
          type: string
          example: FA//X
          description: 'Specifies the array model.

            '
        is_local:
          type: boolean
          example: false
          description: 'If set to `true`, this array is the current array.

            '
        version:
          type: string
          example: 6.8.0
          description: 'Specifies the operating system version.

            '
  SupportRemoteAssistSessionsSettingsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SupportRemoteAssistSessionsSettingsResponse'
  DirectoryPerformance:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        read_bytes_per_sec:
          type: integer
          format: int64
          description: The number of bytes read per second.
          readOnly: true
          minimum: 0
        others_per_sec:
          type: integer
          format: int64
          description: The number of other requests processed per second.
          readOnly: true
          minimum: 0
        reads_per_sec:
          type: integer
          format: int64
          description: The number of read requests processed per second.
          readOnly: true
          minimum: 0
        write_bytes_per_sec:
          type: integer
          format: int64
          description: The number of bytes written per second.
          readOnly: true
          minimum: 0
        bytes_per_write:
          type: integer
          format: int64
          description: The average I/O size per write, measured in bytes.
          readOnly: true
          minimum: 0
        usec_per_other_op:
          type: integer
          format: int64
          description: 'The average time it takes the array to process an I/O other
            request, measured in

            microseconds.

            '
          readOnly: true
          minimum: 0
        writes_per_sec:
          type: integer
          format: int64
          description: The number of write requests processed per second.
          readOnly: true
          minimum: 0
        time:
          type: integer
          format: int64
          description: 'The time when the sample performance data was taken.

            Measured in milliseconds since the UNIX epoch.

            '
          readOnly: true
        bytes_per_op:
          type: integer
          format: int64
          description: 'The average I/O size for both read and write (all) operations.

            '
          readOnly: true
          minimum: 0
        usec_per_write_op:
          type: integer
          format: int64
          description: 'The average time it takes the array to process an I/O write
            request, measured in

            microseconds.

            '
          readOnly: true
          minimum: 0
        bytes_per_read:
          type: integer
          format: int64
          description: The average I/O size per read, measured in bytes.
          readOnly: true
          minimum: 0
        usec_per_read_op:
          type: integer
          format: int64
          description: 'The average time it takes the array to process an I/O read
            request, measured in

            microseconds.

            '
          readOnly: true
          minimum: 0
  CertificateGroupGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/CertificateGroupResponse'
  HostGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/HostResponse'
    - $ref: '#/definitions/_errorContextResponse'
  _policyrulesmbclientpostRules:
    type: object
    properties:
      anonymous_access_allowed:
        type: boolean
        description: 'Specifies whether access to information is allowed for anonymous
          users.

          If not specified, defaults to `false`.

          '
      client:
        type: string
        description: 'Specifies which clients are given access.

          Accepted notation, IP, IP mask, or hostname.

          The default is `*` if not specified.

          '
      smb_encryption_required:
        type: boolean
        description: 'Specifies whether the remote client is required to use SMB encryption.

          If not specified, defaults to `false`.

          '
  ArrayPerformanceByLinkResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Array-level front-end I/O performance metrics grouped by link.

          A link represents a set of arrays that an I/O operation depends on.

          '
        items:
          $ref: '#/definitions/ArrayPerformanceByLink'
  Session:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - type: object
      properties:
        start_time:
          type: integer
          format: int64
          example: 1502234212
          description: Date and time the user logged in to the Purity//FA interface.
          readOnly: true
        method:
          type: string
          example: public key
          description: 'Method by which the user attempted to log in.

            Valid values include `API token`, `JWT`, `password`, and `public key`.

            '
          readOnly: true
        event_count:
          type: integer
          format: int32
          example: 3
          description: Number of session events.
          readOnly: true
        end_time:
          type: integer
          format: int64
          example: 1502234638
          description: 'Date and time the user logged out of the Purity//FA interface.

            Not set if the session is still active.

            '
          readOnly: true
        user_interface:
          type: string
          example: CLI
          description: 'The user interface through which the user session event was
            performed.

            Valid values include `CLI`, `GUI`, and `REST`.

            '
          readOnly: true
        location:
          type: string
          example: 10.202.101.11
          description: 'IP address of the user client connecting to the array or console
            if connected through local

            console.

            '
          readOnly: true
        event:
          type: string
          example: user session
          description: 'Description of session events such as login and user session.

            Valid values include `failed authentication`, `user session`, `login`,

            `logout`, `API token obtained`, and `request without session`.

            '
          readOnly: true
        user:
          type: string
          example: pureuser
          description: Username of the Purity//FA user who triggered the user session
            event.
          readOnly: true
      description: 'User session logs represent user login events performed in the
        Purity//FA GUI, CLI, and

        REST API.

        '
  PolicySmbPost:
    allOf:
    - $ref: '#/definitions/PolicyPost'
    - type: object
      properties:
        policy_mapping:
          $ref: '#/definitions/_policynfspostPolicyMapping'
        access_based_enumeration_enabled:
          type: boolean
          description: 'If set to `true`, enables access based enumeration on the
            policy.

            When access based enumeration is enabled on a policy,

            files and folders within exports that are attached to the policy

            will be hidden from users who do not have permission to view them.

            If not specified, defaults to `false`.

            '
  VolumeSnapshotPost:
    allOf:
    - $ref: '#/definitions/_destroyedPatchPost'
    - type: object
      properties:
        suffix:
          type: string
          description: 'The suffix that is appended to the `source_name` value to
            generate the

            full volume snapshot name in the form `VOL.SUFFIX`.

            If the suffix is not specified, the system constructs the snapshot name

            in the form `VOL.NNN`, where `VOL` is the volume name, and `NNN` is a

            monotonically increasing number.

            '
  LocalGroupMembershipPost:
    type: object
    properties:
      members:
        type: array
        description: 'A list of resources to be a member of the group.

          '
        items:
          $ref: '#/definitions/_localgroupmembershippostMembers'
  _workloadConfigurationFixedReferenceWorkload:
    allOf:
    - $ref: '#/definitions/_fixedReferenceWithType'
    - type: object
      properties:
        configuration:
          type: string
          description: 'The name of the preset configuration object.

            '
          readOnly: true
    description: 'A reference to the workload that is managing this resource.

      The `name` parameter might show as an empty string briefly after controller
      failover.

      '
  ArrayConnectionPatch:
    allOf:
    - $ref: '#/definitions/_throttle_deprecated'
    - type: object
      properties:
        replication_addresses:
          type: array
          description: 'IP addresses and FQDNs of the target arrays. Configurable
            only when

            `replication_transport` is set to `ip`.

            '
          items:
            type: string
            example: 10.202.101.70
        throttle:
          $ref: '#/definitions/_throttle'
        connection_key:
          type: string
          example: 6207d123-d123-0b5c-5fa1-95fabc5c7123
          description: 'The connection key of the target array.

            It is only required when `encryption` is changed from `unencrypted` to
            `encrypted`,

            or when `type` is changed from `async-replication` to `sync-replication`.

            '
        encryption:
          type: string
          example: encrypted
          description: 'If `encrypted`, encryption will be enabled for all traffic
            over this array connection.

            `connection_key` must be specified when encrypted is set to `true`.

            If `unencrypted`, encryption will be disabled for all traffic over this
            array connection.

            If not specified, the current encryption option for the array connection
            will remain

            unchanged.

            '
        management_address:
          type: string
          example: 10.202.101.78
          description: 'Management IP address of the target array.

            '
        type:
          type: string
          description: 'The type of replication.

            Valid values are `async-replication` and `sync-replication`.

            '
  PolicyManagementAccessPost:
    allOf:
    - $ref: '#/definitions/PolicyPost'
    - type: object
      properties:
        rules:
          type: array
          description: 'List the rules that defines the scoped roles.

            '
          items:
            $ref: '#/definitions/_policyrulemanagementaccessRules'
        aggregation_strategy:
          type: string
          description: 'When set to `least-common-permissions`, any users to whom
            this policy applies can

            receive no access rights exceeding those defined in this policy''s capability
            and resource.

            When this is set to `all-permissions`, any users to whom this policy applies
            are capable of

            receiving additional access rights from other policies that apply to them.

            If not specified, defaults to `all-permissions`.

            '
  VchostEndpointGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/VchostEndpointResponse'
  _podSpace:
    allOf:
    - $ref: '#/definitions/_space'
    - type: object
      properties:
        replication:
          type: integer
          format: int64
          description: 'The space consumed by unique data that has not been replicated
            to the

            target pod, on source pods.

            The space consumed by unique data that has been replicated to the target,

            but not yet incorporated into the target pod, on target pods.

            On Evergreen//One arrays, the effective space consumed by unique data
            that has

            not been replicated to the target pod, on source pods.

            The effective space consumed by unique data that has been replicated

            to the target, but not yet incorporated into the target pod, on target
            pods.

            '
          minimum: 0
        replication_effective:
          type: integer
          format: int64
          description: 'This field has been deprecated.

            The effective space consumed by unique data that has not been

            replicated to the target pod, on source pods.

            The effective space consumed by unique data that has been replicated

            to the target but not yet incorporated into the target pod, on target
            pods.

            Please use the `replication` field in the future, as it contains the same
            information

            for Evergreen//One arrays.

            '
          readOnly: true
          minimum: 0
        shared_effective:
          type: integer
          format: int64
          description: 'This field has been deprecated.

            The effective space contributed by data that is not unique to a specific
            volume,

            managed directory, or snapshot, measured in bytes.

            Please use the `shared` field in the future, as it contains the same information

            for Evergreen//One arrays.

            '
          readOnly: true
          minimum: 0
  TestResultWithResourceGetResponse:
    allOf:
    - $ref: '#/definitions/TotalItemCountResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/TestResultWithResource'
  PolicyPassword:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        max_password_age:
          type: integer
          format: int64
          example: 86400000
          description: 'The maximum age of password before password change is required.

            Ranges from 1 day to 99999 days, with 0 meaning password expiration is
            disabled.

            '
          minimum: 0
          maximum: 8639913600000
        enforce_dictionary_check:
          type: boolean
          example: true
          description: 'If `true`, test password against dictionary of known leaked
            passwords.

            Only applies to passwords longer than 6 characters.

            '
        lockout_duration:
          type: integer
          format: int64
          example: 3600000
          description: 'The lockout duration, in milliseconds, if a user is locked
            out after reaching the maximum

            number of login attempts. Ranges from 1 second to 90 days.

            '
          minimum: 1000
          maximum: 7776000000
        policy_type:
          type: string
          description: 'The type of policy. Valid values include `autodir`, `nfs`,

            `password`, `smb`, `snapshot`, and `quota`.

            '
          readOnly: true
        min_password_length:
          type: integer
          format: int32
          example: 1
          description: 'Minimum password length. If not specified, defaults to 1.

            '
          minimum: 1
          maximum: 100
        min_character_groups:
          type: integer
          format: int32
          example: 3
          description: 'The minimum number of character groups ([a-z], [A-Z], [0-9],
            other) required to be present

            in a password.

            '
          minimum: 0
          maximum: 4
        max_login_attempts:
          type: integer
          format: int32
          example: 10
          description: 'Maximum number of failed login attempts allowed before the
            user is locked out.

            '
          minimum: 1
          maximum: 100
        enforce_username_check:
          type: boolean
          example: true
          description: 'If `true`, the username cannot be a substring of the password.

            It only applies to usernames of 3 characters and longer.

            '
        min_characters_per_group:
          type: integer
          format: int32
          example: 1
          description: 'The minimum number of characters per group to count the group
            as present.

            Maximum is limited by the minimum password length divided by the number
            of character groups

            (e.g. min_password_length = 9, min_character_groups = 4, then maximum
            is 2).

            '
          minimum: 1
        enabled:
          type: boolean
          description: 'Returns a value of `true` if the policy is enabled.

            '
        password_history:
          type: integer
          format: int32
          example: 5
          description: 'The number of passwords tracked to prevent reuse of passwords.

            '
          minimum: 0
          maximum: 64
        min_password_age:
          type: integer
          format: int64
          example: 86400000
          description: 'The minimum age, in milliseconds, of password before password
            change is allowed.

            Ranges from 0 ms to 7 days

            '
          minimum: 0
          maximum: 604800000
  ContainerEradicationConfig:
    type: object
    properties:
      manual_eradication:
        type: string
        example: all-enabled
        description: 'The manual eradication status of the container.

          If `all-enabled`, the container and its contents can be eradicated.

          If `partially-disabled`, the container cannot be eradicated because the
          container

          contains some non-empty protection groups that are retention-locked.

          If `all-disabled`, the container and its contents cannot be eradicated.

          Values include `all-disabled`, `all-enabled`, and `partially-disabled`.

          '
        readOnly: true
    description: The status of the eradication functionality
  _builtInResourceNoId:
    type: object
    properties:
      name:
        type: string
        description: 'A locally unique, system-generated name. The name cannot be
          modified.

          '
        readOnly: true
    description: 'A built-in resource. Many are singletons predefined by Purity (e.g.,
      support

      settings). Some correspond to a piece of software, like an app, or hardware,

      like a controller. Others are created by the system in response to some event

      (e.g., alerts, audit records).


      Typically, a user can''t create, delete or rename a built-in resource. A few

      can be created or deleted, but not renamed because the names are meaningful

      to Purity (e.g., VIFs, file systems, file system snapshots, volume snapshot).

      '
  VolumeSnapshotTransfer:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_transfer'
    - $ref: '#/definitions/_context'
    - type: object
      description: 'The transfer statistics for snapshot replication data.

        '
  CloudConfigModel:
    type: object
    properties:
      current:
        type: string
        example: CBS-V10MP2R2
        description: The current model of the CBS array.
      requested:
        type: string
        example: CBS-V20MP2R2
        description: A requested model of the CBS array.
      override_checks:
        type: array
        description: A list of upgrade checks to be overridden.
        items:
          type: string
      details:
        type: string
        example: ''
        description: 'Details about the hardware upgrade. This field can contain error
          details

          or progress information when the status is `upgrading`.

          The default value is an empty string.

          '
      step:
        type: string
        example: mid-upgrade-check
        description: 'The current step of the hardware upgrade process.

          Can be `pre-upgrade-check`, `upgrade-ct0`, `failover` and so on.

          The default value is an empty string.

          '
      status:
        type: string
        example: upgrading
        description: 'The status of the hardware upgrade process.

          Can be one of `idle`, `paused`, `upgrading`.

          '
  ProtectionGroupTargetGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ProtectionGroupTargetResponse'
    - $ref: '#/definitions/_errorContextResponse'
  AlertEventResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/AlertEvent'
  FleetKeyGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/FleetKeyResponse'
  VolumeDiff:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        offset:
          type: integer
          format: int64
          description: 'Absolute offset of the chunk that is different, in bytes.

            '
          readOnly: true
        length:
          type: integer
          format: int64
          description: 'Length of the chunk that is different, in bytes.

            '
          readOnly: true
  Space:
    allOf:
    - $ref: '#/definitions/_space'
    - type: object
      properties:
        replication:
          type: integer
          format: int64
          description: 'The sum of replication space consumed by all pods on this
            array.

            '
          readOnly: true
          minimum: 0
        shared_effective:
          type: integer
          format: int64
          description: 'This field has been deprecated. It will return `null`.

            Use the `shared` field in the future, as it contains the same information

            for Evergreen//One arrays.

            '
          readOnly: true
          minimum: 0
        snapshots_effective:
          type: integer
          format: int64
          description: 'This field has deprecated. It will return `null`.

            Use the `snapshots` field in the future, as it contains the same information

            for Evergreen//One arrays.

            '
          readOnly: true
          minimum: 0
        unique_effective:
          type: integer
          format: int64
          description: 'This field has been deprecated. It will return `null`.

            Use the `unique` field in the future, as it contains the same information

            for Evergreen//One arrays.

            '
          readOnly: true
          minimum: 0
        total_effective:
          type: integer
          format: int64
          description: 'This field has been deprecated. It will return `null`.

            PUse the `total_physical` field instead, as it contains the same information

            for Evergreen//One arrays.

            '
          readOnly: true
          minimum: 0
    description: If the user does not have sufficient access, all fields will return
      `null`.
  ArrayFactoryResetTokenGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ArrayFactoryResetTokenResponse'
  TagResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Tag'
  RemoteVolumeSnapshotResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/RemoteVolumeSnapshot'
  _workloadParameter:
    type: object
    properties:
      name:
        type: string
        description: The name of the parameter.
      value:
        $ref: '#/definitions/_workloadParameterValue'
    description: The name and value of a single parameter.
  RemoteVolumeSnapshotTransferGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/RemoteVolumeSnapshotTransferResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where it makes more sense, the average value is displayed instead.

            The values are displayed for each field where meaningful.

            '
          items:
            $ref: '#/definitions/RemoteVolumeSnapshotTransfer'
  TestResult:
    type: object
    properties:
      success:
        type: boolean
        description: 'Whether the object being tested passed the test or not.

          Returns a value of `true` if the specified test has succeeded.

          Returns a value of `false` if the specified test has failed.

          '
      component_name:
        type: string
        example: CT0
        description: Name of the component running the test.
      destination:
        type: string
        example: ra.cloud-support.purestorage.com
        description: The URI of the target server being tested.
      description:
        type: string
        example: Testing phonehome connectivity
        description: What the test is doing.
      component_address:
        type: string
        example: 10.230.94.21
        description: Address of the component running the test.
      enabled:
        type: boolean
        example: true
        description: 'Whether the object being tested is enabled or not.

          Returns a value of `true` if the the service is enabled.

          Returns a value of `false` if the service is disabled.

          '
      result_details:
        type: string
        example: Timeout connecting to phonehome endpoint
        description: Additional information about the test result.
      test_type:
        type: string
        example: phonehome
        description: 'Displays the type of test being performed. The returned values
          are determined

          by the `resource` being tested and its configuration.

          Values include `array-admin-group-searching`, `binding`, `connecting`, `phonehome`,

          `phonehome-ping`, `remote-assist`, `rootdse-searching`, `read-only-group-searching`,

          `storage-admin-group-searching`, and `validate-ntp-configuration`.

          '
  ResourceSpace:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time:
          type: integer
          format: int64
          description: 'The timestamp of when the data was taken, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
        space:
          type: object
          description: 'Displays size and space consumption information.

            '
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
  Saml2SsoPatch:
    allOf:
    - $ref: '#/definitions/Saml2SsoPost'
    - type: object
      properties:
        enabled:
          type: boolean
          description: 'If set to `true`, the SAML2 SSO configuration is enabled.

            '
  DirectoryQuotasResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/DirectoryQuota'
  PodPerformanceReplicationResponse:
    type: object
    properties:
      total:
        type: array
        description: 'The aggregate value of all items after filtering.

          For real-time performance, the values are aggregated for the latest timestamp.

          For historical performance, the values are aggregated for each timestamp
          from

          `start_time` to `end_time`. When applicable, the average value is displayed

          instead. The values are displayed for each field if meaningful.

          '
        items:
          $ref: '#/definitions/PodPerformanceReplication'
      items:
        type: array
        description: A list of pod performance replication objects.
        items:
          $ref: '#/definitions/PodPerformanceReplication'
  _policymemberexportpostMembers:
    type: object
    properties:
      export_enabled:
        type: boolean
        example: false
        description: 'If set to `true`, the export is enabled.

          If not specified, defaults to `true`.

          '
      export_name:
        type: string
        description: 'The name of the export to create when applying the export policy
          to the

          directory. Export names must be unique within the same protocol.

          '
      member:
        type: object
        description: 'Reference to the directory to which the export policy may be
          applied.

          The `id` or `name` parameter is required, but they cannot be set together.

          If the `name` parameter is set, `resource_type` must also be set.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
  PolicyRuleSmbClientPost:
    type: object
    properties:
      rules:
        type: array
        description: A list of SMB policy rules to create.
        items:
          $ref: '#/definitions/_policyrulesmbclientpostRules'
  LogTargetFileResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/LogTargetFile'
  DirectoryLockNlmReclamationResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/DirectoryLockNlmReclamation'
  MaintenanceWindow:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - type: object
      properties:
        expires:
          type: integer
          format: int64
          description: 'The maintenance window end time, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
        created:
          type: integer
          format: int64
          description: 'The maintenance window start time, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
      description: 'The period of time during which maintenance will occur.

        Alerts are suppressed during the maintenance window.

        '
  PortInitiator:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        initiator:
          $ref: '#/definitions/_portCommon'
        target:
          $ref: '#/definitions/PortInitiatorTarget'
      description: Path from an initiator port to a target port.
  Kmip_certificate:
    type: object
    properties:
      name:
        type: string
        description: 'The resource name, such as volume name, pod name, snapshot name,
          and so on.

          '
  SupportRemoteAssistSessionsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SupportRemoteAssistSessions'
  VolumeSnapshotGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/VolumeSnapshotResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where meaningful, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/VolumeSnapshot'
  SnmpAgentGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SnmpAgentResponse'
  PolicyMemberExportResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/PolicyMemberExport'
  PolicyMemberExportGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyMemberExportResponse'
    - $ref: '#/definitions/_errorContextResponse'
  LogTargetFile:
    allOf:
    - $ref: '#/definitions/LogTarget'
    - type: object
      properties:
        keep_for:
          type: integer
          format: int64
          example: 86400000
          description: 'Specifies the period that audit logs are retained before they
            are deleted,

            in milliseconds.

            Default value is `null` which means size based retention does not apply.

            Use 0 to reset the value to `null`.

            At least one of the `keep_for` or `keep_size` parameters is required,

            and they can be set together.

            '
          minimum: 86400000
        directory:
          type: object
          description: 'Directory name to be used as log target.

            '
          title: ReferenceWithType
          allOf:
          - $ref: '#/definitions/_referenceWithType'
        keep_size:
          type: integer
          format: int64
          example: 1000000
          description: 'Specifies the maximum size of audit logs to be retained.

            Measured in bytes. When exceeded, older logs will be deleted.

            Default value is `null` which means size-based retention does not apply.

            Use 0 to reset the value to `null`.

            '
          minimum: 1000000
      description: A file log target
  SupportDiagnosticsSettings:
    type: object
    properties:
      last_updated:
        type: integer
        format: int64
        example: 1672531200000
        description: 'Date the diagnostics tool was last updated.

          '
      version:
        type: string
        example: 1.0.0
        description: 'Version of the diagnostics tool.

          '
        readOnly: true
    description: 'Diagnostics tools

      '
  CloudProviderTagGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/CloudProviderTagResponse'
  Offload:
    allOf:
    - $ref: '#/definitions/OffloadPost'
    - $ref: '#/definitions/_resourceNoId'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        protocol:
          type: string
          description: 'Type of offload.

            Valid values include `azure`, `google-cloud`, `nfs`, and `s3`.

            '
          readOnly: true
        target_id:
          type: string
          description: 'Unique ID for the offload target.

            When multiple connections to one offload target are created, they each
            have distinct IDs

            but share the same `target_id`.

            '
          readOnly: true
        space:
          type: object
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
        status:
          type: string
          description: 'Offload status.

            Valid values are `connecting`, `connected`, `disconnecting`, `not connected`,
            and

            `scanning`.

            '
          readOnly: true
  _directoryServiceManagement:
    type: object
    properties:
      user_login_attribute:
        type: string
        example: email
        description: 'User login attribute in the structure of the configured LDAP
          servers.

          Typically the attribute field that holds the user''s unique login name.

          Default value is `sAMAccountName` for Active Directory or `uid` for all
          other

          directory services.

          '
      ssh_public_key_attribute:
        type: string
        example: sshPublicKey
        description: 'SSH public key attribute in the structure of the configured
          LDAP servers.

          The attribute field that holds the user''s SSH public key.

          This attribute can represent a single-valued or multi-valued field.

          If this attribute resolves to a multi-valued field, all valid SSH keys will
          be used.

          If the user has specified keys under Admin endpoints, these are used as
          well.

          '
      user_object_class:
        type: string
        example: inetOrgPerson
        description: 'Value of the object class for a management LDAP user.

          Defaults to `User` for Active Directory servers, `posixAccount` or `shadowAccount`
          for

          OpenLDAP servers dependent on the group type of the server, or `person`
          for all other

          directory servers.

          '
    description: Properties specific to the management service.
  SupportDiagnosticsDetailsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SupportDiagnosticsDetails'
  AuditResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Audit'
  RemoteVolumeSnapshot:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_snapshot'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        remote:
          type: object
          description: 'Remote target where this volume snapshot is located.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
      description: 'A volume snapshot on a remote target.

        '
  CloudConfigSupportedConfigsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/CloudConfigSupported'
  ProtectionGroupEradicationConfig:
    type: object
    properties:
      manual_eradication:
        type: string
        example: enabled
        description: 'Manual eradication status of objects like protection group and

          protection group snapshots.

          If manual_eradication is `disabled`, you cannot eradicate objects.

          Values include `disabled` and `enabled`.

          '
        readOnly: true
    description: The configuration of the eradication feature.
  SnmpManagerPatch:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - type: object
      properties:
        notification:
          type: string
          example: trap
          description: 'The type of notification the agent will send.

            Valid values are `inform` and `trap`.

            '
        v2c:
          $ref: '#/definitions/_snmp_v2c'
        host:
          type: string
          example: snmp.purestorage.com
          description: 'DNS hostname or IP address of a computer that hosts an SNMP
            manager

            to which Purity//FA is to send trap messages when it generates alerts.

            '
        v3:
          $ref: '#/definitions/_snmp_v3_patch'
        version:
          type: string
          example: v3
          description: 'Version of the SNMP protocol to be used by Purity//FA to communicate
            with the specified

            manager. Valid values are `v2c` and `v3`.

            '
  _snmp_v3_patch:
    type: object
    properties:
      auth_protocol:
        type: string
        example: MD5
        description: 'Hash algorithm used to validate the authentication passphrase.

          Valid values are `MD5` and `SHA`.

          '
      privacy_protocol:
        type: string
        example: DES
        description: 'Encryption protocol for SNMP messages.

          Valid values are `AES` and `DES`.

          '
      auth_passphrase:
        type: string
        example: '********'
        description: 'Passphrase used by Purity//FA to authenticate the array with
          the specified managers.

          '
        maxLength: 32
      privacy_passphrase:
        type: string
        example: '********'
        description: Passphrase used to encrypt SNMP messages.
        minLength: 8
        maxLength: 63
      user:
        type: string
        example: User1
        description: 'User ID recognized by the specified SNMP managers which Purity//FA

          is to use in communications with them.

          '
    description: 'The v3 configurations of SNMP.

      '
  DirectoryExportResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name if meaningful.

          '
        items:
          $ref: '#/definitions/DirectoryExport'
  _throttle_deprecated:
    type: object
    properties:
      window:
        type: object
        description: Deprecated. The time during which the `window_limit` threshold
          is in effect.
        title: TimeWindow
        allOf:
        - $ref: '#/definitions/_time_window'
      default_limit:
        type: integer
        format: int64
        example: 1073741824
        description: 'Deprecated. Default maximum bandwidth threshold for outbound
          traffic in bytes. Once

          exceeded, bandwidth throttling occurs.

          '
      window_limit:
        type: integer
        format: int64
        example: 2097152
        description: 'Deprecated. Maximum bandwidth threshold for outbound traffic
          during the specified

          `window_limit` time range in bytes. Once exceeded, bandwidth throttling

          occurs.

          '
  ArrayResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Arrays'
  PolicyMemberGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyMemberResponse'
    - $ref: '#/definitions/_errorContextResponse'
  _podReplicaLinkReference:
    allOf:
    - $ref: '#/definitions/_builtInRelationship'
    - type: object
      properties:
        remotes:
          type: array
          description: 'Reference to remote arrays.

            '
          items:
            $ref: '#/definitions/_fixedReference'
        remote_pod:
          type: object
          description: 'Reference to a remote pod.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        local_pod:
          type: object
          description: 'Reference to a local pod.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
  CloudCapacityStep:
    type: object
    properties:
      supported_capacity:
        type: integer
        format: int64
        example: 10995116277760
        description: 'A possible supported raw capacity of the CBS array in bytes.

          '
  SubscriptionGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SubscriptionResponse'
  CloudConfigPatchResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/CloudConfig'
  SupportDiagnosticsDetails:
    type: object
    properties:
      severity:
        type: string
        description: Severity level of the test. Valid values are `info`, `warning`,
          and `critical`.
      test_name:
        type: string
        description: 'Name of the test that was performed.

          '
      result_details:
        type: string
        example: enabled
        description: 'Additional details related to the test.

          This field also provides remediation information.

          '
        readOnly: true
      test_type:
        type: string
        example: generic
        description: 'The category to which the test belongs to.

          '
        readOnly: true
    description: 'Details of diagnostics tests tasks

      '
  PodReplicaLinkGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PodReplicaLinkResponse'
    - $ref: '#/definitions/_errorContextResponse'
  ArrayPerformance:
    allOf:
    - $ref: '#/definitions/_performance'
    - $ref: '#/definitions/_builtIn'
    - type: object
      properties:
        others_per_sec:
          type: integer
          format: int64
          description: The number of other requests processed per second.
          readOnly: true
          minimum: 0
        local_queue_usec_per_op:
          type: integer
          format: int64
          description: 'Deprecated. The number displayed here may not be accurate
            and in later versions

            of the product this field will always display `null`.

            Instead, use `queue_usec_per_mirrored_write_op`, `queue_usec_per_read_op`,
            and

            `queue_usec_per_write_op` fields to measure IO queuing.

            '
          readOnly: true
          minimum: 0
        usec_per_other_op:
          type: integer
          format: int64
          description: 'The average time it takes the array to process an I/O other
            request, measured in

            microseconds.

            '
          readOnly: true
          minimum: 0
        queue_depth:
          type: integer
          format: int64
          description: 'Deprecated. The number displayed here may not be accurate
            and in later versions

            of the product this field will always display `null`.

            Instead, use `queue_usec_per_mirrored_write_op`, `queue_usec_per_read_op`,
            and

            `queue_usec_per_write_op` fields to measure IO queuing.

            '
          readOnly: true
          minimum: 0
  _resource:
    type: object
    properties:
      name:
        type: string
        description: 'A user-specified name.

          The name must be locally unique and can be changed.

          '
      id:
        type: string
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified and cannot refer to another resource.

          '
        readOnly: true
    description: 'An ordinary (as opposed to built-in) resource that can be created,
      named,

      renamed or deleted by the user. This might be a virtual resource (e.g., a

      volume), or correspond to something in the environment, like a host or a

      server.

      '
  WorkloadResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/Workload'
  SoftwareInstallationsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SoftwareInstallationsResponse'
  Port:
    allOf:
    - $ref: '#/definitions/_basePort'
    - $ref: '#/definitions/_context'
  PolicyAssignmentPost:
    type: object
    properties:
      policies:
        type: array
        description: 'A list of policies to apply to the resource.

          One of `id` or `name` is required in each `policy`.

          '
        items:
          $ref: '#/definitions/PolicyAssignmentPostPolicy'
  SoftwareVersionsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SoftwareVersionsResponse'
  ContainerDefaultProtection:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        default_protections:
          type: array
          description: 'The default protection that is applied to newly created volumes.

            Volumes can opt out of the default protection at creation.

            The pod''s `default_protections` defaults to the array''s `default_protections`
            at pod creation.

            To clear the list of default protections, set to an empty list `[]`.

            '
          items:
            $ref: '#/definitions/_default_protection_reference'
        name:
          type: string
          description: 'The name of an administrative domain, a data container, and
            a namespace for

            volumes and protection groups.

            Values can be `pod name` or `null`. `null` represents the local array
            name.

            '
          readOnly: true
        id:
          type: string
          description: 'The id of an administrative domain, a data container, and
            a namespace for

            volumes and protection groups.

            Values can be `pod id` or `null`. `Null` represents the local array id.

            '
          readOnly: true
        type:
          type: string
          description: 'The type of an administrative domain, a data container, and
            a namespace for

            volumes and protection groups.

            Values can be `pod` or `null`. `null` represents the local array type.

            '
          readOnly: true
  _directorypolicypostPolicies:
    type: object
    properties:
      policy:
        type: object
        description: Reference to the policy to apply to the resource.
        title: Reference
        allOf:
        - $ref: '#/definitions/_reference'
  _presetWorkloadConstraintsResourceReferenceAllowedValues:
    type: object
    required:
    - resource_type
    properties:
      resource_type:
        type: string
        description: 'The type of resource the parameter references.

          Valid values include `storage-classes` and `remote-arrays`.

          '
  PolicyRuleSnapshotPost:
    type: object
    properties:
      rules:
        type: array
        description: A list of snapshot policy rules to create.
        items:
          $ref: '#/definitions/_policyrulesnapshotpostRules'
  DirectorySnapshotGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/DirectorySnapshotResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            When applicable, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/DirectorySnapshot'
  CloudCapacityPatchResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/CloudCapacityStatus'
  VolumesBatchResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/Volume'
  ProtectionGroupSnapshotReplicaResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after the operation.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/ProtectionGroupSnapshotReplica'
  AppNodeResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/AppNode'
  AdminPatch:
    allOf:
    - $ref: '#/definitions/Admin'
    - type: object
      properties:
        old_password:
          type: string
          description: 'The current password.

            '
  _space:
    type: object
    properties:
      data_reduction:
        type: number
        format: float
        description: 'The ratio of mapped sectors within a volume versus

          the amount of physical space the data occupies after data compression and
          deduplication.

          The data reduction ratio does not include thin provisioning savings.

          For example, a data reduction ratio of 5&#58;1 means that

          for every 5 MB the host writes to the array, 1 MB is stored on the array''s
          flash modules.

          '
        readOnly: true
      shared:
        type: integer
        format: int64
        example: 111863360624
        description: 'The physical space occupied by deduplicated data, meaning that
          the space is shared with other

          volumes and snapshots as a result of data deduplication.

          Measured in bytes.

          On Evergreen//One arrays, this is the effective space contributed by data
          that is not unique

          to a specific volume, managed directory, or snapshot, measured in bytes.

          '
        readOnly: true
        minimum: 0
      virtual:
        type: integer
        format: int64
        description: 'The amount of logically written data that a volume or a snapshot
          references.

          Measured in bytes.

          '
        readOnly: true
        minimum: 0
      used_provisioned:
        type: integer
        format: int64
        example: 19937690345472
        description: 'The amount of logical space a container has consumed,

          compared against the quota limit if the container has one configured.

          Used provisioned does not include destroyed objects inside the container.

          Used provisioned can include destroyed objects for a destroyed container

          and represents how much logical space it would take to recover the container.

          '
        readOnly: true
        minimum: 0
      total_reduction:
        type: number
        format: float
        description: 'The ratio of provisioned sectors within a volume versus

          the amount of physical space the data occupies

          after reduction via data compression and deduplication

          and with thin provisioning savings.

          Total reduction is data reduction with thin provisioning savings.

          For example, a total reduction ratio of 10&#58;1 means that

          for every 10 MB of provisioned space,

          1 MB is stored on the array''s flash modules.


          '
        readOnly: true
      total_used:
        type: integer
        format: int64
        description: 'The total space contributed by customer data, measured in bytes.

          '
        readOnly: true
        minimum: 0
      thin_provisioning:
        type: number
        format: float
        description: 'The percentage of volume sectors that do not contain host-written
          data

          because the hosts have not written data to them or the sectors have been
          explicitly trimmed.

          '
        readOnly: true
        minimum: 0
        maximum: 1
      snapshots:
        type: integer
        format: int64
        description: 'The physical space occupied by data unique to one or more snapshots.

          Measured in bytes.

          On Evergreen//One arrays, this is the effective space contributed by data
          unique to one or

          more snapshots, measured in bytes.

          '
        readOnly: true
        minimum: 0
      total_physical:
        type: integer
        format: int64
        description: 'This field has been deprecated.

          Use the `total_used` field, as it contains the same information.

          '
        readOnly: true
        minimum: 0
      footprint:
        type: integer
        format: int64
        description: 'The maximum amount of physical space the container consumes
          on an array,

          ignoring any data shared outside the container, measured in bytes.

          On Evergreen//One arrays, this is the maximum amount of effective used space.

          The footprint metric is mostly used for capacity planning.

          This field will be null in non-container contexts.

          '
        readOnly: true
        minimum: 0
      system:
        type: integer
        format: int64
        description: 'The physical space occupied by internal array metadata. Measured
          in bytes.

          '
        readOnly: true
        minimum: 0
      unique:
        type: integer
        format: int64
        description: 'The unique physical space occupied by customer data.

          Unique physical space does not include shared space, snapshots, and internal
          array metadata.

          Measured in bytes.

          On Evergreen//One arrays, this is the effective space contributed by unique
          customer data,

          measured in bytes.

          Unique data does not include shared space, snapshots, and internal array
          metadata.

          '
        readOnly: true
        minimum: 0
      total_provisioned:
        type: integer
        format: int64
        example: 19937690345472
        description: 'The provisioned size of a volume for a single volume, host or
          host group,

          protocol endpoint, managed directory, and containers can be infinite or
          measured in bytes.

          Infinite is represented by `null`.

          The provisioned size for a host or host group, includes all volumes

          that are connected to the resource.

          The provisioned size for a protocol endpoint is `null''.

          The provisioned size for a managed directory is the quota limit if it

          or its parent has a managed directory configured, otherwise it defaults
          to `null`.

          The provisioned size for a container is the sum of the total_provisioned

          of the object it contains, capped by the container''s quota limit

          (or the container''s used_provisioned if current usage is above the quota
          limit), if any.

          Provisioned size represents the storage capacity reported to hosts.

          '
        readOnly: true
        minimum: 0
  SubnetPost:
    type: object
    properties:
      vlan:
        type: integer
        format: int32
        description: VLAN ID number.
      prefix:
        type: string
        description: 'The IPv4 or IPv6 address to be associated with the specified
          subnet.

          '
      enabled:
        type: boolean
        description: 'Returns a value of `true` if subnet is enabled.

          Returns a value of `false` if subnet is disabled.

          If not specified, defaults to `true`.

          '
      gateway:
        type: string
        description: 'The IPv4 or IPv6 address of the gateway through which the specified

          subnet is to communicate with the network.

          '
      mtu:
        type: integer
        format: int32
        description: 'Maximum message transfer unit (packet) size for the subnet in
          bytes.

          MTU setting cannot exceed the MTU of the corresponding physical interface.

          If not specified, defaults to `1500`.

          '
        minimum: 1280
        maximum: 9216
  CertificateSigningRequest:
    type: object
    properties:
      certificate_signing_request:
        type: string
        description: 'The text of a new certificate signing request.

          '
    description: 'SSL certificate managed by Purity//FA.

      '
  _softwareChecksChecks:
    type: object
    properties:
      name:
        type: string
        example: HostIOCheck
        description: Name of the upgrade check.
      details:
        type: string
        description: 'Detailed result of the check used to diagnose check failures.

          '
      status:
        type: string
        description: 'Status of the check. Valid values are `running`, `failed`, and
          `passed`.

          A status of `running` indicates that the check has not finished.

          A status of `failed` indicates that the check has failed.

          A status of `passed` indicates that the check has passed.

          '
  VolumeGroupResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/VolumeGroup'
  ApiClient:
    type: object
    properties:
      public_key:
        type: string
        example: '-----BEGIN PUBLIC KEY-----

          

          -----END PUBLIC KEY-----

          '
        description: 'The API client''s PEM formatted (Base64 encoded) RSA public
          key.

          '
        readOnly: true
      max_role:
        type: string
        example: storage_admin
        description: 'This field has been deprecated.

          The maximum Admin Access Policy (previously ''role'') allowed for ID

          Tokens issued by this API client. The bearer of an access token will be

          authorized to perform actions within the intersection of this policy and

          that of the array user specified as the JWT `sub` (subject) claim.

          `max_role` is deprecated in favor of `access_policies`, but remains for
          backwards

          compatibility. If a client has exactly one access policy which corresponds
          to a

          valid legacy role of the same name, `max_role` will be the name of that
          policy.

          Otherwise, it will be null.

          '
        readOnly: true
      key_id:
        type: string
        example: 6207d123-d123-0b5c-5fa1-95fabc5c7123
        description: 'The unique identifier for the associated public key of this
          API client.

          This string must match the JWT `kid` (key ID) claim in ID Tokens issued
          for this API client.

          '
        readOnly: true
      name:
        type: string
        example: NightlyStatsAggregationScript
        description: 'The API client name.

          '
        readOnly: true
      access_policies:
        type: array
        description: 'The access policies allowed for ID Tokens issued by this API
          client.

          The bearer of an access token will be authorized to perform actions

          within the intersection of these policies and those of the array

          user specified as the JWT `sub` (subject) claim.

          '
        readOnly: true
        items:
          $ref: '#/definitions/_fixedReferenceWithType'
      id:
        type: string
        example: 6207d123-d123-0b5c-5fa1-95fabc5c7123
        description: 'The unique identifier for the associated API client.

          The ID represents the JWT `aud` (audience) claim in ID Tokens issued for
          this API client.

          '
        readOnly: true
      enabled:
        type: boolean
        example: true
        description: 'If `true`, the API client is permitted to exchange ID Tokens
          for access tokens.

          API clients are disabled by default.

          '
        readOnly: true
      issuer:
        type: string
        example: https://purestorage.idp.okta.com
        description: 'The name of the identity provider that will be issuing ID Tokens
          for this API client.

          This string represents the JWT `iss` (issuer) claim in ID Tokens issued
          for this API client.

          '
        readOnly: true
      access_token_ttl_in_ms:
        type: integer
        format: int64
        example: 15000
        description: 'The requested TTL (Time To Live) length of time for the exchanged
          access token,

          measured in milliseconds.

          '
        readOnly: true
        x-minimum: 1000
        x-maximum: 86400000
  _upgrade_parameters:
    type: object
    properties:
      name:
        type: string
        description: 'The name of the upgrade parameter to be sent to the upgrade
          process.

          '
      value:
        type: string
        description: 'The value of the upgrade parameter to be sent to the upgrade
          process.

          '
        maxLength: 1024
  ResourcePerformanceNoIdByArray:
    allOf:
    - $ref: '#/definitions/ResourcePerformanceNoId'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        array:
          type: object
          description: 'The array on which the performance metrics were recorded.

            '
          title: Resource
          allOf:
          - $ref: '#/definitions/_resource'
  _presetWorkloadSnapshotRule:
    type: object
    required:
    - every
    - keep_for
    properties:
      at:
        type: string
        description: 'Specifies the number of milliseconds since midnight at which
          to take a snapshot.

          The `at` value cannot be set if the `every` value is not measured in days.

          The `at` value can only be set on the first rule.

          '
      keep_for:
        type: string
        description: 'Specifies the period that snapshots are retained before they
          are eradicated, in milliseconds.

          The `keep_for` value must be between 10 minutes and 24855 days for the first
          rule, and a

          multiple of a second. The `keep_for` value must be between 10 minutes and
          2147483647 days

          for the second rule, and must be greater than or equal to the `keep_for`
          value of the first

          rule.

          '
      every:
        type: string
        description: 'Specifies the interval between snapshots, in milliseconds.

          The `every` value for all rules must be multiples of one another.

          The `every` value must be between five minutes and 400 days for the first
          rule.

          The `every` value must be between five minutes and one day for the second
          rule.

          '
  FleetKeyResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/FleetKey'
  DirectoryLockNlmReclamation:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        start:
          type: integer
          format: int64
          description: 'Start time of the NLM reclamation in milliseconds since the
            UNIX epoch.

            '
        end:
          type: integer
          format: int64
          description: 'End time of the NLM reclamation in milliseconds since the
            UNIX epoch.

            '
          readOnly: true
  ProtectionGroupTargetResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/TargetProtectionGroup'
  _presetWorkloadPeriodicReplicationConfiguration:
    type: object
    required:
    - name
    - remote_targets
    - rules
    properties:
      remote_targets:
        type: array
        description: The remote targets to which snapshots may be replicated.
        items:
          type: object
          description: 'A remote target to which snapshots may be replicated.

            Supports parameterization.

            '
          title: ReferenceWithType
          allOf:
          - $ref: '#/definitions/_referenceWithType'
        maxItems: 1
        minItems: 1
      name:
        type: string
        description: 'The name of the periodic replication configuration, by which
          other configuration objects in

          the preset can reference it. Name must be unique across all configuration
          objects in

          the preset.

          '
      rules:
        type: array
        description: Rules describe the frequency and retention of snapshots taken
          by the configuration.
        items:
          $ref: '#/definitions/_presetWorkloadSnapshotRule'
        maxItems: 2
        minItems: 1
  PolicyMemberCleanResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/PolicyMemberClean'
  _workloadPreset:
    allOf:
    - $ref: '#/definitions/_fixedReferenceWithType'
    - type: object
      properties:
        revision:
          type: integer
          format: int64
          description: 'The revision of the preset from which the workload was deployed.

            '
          readOnly: true
  MappingPolicyPatch:
    type: object
    properties:
      mapping:
        type: string
        description: 'The mapping to set on this policy mapping.

          Valid values are `connected` and `disconnected`.

          '
  SoftwareInstallationStepsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SoftwareInstallationStep'
  ResourcePodSpace:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time:
          type: integer
          format: int64
          description: 'The timestamp of when the data was taken, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
        space:
          type: object
          description: 'Displays size and space consumption information.

            '
          title: PodSpace
          allOf:
          - $ref: '#/definitions/_podSpace'
  ArrayConnectionKeyGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/ArrayConnectionKey'
  PolicyRuleQuotaGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PolicyRuleQuotaResponse'
    - $ref: '#/definitions/_errorContextResponse'
  SubnetPatch:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - $ref: '#/definitions/SubnetPost'
  _volumePostBase:
    allOf:
    - $ref: '#/definitions/_destroyedPatchPost'
    - type: object
      properties:
        protocol_endpoint:
          type: object
          description: 'Sets the properties that are specific to protocol endpoints.

            This can only be used in conjunction to `subtype=protocol_endpoint`.

            '
          title: ProtocolEndpoint
          allOf:
          - $ref: '#/definitions/_protocol_endpoint'
        provisioned:
          type: integer
          format: int64
          description: 'Sets the virtual size of the volume, measured in bytes.

            '
          maximum: 4503599627370496
        qos:
          type: object
          description: Sets QoS limits.
          title: Qos
          allOf:
          - $ref: '#/definitions/_qos'
        subtype:
          type: string
          description: 'The type of volume. Valid values are `protocol_endpoint` and
            `regular`.

            '
        source:
          type: object
          description: The source volume of a volume copy.
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        priority_adjustment:
          type: object
          description: Adjusts volume priority.
          title: PriorityAdjustment
          allOf:
          - $ref: '#/definitions/_priorityAdjustment'
        tags:
          type: array
          description: 'The list of tags to be upserted with the object.

            '
          items:
            $ref: '#/definitions/Tag'
  PolicyRuleSmbClientResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/PolicyRuleSmbClient'
  ConnectionGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ConnectionResponse'
    - $ref: '#/definitions/_errorContextResponse'
  _performance:
    type: object
    properties:
      san_usec_per_read_op:
        type: integer
        format: int64
        description: 'The average time required to transfer data from the array to
          the initiator for a read request,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      bytes_per_mirrored_write:
        type: integer
        format: int64
        description: 'The average I/O size per mirrored write, measured in bytes.

          '
        readOnly: true
        minimum: 0
      mirrored_writes_per_sec:
        type: integer
        format: int64
        description: 'The number of mirrored writes per second.

          '
        readOnly: true
        minimum: 0
      qos_rate_limit_usec_per_mirrored_write_op:
        type: integer
        format: int64
        description: 'The average time it takes the array to process a mirrored I/O
          write request,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      bytes_per_op:
        type: integer
        format: int64
        description: 'The average I/O size for both read and write (all) operations.

          '
        readOnly: true
        minimum: 0
      service_usec_per_read_op_cache_reduction:
        type: number
        format: float
        description: 'The percentage reduction in `service_usec_per_read_op` due to
          data cache

          hits. For example, a value of 0.25 indicates that the value of

          `service_usec_per_read_op` is 25&#37; lower than it would have been without

          any data cache hits.

          '
        minimum: 0
        maximum: 1
      bytes_per_read:
        type: integer
        format: int64
        description: The average I/O size per read, measured in bytes.
        readOnly: true
        minimum: 0
      usec_per_read_op:
        type: integer
        format: int64
        description: 'The average time it takes the array to process an I/O read request,

          measured in microseconds.

          Beginning in Purity 6.3.14 and 6.4.10 and later,

          including later major versions (6.5.x, 6.6.x and beyond),

          queue time is included.

          The average time does not include SAN time or QoS rate limit time.

          '
        readOnly: true
        minimum: 0
      qos_rate_limit_usec_per_read_op:
        type: integer
        format: int64
        description: 'The average time spent waiting due to QoS rate limiting for
          a read request,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      queue_usec_per_write_op:
        type: integer
        format: int64
        description: 'The average time that a write I/O request spends in the array
          waiting to be served,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      service_usec_per_mirrored_write_op:
        type: integer
        format: int64
        description: 'The average time required for the array to service a mirrored
          write request,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      reads_per_sec:
        type: integer
        format: int64
        description: The number of read requests processed per second.
        readOnly: true
        minimum: 0
      bytes_per_write:
        type: integer
        format: int64
        description: The average I/O size per write, measured in bytes.
        readOnly: true
        minimum: 0
      qos_rate_limit_usec_per_write_op:
        type: integer
        format: int64
        description: 'The average time that a write I/O request spends waiting

          as a result of the volume reaching its QoS bandwidth limit,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      usec_per_mirrored_write_op:
        type: integer
        format: int64
        description: 'The average time it takes the array to process a mirrored I/O
          write request,

          measured in microseconds.

          Beginning in Purity 6.3.14 and 6.4.10 and later,

          including later major versions (6.5.x, 6.6.x and beyond),

          queue time is included.

          The average time does not include SAN time or QoS rate limit time.

          '
        readOnly: true
        minimum: 0
      writes_per_sec:
        type: integer
        format: int64
        description: The number of write requests processed per second.
        readOnly: true
        minimum: 0
      san_usec_per_write_op:
        type: integer
        format: int64
        description: 'The average time required to transfer data

          from the initiator to the array for a write request,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      usec_per_write_op:
        type: integer
        format: int64
        description: 'The average time it takes the array to process an I/O write
          request,

          measured in microseconds.

          Beginning in Purity 6.3.14 and 6.4.10 and later,

          including later major versions (6.5.x, 6.6.x and beyond),

          queue time is included.

          The average time does not include SAN time or QoS rate limit time.

          '
        readOnly: true
        minimum: 0
      san_usec_per_mirrored_write_op:
        type: integer
        format: int64
        description: 'The average time required to transfer data

          from the initiator to the array for a mirrored write request,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      read_bytes_per_sec:
        type: integer
        format: int64
        description: The number of bytes read per second.
        readOnly: true
        minimum: 0
      mirrored_write_bytes_per_sec:
        type: integer
        format: int64
        description: 'The number of mirrored bytes written per second.

          '
        readOnly: true
        minimum: 0
      write_bytes_per_sec:
        type: integer
        format: int64
        description: The number of bytes written per second.
        readOnly: true
        minimum: 0
      service_usec_per_write_op:
        type: integer
        format: int64
        description: 'The average time required for the array to service a write request,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      queue_usec_per_mirrored_write_op:
        type: integer
        format: int64
        description: 'The average time that a mirrored write I/O request spends in
          the array waiting to be served,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      service_usec_per_read_op:
        type: integer
        format: int64
        description: 'The average time required for the array to service a read request,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      queue_usec_per_read_op:
        type: integer
        format: int64
        description: 'The average time that a read I/O request spends in the array
          waiting to be served,

          measured in microseconds.

          '
        readOnly: true
        minimum: 0
      time:
        type: integer
        format: int64
        description: 'The time when the sample performance data was taken,

          measured in milliseconds since the UNIX epoch.

          '
        readOnly: true
  RealmPost:
    type: object
    properties:
      quota_limit:
        type: integer
        format: int64
        description: 'The logical quota limit of the realm, measured in bytes. Must
          be a multiple of 512.

          '
        minimum: 1048576
        maximum: 4503599627370496
      qos:
        type: object
        description: Sets QoS limits.
        title: ContainerQos
        allOf:
        - $ref: '#/definitions/_containerQos'
      tags:
        type: array
        description: 'The list of tags to be upserted with the object.

          '
        items:
          $ref: '#/definitions/NonCopyableTag'
  _networkInterfacePortDetailsVoltage:
    type: object
    properties:
      channel:
        type: integer
        format: int32
        example: 0
        description: 'The channel number if the diagnostic has an associated channel
          number.

          If not specified, defaults to `null`.

          '
      measurement:
        type: number
        format: double
        description: 'Supply voltage in volts.

          '
      status:
        type: string
        example: ok
        description: 'Indicates whether the measurement is outside the high alarm,
          low alarm, high warning,

          or low warning thresholds for supply voltage.

          Valid values are `alarm low`, `alarm high`, `warn low`, `warn high`, and
          `ok`.

          '
  AlertEventGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/AlertEventResponse'
    - $ref: '#/definitions/_errorContextResponse'
  _policyrulealertwatcherpostRules:
    type: object
    properties:
      minimum_notification_severity:
        type: string
        description: 'The minimum severity that an alert must have in order for emails
          to

          be sent to the recipient.

          Possible values include `info`, `warning`, and `critical`.

          If not specified, defaults to `info`.

          '
      included_codes:
        type: array
        description: 'An alert must have one of these codes in order for emails to
          be sent to

          the recipient.

          Cannot be specified with `exclude_codes`. If specified while

          `exclude_codes` is already set, `exclude_codes` will be cleared.

          Use "" to clear.

          If both `exclude_codes` and `include_codes` are cleared, defaults to an

          empty list for `exclude_codes`.

          '
        items:
          type: integer
          format: int32
          example: 101
      email:
        type: string
        description: 'The email address that will receive the alert notification emails.

          '
      excluded_codes:
        type: array
        description: 'An alert with one of these codes will not have emails sent to
          the

          recipient.

          Cannot be specified with `include_codes`. If specified while

          `include_codes` is already set, `include_codes` will be cleared.

          Use "" to clear.

          If both `exclude_codes` and `include_codes` are cleared, defaults to an

          empty list for `exclude_codes`.

          '
        items:
          type: integer
          format: int32
          example: 101
  SoftwareInstallationPost:
    type: object
    properties:
      mode:
        type: string
        example: interactive
        description: 'Mode that the upgrade is in. Valid values are `check-only`,
          `interactive`, `semi-interactive`,

          and `one-click`.

          The `check_only` mode is deprecated. Use `/software-checks`. In this mode,
          the upgrade only

          runs pre-upgrade checks and returns.

          In `interactive` mode, the upgrade pauses at several points, at which users
          must

          enter certain commands to proceed.

          In `semi-interactive` mode, the upgrade pauses if there are any upgrade
          check failures and

          functions like `one-click` mode otherwise.

          In `one-click` mode, the upgrade proceeds automatically without pausing.

          '
      override_checks:
        type: array
        description: 'A list of upgrade checks whose failures are overridden during
          the upgrade.

          If any optional `args` are provided, they are validated later when the corresponding
          check

          script runs.

          '
        items:
          $ref: '#/definitions/_override_check'
      upgrade_parameters:
        type: array
        description: 'A list of parameters to be sent to the upgrade process. These
          parameters can be used to

          change the behavior of the upgrade process in specific ways such as enabling
          or disabling

          features in the target version or changing the attributes of the appliance
          at upgrade time.

          When there is a need or option to set an upgrade parameter, Pure Storage
          will provide

          documentation for the specific parameter. If you have been provided documentation

          related to a specific upgrade parameter,

          follow the instructions in the documentation.

          Otherwise, do not set any upgrade parameters.

          '
        items:
          $ref: '#/definitions/_upgrade_parameters'
    description: Software upgrade attempt.
  KmipGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/KmipResponse'
  MemberNoIdAllResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name where meaningful.

          '
        items:
          $ref: '#/definitions/MemberNoIdAll'
  _softwareUpgradePlan:
    type: object
    properties:
      step_name:
        type: string
        example: Pre-upgrade check
        description: Name of the upgrade step.
      hop_version:
        type: string
        example: 99.9.9
        description: The version to which the step is upgrading.
      description:
        type: string
        description: Description of the upgrade step.
  HostGroupPatch:
    allOf:
    - $ref: '#/definitions/_newName'
  ResourceSpaceNoId:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time:
          type: integer
          format: int64
          readOnly: true
        space:
          type: object
          description: 'Displays size and space consumption information.

            '
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
  VolumesDiffGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/VolumesDiffResponse'
    - $ref: '#/definitions/_errorContextResponse'
  ResourceAccess:
    type: object
    properties:
      resource:
        type: object
        description: Resource to which the scope has access.
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      scope:
        type: object
        description: Scope which has access to the resource.
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      id:
        type: string
        description: ID of a resource access object.
        readOnly: true
    description: 'A resource access captures configuration of sharing a resource to
      a scope.

      For example, resource access can specify that an array host is accessible from
      a realm.

      '
  _memberWithType:
    type: object
    properties:
      member:
        $ref: '#/definitions/_referenceWithType'
      group:
        $ref: '#/definitions/_referenceWithType'
  SoftwareInstallationStep:
    allOf:
    - $ref: '#/definitions/_resourceFixedNonUniqueName'
    - $ref: '#/definitions/_start_end_time'
    - type: object
      properties:
        checks:
          type: array
          description: 'A list of checks in this upgrade step.

            '
          items:
            $ref: '#/definitions/_softwareInstallationStepsChecks'
        hop_version:
          type: string
          example: 99.9.9
          description: The version to which the current hop is upgrading.
        installation:
          type: object
          description: 'Referenced `software-installation` to which the step belongs.

            '
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        description:
          type: string
          description: 'Detailed description of the step.

            '
        details:
          type: string
          description: 'Detailed result of the step used to diagnose step failures.

            '
        status:
          type: string
          description: 'Status of the step. Valid values are `running` and `finished`.

            A status of `running` indicates that the step has not finished.

            A status of `finished` indicates that the check has finished.

            '
      description: 'Step needed to complete an upgrade.

        '
  Dns:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - $ref: '#/definitions/DnsPost'
  _workloadPlacementRecommendationResult:
    type: object
    properties:
      score:
        type: string
        description: 'The score of the recommendation from Pure1.

          Possible values include `optimal`, `optimal-with-warnings`, and `acceptable`.

          '
        readOnly: true
      placements:
        type: array
        description: 'The placements of the preset.

          '
        readOnly: true
        items:
          $ref: '#/definitions/_workloadPlacementRecommendationPlacement'
    description: Single workload placement recommendation result.
  LocalMember:
    type: object
    properties:
      member_id:
        type: integer
        format: int32
        description: 'GID if the `member_type` is `Group` and UID if the `member_type`
          is `User`.

          '
      group_gid:
        type: integer
        format: int32
        description: 'GID of the group to which the member belongs.

          '
      member:
        type: object
        description: Reference to the member of the group.
        title: FixedReferenceWithType
        allOf:
        - $ref: '#/definitions/_fixedReferenceWithType'
      is_primary_group:
        type: boolean
        description: 'When a membership of `member_type` has the value `User`,

          this specifies whether this membership is a primary-group mapping or not.

          In any other case, this field will be `false`.

          '
        readOnly: true
      group:
        type: object
        description: Reference to the group to which the member belongs.
        title: FixedReferenceWithType
        allOf:
        - $ref: '#/definitions/_fixedReferenceWithType'
  _networkInterfacePortDetailsTx_bias:
    type: object
    properties:
      channel:
        type: integer
        format: int32
        example: 0
        description: 'The channel number if the transceiver is a multilane transceiver.

          If not specified, defaults to `null`.

          '
      measurement:
        type: number
        format: double
        description: 'Tx bias current in mA.

          '
      status:
        type: string
        example: ok
        description: 'Indicates whether the measurement is outside the high alarm,
          low alarm, high warning,

          or low warning thresholds for Tx bias current.

          Valid values are `alarm low`, `alarm high`, `warn low`, `warn high`, and
          `ok`.

          '
  DirectoryGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/DirectoryResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            When applicable, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/Directory'
  ProtectionGroupTarget:
    type: object
    properties:
      protection_group:
        $ref: '#/definitions/_referenceNoId'
      allowed:
        type: boolean
        description: 'If set to `true`, the target array has allowed the source array

          to replicate protection group data to the target array.

          If set to `false`, the target array has not allowed the source array

          to replicate protection group data to the target.

          '
      target:
        $ref: '#/definitions/_referenceNoId'
  _networkInterfacePortDetailsStaticRxPowerThresholds:
    type: object
    properties:
      warn_low:
        type: number
        format: double
        description: The warn low threshold for Rx input power in mW.
      warn_high:
        type: number
        format: double
        description: The warn high threshold for Rx input power in mW.
      alarm_high:
        type: number
        format: double
        description: The alarm high threshold for Rx input power in mW.
      alarm_low:
        type: number
        format: double
        description: The alarm low threshold for Rx input power in mW.
    description: 'The alarm low, warn low, warn high, alarm high thresholds for Rx
      input power in mW.

      '
  _start_end_time:
    type: object
    properties:
      start_time:
        type: integer
        format: int64
        example: 1567026779000
        description: Start time in milliseconds since the UNIX epoch.
        x-minimum: 0
      end_time:
        type: integer
        format: int64
        example: 1567026888000
        description: End time in milliseconds since the UNIX epoch.
        x-minimum: 0
    description: A resource with a start time and end time.
  PolicyRuleAlertWatcherResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/PolicyRuleAlertWatcher'
  EulaGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/EulaResponse'
  SnmpAgentPatch:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        engine_id:
          type: string
          example: 80009e2205d7c0bd31fef5b87f4f44da
          description: 'The administration domain unique name of an SNMP agent.

            '
          readOnly: true
          maxLength: 32
        v2c:
          $ref: '#/definitions/_snmp_v2c'
        v3:
          $ref: '#/definitions/_snmp_v3_patch'
        version:
          type: string
          example: v3
          description: 'Version of the SNMP protocol to be used by Purity//FA in communications

            with the specified manager.

            Valid values are `v2c` and `v3`.

            '
  _networkInterfaceNeighborNeighborChassisId:
    type: object
    properties:
      type:
        type: string
        example: mac
        description: 'The chassis ID subtype. Valid values are `ifname`, `ifalias`,

          `local`, `mac`, `ip`, and `unhandled`.

          '
      value:
        type: string
        example: 00:12:ab:cd:ab:cd
        description: The specific identifier for the particular chassis.
    description: 'An administratively assigned identifier of the particular

      neighboring chassis.

      '
  TestResultWithResourcePatchResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/TestResultWithResource'
  PodReplicaLinkPerformanceReplication:
    allOf:
    - $ref: '#/definitions/_builtInRelationship'
    - $ref: '#/definitions/_replicaLinkPerformanceReplication'
    - $ref: '#/definitions/_context'
  _networkinterfacepostEth:
    type: object
    properties:
      subnet:
        type: object
        description: 'Subnet that is associated with the specified network interface.

          '
        title: ReferenceNoId
        allOf:
        - $ref: '#/definitions/_referenceNoId'
      address:
        type: string
        description: 'The IPv4 or IPv6 address to be associated with the specified
          network interface.

          '
      subtype:
        type: string
        description: 'The subtype of the specified network interface. Only interfaces
          of subtype

          `vif` and `lacp_bond` can be created. Configurable on POST only.

          Valid values are `failover_bond`, `lacp_bond`, `physical`, and

          `vif`. If the subtype is `vif`, the services parameter must not be set.

          '
      subinterfaces:
        type: array
        description: 'List of network interfaces configured to be a subinterface of
          the specified network

          interface.

          '
        items:
          $ref: '#/definitions/_referenceNoId'
    description: Ethernet network interface properties.
  ArrayErasureResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/ArrayErasure'
  _networkInterfacePortDetailsStaticVoltageThresholds:
    type: object
    properties:
      warn_low:
        type: number
        format: double
        description: The warn low threshold for supply voltage in volts.
      warn_high:
        type: number
        format: double
        description: The warn high threshold for supply voltage in volts.
      alarm_high:
        type: number
        format: double
        description: The alarm high threshold for supply voltage in volts.
      alarm_low:
        type: number
        format: double
        description: The alarm low threshold for supply voltage in volts.
    description: 'The alarm low, warn low, warn high, alarm high thresholds for supply
      voltage (in volts).

      '
  VirtualMachine:
    type: object
    properties:
      time_remaining:
        type: integer
        format: int64
        description: 'The amount of time left until the destroyed volume is permanently
          eradicated,

          measured in milliseconds.

          Before the `time_remaining` period has elapsed, the destroyed volume can
          be recovered

          by setting `destroyed=false`.

          '
        readOnly: true
      destroyed:
        type: boolean
        description: 'Returns a value of `true` if the virtual machine has been destroyed
          and is pending

          eradication.

          '
        readOnly: true
      vm_id:
        type: string
        description: 'The ID of the virtual machine, as assigned by the external system.

          '
        readOnly: true
      vm_type:
        type: string
        example: vvol
        description: 'The type of virtual machine.

          The only valid value is `vvol`.

          '
        readOnly: true
      created:
        type: integer
        format: int64
        description: 'The virtual machine creation time, measured in milliseconds
          since the UNIX epoch.

          '
        readOnly: true
      name:
        type: string
        description: 'The name of the virtual machine, as assigned by the external
          system

          '
        readOnly: true
      recover_context:
        type: object
        description: 'A reference to any additional entities needed to recover this
          virtual machine.

          '
        title: FixedReference
        allOf:
        - $ref: '#/definitions/_fixedReference'
      id:
        type: string
        description: 'The ID of the virtual machine to create or modify, as assigned
          by the external system.

          `id` is deprecated. Use `vm_id` instead.

          '
  CloudCapacityGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/CloudCapacityPatchResponse'
  LogTargetFileGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/LogTargetFileResponse'
    - $ref: '#/definitions/_errorContextResponse'
  _presetWorkloadWorkloadTag:
    type: object
    required:
    - key
    - value
    properties:
      copyable:
        type: string
        example: 'true'
        description: 'Specifies whether to include the tag when copying the parent
          resource.

          If set to `true`, the tag is included in resource copying.

          If set to `false`, the tag is not included.

          If not specified, defaults to `true`.

          '
      namespace:
        type: string
        example: default
        description: 'Optional namespace of the tag.

          Namespace identifies the category of the tag. Omitting the namespace

          defaults to the namespace `default`. The `pure&#42;` namespaces are

          reserved for plugins and integration partners. It is recommended

          that customers avoid using reserved namespaces.

          '
      value:
        type: string
        example: staging
        description: 'Value of the tag. Supports up to 256 Unicode characters. Supports
          parameterization.

          '
      key:
        type: string
        example: environment
        description: 'Key of the tag. Supports up to 64 Unicode characters.

          '
  VchostResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Returns a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/Vchost'
  ProtectionGroupPerformance:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_aggregate_replication_performance'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time:
          type: integer
          format: int64
          description: 'The time when the sample performance data was taken.

            Measured in milliseconds since the UNIX epoch.

            '
          readOnly: true
  CloudConfig:
    type: object
    properties:
      name:
        type: string
        example: string
        description: 'A locally unique, system-generated name. The name cannot be
          modified.

          '
      model:
        type: object
        description: The model configuration of the CBS array.
        title: CloudConfigModel
        allOf:
        - $ref: '#/definitions/CloudConfigModel'
      id:
        type: string
        example: 0f58952a-35e4-7457-b1dd-23454257a1f1
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified and cannot refer to another resource.

          '
  _subscriptionAssetSubscription:
    type: object
    properties:
      id:
        type: string
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified.

          '
        readOnly: true
    description: 'A reference to which subscription this appliance belongs.

      '
  ResourceRealmSpace:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - type: object
      properties:
        time:
          type: integer
          format: int64
          description: 'The timestamp of when the data was taken, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
        space:
          type: object
          description: 'Displays size and space consumption information.

            '
          title: SpaceNoDeprecatedPhysicalOrEffective
          allOf:
          - $ref: '#/definitions/_spaceNoDeprecatedPhysicalOrEffective'
  _networkInterfacePortDetailsTx_fault:
    type: object
    properties:
      flag:
        type: boolean
        description: A value of `true` indicates transmitter/laser fault.
      channel:
        type: integer
        format: int32
        example: 0
        description: 'The channel number if the transceiver is a multilane transceiver.

          If not specified, defaults to `null`.

          '
  _networkInterfaceNeighborCapability:
    type: object
    properties:
      enabled:
        type: boolean
        example: false
        description: 'If true, this capability is enabled by this neighbor; false
          otherwise.

          '
        readOnly: true
      supported:
        type: boolean
        example: false
        description: 'If true, this capability is supported by this neighbor; false
          otherwise.

          '
        readOnly: true
  SnmpManagerGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SnmpManagerResponse'
  _limitedBy:
    type: object
    properties:
      member:
        type: object
        description: Reference to the resource to which the effective quota policy
          is attached.
        title: FixedReferenceWithType
        allOf:
        - $ref: '#/definitions/_fixedReferenceWithType'
      policy:
        type: object
        description: Reference to the effective quota policy.
        title: FixedReferenceWithType
        allOf:
        - $ref: '#/definitions/_fixedReferenceWithType'
  _builtIn:
    type: object
    properties:
      name:
        type: string
        description: 'A locally unique, system-generated name. The name cannot be
          modified.

          '
        readOnly: true
      id:
        type: string
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified and cannot refer to another resource.

          '
        readOnly: true
    description: 'A built-in resource. Many are singletons predefined by Purity (e.g.,
      support

      settings). Some correspond to a piece of software, like an app, or hardware,

      like a controller. Others are created by the system in response to some event

      (e.g., alerts, audit records).


      Typically, a user can''t create, delete or rename a built-in resource. A few

      can be created or deleted, but not renamed because the names are meaningful

      to Purity (e.g., VIFs).

      '
  Eradication_config:
    type: object
    properties:
      eradication_delay:
        type: integer
        format: int64
        example: 86400000
        description: 'Eradication delay in milliseconds for all destroyed objects.
          Automatically eradicate

          destroyed objects after the configured delay time passes. Updating this
          field will

          update both `eradication_disabled_delay` and `eradication_enabled_delay`
          if both

          `eradication_disabled_delay` and `eradication_enabled_delay` have the same
          value.

          If `eradication_disabled_delay` and `eradication_enabled_delay` have different
          values,

          trying to configure `eradication_delay` will result in an error.

          This field has been deprecated and will be removed in a future REST version.

          Valid values are `86400000` and any multiple of `86400000` in the range
          of `86400000` and

          `2592000000`.

          Any other values are rounded down to the nearest multiple of `86400000`.

          '
      disabled_delay:
        type: integer
        format: int64
        example: 86400000
        description: 'The eradication delay in milliseconds for destroyed objects
          that cannot be manually

          eradicated. Destroyed objects that cannot be manually eradicated will be
          automatically

          eradicated after the configured delay time passes.

          Valid values are `86400000` and any multiple of `86400000` in the range
          of `86400000` and

          `2592000000`.

          Any other values are rounded down to the nearest multiple of `86400000`.

          '
      enabled_delay:
        type: integer
        format: int64
        example: 86400000
        description: 'The eradication delay in milliseconds for destroyed objects
          that can be manually eradicated.

          Destroyed objects that can be manually eradicated will be automatically
          eradicated after the

          configured delay time passes.

          Valid values are `86400000` and any multiple of `86400000` in the range
          of `86400000` and

          `2592000000`.

          Any other values are rounded down to the nearest multiple of `86400000`.

          '
      manual_eradication:
        type: string
        example: all-disabled
        description: 'Manual eradication status applied on the array level.

          If status is `all-disabled`, you cannot eradicate objects, files, and directories.

          If at least one local object has `manual_eradication` disabled,

          then the status will be `partially-disabled`.

          Values include `all-disabled`, `partially-disabled`, and `all-enabled`.

          If the user does not have sufficient access, this field will return `null`.

          '
        readOnly: true
    description: The configuration of eradication feature.
  DirectoryServiceRoleGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/DirectoryServiceRoleResponse'
    - $ref: '#/definitions/_errorContextResponse'
  NetworkInterfacesPortDetails:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        rx_power:
          type: array
          description: 'Displays real-time measurement of Rx input power and whether
            it is within range.

            For four-lane modules, the array contains a measurement and status for
            each channel.

            If the transceiver does not support digital diagnostic monitoring, the
            array is empty.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_networkInterfacePortDetailsRx_power'
        static:
          type: object
          title: NetworkInterfacePortDetailsStatic
          allOf:
          - $ref: '#/definitions/_networkInterfacePortDetailsStatic'
        tx_bias:
          type: array
          description: 'Displays real-time measurement of Tx bias current and whether
            it is within range.

            For four-lane modules, the array contains a measurement and status for
            each channel.

            If the transceiver does not support digital diagnostic monitoring, the
            array is empty.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_networkInterfacePortDetailsTx_bias'
        tx_power:
          type: array
          description: 'Displays real-time measurement of Tx output power and whether
            it is within range.

            For four-lane modules, the array contains a measurement and status for
            each channel.

            If the transceiver does not support digital diagnostic monitoring, the
            array is empty.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_networkInterfacePortDetailsTx_power'
        rx_los:
          type: array
          description: 'Displays status flags for Rx LOS. A value of `true` indicates
            Rx Loss-of-Signal.

            For four-lane modules, the array contains a flag for each channel.

            If the transceiver does not support digital diagnostic monitoring, the
            array is empty.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_networkInterfacePortDetailsRx_los'
        temperature:
          type: array
          description: 'Displays real-time measurement of transceiver temperature
            and range.

            If the transceiver does not support digital diagnostic monitoring, the
            array is empty.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_networkInterfacePortDetailsTemperature'
        interface_type:
          type: string
          description: 'The interface type. Valid values are `eth` and `fc`.

            '
          readOnly: true
        tx_fault:
          type: array
          description: 'Displays status flags for Tx Fault. A value of `true` indicates
            transmitter/laser fault.

            For four-lane modules, the array contains a flag for each channel.

            If the transceiver does not support digital diagnostic monitoring, the
            array is empty.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_networkInterfacePortDetailsTx_fault'
        voltage:
          type: array
          description: 'Displays real-time measurement of supply voltage and whether
            it is within range.

            If the transceiver does not support digital diagnostic monitoring, the
            array is empty.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_networkInterfacePortDetailsVoltage'
    description: 'Network interface SFP details.

      '
  ResourceSpaceNoIdGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/ResourceSpaceNoId'
  DirectoryService:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        ca_certificate:
          type: string
          description: 'The certificate of the Certificate Authority (CA) that signed
            the certificates of the

            directory servers, which is used to validate the authenticity of the configured
            servers.

            '
        check_peer:
          type: boolean
          example: false
          description: 'Whether or not server authenticity is enforced when a certificate
            is provided.

            '
        uris:
          type: array
          description: 'List of URIs for the configured directory servers.

            '
          items:
            type: string
            example: ldaps://ad1.mycompany.com
            pattern: ^(ldap|ldaps)
        base_dn:
          type: string
          example: DC=mycompany,DC=com
          description: 'Base of the Distinguished Name (DN) of the directory service
            groups.

            '
        bind_password:
          type: string
          example: '****'
          description: 'Masked password used to query the directory.

            '
        management:
          $ref: '#/definitions/_directoryServiceManagement'
        services:
          type: array
          description: 'The services the directory service configuration is used for.

            '
          readOnly: true
          items:
            type: string
            example: data
            description: 'Valid values are `data` and `management`.

              '
        bind_user:
          type: string
          example: CN=John,OU=Users,DC=example,DC=com
          description: 'Username used to query the directory.

            '
        enabled:
          type: boolean
          example: true
          description: 'Whether or not the directory service is enabled.

            '
      description: Configuration information for the directory service.
  ProtectionGroupSnapshot:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left until the destroyed snapshot is permanently
            eradicated.

            Measured in milliseconds.

            Before the `time_remaining` period has elapsed, the destroyed snapshot
            can be recovered

            by setting `destroyed=false`.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the protection group snapshot

            has been destroyed and is pending eradication.

            The `time_remaining` value displays the amount of time left

            until the destroyed snapshot is permanently eradicated.

            Before the `time_remaining` period has elapsed,

            the destroyed snapshot can be recovered

            by setting `destroyed=false`.

            Once the `time_remaining` period has elapsed,

            the snapshot is permanently eradicated and can no longer be recovered.

            '
        pod:
          type: object
          description: The pod in which the protection group of the protection group
            snapshot resides.
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        created:
          type: integer
          format: int64
          example: 1503520993
          description: 'The snapshot creation time of the original snapshot source.

            Measured in milliseconds since the UNIX epoch.

            '
          readOnly: true
        eradication_config:
          $ref: '#/definitions/ProtectionGroupEradicationConfig'
        source:
          type: object
          description: 'The original protection group from which this snapshot was
            taken.

            For a replicated protection group snapshot being viewed on the

            target side, the `source` is the replica protection group.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        suffix:
          type: string
          description: 'The name suffix appended to the protection group name

            to make up the full protection group snapshot name

            in the form `PGROUP.SUFFIX`.

            If `suffix` is not specified,

            the protection group name is in the form `PGROUP.NNN`,

            where `NNN` is a unique monotonically increasing number.

            If multiple protection group snapshots are created at a time,

            the suffix name is appended to those snapshots.

            '
        space:
          type: object
          description: 'Displays provisioned size and physical storage consumption
            data

            for each protection group.

            '
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
      description: 'A protection group snapshot is a collection of point-in-time consistent

        volume snapshots, for volumes associated with the protection group when the

        snapshot was taken.

        '
  VchostConnectionGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/VchostConnectionResponse'
  _qos:
    type: object
    properties:
      bandwidth_limit:
        type: integer
        format: int64
        description: 'The maximum QoS bandwidth limit for the volume.

          Whenever throughput exceeds the bandwidth limit, throttling occurs.

          Measured in bytes per second. Maximum limit is 512 GB/s.

          '
        minimum: 1048576
        maximum: 549755813888
      iops_limit:
        type: integer
        format: int64
        description: 'The QoS IOPs limit for the volume.

          '
        minimum: 100
        maximum: 100000000
  FileSystemPatch:
    allOf:
    - $ref: '#/definitions/_destroyedPatchPost'
    - $ref: '#/definitions/_newName'
    - type: object
      properties:
        pod:
          type: object
          description: The pod to move existing file systems into.
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
  _spaceNoDeprecatedPhysicalOrEffective:
    type: object
    properties:
      snapshots:
        type: integer
        format: int64
        description: 'The physical space occupied by data unique to one or more snapshots,
          measured in bytes.

          On Evergreen//One arrays, this is the effective space contributed by data
          unique to one or

          more snapshots, measured in bytes.

          '
        minimum: 0
      data_reduction:
        type: number
        format: float
        description: 'The ratio of mapped sectors within a volume versus

          the amount of physical space the data occupies after data compression and
          deduplication.

          The data reduction ratio does not include thin provisioning savings.

          For example, a data reduction ratio of 5&#58;1 means that

          for every 5 MB the host writes to the array, 1 MB is stored on the array''s
          flash modules.

          '
      shared:
        type: integer
        format: int64
        example: 111863360624
        description: 'The physical space occupied by deduplicated data,

          meaning that the space is shared with other volumes and snapshots

          as a result of data deduplication, measured in bytes.

          On Evergreen//One arrays, this is the effective space contributed by data
          that is not unique

          to a specific volume, managed directory, or snapshot, measured in bytes.

          '
        minimum: 0
      virtual:
        type: integer
        format: int64
        description: 'The amount of logically written data that a volume or a snapshot
          references,

          measured in bytes.

          '
        minimum: 0
      footprint:
        type: integer
        format: int64
        description: 'The maximum amount of physical space the container would take
          up on any array,

          ignoring any data shared outside the container.

          Measured in bytes.

          On Evergreen//One arrays, this is the maximum amount of effective used space,

          measured in bytes.

          The footprint metric is mostly used for capacity planning.

          This field will be null in non-container contexts.

          '
        minimum: 0
      system:
        type: integer
        format: int64
        description: 'The physical space occupied by internal array metadata, measured
          in bytes.

          '
        minimum: 0
      used_provisioned:
        type: integer
        format: int64
        example: 19937690345472
        description: 'The amount of logical space a container has consumed.

          The amount of logical space is compared against the quota limit

          if the container has one configured.

          Used provisioned does not include destroyed objects inside the container.

          For a destroyed container, used provisioned can include destroyed objects

          and represents how much logical space it would take to recover the container.

          '
        readOnly: true
        minimum: 0
      unique:
        type: integer
        format: int64
        description: 'The unique physical space occupied by customer data.

          Unique physical space does not include shared space, snapshots, and internal
          array metadata.

          Measured in bytes.

          On Evergreen//One arrays, this is the effective space contributed by unique
          customer data,

          measured in bytes.

          Unique data does not include shared space, snapshots, and internal array
          metadata.

          '
        minimum: 0
      total_provisioned:
        type: integer
        format: int64
        example: 19937690345472
        description: 'The provisioned size of a volume for a single volume, host or
          host group,

          protocol endpoint, managed directory, and containers can be infinite or
          measured in bytes.

          Infinite is represented by `null`.

          The provisioned size for a host or host group, includes all volumes

          that are connected to the resource.

          The provisioned size for a protocol endpoint is `null''.

          The provisioned size for a managed directory is the quota limit if it

          or its parent has a managed directory configured, otherwise it defaults
          to `null`.

          The provisioned size for a container is the sum of the total_provisioned

          of the object it contains, capped by the container''s quota limit

          (or the container''s used_provisioned if current usage is above the quota
          limit), if any.

          Provisioned size represents the storage capacity reported to hosts.

          '
        readOnly: true
        minimum: 0
      total_reduction:
        type: number
        format: float
        description: 'The ratio of provisioned sectors within a volume versus the
          amount of physical space

          the data occupies after reduction via data compression and deduplication

          and with thin provisioning savings.

          Total reduction is data reduction with thin provisioning savings.

          For example, a total reduction ratio of 10&#58;1 means that

          for every 10 MB of provisioned space, 1 MB is stored on the array''s flash
          modules.

          '
      total_used:
        type: integer
        format: int64
        description: 'The total space contributed by customer data, measured in bytes.

          '
        readOnly: true
        minimum: 0
      thin_provisioning:
        type: number
        format: float
        description: 'The percentage of volume sectors that do not contain host-written
          data

          because the hosts have not written data to them or the sectors have been
          explicitly trimmed.

          '
        minimum: 0
        maximum: 1
  _newName:
    type: object
    properties:
      name:
        type: string
        description: The new name for the resource.
  PolicyRuleSnapshotResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/PolicyRuleSnapshot'
  ActiveDirectoryPatch:
    type: object
    properties:
      tls:
        type: string
        example: required
        description: 'TLS mode for communication with domain controllers.

          Valid values are `required` and `optional`.

          `required` forces TLS communication with domain controller.

          `optional` allows the use of non-TLS communication,

          TLS will still be preferred, if available.

          If not specified, defaults to `required`.

          '
  PodEradicationConfig:
    type: object
    properties:
      manual_eradication:
        type: string
        example: all-enabled
        description: 'The manual eradication status of pod.

          If `all-enabled`, the pod and its contents can be eradicated.

          If `partially-disabled`, the pod cannot be eradicated because the pod

          contains some non-empty protection groups that are retention-locked.

          If `all-disabled`, the pod and its contents cannot be eradicated.

          Values include `all-disabled`, `all-enabled`, and `partially-disabled`.

          '
        readOnly: true
    description: The status of the eradication functionality
  AlertRules:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        code:
          type: integer
          format: int32
          example: 25
          description: 'The alert code that the rule applies to. Available alert codes

            for customization can be found in the alert rules catalog.

            '
          readOnly: true
        parameter:
          type: string
          example: info
          description: 'The parameter of the custom alert rule to modify. Values include
            `info` and `warning`.

            Available parameter values can be found in the alert rules catalog.

            '
          readOnly: true
        value:
          type: string
          example: '85'
          description: 'The value to use for the custom alert rule. Valid values for

            an alert code and parameter combination can be found in the alert rules
            catalog.

            '
      description: 'Custom rules for alerts.

        '
  MemberNoIdGroup:
    type: object
    properties:
      member:
        $ref: '#/definitions/_reference'
      group:
        $ref: '#/definitions/_referenceNoId'
  SoftwareInstallationsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SoftwareInstallation'
  RemoteProtectionGroupSnapshotTransferGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/RemoteProtectionGroupSnapshotTransferResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where it makes more sense, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/RemoteProtectionGroupSnapshotTransfer'
  SnmpAgent:
    allOf:
    - $ref: '#/definitions/_builtInResourceNoId'
    - type: object
      properties:
        engine_id:
          type: string
          example: 80009e2205d7c0bd31fef5b87f4f44da
          description: 'The administration domain unique name of an SNMP agent.

            '
          readOnly: true
          maxLength: 32
        v2c:
          $ref: '#/definitions/_snmp_v2c'
        v3:
          $ref: '#/definitions/_snmp_v3'
        version:
          type: string
          example: v3
          description: 'Version of the SNMP protocol to be used by Purity//FA in communications

            with the specified manager.

            Valid values are `v2c` and `v3`.

            '
  VchostGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/VchostResponse'
  _default_protection_reference:
    allOf:
    - $ref: '#/definitions/_reference'
    - type: object
      properties:
        type:
          type: string
          example: protection_group
          description: 'The type of default protection.

            Currently, the only valid value is `protection_group`.

            '
  NetworkInterfaceResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/NetworkInterface'
  ArrayGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ArrayResponse'
    - $ref: '#/definitions/_errorContextResponse'
  ProtectionGroupSnapshotResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/ProtectionGroupSnapshot'
  _workloadPlacementRecommendationTargetCapacityUsedProjections:
    type: object
    properties:
      days_until_full:
        type: integer
        format: int32
        example: 10
        description: 'The number of days until the target is at full capacity.

          '
      projection:
        type: object
        description: 'The projection of the used capacity, assuming the workload

          is provisioned on this target.

          '
        title: WorkloadPlacementRecommendationProjection
        allOf:
        - $ref: '#/definitions/_workloadPlacementRecommendationProjection'
    description: 'Projections for the capacity consumed by customer data.

      '
  LocalUserMembershipPost:
    type: object
    properties:
      is_primary:
        type: boolean
        description: Determines whether memberships are primary group memberships
          or not.
        default: false
      groups:
        type: array
        description: 'A list of groups of which the user is a member.

          '
        items:
          $ref: '#/definitions/_localusermembershippostGroups'
  VchostCertificateGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/VchostCertificateResponse'
  _snapshot_schedule:
    type: object
    properties:
      at:
        type: integer
        format: int64
        example: 7200
        description: 'The time of day the snapshot is scheduled to be taken

          and retained on the local array or immediately replicated to the target(s).

          Measured in seconds since midnight.

          The `at` value is only used if the `frequency` parameter is in days

          (e.g., `259200000`, which is equal to 3 days).

          '
        x-minimum: 0
        x-maximum: 86399
      enabled:
        type: boolean
        example: false
        description: If set to `true`, the policy is enabled.
      frequency:
        type: integer
        format: int64
        example: 259200000
        description: The frequency of the scheduled action. Measured in milliseconds.
        x-minimum: 300000
        x-maximum: 34560000000
    description: The replication or snapshot schedule.
  RemoteProtectionGroupResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name where meaningful.

          '
        items:
          $ref: '#/definitions/RemoteProtectionGroup'
  ResourceSpaceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where it makes more sense, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/ResourceSpace'
        items:
          type: array
          description: 'Displays a list of all items after filtering.

            If applicable, the values are displayed for each name.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/ResourceSpace'
  RemoteVolumeSnapshotGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/RemoteVolumeSnapshotResponse'
    - $ref: '#/definitions/_errorContextResponse'
  SmisResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Smis'
  DriveResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Drive'
  OffloadPost:
    type: object
    properties:
      s3:
        type: object
        description: 'S3 settings.

          '
        title: OffloadS3
        allOf:
        - $ref: '#/definitions/_offload_s3'
      google-cloud:
        type: object
        description: 'Google Cloud Storage settings.

          '
        title: OffloadGoogleCloud
        allOf:
        - $ref: '#/definitions/_offloadGoogleCloud'
      nfs:
        type: object
        description: 'NFS settings.

          Deprecated from version 6.6.0 onwards - Contact support for additional information.'
        title: OffloadNfs
        allOf:
        - $ref: '#/definitions/_offload_nfs'
      azure:
        type: object
        description: 'Microsoft Azure Blob storage settings.

          '
        title: OffloadAzure
        allOf:
        - $ref: '#/definitions/_offload_azure'
  VchostCertificateResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Returns a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/VchostCertificate'
  ResourcePerformanceByArrayGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where it makes more sense, the average value is displayed instead.

            The values are displayed for each field where meaningful.

            '
          items:
            $ref: '#/definitions/ResourcePerformanceByArray'
        items:
          type: array
          description: 'Performance data, broken down by array.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/ResourcePerformanceByArray'
  _destroyedPatchPost:
    type: object
    properties:
      destroyed:
        type: boolean
        description: 'If set to `true`, destroys a resource. Once set to `true`,

          the `time_remaining` value will display the amount of time left

          until the destroyed resource is permanently eradicated.

          Before the `time_remaining` period has elapsed, the destroyed resource

          can be recovered by setting `destroyed=false`.

          Once the `time_remaining` period has elapsed,

          the resource is permanently eradicated and can no longer be recovered.

          '
  _containerQos:
    type: object
    properties:
      bandwidth_limit:
        type: integer
        format: int64
        description: 'The maximum QoS bandwidth limit for the container.

          Whenever throughput exceeds the bandwidth limit, throttling occurs.

          Measured in bytes per second. Maximum limit is 512 GB/s.

          '
        minimum: 1048576
        maximum: 549755813888
      iops_limit:
        type: integer
        format: int64
        description: 'The QoS IOPs limit for the container.

          '
        minimum: 100
        maximum: 100000000
  VirtualMachinePost:
    type: object
    properties:
      vm_id:
        type: string
        description: 'The ID of the virtual machine to create or modify, as assigned
          by the external system.

          '
      vm_type:
        type: string
        example: vvol
        description: 'The type of virtual machine.

          The only valid value is `vvol`.

          '
      id:
        type: string
        description: 'The ID of the virtual machine to create or modify, as assigned
          by the external system.

          `id` is deprecated. Use `vm_id` instead.

          '
      source:
        type: object
        description: 'The recovery context for the virtual machine or virtual machine
          snapshot

          being modified.

          '
        title: Reference
        allOf:
        - $ref: '#/definitions/_reference'
  PolicyRuleQuotaResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/PolicyRuleQuota'
  CloudCapacityStatus:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - type: object
      properties:
        current_capacity:
          type: integer
          format: int64
          example: 10995116277760
          description: 'The current raw capacity of the CBS array in bytes.

            '
          readOnly: true
        details:
          type: string
          description: 'Details about the capacity update. This field can contain
            error details

            when status is `failed` or progress information when status is

            `expanding`. For status `idle` the value is null.

            '
          readOnly: true
        requested_capacity:
          type: integer
          format: int64
          example: 10995116277760
          description: 'The requested raw capacity of the CBS array in bytes. When
            the capacity

            update process finishes, it should be the same as `current_capacity`.

            '
        status:
          type: string
          example: idle
          description: 'The status of the capacity update process.

            Can be one of `idle`, `failed`, `expanding`.

            '
          readOnly: true
  RemoteVolumeSnapshotPost:
    type: object
    properties:
      suffix:
        type: string
        description: 'The suffix that is appended to the `source_name` value to generate
          the

          full volume snapshot name in the form `VOL.SUFFIX`.

          If the suffix is not specified, the system constructs the snapshot name

          in the form `VOL.NNN`, where `VOL` is the volume name, and `NNN` is a

          monotonically increasing number.

          '
  _presetWorkloadMetadata:
    type: object
    properties:
      subtype:
        type: string
        description: 'The subtype of the parameter, which the GUI will use to contextualize
          the prompt

          for the parameter value. For example, when set to size, the GUI will display
          an input

          field with a dropdown menu that contains common size units such as MB, GB,
          TB, etc.

          Valid values include `size`, `iops`, `bandwidth`, `time` and `duration`.

          Subtype can only be used with integer parameters.

          '
      description:
        type: string
        description: 'A brief description of the parameter and how it is used within
          the preset.

          Supports up to 1KB of unicode characters.

          '
      display_name:
        type: string
        example: Billing ID
        description: 'The human-friendly name of the parameter, which will be shown
          in the GUI

          instead of the standard name if configured. Supports up to 64 unicode characters.

          '
  ControllerGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/Controller'
  Kmip:
    type: object
    properties:
      ca_certificate:
        type: string
        example: '-----BEGIN CERTIFICATE-----...-----END CERTIFICATE-----'
        description: 'CA certificate text for the KMIP server.

          '
        maxLength: 3000
      uris:
        type: array
        description: 'List of URIs for the configured KMIP servers.

          '
        items:
          type: string
      certificate:
        type: object
        description: 'The certificate used to verify FlashArray authenticity to the
          KMIP servers.

          '
        title: KmipCertificate
        allOf:
        - $ref: '#/definitions/Kmip_certificate'
      name:
        type: string
        description: 'A locally unique, system-generated name. The name cannot be
          modified.

          '
        readOnly: true
      kmip_objects:
        type: array
        description: 'List of the name and UID of the KMIP objects.

          '
        items:
          $ref: '#/definitions/Kmip_object'
    description: 'A built-in resource. Many are singletons predefined by Purity (e.g.,
      support

      settings). Some correspond to a piece of software, like an app, or hardware,

      like a controller. Others are created by the system in response to some event

      (e.g., alerts, audit records).


      Typically, a user can''t create, delete or rename a built-in resource. A few

      can be created or deleted, but not renamed because the names are meaningful

      to Purity (e.g., VIFs, file systems, file system snapshots, volume snapshot).

      '
  PodReplicaLinkLagResponse:
    type: object
    properties:
      items:
        type: array
        description: A list of pod replica link lag objects.
        items:
          $ref: '#/definitions/PodReplicaLinkLag'
  DnsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/DnsResponse'
  PolicyAuditFilePatch:
    allOf:
    - $ref: '#/definitions/PolicyPatch'
    - type: object
      properties:
        log_targets:
          type: array
          description: 'A list of targets to which audit logs will be sent.

            This list, if sent, overrides existing target list.

            '
          items:
            $ref: '#/definitions/_referenceNoIdWithType'
  PolicyMemberExportPost:
    type: object
    properties:
      members:
        type: array
        description: 'A list of directories to which the export policy may be applied.

          '
        items:
          $ref: '#/definitions/_policymemberexportpostMembers'
  VolumeGroupPost:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_resource'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left until the destroyed volume group is
            permanently eradicated,

            measured in milliseconds.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'If set to `true`, destroys a volume group.

            If set to `false`, recovers a destroyed volume group. If not specified,
            defaults to `false`.

            '
          readOnly: true
        qos:
          $ref: '#/definitions/_qos'
        volume_count:
          type: integer
          format: int64
          example: 3
          description: 'The number of volumes in the volume group.

            '
          readOnly: true
        priority_adjustment:
          $ref: '#/definitions/_priorityAdjustment'
        space:
          type: object
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
        tags:
          type: array
          description: 'The list of tags to be upserted with the object.

            '
          items:
            $ref: '#/definitions/Tag'
      description: 'Volume groups organize FlashArray volumes into logical groupings.

        '
  MaintenanceWindowsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/MaintenanceWindow'
  MemberGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/MemberResponse'
    - $ref: '#/definitions/_errorContextResponse'
  AlertResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Alert'
  PolicySmbPatch:
    allOf:
    - $ref: '#/definitions/PolicyPatch'
    - type: object
      properties:
        access_based_enumeration_enabled:
          type: boolean
          description: 'If set to `true`, enables access based enumeration on the
            policy.

            When access based enumeration is enabled on a policy,

            files and folders within exports that are attached to the policy

            will be hidden from users who do not have permission to view them.

            '
  SyslogServerResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SyslogServer'
  LocalUser:
    type: object
    properties:
      uid:
        type: integer
        format: int32
        description: 'The POSIX user ID of this object (user ID).

          '
      primary_group:
        type: object
        description: 'Primary group of the local user.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      built_in:
        type: boolean
        description: 'Returns a value of `true` if the local user is built in.

          Otherwise returns a value of `false` if the user has been created manually.

          '
        readOnly: true
      name:
        type: string
        description: 'The name of the local user.

          '
      email:
        type: string
        description: 'The email address of the local user.

          '
      enabled:
        type: boolean
        description: 'Returns a value of `true` if the local user is enabled.

          '
      sid:
        type: string
        description: 'The SID (security ID) of the local user.

          '
        readOnly: true
  _softwareInstallationStepsChecks:
    type: object
    properties:
      name:
        type: string
        example: HostIOCheck
        description: Name of the upgrade check.
      overridable:
        type: boolean
        description: 'Whether the check failure can be overridden.

          '
      details:
        type: string
        description: 'Detailed result of the check used to diagnose check failures.

          '
      status:
        type: string
        description: 'Status of the check. Valid values are `running`, `failed`, `passed`,
          and

          `overridden`.

          A status of `running` indicates that the check has not finished.

          A status of `failed` indicates that the check has failed.

          A status of `passed` indicates that the check has passed.

          A status of `overridden` indicates that the check has failed, but the failure
          has been

          overridden.

          '
  _localusermembershippostGroups:
    type: object
    properties:
      group_gid:
        type: integer
        format: int32
        description: The group GID that should be mapped.
      group:
        type: object
        description: 'Reference to a group of which the user is a member.

          The `id` or `name` parameter is required, but cannot be set together.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
  RealmSpaceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            When applicable, the average value is displayed instead.

            The values are displayed for each field, if meaningful.

            '
          items:
            $ref: '#/definitions/ResourceRealmSpace'
        items:
          type: array
          description: 'Displays a list of all items after filtering.

            If applicable, the values are displayed for each name.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/ResourceRealmSpace'
  _policymemberpostMembers:
    type: object
    properties:
      member:
        type: object
        description: 'Reference to the resource to which the policy may be applied.

          The `id` or `name` parameter is required, but they cannot be set together.

          If the `name` parameter is set, `resource_type` must also be set.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
  _snmp_v3:
    type: object
    properties:
      auth_protocol:
        type: string
        example: MD5
        description: 'Hash algorithm used to validate the authentication passphrase.

          Valid values are `MD5` and `SHA`.

          '
      privacy_protocol:
        type: string
        example: DES
        description: 'Encryption protocol for SNMP messages.

          Valid values are `AES` and `DES`.

          '
      auth_passphrase:
        type: string
        example: '****'
        description: 'Passphrase used by Purity//FA to authenticate the array with
          the specified managers.

          '
      privacy_passphrase:
        type: string
        example: '****'
        description: Passphrase used to encrypt SNMP messages.
      user:
        type: string
        example: User1
        description: 'User ID recognized by the specified SNMP managers which Purity//FA

          is to use in communications with them.

          '
    description: 'The v3 configurations of SNMP.

      '
  AlertRulesCatalog:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        allowed_values:
          type: string
          example: Range 0-100 (must be equal to or lower than Warning Threshold)
          description: 'The range of values allowed to set the custom alert rule to.

            '
        code:
          type: integer
          format: int32
          example: 25
          description: 'The alert code that the rule applies to.

            '
        subject:
          type: string
          example: Storage consumption has reached {percent_usage}% of usable capacity
          description: 'The alert code description.

            '
        parameter:
          type: string
          example: info
          description: 'The parameter of the custom alert rule.

            '
        default_value:
          type: string
          example: 80%
          description: 'The system default values for the alert code and parameter.

            '
      description: 'Available alerts for customization

        '
  VchostCertificatePatch:
    type: object
    properties:
      endpoints:
        type: array
        description: 'The IPv4 or IPv6 addresses of the endpoints to configure for
          the vchost, over which the

          configured certificate will be presented.

          '
        items:
          type: string
          example: 1.2.3.4
  TagGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/TagResponse'
    - $ref: '#/definitions/_errorContextResponse'
  VchostConnectionResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/VchostConnection'
  EulaResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Eula'
  PodReplicaLinkPerformanceReplicationGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PodReplicaLinkPerformanceReplicationResponse'
    - $ref: '#/definitions/_errorContextResponse'
  SoftwareCheck:
    allOf:
    - $ref: '#/definitions/_resourceFixedNonUniqueName'
    - $ref: '#/definitions/_start_end_time'
    - type: object
      properties:
        checks:
          type: array
          description: 'A list of checks in this check task.

            '
          items:
            $ref: '#/definitions/_softwareChecksChecks'
        details:
          type: string
          description: 'The detailed reason for the `status`.

            '
        software_name:
          type: string
          description: 'The name of the software the upgrade checks will test.

            '
        software_version:
          type: string
          example: 6.2.0
          description: 'The version of the software that the upgrade checks will test.

            '
        software_upgrade_hops:
          type: array
          description: 'The list of software versions the upgrade goes through. The
            upgrade checks

            also test this particular path.

            '
          items:
            type: string
            description: 'A software version in the upgrade path.

              '
        status:
          type: string
          example: running
          description: 'The status of the check task.

            Valid values are `queued`, `running`, `passed`, and `failed`.

            A status of `queued` indicates that the task is queued to run and is waiting

            for other check tasks to complete.

            A status of `running` indicates that the task is currently running.

            A status of `passed` indicates that all upgrade checks in the task are
            complete.

            A status of `failed` indicates that at least one upgrade check in the
            task has failed.

            '
      description: Software upgrade check task to be run on the array.
  NetworkInterfaceNeighborGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/NetworkInterfaceNeighborResponse'
  RemoteArraysResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          description: 'Returns a list of all items after filtering.

            The values are displayed for each name where meaningful.

            '
          items:
            $ref: '#/definitions/RemoteArray'
  AdminApiTokenResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/AdminApiToken'
  PolicySmbResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/PolicySmb'
  _referenceNoIdWithType:
    allOf:
    - $ref: '#/definitions/_referenceNoId'
    - type: object
      properties:
        resource_type:
          type: string
          description: 'Type of the object (full name of the endpoint).

            Valid values include `hosts`, `host-groups`, `network-interfaces`, `pods`,

            `ports`, `pod-replica-links`, `subnets`, `volumes`, `volume-snapshots`,

            `volume-groups`, `directories`, `policies/nfs`, `policies/smb`, and

            `policies/snapshot`, etc.

            '
  ActiveDirectoryPost:
    type: object
    properties:
      kerberos_servers:
        type: array
        description: 'A list of key distribution servers to use for Kerberos protocol.

          Servers must be specified in FQDN format.

          All specified servers must be registered to the domain appropriately in
          the configured DNS

          of the array.

          If not specified, servers are resolved for the domain in DNS.

          '
        items:
          type: string
          example: kdc.my-corporation.com
      computer_name:
        type: string
        example: FLASHARRAY01
        description: 'The name of the computer account to be created in the Active
          Directory

          domain.

          If not specified, defaults to the name of the Active Directory

          configuration.

          '
      join_ou:
        type: string
        example: OU=Dev,OU=Sweden,DC=purestorage,DC=com
        description: 'The distinguished name of the organizational unit in which the

          computer account should be created when joining the domain.

          The `DC=...` components of the distinguished name can be optionally omitted.

          If not specified, defaults to `CN=Computers`.

          '
      password:
        type: string
        example: password
        description: 'The login password of the user with privileges to create the
          computer account

          in the domain.

          This is not persisted on the array.

          '
      directory_servers:
        type: array
        description: 'A list of directory servers used for lookups related to user
          authorization.

          Servers must be specified in FQDN format.

          All specified servers must be registered to the domain appropriately in
          the configured DNS

          of the array and are only communicated with over the secure LDAP (LDAPS)
          protocol.

          If not specified, servers are resolved for the domain in DNS.

          '
        items:
          type: string
          example: ldap.my-corporation.com
      domain:
        type: string
        example: my-corporation.com
        description: The Active Directory domain to join.
      tls:
        type: string
        example: required
        description: 'TLS mode for communication with domain controllers.

          Valid values are `required` and `optional`.

          `required` forces TLS communication with a domain controller.

          `optional` allows the use of non-TLS communication,

          TLS will still be preferred, if available.

          If not specified, defaults to `required`.

          '
      user:
        type: string
        example: Administrator
        description: 'The login name of the user with privileges to create the computer
          account in

          the domain.

          This is not persisted on the array.

          '
  PodPerformanceReplicationByArray:
    allOf:
    - $ref: '#/definitions/_podPerformanceReplication'
    - type: object
      properties:
        array:
          type: object
          description: 'Array that shares this pod.

            '
          title: Resource
          allOf:
          - $ref: '#/definitions/_resource'
  SoftwarePatchesGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SoftwarePatchesResponse'
  DirectoryPolicyPost:
    type: object
    properties:
      policies:
        type: array
        description: 'A list of policies to apply to the resource.

          The `id` or `name` field in each `policy` parameter is required,

          but they cannot be set together.

          '
        items:
          $ref: '#/definitions/_directorypolicypostPolicies'
  PolicyManagementAccess:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        policy_type:
          type: string
          description: 'The type of policy. Valid values include `management-access`,
            `autodir`, `nfs`, `smb`,

            `snapshot`, and `quota`.

            '
          readOnly: true
        rules:
          type: array
          description: 'List the rules that defines the scoped roles.

            '
          items:
            $ref: '#/definitions/_policyrulemanagementaccessRules'
        aggregation_strategy:
          type: string
          description: 'When this is set to `least-common-permissions`, any users
            to whom this policy applies can

            receive no access rights exceeding those defined in this policy''s rules.

            When this is set to `all-permissions`, any users to whom this policy applies
            are capable of

            receiving additional access rights from other policies that apply to them.

            '
        enabled:
          type: boolean
          description: 'Returns a value of `true` if the policy is enabled.

            '
  LocalGroupGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/LocalGroupResponse'
  DirectoryServiceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/DirectoryServiceResponse'
    - $ref: '#/definitions/_errorContextResponse'
  PolicyRuleQuotaPost:
    type: object
    properties:
      rules:
        type: array
        description: A list of quota policy rules to create.
        items:
          $ref: '#/definitions/_policyrulequotapostRules'
  PolicyNfsPatch:
    allOf:
    - $ref: '#/definitions/PolicyPatch'
    - type: object
      properties:
        security:
          type: array
          description: 'The security flavors to use for accessing files on this mount
            point.

            Values include `auth_sys`, `krb5`, `krb5i`, and `krb5p`.

            If the server does not support the requested flavor, the mount operation
            fails.

            This operation updates all rules of the specified policy.

            If `auth_sys`, the client is trusted to specify the identity of the user.

            If `krb5`, cryptographic proof of the identity of the user is provided
            in each RPC request.

            This provides strong verification of the identity of users accessing data
            on the server.

            Note that additional configuration besides adding this mount option is
            required to

            enable Kerberos security. If `krb5i`, integrity checking is added to krb5.
            This ensures

            the data has not been tampered with. If `krb5p`, integrity checking and
            encryption is added

            to `krb5`. This is the most secure setting, but it also involves the most
            performance

            overhead.

            '
          items:
            type: string
            example: auth_sys
        user_mapping_enabled:
          type: boolean
          description: 'If set to `true`, FlashArray queries the joined AD/OpenLDAP
            server

            to find the user corresponding to the incoming UID.

            If set to `false`, users are defined by UID/GID pair.

            '
        nfs_version:
          type: array
          description: 'NFS protocol version allowed for the export to set for the
            policy.

            This operation updates all rules of the specified policy.

            Valid values are `nfsv3` and `nfsv4`.

            '
          items:
            type: string
            example: nfsv3
  _resourceNoId:
    type: object
    properties:
      name:
        type: string
        description: 'A user-specified name.

          The name must be locally unique and can be changed.

          '
    description: 'An ordinary (as opposed to built-in) resource that can be created,
      named,

      renamed or deleted by the user. This might be a virtual resource (e.g., a

      volume), or correspond to something in the environment, like a host or a

      server.

      '
  VchostConnection:
    type: object
    properties:
      protocol_endpoint:
        type: object
        description: 'A reference to the protocol endpoint, representing a storage
          container that vCenter can use.

          '
        title: FixedReference
        allOf:
        - $ref: '#/definitions/_fixedReference'
      vchost:
        type: object
        description: 'For vchost-connections, a vchost represents a vCenter.

          By connecting to a protocol endpoint, the corresponding vCenter gets

          the access to the storage container represented by this protocol endpoint.

          The vchost name should be null if `all_vchosts` is set to `true`, which
          means

          the storage container is accessible to all vchosts.

          '
        title: FixedReference
        allOf:
        - $ref: '#/definitions/_fixedReference'
      all_vchosts:
        type: boolean
        description: 'If set to `true`, the storage container represented by the protocol
          endpoint

          is accessible to all vchosts. If set to `false`, the storage container

          represented by the protocol endpoint is only accesible to the vchosts that

          have explicit vchost-connections to this protocol endpoint. The default
          is `false`.

          '
        default: false
  TestResultWithResourceWithIdPostResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/TestResultWithResourceWithId'
  _retentionPolicy:
    type: object
    properties:
      all_for_sec:
        type: integer
        format: int32
        example: 86400
        description: 'The length of time to keep the specified snapshots. Measured
          in seconds.

          Prior to 6.8.2 the range of 60 to 34560000 is accepted.

          In 6.8.2 and onwards the range of 60 to 2147483647 is accepted.

          '
      days:
        type: integer
        format: int32
        example: 7
        description: 'The number of days to keep the snapshots after the `all_for_sec`
          period has passed.

          Prior to 6.6.4 the range of 0 to 4000 is accepted.

          In 6.6.4 and onwards the range of 0 to 2147483647 is accepted.

          '
      per_day:
        type: integer
        format: int32
        example: 3
        description: 'The number of snapshots to keep per day after the `all_for_sec`
          period has passed.

          Prior to 6.8.2 the range of 0 to 1440 is accepted.

          In 6.8.2 and onwards the range of 0 to 2147483647 is accepted.

          '
    description: The snapshot retention policy.
  CertificateGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/CertificateResponse'
  _networkInterfaceEth:
    type: object
    properties:
      subnet:
        type: object
        description: 'Subnet that is associated with the specified network interface.

          '
        title: ReferenceNoId
        allOf:
        - $ref: '#/definitions/_referenceNoId'
      address:
        type: string
        description: 'The IPv4 or IPv6 address to be associated with the specified
          network

          interface.

          '
      vlan:
        type: integer
        format: int32
        description: VLAN ID
        readOnly: true
      netmask:
        type: string
        description: 'Netmask of the specified network interface that, when combined
          with the address of the

          interface, determines the network address of the interface.

          '
      subtype:
        type: string
        description: 'The subtype of the specified network interface. Only interfaces
          of subtype

          `virtual` can be created. Configurable on POST only.

          Valid values are `failover_bond`, `lacp_bond`, `physical`, and `virtual`.

          '
      mac_address:
        type: string
        description: 'The media access control address associated with the specified
          network interface.

          '
        readOnly: true
      subinterfaces:
        type: array
        description: 'List of network interfaces configured to be a subinterface of
          the specified network

          interface.

          '
        items:
          $ref: '#/definitions/_fixedReferenceNoId'
      gateway:
        type: string
        description: 'The IPv4 or IPv6 address of the gateway through which the specified
          network interface

          is to communicate with the network.

          '
      mtu:
        type: integer
        format: int32
        description: 'Maximum message transfer unit (packet) size for the network
          interface, in bytes.

          MTU setting cannot exceed the MTU of the corresponding physical interface.

          '
    description: Ethernet network interface properties.
  ResourceDirectorySpaceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where meaningful, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/ResourceDirectorySpace'
        items:
          type: array
          description: 'Displays a list of all items after filtering.

            If applicable, the values are displayed for each name.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/ResourceDirectorySpace'
  _snmp_v2c:
    type: object
    properties:
      community:
        type: string
        example: '****'
        description: 'Manager community ID under which Purity//FA is to communicate
          with the specified managers.

          '
        maxLength: 32
    description: 'The v2c configurations of SNMP.

      '
  SoftwareVersionsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SoftwareVersion'
  HostResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/Host'
  DirectoryServiceRoleResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/DirectoryServiceRole'
  AlertRulesCatalogGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/AlertRulesCatalogResponse'
    - $ref: '#/definitions/_errorContextResponse'
  CloudConfigGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/CloudConfig'
  _policyrulealertwatcherpatchRules:
    type: object
    properties:
      minimum_notification_severity:
        type: string
        description: 'The minimum severity that an alert must have in order for emails
          to

          be sent to the recipient.

          Possible values include `info`, `warning`, and `critical`.

          '
      included_codes:
        type: array
        description: 'An alert must have one of these codes in order for emails to
          be sent to

          the recipient.

          Cannot be specified with `exclude_codes`. If specified while

          `exclude_codes` is already set, `exclude_codes` will be cleared.

          Use "" to clear.

          If both `exclude_codes` and `include_codes` are cleared, defaults to an

          empty list for `exclude_codes`.

          '
        items:
          type: integer
          format: int32
          example: 101
      email:
        type: string
        description: 'The email address that will receive the alert notification emails.

          '
      excluded_codes:
        type: array
        description: 'An alert with one of these codes will not have emails sent to
          the

          recipient.

          Cannot be specified with `include_codes`. If specified while

          `include_codes` is already set, `include_codes` will be cleared.

          Use "" to clear.

          If both `exclude_codes` and `include_codes` are cleared, defaults to an

          empty list for `exclude_codes`.

          '
        items:
          type: integer
          format: int32
          example: 101
  MemberNoIdAllGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/MemberNoIdAllResponse'
    - $ref: '#/definitions/_errorContextResponse'
  PolicyRuleNfsClientResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name, if meaningful.

          '
        items:
          $ref: '#/definitions/PolicyRuleNfsClient'
  _offload_s3:
    type: object
    properties:
      access_key_id:
        type: string
        description: 'The access key ID of the AWS account used to create a connection
          between the array and

          an Amazon S3 offload target.

          The access key ID is 20 characters in length and is only accepted

          when creating the connection between the array and the S3 offload target.

          The `access_key_id`, `secret_access_key`, and `bucket` parameters must be
          set together.

          '
      bucket:
        type: string
        description: 'The name of the Amazon S3 bucket to where the data will be offloaded.

          Grant basic read and write ACL permissions to the bucket,

          and enable default (server-side) encryption for the bucket.

          Also verify that the bucket is empty of all objects and does not have any
          lifecycle policies.

          The `access_key_id`, `secret_access_key`, and `bucket` parameters must be
          set together.

          '
      placement_strategy:
        type: string
        description: 'The storage placement strategy used for the dynamic placement
          of data

          in an Amazon S3 offload target.

          Valid values are `aws-intelligent-tiering`, `aws-standard-class`, and `retention-based`.

          If set to `aws-intelligent-tiering`, data is stored in the

          Amazon S3 INTELLIGENT_TIERING storage class regardless of the retention
          period.

          If set to `aws-standard-access`, the data is stored in the

          Amazon S3 STANDARD storage class regardless of the retention period.

          If set to `retention-based`, the data for protection groups with longer
          retention periods

          is placed in the Amazon S3 STANDARD_IA

          (infrequently accessed, more cost-effective) storage class.

          All other data is placed in the STANDARD storage class.

          When the array is initially connected to an S3 offload target,

          `placement_strategy` is automatically set to `retention-based`.

          The `placement_strategy` or `uri` parameter is required, but they cannot
          be set together.

          '
      auth_region:
        type: string
        description: 'The region that will be used for initial authentication request.

          This parameter is optional and should be used only when region autodetection
          fails.

          '
      profile:
        type: string
        description: 'The offload target profile that will be selected for this target.
          This option

          allows more granular configuration for the target on top of the `protocol`

          parameter. Values include `s3-aws`, `s3-flashblade`, `s3-scality-ring`,

          `s3-wasabi-pay-as-you-go`, `s3-wasabi-rcs`, and `s3-other`.

          '
      secret_access_key:
        type: string
        description: 'The secret access key that goes with the access key ID (`access_key_id`)
          of the AWS account.

          The secret access key is 40 characters in length is only accepted

          when creating the connection between the array and the S3 offload target.

          The `access_key_id`, `secret_access_key`, and `bucket` parameters must be
          set together.

          '
      uri:
        type: string
        description: 'The URI used to create a connection between the array and a
          non-AWS S3 offload target.

          Storage placement strategies are not supported for non-AWS S3 offload targets.

          Both the HTTP and HTTPS protocols are allowed.

          '
  KmipTestResult:
    type: object
    properties:
      success:
        type: boolean
        example: true
        description: 'Expresses whether communication between the FlashArray and the
          KMIP server succeeded

          or not.

          '
      component_name:
        type: string
        example: kmip-test1
        description: Name of the component running the test.
      destination:
        type: string
        example: kmip-test-server.dev.purestorage.com
        description: The URI of the target server being tested.
      description:
        type: string
        example: Testing kmip connectivity.
        description: Description of what is being tested.
      component_address:
        type: string
        example: 10.230.94.21
        description: Address of the component running the test.
      enabled:
        type: boolean
        example: true
        description: Expresses whether the service is enabled or not.
      result_details:
        type: string
        example: KMIP test failed
        description: Additional information about the test result.
      test_type:
        type: string
        example: kmip
        description: Description of what is being tested.
  _networkInterfaceNeighborNeighborPort:
    type: object
    properties:
      description:
        type: string
        example: eth7
        description: A description of the neighbor port.
      id:
        $ref: '#/definitions/_networkInterfaceNeighborNeighborPortId'
    description: 'The neighboring port connected to the FlashArray local port.

      '
  PresetWorkloadPatch:
    allOf:
    - $ref: '#/definitions/_newName'
    - type: object
  VchostEndpointResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Returns a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/VchostEndpoint'
  DirectoryResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/Directory'
  _workloadPlacementRecommendationPlacement:
    type: object
    properties:
      name:
        type: string
        description: 'The name of the placement.

          '
      targets:
        type: array
        description: 'The targets for the placement.

          '
        items:
          $ref: '#/definitions/_workloadPlacementRecommendationTarget'
    description: Placement data in the recommendation result.
  _networkInterfaceNeighborNeighborChassis:
    type: object
    properties:
      station_only:
        type: object
        description: Station only status of the neighbor system.
        title: NetworkInterfaceNeighborCapability
        allOf:
        - $ref: '#/definitions/_networkInterfaceNeighborCapability'
      addresses:
        type: array
        description: Management IP addresses of the neighbor.
        items:
          type: string
          example: 10.202.101.70
      router:
        type: object
        description: IP router capability of the neighbor system.
        title: NetworkInterfaceNeighborCapability
        allOf:
        - $ref: '#/definitions/_networkInterfaceNeighborCapability'
      wlan_access_point:
        type: object
        description: WLAN access point capability of the neighbor system.
        title: NetworkInterfaceNeighborCapability
        allOf:
        - $ref: '#/definitions/_networkInterfaceNeighborCapability'
      name:
        type: string
        example: iscsi-sw-100-b.example.com
        description: Administratively assigned name of the neighbour.
      description:
        type: string
        example: Example Switch iscsi-sw-100-b version 1.0
        description: 'The textual description of the neighbor. The description may
          include

          the full name and version identification of the system hardware

          type, software operating system, and networking software.

          '
      telephone:
        type: object
        description: Telephone capability of the neighbor system.
        title: NetworkInterfaceNeighborCapability
        allOf:
        - $ref: '#/definitions/_networkInterfaceNeighborCapability'
      bridge:
        type: object
        description: Bridge capability of the neighbor system.
        title: NetworkInterfaceNeighborCapability
        allOf:
        - $ref: '#/definitions/_networkInterfaceNeighborCapability'
      id:
        $ref: '#/definitions/_networkInterfaceNeighborNeighborChassisId'
      docsis_cable_device:
        type: object
        description: DOCSIS cable device capability of the neighbor system.
        title: NetworkInterfaceNeighborCapability
        allOf:
        - $ref: '#/definitions/_networkInterfaceNeighborCapability'
      repeater:
        type: object
        description: Neighbor system's repeater capability.
        title: NetworkInterfaceNeighborCapability
        allOf:
        - $ref: '#/definitions/_networkInterfaceNeighborCapability'
    description: 'The neighboring physical component incorporating one or more IEEE
      802

      LAN stations and their associated application functionality.

      '
  ResourceDirectorySpace:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time:
          type: integer
          format: int64
          description: 'The timestamp of when the data was taken, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
        space:
          type: object
          description: 'Displays size and space consumption information.

            '
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
  DirectoryPolicyExportPost:
    type: object
    properties:
      policies:
        type: array
        description: 'A list of export policies to apply to the directory.

          The `id` and `name` fields in each `policy` parameter are required,

          but cannot be set together.

          '
        items:
          $ref: '#/definitions/_directorypolicyexportpostPolicies'
  _priorityAdjustment:
    type: object
    properties:
      priority_adjustment_value:
        type: integer
        format: int32
        description: 'Adjust priority by the specified amount, using the `priority_adjustment_operator`.

          Valid values are 0 and +10 for `+` and `-` operators, -10, 0, and +10 for
          the `=` operator.

          '
      priority_adjustment_operator:
        type: string
        description: 'Valid values are `+`, `-`, and `=`.

          The values `+` and `-` may be applied to volumes and volume groups to reflect
          the relative

          importance of their workloads. Volumes and volume groups can be assigned
          a priority adjustment

          of -10, 0, or +10. In addition, volumes can be assigned values of =-10,
          =0, or =+10.

          Volumes with settings of -10, 0, +10 can be modified by the priority adjustment
          setting of a

          volume group that contains the volume. However, if a volume has a priority
          adjustment

          set with the `=` operator (for example, =+10), it retains that value and

          is unaffected by any volume group priority adjustment settings.

          '
  PolicyAssignmentPostPolicy:
    type: object
    properties:
      policy:
        type: object
        description: 'Reference to the policy to apply to the resource.

          '
        title: Reference
        allOf:
        - $ref: '#/definitions/_reference'
  Certificate:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_certificateBase'
    - type: object
  _presetWorkloadVolumeConfiguration:
    type: object
    required:
    - count
    - name
    - placement_configurations
    - provisioned_size
    properties:
      periodic_replication_configurations:
        type: array
        description: 'The names of the periodic replication configurations to apply
          to the volumes.

          '
        items:
          type: string
      count:
        type: string
        description: The number of volumes to provision. Supports parameterization.
      name:
        type: string
        description: 'The name of the volume configuration, by which other configuration
          objects in the preset

          can reference it. Name must be unique across all configuration objects in
          the preset.

          '
      placement_configurations:
        type: array
        description: 'The names of the placement configurations with which to associate
          the volumes.

          '
        items:
          type: string
        maxItems: 1
        minItems: 1
      provisioned_size:
        type: string
        description: 'The virtual size of each volume. Measured in bytes

          and must be a multiple of 512. The maximum size is

          4503599627370496 (4PB). Supports parameterization.

          '
      snapshot_configurations:
        type: array
        description: 'The names of the snapshot configurations to apply to the volumes.

          '
        items:
          type: string
  FileSystemGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/FileSystemResponse'
    - $ref: '#/definitions/_errorContextResponse'
  LocalGroup:
    type: object
    properties:
      gid:
        type: integer
        format: int32
        description: 'The POSIX user ID of this object (group ID).

          '
      built_in:
        type: boolean
        description: 'Returns a value of `true` if the local group is built in.

          Returns a value of `false` if the group has been created manually.

          '
        readOnly: true
      name:
        type: string
        description: 'The local group name.

          '
      email:
        type: string
        description: 'The email address of the local group.

          '
      sid:
        type: string
        description: 'The SID (security ID) of the local group.

          '
        readOnly: true
  SoftwareGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SoftwareResponse'
  VchostPatch:
    type: object
    properties:
      name:
        type: string
        example: vchost-b
        description: 'The new name of the vchost.

          '
  AlertWatcherGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/AlertWatcherResponse'
  ContainerDefaultProtectionGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ContainerDefaultProtectionResponse'
    - $ref: '#/definitions/_errorContextResponse'
  _fixedReferenceWithType:
    allOf:
    - $ref: '#/definitions/_fixedReference'
    - type: object
      properties:
        resource_type:
          type: string
          description: 'Type of the object (full name of the endpoint).

            Valid values include `hosts`, `host-groups`, `network-interfaces`, `pods`,

            `ports`, `pod-replica-links`, `subnets`, `volumes`, `volume-snapshots`,

            `volume-groups`, `directories`, `policies/nfs`, `policies/smb`,

            `policies/snapshot`, and `policies/quota`, etc.

            '
          readOnly: true
  AlertWatcherPatch:
    type: object
    properties:
      enabled:
        type: boolean
        description: 'If set to `true`, email notifications will be sent to this watcher
          for alerts.

          If set to `false`, email notifications are disabled.

          '
  Realm:
    allOf:
    - $ref: '#/definitions/_resource'
    - type: object
      properties:
        quota_limit:
          type: integer
          format: int64
          description: 'The logical quota limit of the realm, measured in bytes.

            '
          readOnly: true
          maximum: 4503599627370496
        time_remaining:
          type: integer
          format: int64
          description: 'Time in milliseconds before the realm is eradicated.

            `null` if not destroyed.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the realm has been destroyed
            and is pending eradication.

            The realm cannot be modified while it is in the destroyed state.

            The `time_remaining` value displays the amount of time left until the
            destroyed

            realm is permanently eradicated. Once eradication has begun, the realm
            can no

            longer be recovered. Before the `time_remaining` period has elapsed,

            the destroyed realm can be recovered through the PATCH method

            '
        qos:
          type: object
          description: 'Displays QoS limit information.

            '
          title: ContainerQos
          allOf:
          - $ref: '#/definitions/_containerQos'
        created:
          type: integer
          format: int64
          description: 'Creation timestamp of the realm.

            '
          readOnly: true
        eradication_config:
          type: object
          title: ContainerEradicationConfig
          allOf:
          - $ref: '#/definitions/ContainerEradicationConfig'
        space:
          type: object
          description: 'Displays provisioned size and physical storage consumption

            information for the realm.

            '
          title: SpaceNoDeprecatedPhysicalOrEffective
          allOf:
          - $ref: '#/definitions/_spaceNoDeprecatedPhysicalOrEffective'
      description: 'A realm is an administrative domain, a data container,

        and a namespace for pods, hosts, and host groups.

        '
  LocalUserPatch:
    type: object
    properties:
      uid:
        type: integer
        format: int32
        description: Optional field to set the UID of the local user.
      password:
        type: string
        description: 'The password of the local user. This field is only required
          if the `enabled` field is true.

          '
      primary_group:
        type: object
        description: 'Local group that would be assigned as the primary group of the
          local user.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
      name:
        type: string
        description: 'The local user name.

          '
      email:
        type: string
        description: Optional field to set the email of the local user.
      enabled:
        type: boolean
        description: 'If this field is `false`, the local user will be disabled on
          creation.

          Otherwise, the local user will be enabled and functional from the beginning.

          '
  ArrayConnectionPost:
    type: object
    properties:
      replication_addresses:
        type: array
        description: 'IP addresses and FQDNs of the target arrays. Configurable only
          when

          `replication_transport` is set to `ip`. If not configured, will be set

          to all the replication addresses available on the target array

          at the time of the POST.

          '
        items:
          type: string
          example: 10.202.101.70
      connection_key:
        type: string
        example: 6207d123-d123-0b5c-5fa1-95fabc5c7123
        description: 'The connection key of the target array.

          '
      encryption:
        type: string
        example: encrypted
        description: 'If `encrypted`, all traffic over this array connection will
          be encrypted.

          If `unencrypted`, all traffic over this array connection will be unencrypted.

          Defaults to `unencrypted`.

          '
      management_address:
        type: string
        example: 10.202.101.78
        description: 'Management IP address of the target array.

          '
      replication_transport:
        type: string
        example: ip
        description: 'The protocol used to transport data between the local array
          and

          the remote array.

          Valid values are `ip` and `fc`. The default is `ip`.

          '
      type:
        type: string
        example: async-replication
        description: 'The type of replication.

          Valid values are `async-replication` and `sync-replication`.

          '
  _fixedNameResourceNoId:
    type: object
    properties:
      name:
        type: string
        description: 'A user-specified name.

          The name must be locally unique and cannot be changed.

          '
        readOnly: true
    description: 'An ordinary (as opposed to built-in) resource that can be created,
      named, or

      deleted by the user. This might be a virtual resource (e.g., a

      volume) or correspond to something in the environment, like a host or server.

      '
  PodReplicaLinkMappingPolicyResponse:
    type: object
    properties:
      items:
        type: array
        description: 'A list of policy mappings.

          '
        items:
          $ref: '#/definitions/MappingPolicy'
  CertificateResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Certificate'
  ArrayFactoryResetTokenResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/ArrayFactoryResetToken'
  FleetMemberPost:
    type: object
    properties:
      members:
        type: array
        description: 'Details about the members being added to fleet.

          '
        items:
          $ref: '#/definitions/_fleetmemberpostMembers'
  _workloadConfigurationFixedReference:
    type: object
    properties:
      workload:
        type: object
        title: WorkloadConfigurationFixedReferenceWorkload
        allOf:
        - $ref: '#/definitions/_workloadConfigurationFixedReferenceWorkload'
  PolicyNfs:
    allOf:
    - $ref: '#/definitions/Policy'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        security:
          type: array
          description: 'The security flavors to use for accessing files on this mount
            point.

            Values include `auth_sys`, `auth_sys-partial`, `krb5`, `krb5-partial`,
            `krb5i`, and `krb5p`.

            If the server does not support the requested flavor, the mount operation
            fails.

            If `auth_sys`, the client is trusted to specify the identity of the user.

            If `krb5`, cryptographic proof of the identity of the user is provided
            in each RPC request.

            Note that additional configuration besides adding this mount option is
            required in order to

            enable Kerberos security. If `krb5i`, integrity checking is added to krb5,
            to ensure

            the data has not been tampered with. If `krb5p`, integrity checking and
            encryption

            are added to `krb5`. This is the most secure setting, but it also involves
            the most

            performance overhead. If security option is allowed for all rules of the
            policy,

            it is cascaded exactly. Examples: `auth_sys`, `krb5`.

            If the security option is supported partially by the rules of the policy,

            it will be cascaded with the `-partial` suffix. Examples include: `auth_sys-partial`,

            `krb5-partial`. If the security option is not supported by any rules or
            there are no

            rules of the policy, then it will not be in the result array.

            If there are no rules in the policy the array of values will be empty.

            If there are two rules, one supporting auth_sys and the other supporting
            auth_sys and krb5

            the array of values would contain `auth_sys` and `krb5-partial`.

            '
          items:
            type: string
            example: auth_sys-partial
        policy_mapping:
          type: object
          description: 'Specifies the mapping of this policy across a pod replica
            link. If

            this policy is not inside a pod of a pod replica link, mapping is `null`.

            '
          title: PolicyNfsPolicyMapping
          allOf:
          - $ref: '#/definitions/PolicyNfsPolicyMapping'
        user_mapping_enabled:
          type: boolean
          description: 'Returns `true` if user mapping is enabled on the policy.

            If `true`, FlashArray queries the joined AD/OpenLDAP server

            to find the user corresponding to the incoming UID.

            If `false`, users are defined by UID/GID pair.

            '
        nfs_version:
          type: array
          description: 'NFS protocol version allowed for the export.

            If NFS version is allowed for all rules of the policy

            it is cascaded exactly as: `nfsv3`, `nfsv4`.

            If the NFS version is supported partially by the rules of the policy

            it will be cascaded as: `nfsv3-partial`, `nfsv4-partial`.

            If the NFS version is not supported by any rules or there are no rules
            of the policy

            then it will not be in the result array.

            If there are no rules in the policy the array will be empty.

            If there are two rules, one supporting `nfsv3` and the other supporting
            `nfsv3` and `nfsv4`

            then the array would contain `nfsv3` and `nfsv4-partial`.

            '
          items:
            type: string
            example: nfsv3-partial
  VolumeSnapshot:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_snapshot'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        serial:
          type: string
          description: 'A serial number generated by the system when the snapshot
            is created.

            The serial number is unique across all arrays.

            '
          readOnly: true
        volume_group:
          type: object
          description: 'The volume group to which the volume that is the source of
            the volume snapshot belongs.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        space:
          type: object
          description: 'Displays size and space consumption information.

            '
          title: SnapshotSpace
          allOf:
          - $ref: '#/definitions/_snapshotSpace'
      description: 'A point-in-time image of the contents of the volume.

        '
  PolicyManagementAccessEffective:
    type: object
    properties:
      details:
        type: string
        description: 'Additional details about policy resolution.

          '
      rules:
        type: array
        description: 'List the rules that defines the scoped roles.

          '
        items:
          $ref: '#/definitions/_policyrulemanagementaccessRules'
  CloudProviderTagResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/CloudProviderTag'
  HostPerformanceBalance:
    allOf:
    - $ref: '#/definitions/_resourceNoId'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        op_count:
          type: integer
          format: int64
          description: 'Count of I/O operations for the host path, over the specified
            resolution.

            '
          readOnly: true
        initiator:
          type: object
          title: PortCommon
          allOf:
          - $ref: '#/definitions/_portCommon'
        time:
          type: integer
          format: int64
          description: Sample time in milliseconds since UNIX epoch.
          readOnly: true
        fraction_relative_to_max:
          type: number
          format: float
          example: 0.14
          description: 'The path with the highest number of operation counts is displayed
            with a

            fraction_relative_to_max of 1.0. The fraction values of all other

            paths in the host are then calculated relative to the path with the

            highest number of operation counts.

            '
          readOnly: true
        target:
          type: object
          title: PortInitiatorTarget
          allOf:
          - $ref: '#/definitions/PortInitiatorTarget'
  _networkInterfacePerformanceEth:
    type: object
    properties:
      flow_control_transmitted_lossless_bytes_per_sec:
        type: integer
        format: int64
        description: 'Bytes transmitted per second with lossless flow control settings.

          '
        minimum: 0
      transmitted_dropped_errors_per_sec:
        type: integer
        format: int64
        description: 'Transmitted packets dropped per second.

          '
        minimum: 0
      rdma_received_sequence_errors_per_sec:
        type: integer
        format: int64
        description: 'The number of received RDMA packet sequence errors per second.

          '
        minimum: 0
      transmitted_carrier_errors_per_sec:
        type: integer
        format: int64
        description: 'Transmission carrier errors per second.

          '
        minimum: 0
      flow_control_transmitted_pause_frames_per_sec:
        type: integer
        format: int64
        description: 'Transmitted pause frames per second.

          '
        minimum: 0
      received_bytes_per_sec:
        type: integer
        format: int64
        description: 'Bytes received per second.

          '
        minimum: 0
      flow_control_transmitted_congestion_packets_per_sec:
        type: integer
        format: int64
        description: 'Number of congestion control packets transmitted per second.

          '
        minimum: 0
      rdma_transmitted_local_ack_timeout_errors_per_sec:
        type: integer
        format: int64
        description: 'The number of times RDMA ack timer expired for QPs per second.

          '
        minimum: 0
      flow_control_received_discarded_packets_per_sec:
        type: integer
        format: int64
        description: 'Number of received packets that were physically discarded by
          the

          NIC per second due to errors or other reasons.

          '
        minimum: 0
      received_packets_per_sec:
        type: integer
        format: int64
        description: 'Packets received per second.

          '
        minimum: 0
      flow_control_received_lossless_bytes_per_sec:
        type: integer
        format: int64
        description: 'Bytes received per second with lossless flow control settings.

          '
        minimum: 0
      flow_control_received_pause_frames_per_sec:
        type: integer
        format: int64
        description: 'Received pause frames per second.

          '
        minimum: 0
      received_crc_errors_per_sec:
        type: integer
        format: int64
        description: 'Reception CRC errors per second.

          '
        minimum: 0
      rdma_received_req_cqe_errors_per_sec:
        type: integer
        format: int64
        description: 'The number of times port detected RDMA CQEs with error per second.

          '
        minimum: 0
      transmitted_packets_per_sec:
        type: integer
        format: int64
        description: 'Packets transmitted per second.

          '
        minimum: 0
      transmitted_bytes_per_sec:
        type: integer
        format: int64
        description: 'Bytes transmitted per second.

          '
        minimum: 0
      flow_control_received_congestion_packets_per_sec:
        type: integer
        format: int64
        description: 'Number of congestion control packets received per second.

          '
        minimum: 0
      received_frame_errors_per_sec:
        type: integer
        format: int64
        description: 'Received packet frame errors per second.

          '
        minimum: 0
      other_errors_per_sec:
        type: integer
        format: int64
        description: 'This field has been deprecated.

          Since Purity version 6.6.8, this will always return `null`.

          The sum of unspecified reception and transmission errors per second.

          '
        minimum: 0
      total_errors_per_sec:
        type: integer
        format: int64
        description: 'The sum of all reception and transmission errors per second.

          '
        minimum: 0
      flow_control_transmitted_discarded_packets_per_sec:
        type: integer
        format: int64
        description: 'Number of transmitted packets that were physically discarded
          by

          the NIC per second due to errors or other reasons.

          '
        minimum: 0
    description: 'Ethernet network interface statistics.

      '
  _builtInRelationship:
    type: object
    properties:
      id:
        type: string
        description: 'A non-modifiable, globally unique ID chosen by the system.

          '
        readOnly: true
  DriveGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/DriveResponse'
  RemoteProtectionGroupSnapshot:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The time remaining until eradication, measured in milliseconds,

            if the snapshot has been destroyed.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Destroyed and pending eradication? If not specified, defaults
            to false.

            '
        created:
          type: integer
          format: int64
          example: 1503520993
          description: 'The creation time of the snapshot on the original source of
            the snapshot.

            Measured in milliseconds since the UNIX epoch.

            '
          readOnly: true
        source:
          type: object
          description: 'The original protection group from which this snapshot was
            taken.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        is_local:
          type: boolean
          description: 'The status of whether or not the remote protection group snapshot
            is replicated from

            the current array.

            '
          readOnly: true
        remote:
          type: object
          description: 'The offload target where the remote protection group snapshot
            is located.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        suffix:
          type: string
          description: 'The suffix that is appended to the `source_name` value to
            generate the full

            remote protection group snapshot name in the form `PGROUP.SUFFIX`.

            If the suffix is not specified, the system constructs the snapshot name
            in the form

            `PGROUP.NNN`, where `PGROUP` is the protection group name, and `NNN` is
            a monotonically

            increasing number.

            '
      description: 'A remote protection group snapshot is a collection of point-in-time

        consistent volume snapshots, for volumes associated with the remote

        protection group when the snapshot was taken.

        '
  SoftwareBundlePost:
    type: object
    properties:
      source:
        type: string
        example: ftp://local/upgrade_bundle.sh
        description: The source of the software bundle. Accepts HTTP, HTTPS or FTP,
          and paths.
    description: The source of the software bundle.
  SupportDiagnosticsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SupportDiagnosticsResponse'
  PageInfoWithoutTotalItemCountResponse:
    type: object
    properties:
      continuation_token:
        type: string
        description: 'Continuation token that can be provided in the `continuation_token`

          query parameter to get the next page of data.

          If you use the continuation token to page through data you

          are guaranteed to get all items exactly once regardless of

          how items are modified. If an item is added or deleted during

          the pagination, then it may or may not be returned.

          The continuation token is generated if the limit is less

          than the remaining number of items, and the default sort is used

          (no sort is specified).

          '
      more_items_remaining:
        type: boolean
        example: false
        description: 'Displays a value of `true` if subsequent items can be retrieved.

          '
  CloudConfigPatch:
    type: object
    properties:
      model:
        type: object
        description: The requested model configuration of the CBS array.
        title: CloudConfigModelPatch
        allOf:
        - $ref: '#/definitions/CloudConfigModelPatch'
  PodReplicaLinkPerformanceReplicationResponse:
    type: object
    properties:
      total:
        type: array
        description: 'The aggregate value of all items after filtering.

          For real-time performance, the values are aggregated for the latest timestamp.

          For historical performance, the values are aggregated for each timestamp
          from

          `start_time` to `end_time`. Where it makes more sense, the average value
          is

          displayed instead. The values are displayed for each field where meaningful.

          '
        items:
          $ref: '#/definitions/PodReplicaLinkPerformanceReplication'
      items:
        type: array
        description: A list of pod replica link performance objects.
        items:
          $ref: '#/definitions/PodReplicaLinkPerformanceReplication'
  ArrayConnection:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        replication_addresses:
          type: array
          description: 'IP addresses of the target arrays when `replication_transport`
            is `ip`.

            WWNs of the target arrays when `replication_transport` is `fc`.

            '
          items:
            type: string
            example: 10.202.101.70
        throttle:
          $ref: '#/definitions/_throttle'
        encryption:
          type: string
          example: encrypted
          description: 'If `encrypted`, all traffic over this array connection will
            be encrypted.

            If `unencrypted`, all traffic over this array connection will be unencrypted.

            '
        os:
          type: string
          description: 'The operating system of the connected array.

            '
          readOnly: true
        management_address:
          type: string
          example: 10.202.101.78
          description: 'Management IP address or FQDN of the target array.

            '
        replication_transport:
          type: string
          example: ip
          description: 'The protocol used to transport data between the local array
            and the remote array.

            Valid values are `ip` and `fc`. The default value is `ip`.

            '
        encryption_mode:
          type: string
          example: TLS-PSK-AES-256
          description: 'Cryptographic protocol, trust model, and encryption algorithm
            information.

            Will be `null` if `encrypted` is `false`.

            '
        remote:
          type: object
          description: 'The remote array.

            '
          title: ReferenceWithType
          allOf:
          - $ref: '#/definitions/_referenceWithType'
        type:
          type: string
          description: 'The type of connection. Valid values include `async-replication`,
            `sync-replication`, and

            `fleet-management`.

            '
        version:
          type: string
          example: 5.0.0
          description: The Purity version on the target array.
        status:
          type: string
          description: 'Status of the connection. Valid values include `connected`,
            `connecting`,

            `partially_connected`, and `unbalanced`.

            A status of `connected` indicates that arrays are communicating.

            A status of `connecting` indicates that the array is trying to establish
            a connection.

            A status of `partially_connected` indicates that some replication addresses
            are

            communicating but others are not.

            A status of `unbalanced` indicates that the arrays are communicating,
            but the set of

            paths is either not redundant or not symmetrical.

            '
      description: 'Connection to a target array. The `name` and `id` values refer
        to the name and ID of the

        target array.

        '
  PolicyRuleNfsClient:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left, in milliseconds, until the destroyed
            NFS policy rule is permanently

            eradicated.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the pod containing the NFS policy
            rule has been destroyed and

            is pending eradication.

            The `time_remaining` value displays the amount of time left until the
            destroyed policy is

            permanently eradicated.

            Once the `time_remaining` period has elapsed, the NFS policy rule is permanently
            eradicated

            and can no longer be recovered.

            '
        security:
          type: array
          description: 'The security flavors to use for accessing files on this mount
            point.

            Values include `auth_sys`, `krb5`, `krb5i`, and `krb5p`.

            If the server does not support the requested flavor, the mount operation
            fails.

            This operation updates all rules of the specified policy.

            If `auth_sys`, the client is trusted to specify the identity of the user.

            If `krb5`, cryptographic proof of the identity of the user is provided
            in each RPC request.

            This provides strong verification of the identity of users accessing data
            on the server.

            Note that additional configuration besides adding this mount option is
            required to

            enable Kerberos security. If `krb5i`, integrity checking is added to krb5.
            This ensures

            the data has not been tampered with. If `krb5p`, integrity checking and
            encryption is added

            to krb5. This is the most secure setting, but it also involves the most
            performance

            overhead.

            '
          items:
            type: string
            example: auth_sys
        access:
          type: string
          description: 'Specifies access control for the export. Valid values are
            `root-squash`, `all-squash`,

            and `no-root-squash`.

            `root-squash` prevents client users and groups with root privilege from
            mapping their

            root privilege to a file system. All users with UID 0 will have their
            UID mapped to

            anonuid. All users with GID 0 will have their GID mapped to anongid.

            `all-squash` maps all UIDs (including root) to anonuid and all GIDs (including
            root) to

            anongid.

            `no-root-squash` allows users and groups to access the file system with
            their UIDs and GIDs.

            The default is `root-squash` if not specified.

            '
          readOnly: true
        anonuid:
          type: string
          example: '65530'
          description: 'Any user whose UID is affected by an `access` of `root_squash`
            or `all_squash` will have

            their UID mapped to `anonuid`. The default `anonuid` is null, which means
            65534.

            Use "" to clear.

            '
          readOnly: true
        anongid:
          type: string
          example: '65530'
          description: 'Any user whose GID is affected by an `access` of `root_squash`
            or `all_squash` will have

            their GID mapped to `anongid`. The default `anongid` is null, which means
            65534.

            Use "" to clear.

            This value is ignored when user mapping is enabled - see User Guide for
            more details.

            '
          readOnly: true
        name:
          type: string
          description: 'Name of this rule. The name is automatically generated by
            the system.

            '
          readOnly: true
        client:
          type: string
          description: 'Specifies the clients that will be permitted to access the
            export.

            Accepted notation includes IP, IP mask, or hostname.

            The default is `*` if not specified.

            '
          readOnly: true
        permission:
          type: string
          description: 'Specifies which read-write client access permissions are allowed
            for the export.

            Valid values are `rw` and `ro`. The default is `rw` if not specified.

            '
          readOnly: true
        nfs_version:
          type: array
          description: 'The NFS protocol version allowed for the export. Valid values
            are `nfsv3` and `nfsv4`.

            '
          items:
            type: string
            example: nfsv3
        policy:
          type: object
          description: The policy to which this rule belongs.
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
  VolumeGroupPerformance:
    allOf:
    - $ref: '#/definitions/_performance'
    - $ref: '#/definitions/_resource'
  _apiToken:
    type: object
    properties:
      expires_at:
        type: integer
        format: int64
        description: Expiration time in milliseconds since the UNIX epoch.
        readOnly: true
      created_at:
        type: integer
        format: int64
        description: Creation time in milliseconds since the UNIX epoch.
        readOnly: true
      token:
        type: string
        readOnly: true
    description: API token
  ArrayPerformanceByLink:
    allOf:
    - $ref: '#/definitions/ArrayPerformance'
    - type: object
      properties:
        link:
          type: array
          description: 'List of IDs and array names for the link

            '
          items:
            $ref: '#/definitions/_resource'
      description: 'Array-level front-end I/O performance per link.

        A link represents a set of arrays that an I/O operation depends on.

        '
  _offload_nfs:
    type: object
    properties:
      address:
        type: string
        description: The hostname or IP address of the NFS server to where the data
          will be offloaded. An array can be connected to one offload target at a
          time, while multiple arrays can be connected to the same offload target.
          If the protection group is in a stretched pod, for high availability, connect
          both arrays in the stretched pod to the offload target.
      mount_options:
        type: string
        description: The custom mount options on the NFS server. Enter multiple mount
          options in comma-separated format. Valid values include `port`, `rsize`,
          `wsize`, `nfsvers`, and `tcp` or `udp`. These mount options are common to
          all NFS file systems.
      mount_point:
        type: string
        description: The mount point of the NFS export on the NFS server. For example,
          `/mnt`. The `access_key_id`, `secret_access_key`, and `bucket` parameters
          must be set together.
      profile:
        type: string
        description: 'The offload target profile that will be selected for this target.
          This option

          allows more granular configuration for the target on top of the `protocol`

          parameter. Values include `nfs`, and `nfs-flashblade`.

          '
  _presetWorkloadConstraintsResourceReference:
    type: object
    required:
    - allowed_values
    properties:
      allowed_values:
        type: array
        description: 'The valid values that can be supplied to the parameter.

          Exactly one value must be specified.

          '
        items:
          $ref: '#/definitions/_presetWorkloadConstraintsResourceReferenceAllowedValues'
      default:
        type: object
        description: 'The default value to use if no value is provided.

          Must match the `resource_type` set in `allowed_values`.

          One of `id` or `name` must be set, but they cannot be set together.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
    description: Constraints to apply to resource reference parameters
  HostGroupPost:
    type: object
    properties:
      tags:
        type: array
        description: 'The list of tags to be upserted with the object.

          '
        items:
          $ref: '#/definitions/NonCopyableTag'
  AdminSettingsGetResponse:
    allOf:
    - $ref: '#/definitions/AdminSettingsResponse'
    - type: object
      properties:
        more_items_remaining:
          type: boolean
          example: false
          description: 'Returns a value of `true` if subsequent items can be retrieved.

            '
        total_item_count:
          type: integer
          format: int32
          description: 'The total number of records after applying all filter query
            parameters.

            The `total_item_count` will be calculated if and only if the corresponding

            query parameter `total_item_count` is set to `true`. If this query parameter

            is not set or set to `false`, a value of `null` will be returned.

            '
  SmtpServerGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SmtpServerResponse'
  ConnectionPost:
    type: object
    properties:
      protocol_endpoint:
        type: object
        description: 'A protocol endpoint (also known as a conglomerate volume) which
          acts as a proxy through which

          virtual volumes are created and then connected to VMware ESXi hosts or host
          groups.

          The protocol endpoint itself does not serve I/Os; instead,

          its job is to form connections between FlashArray volumes and ESXi hosts
          and host groups.

          '
        title: Reference
        allOf:
        - $ref: '#/definitions/_reference'
      lun:
        type: integer
        format: int32
        example: 7
        description: 'The logical unit number (LUN) by which the specified hosts

          are to address the specified volume. If the LUN is not specified,

          the system automatically assigns a LUN to the connection.

          To automatically assign a LUN to a private connection,

          the system starts at LUN `1` and counts up to the maximum LUN `4095`,

          assigning the first available LUN to the connection.

          For shared connections, the system starts at LUN `254` and

          counts down to the minimum LUN `1`, assigning the first available LUN

          to the connection. If all LUNs in the `[1...254]` range are taken,

          the system starts at LUN `255` and counts up to the maximum LUN `4095`,

          assigning the first available LUN to the connection.

          Should not be used together with an NVMe host or host group.

          '
        minimum: 1
        maximum: 4095
  VolumePatch:
    allOf:
    - $ref: '#/definitions/_destroyedPatchPost'
    - $ref: '#/definitions/_newName'
    - type: object
      properties:
        protocol_endpoint:
          type: object
          description: 'Sets the properties that are specific to protocol endpoints.

            This can only be used in conjunction to `subtype=protocol_endpoint`.

            '
          title: ProtocolEndpoint
          allOf:
          - $ref: '#/definitions/_protocol_endpoint'
        pod:
          type: object
          description: Moves the volume into the specified pod.
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        provisioned:
          type: integer
          format: int64
          description: Updates the virtual size of the volume, measured in bytes.
          maximum: 4503599627370496
        qos:
          type: object
          description: Sets QoS limits.
          title: Qos
          allOf:
          - $ref: '#/definitions/_qos'
        requested_promotion_state:
          type: string
          description: 'Valid values are `promoted` and `demoted`.

            Patch `requested_promotion_state` to `demoted` to demote the volume so
            that the volume

            stops accepting write requests.

            Patch `requested_promotion_state` to `promoted` to promote the volume
            so that the volume

            starts accepting write requests.

            '
        workload:
          type: object
          description: 'Set the `id` or `name` of the workload to an empty string
            to remove the

            volume from the workload.

            '
          title: ReferenceWithFixedType
          allOf:
          - $ref: '#/definitions/_referenceWithFixedType'
        volume_group:
          type: object
          description: Adds the volume to the specified volume group.
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        priority_adjustment:
          type: object
          description: Adjusts volume priority.
          title: PriorityAdjustment
          allOf:
          - $ref: '#/definitions/_priorityAdjustment'
  MemberNoIdMemberGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/MemberNoIdMemberResponse'
    - $ref: '#/definitions/_errorContextResponse'
  VolumeSnapshotTransferGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/VolumeSnapshotTransferResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where it makes more sense, the average value is displayed instead.

            The values are displayed for each field where meaningful.

            '
          items:
            $ref: '#/definitions/VolumeSnapshotTransfer'
  _resourceFixedNonUniqueName:
    type: object
    properties:
      name:
        type: string
        description: 'Name of the resource. The name cannot be modified.

          '
        readOnly: true
      id:
        type: string
        description: 'A globally unique, system-generated ID. The ID cannot be modified.

          '
        readOnly: true
    description: 'A resource with a non-unique name.

      '
  SyslogServerSettingsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SyslogServerSettingsResponse'
    - $ref: '#/definitions/_errorContextResponse'
  ArraySpaceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/ArraySpace'
  TagBatch:
    type: object
    properties:
      copyable:
        type: boolean
        example: true
        description: 'Specifies whether or not to include the tag when copying the
          parent resource.

          If set to `true`, the tag is included in resource copying.

          If set to `false`, the tag is not included.

          If not specified, defaults to `true`.

          '
      resource:
        type: object
        description: 'A reference to the parent object.

          '
        title: Reference
        allOf:
        - $ref: '#/definitions/_reference'
      namespace:
        type: string
        example: default
        description: 'Optional namespace of the tag.

          Namespace identifies the category of the tag. Omitting the namespace

          defaults to the namespace `default`. The `pure&#42;` namespaces are

          reserved for plugins and integration partners. It is recommended

          that customers avoid using reserved namespaces.

          '
      value:
        type: string
        example: staging
        description: 'Value of the tag. Supports up to 256 Unicode characters.

          '
      key:
        type: string
        example: environment
        description: 'Key of the tag. Supports up to 64 Unicode characters.

          '
  CloudCapacityStepsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/CloudCapacityStep'
  ArrayFactoryResetToken:
    allOf:
    - $ref: '#/definitions/_resource'
    - type: object
      properties:
        token:
          type: integer
          format: int64
          example: 4671919
          description: 'A random number required as input to perform a factory reset
            of the array.

            '
          readOnly: true
      description: 'A token that is required to perform a factory reset of the array.

        A factory reset of the array permanently eradicates all data and deletes all
        configurations,

        restoring the state of the array to the original manufacturer settings.


        '
  SupportRemoteAssistSessionsSettingsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SupportRemoteAssistSessionsSettings'
  TestResultWithResourcePostResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/TestResultWithResource'
  CertificateSigningRequestResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/CertificateSigningRequest'
  SmtpServerResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/SmtpServer'
  ContainerPerformance:
    allOf:
    - $ref: '#/definitions/_performance'
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        others_per_sec:
          type: integer
          format: int64
          description: 'The number of other requests processed per second.

            '
          minimum: 0
        usec_per_other_op:
          type: integer
          format: int64
          description: 'The average time it takes the pod to process an I/O other
            request, measured in

            microseconds.

            '
          minimum: 0
  PodPost:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - type: object
      properties:
        quota_limit:
          type: integer
          format: int64
          description: 'The logical quota limit of the pod, measured in bytes. Must
            be a multiple of 512.

            '
          minimum: 1048576
          maximum: 4503599627370496
        failover_preferences:
          type: array
          description: 'Determines which array within a stretched pod should be given
            priority to stay online

            should the arrays ever lose contact with each other. The current array
            and

            any peer arrays that are connected to the current array for synchronous
            replication can be

            added to a pod for failover preference.

            By default, `failover_preferences=null`,

            meaning no arrays have been configured for failover preference.

            Enter multiple arrays in comma-separated format.

            To clear the list of failover preferences, set to an empty list `[]`.

            '
          items:
            $ref: '#/definitions/_reference'
        source:
          type: object
          description: The source pod from where data is cloned to create the new
            pod.
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        tags:
          type: array
          description: 'The list of tags to be upserted with the object.

            '
          items:
            $ref: '#/definitions/Tag'
      description: 'A pod is an administrative domain, a data container, and a namespace
        for

        volumes and protection groups.

        '
  CertificatePost:
    allOf:
    - $ref: '#/definitions/_certificateBase'
    - type: object
      properties:
        days:
          type: integer
          format: int32
          example: 3650
          description: 'The number of days that the self-signed certificate is valid.
            Defaults to 3650.

            This field can only be specified when creating a new self-signed certificate.

            '
        passphrase:
          type: string
          description: 'The passphrase used to encrypt `key`.

            This field can only be specified when importing a certificate and key
            pair.

            '
        key:
          type: string
          description: 'The text of the private key.

            This field can only be specified when importing a certificate and key
            pair.

            '
  NetworkInterfacePerformanceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where it makes more sense, the average value is displayed instead.

            The values are displayed for each field where meaningful.

            '
          items:
            $ref: '#/definitions/NetworkInterfacePerformance'
        items:
          type: array
          description: A list of performance objects.
          items:
            $ref: '#/definitions/NetworkInterfacePerformance'
  NetworkInterfacePatch:
    type: object
    properties:
      eth:
        $ref: '#/definitions/_networkinterfacepatchEth'
      override_npiv_check:
        type: boolean
        description: 'N-Port ID Virtualization (NPIV) requires a balanced configuration
          of Fibre Channel ports

          configured for SCSI on both controllers. Enabling or Disabling a Fibre Channel
          port

          configured for SCSI might cause the NPIV status to change from enabled to
          disabled or

          vice versa. Set this option to proceed with enabling or disabling the port.

          '
      services:
        type: array
        description: 'The services provided by the specified network interface or
          Fibre Channel port.

          '
        items:
          type: string
          description: 'Valid values include `iscsi`, `management`, `nvme-fc`, `nvme-roce`,

            `offload`, `replication`, and `scsi-fc`.

            '
      enabled:
        type: boolean
        description: 'Returns a value of `true` if the specified network interface
          or Fibre Channel port is

          enabled. Returns a value of `false` if the specified network interface or
          Fibre Channel

          port is disabled.

          '
  ProtectionGroupSnapshotTransferResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/ProtectionGroupSnapshotTransfer'
  DirectorySnapshot:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left until the directory snapshot is permanently
            eradicated, measured in

            milliseconds.

            Before the `time_remaining` period has elapsed, the snapshot can be recovered

            by setting `destroyed=false` if it is destroyed, by setting `policy=""`
            if it is

            managed by a snapshot policy, or by setting `keep_for=""` if it is a manual

            snapshot.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the snapshot has been destroyed
            and is pending eradication.

            The `time_remaining` value displays the amount of time left until the
            destroyed directory

            snapshot is permanently eradicated.

            Before the `time_remaining` period has elapsed, the destroyed directory
            snapshot can be

            recovered by setting `destroyed=false`.

            Once the `time_remaining` period has elapsed,

            the directory snapshot is permanently eradicated and can no longer be
            recovered.

            '
        created:
          type: integer
          format: int64
          description: 'The snapshot creation time, measured in milliseconds since
            the UNIX epoch.

            '
          readOnly: true
        source:
          type: object
          description: 'The directory from which this snapshot was taken.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
        suffix:
          type: integer
          format: int64
          description: 'The suffix that is appended to the `source_name` value and
            the `client_name` value to

            generate the full directory snapshot name in the form of `DIR.CLIENT_NAME.SUFFIX`
            where

            `DIR` is the managed directory name, `CLIENT_NAME` is the client name,
            and `SUFFIX` is

            the suffix. If the suffix is a string, this field returns `null`. See
            the `name` value

            for the full snapshot name including the suffix.

            '
        client_name:
          type: string
          description: 'The customizable portion of the client-visible snapshot name.

            A full snapshot name is constructed in the form of `DIR.CLIENT_NAME.SUFFIX`
            where

            `DIR` is the full managed directory name, `CLIENT_NAME` is the client
            name,

            and `SUFFIX` is the suffix.

            The client-visible snapshot name is `CLIENT_NAME.SUFFIX`.

            '
        space:
          type: object
          description: 'Displays size and space consumption details.

            '
          title: Space
          allOf:
          - $ref: '#/definitions/_space'
        policy:
          type: object
          description: 'The snapshot policy that manages this snapshot, if applicable.

            '
          title: FixedReference
          allOf:
          - $ref: '#/definitions/_fixedReference'
      description: A point-in-time image of the contents of the directory.
  VirtualMachineResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          The values are displayed for each name if meaningful.

          '
        items:
          $ref: '#/definitions/VirtualMachine'
  _directorypolicyexportpostPolicies:
    type: object
    properties:
      export_enabled:
        type: boolean
        example: false
        description: 'If set to `true`, the export is enabled.

          If not specified, defaults to `true`.

          '
      export_name:
        type: string
        description: 'The name of the export to create when applying the export policy
          to the directory.

          '
      policy:
        type: object
        description: 'Reference to the export policy to apply to the directory.

          '
        title: Reference
        allOf:
        - $ref: '#/definitions/_reference'
  PodPerformanceReplicationByArrayGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/PodPerformanceReplicationByArrayResponse'
    - $ref: '#/definitions/_errorContextResponse'
  VolumeSpace:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time:
          type: integer
          format: int64
          description: 'The timestamp of when the data was taken, measured in milliseconds
            since the UNIX epoch.

            '
          readOnly: true
        space:
          type: object
          description: 'Displays size and space consumption information.

            '
          title: VolumeSpaceCommon
          allOf:
          - $ref: '#/definitions/_volumeSpaceCommon'
  _supportRemoteAssistSessionsPaths:
    type: object
    properties:
      component_name:
        type: string
        example: CT0
        description: 'The name of the local controller running the remote assist session.

          '
      status:
        type: string
        example: connected
        description: 'The status of the remote assist session on the local controller.

          Values include `connected`, `connecting`, `disconnected`, and `session-active`.

          '
  _presetWorkloadBase:
    type: object
    required:
    - placement_configurations
    - volume_configurations
    - workload_type
    properties:
      volume_configurations:
        type: array
        description: 'The volumes that will be provisioned by the preset.

          '
        items:
          $ref: '#/definitions/_presetWorkloadVolumeConfiguration'
        maxItems: 5
        minItems: 1
      periodic_replication_configurations:
        type: array
        description: 'The periodic replication configurations that can be applied
          to storage resources (such

          as volumes) within the preset.

          '
        items:
          $ref: '#/definitions/_presetWorkloadPeriodicReplicationConfiguration'
        maxItems: 2
      description:
        type: string
        description: 'A brief description of the workload the preset will configure.

          Supports up to 1KB of unicode characters.

          '
      placement_configurations:
        type: array
        description: 'The placement configurations that can be applied to storage
          resources (such as volumes)

          within the preset. All storage resources associated with the same placement
          will be

          colocated on the same array.

          '
        items:
          $ref: '#/definitions/_presetWorkloadPlacementConfiguration'
        maxItems: 1
        minItems: 1
      workload_tags:
        type: array
        description: The tags that will be associated with workloads provisioned by
          the preset.
        items:
          $ref: '#/definitions/_presetWorkloadWorkloadTag'
      parameters:
        type: array
        description: The parameters to prompt the user when they deploy workloads
          from the preset.
        items:
          $ref: '#/definitions/_presetWorkloadParameter'
      qos_configurations:
        type: array
        description: 'The QoS configurations that can be applied to storage resources
          (such as volumes) within

          the preset.

          '
        items:
          $ref: '#/definitions/_presetWorkloadQosConfiguration'
        maxItems: 5
      snapshot_configurations:
        type: array
        description: 'The snapshot configurations that can be applied to storage resources
          (such as volumes)

          within the preset.

          '
        items:
          $ref: '#/definitions/_presetWorkloadSnapshotConfiguration'
        maxItems: 2
      workload_type:
        type: string
        description: 'The type of workload the preset will configure. Valid values
          include `VDI`, `File`,

          `MySQL` etc.

          '
      revision:
        type: integer
        format: int64
        description: 'A counter that is automatically incremented by the server when
          the preset is updated.

          '
        readOnly: true
    description: 'Workload presets are reusable templates that provision workloads.

      '
  NetworkInterfacesPortDetailsResponse:
    type: object
    properties:
      items:
        type: array
        description: 'A list of port details objects.

          '
        items:
          $ref: '#/definitions/NetworkInterfacesPortDetails'
  Tag:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        copyable:
          type: boolean
          example: true
          description: 'Specifies whether or not to include the tag when copying the
            parent resource.

            If set to `true`, the tag is included in resource copying.

            If set to `false`, the tag is not included.

            If not specified, defaults to `true`.

            '
        resource:
          $ref: '#/definitions/_fixedReference'
        namespace:
          type: string
          example: default
          description: 'Optional namespace of the tag.

            Namespace identifies the category of the tag. Omitting the namespace

            defaults to the namespace `default`. The `pure&#42;` namespaces are

            reserved for plugins and integration partners. It is recommended

            that customers avoid using reserved namespaces.

            '
        value:
          type: string
          example: staging
          description: 'Value of the tag. Supports up to 256 Unicode characters.

            '
        key:
          type: string
          example: environment
          description: 'Key of the tag. Supports up to 64 Unicode characters.

            '
  Policy:
    allOf:
    - $ref: '#/definitions/_resource'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left, measured in milliseconds, until the
            destroyed policy is

            permanently eradicated.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the policy has been destroyed
            and is pending

            eradication.

            The `time_remaining` value displays the amount of time left

            until the destroyed policy is permanently eradicated.

            Once the `time_remaining` period has elapsed,

            the policy is permanently eradicated and can no longer be recovered.

            '
        policy_type:
          type: string
          description: 'The type of policy. Valid values include `autodir`, `nfs`,
            `smb`, `snapshot`, and `quota`.

            '
          readOnly: true
        pod:
          type: object
          description: 'A reference to the pod.

            '
          title: Reference
          allOf:
          - $ref: '#/definitions/_reference'
        enabled:
          type: boolean
          description: 'Returns a value of `true` if the policy is enabled.

            '
  PodReplicaLinkPatch:
    type: object
    properties:
      paused:
        type: boolean
        description: 'Returns a value of `true` if the replica link is to be created
          in a `paused` state.

          Returns a value of `false` if the replica link is to be created not in a
          `paused` state.

          If not specified, defaults to `false`.

          '
  _adminSettingsRequiredAuthenticationMethods:
    type: object
    properties:
      web_ui:
        type: array
        description: 'List of authentication methods that are required for the web
          UI.

          Possible values include `password`, `webauthn`, and `default`.

          Other possible values include `saml2` and `securid-am`

          but these cannot be changed through PATCH.

          If not specified, defaults to `default`.

          Specify `password` and `webauthn` authentication methods

          to set up local multi-factor authentication for web UI.

          External multi-factor authentication is configured through SAML2 SSO endpoints.

          '
        items:
          type: string
          description: 'Authentication method required for the web UI.

            '
      ssh:
        type: array
        description: 'List of authentication methods that are required for SSH.

          Possible values include `password`, `key`, and `default`.

          `securid-am` is a possible value, but it cannot be changed through PATCH.

          If not specified, defaults to `default`.

          Specify `password` and `key` authentication methods to set up

          local multi-factor authentication for SSH.

          '
        items:
          type: string
          description: 'Authentication method required for SSH.

            '
    description: 'Authentication methods that are required for the array on specific
      user

      interfaces.

      '
  SupportDiagnosticsDetailsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/SupportDiagnosticsDetailsResponse'
  ProtectionGroupPost:
    type: object
    properties:
      tags:
        type: array
        description: 'The list of tags to be upserted with the object.

          '
        items:
          $ref: '#/definitions/Tag'
  _fixedReferenceNoId:
    type: object
    properties:
      name:
        type: string
        description: 'The resource name,

          such as volume name, pod name, snapshot name, and so on.

          '
        readOnly: true
  OffloadResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Offload'
  VchostCertificatePost:
    type: object
    properties:
      endpoints:
        type: array
        description: 'The IPv4 or IPv6 addresses of the endpoints to configure for
          the vchost, over which the

          configured certificate will be presented.

          '
        items:
          type: string
          example: 1.2.3.4
      certificate:
        type: object
        description: 'The certificate to be presented by the vchost over the configured
          endpoints.

          '
        title: ReferenceNoIdWithType
        allOf:
        - $ref: '#/definitions/_referenceNoIdWithType'
  _presetWorkloadQosConfiguration:
    type: object
    required:
    - name
    properties:
      name:
        type: string
        description: 'The name of the QoS configuration, by which other configuration
          objects in the preset

          can reference it. Name must be unique across all configuration objects in
          the preset.

          '
      bandwidth_limit:
        type: string
        description: 'The QoS IOPs limit shared across all volumes in the placement.

          Between 100 and 100000000, inclusive. Supports parameterization.

          '
      iops_limit:
        type: string
        description: 'The maximum QoS bandwidth limit shared across all volumes in
          the placement.

          Whenever throughput exceeds the bandwidth limit, throttling occurs.

          Measured in bytes per second. Between 1MB/s and 512 GB/s, inclusive.

          Supports parameterization.

          '
  AlertWatcherResponse:
    type: object
    properties:
      items:
        type: array
        description: A list of alert watcher objects.
        items:
          $ref: '#/definitions/AlertWatcher'
  ProtectionGroupSnapshotPatch:
    allOf:
    - $ref: '#/definitions/ProtectionGroupSnapshot'
    - type: object
      properties:
        suffix:
          type: string
          readOnly: true
  Member:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        member:
          $ref: '#/definitions/_reference'
        group:
          $ref: '#/definitions/_reference'
  AppGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/AppResponse'
  LocalUserGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/LocalUserResponse'
  _workloadParameterValueResourceReference:
    type: object
    properties:
      name:
        type: string
        description: 'The name of the resource to reference.

          One of `id` or `name` must be set, but they cannot be set together.

          '
      resource_type:
        type: string
        description: 'The type of the resource to reference.

          Resource type is optional, and will be automatically determined by the

          server if not set.

          '
      id:
        type: string
        description: 'The id of the resource to reference.

          One of `id` or `name` must be set, but they cannot be set together.

          '
    description: The value for a resource reference parameter.
  _policynfspostPolicyMapping:
    type: object
    properties:
      mapping:
        type: string
        description: 'The mapping of this policy. Valid values are `connected` and
          `disconnected`.

          '
    description: 'If set specifies the initial mapping of the policy upon its

      creation across a pod replica link.

      Policy being created must be in a source pod of a replica-link.

      '
  PolicyRuleQuota:
    allOf:
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        quota_limit:
          type: integer
          format: int64
          description: 'Logical space limit of the quota assigned by the rule, measured
            in bytes.

            This value cannot be set to 0.

            '
          readOnly: true
        time_remaining:
          type: integer
          format: int64
          description: 'The amount of time left, in milliseconds, until the destroyed
            quota policy rule is

            permanently eradicated.

            '
          readOnly: true
        enforced:
          type: boolean
          description: 'Defines whether the quota rule is enforced or unenforced.

            If the quota rule is enforced and logical space usage exceeds the quota
            limit,

            any modification operations that result in a need for more space are blocked.

            If the quota rule is unenforced and logical space usage exceeds the quota
            limit, notification

            emails are sent to targets that are specified using the `notification`
            parameter.

            No client operations are blocked when an unenforced limit is exceeded.

            If set to `true`, the limit is enforced. If set to `false`, notification
            targets are informed

            when the usage exceeds 80 percent of the limit.

            '
          readOnly: true
        destroyed:
          type: boolean
          description: 'Returns a value of `true` if the pod containing the quota
            policy rule has been destroyed and

            is pending eradication.

            The `time_remaining` value displays the amount of time left until the
            destroyed policy is

            permanently eradicated.

            Once the `time_remaining` period has elapsed, the quota policy rule is
            permanently

            eradicated and can no longer be recovered.

            '
        name:
          type: string
          description: 'Name of this rule. The name is automatically generated by
            the system.

            '
          readOnly: true
        notifications:
          type: string
          description: 'Targets to notify when usage approaches the quota limit.

            The list of notification targets is a comma-separated string.

            Valid values are `user`, and `group`.

            If not specified, notification targets are not assigned for the rule.

            '
          readOnly: true
        policy:
          type: object
          description: The policy to which this rule belongs.
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
  _snapshotSpace:
    allOf:
    - $ref: '#/definitions/_space'
    - type: object
      properties:
        snapshots_effective:
          type: integer
          format: int64
          description: 'This field has been deprecated.

            The effective space contributed by data unique to one or more snapshots,

            measured in bytes.

            Use the `snapshots` field in the future, as it contains the same information

            for Evergreen//One arrays.

            '
          readOnly: true
          minimum: 0
  DirectoryPost:
    type: object
    properties:
      path:
        type: string
        description: 'Path of the managed directory in the file system.

          '
      directory_name:
        type: string
        description: 'The managed directory name without the file system name prefix.

          A full managed directory name is constructed in the form of `FILE_SYSTEM:DIR`
          where

          `FILE_SYSTEM` is the file system name and `DIR` is the value of this field.

          `directory_name` is required if `file_system_names` or `file_system_ids`
          is set.

          `directory_name` cannot be set if `names` is set.

          '
  ArraySpace:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/_context'
    - type: object
      properties:
        parity:
          type: number
          format: float
          example: 0.99
          description: 'A representation of data redundancy on the array.

            Data redundancy is rebuilt automatically by the system whenever parity
            is less than 1.0.

            '
          readOnly: true
        capacity_installed:
          type: integer
          format: int64
          example: 46179488366592
          description: 'Usable capacity of the array if entitled to full capacity
            for all drives.

            '
        time:
          type: integer
          format: int64
          readOnly: true
        space:
          type: object
          title: Space
          allOf:
          - $ref: '#/definitions/Space'
        capacity:
          type: integer
          format: int64
          example: 10995116277760
          description: 'Currently entitled usable capacity.

            '
          readOnly: true
  ContainerDefaultProtectionResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/ContainerDefaultProtection'
  SoftwareVersion:
    type: object
    properties:
      upgrade_hops:
        type: array
        description: 'The list of software versions the upgrade will go through.

          '
        items:
          type: string
          description: 'A software version in the upgrade path.

            '
      name:
        type: string
        example: Purity//FA
        description: 'The name of the software.

          '
      details:
        type: string
        description: 'The description of the version.

          '
      version:
        type: string
        example: 6.2.0
        description: 'The version of the software.

          '
      release_family:
        type: string
        example: 6.2.0
        description: 'The major and minor release number of the version.

          '
    description: Version candidates for software upgrade.
  FleetGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/FleetResponse'
  CertificateGroupUse:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - type: object
      properties:
        use:
          type: object
          description: 'A reference to an object using this certificate group.

            '
          title: FixedReferenceWithType
          allOf:
          - $ref: '#/definitions/_fixedReferenceWithType'
  Software:
    allOf:
    - $ref: '#/definitions/_resourceFixedNonUniqueName'
    - type: object
      properties:
        payload_id:
          type: string
          example: 4f7b319106401d072ff94808535cb3ce
          description: 'A checksum hash referring to the update bundle.

            '
        upgrade_plan:
          type: array
          description: 'A list of steps that are planned to run during the upgrade
            in an optimal scenario

            (i.e., all upgrade checks pass, no step is retried, and the upgrade is
            not aborted).

            Steps are listed in the order that they should occur.

            '
          items:
            $ref: '#/definitions/_softwareUpgradePlan'
        upgrade_hops:
          type: array
          description: 'The plan by which the upgrade will be conducted. The first
            element is the current

            version, the last element is the destination version, and the elements
            in

            between are intermediate versions.

            '
          uniqueItems: true
          items:
            type: string
        progress:
          type: number
          format: float
          description: 'The progress of the software upgrade. Displayed in decimal
            format.

            '
        details:
          type: string
          description: 'The detailed reason for the `status`.

            '
        version:
          type: string
          example: 5.2.3
          description: 'The version of the software package.

            '
        status:
          type: string
          example: Available
          description: 'The status of the software package.

            Valid values are `available`, `downloaded`, `downloading`, `download_failed`,
            `checking`,

            `installing`, `new`, `paused`, `aborting`, `abort`, `canceled`, `partially_installed`,
            and

            `installed`.

            A status of `available` indicates that the package is available for download.
            This only

            applies if `automatic-download` is not enabled.

            A status of `downloaded` indicates that the package is downloaded and
            ready for

            installation.

            A status of `downloading` indicates that the package is currently downloading.

            A status of `download_failed` indicates that the download of the package
            failed.

            A status of `checking` indicates that the package is currently running
            in `check-only` mode.

            A status of `installing` indicates that the package is currently installing.

            A status of `new` indicates that Pure1 cloud is still building the package.

            A status of `paused` indicates that the upgrade is paused and waiting
            for user input to

            proceed.

            A status of `aborting` indicates that the upgrade is being aborted, due
            to an unrecoverable

            error or an `abort` command issued by the user.

            A status of `canceled` indicates that the upgrade has been canceled.

            A status of `partially_installed` indicates that the upgrade has been
            partially installed

            due to an `abort`. The array has been upgraded to an intermediate version
            and the `software`

            is no longer available for installation.

            A status of `installed` indicates that the upgrade has finished.

            '
      description: Software to be installed on the array.
  ApiClientResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Returns a list of all items after filtering.

          The values are displayed for each name, where meaningful.

          '
        items:
          $ref: '#/definitions/ApiClient'
  _policyrulesnapshotpostRules:
    type: object
    properties:
      at:
        type: integer
        format: int64
        description: 'Specifies the number of milliseconds since midnight at which
          to take a snapshot.

          The `at` value can only be set to an hour and must be between 0 and 82800000,
          inclusive.

          The `at` value can only be set on the rule with the smallest `every` value.

          The `at` value cannot be set if the `every` value is not measured in days.

          The `at` value can only be set for at most one rule in the same policy.

          '
      keep_for:
        type: integer
        format: int64
        description: 'Specifies the period that snapshots are retained before they
          are eradicated, in

          milliseconds.

          The `keep_for` value cannot be less than the `every` value of the rule.

          The `keep_for` value must be unique for each rule in the same policy.

          The `keep_for` value must be between 5 minutes and 5 years.

          The `keep_for` value cannot be less than the `keep_for` value of any rule
          in the same policy

          with a smaller `every` value.

          '
      suffix:
        type: string
        description: 'The snapshot suffix name.

          A full snapshot name is constructed in the form of `DIR.CLIENT_NAME.SUFFIX`
          where

          `DIR` is the managed directory name, `CLIENT_NAME` is the snapshot client
          name,

          and `SUFFIX` is the snapshot suffix.

          The client-visible snapshot name is `CLIENT_NAME.SUFFIX`.

          The `suffix` value can only be set for one rule in the same policy.

          The `suffix` value can only be set on a rule with the same `keep_for` value

          and `every` value.

          The `suffix` value can only be set on the rule with the largest `keep_for`
          value.

          If not specified, defaults to a monotonically increasing number generated

          by the system.

          '
      client_name:
        type: string
        description: 'The snapshot client name.

          A full snapshot name is constructed in the form of `DIR.CLIENT_NAME.SUFFIX`
          where

          `DIR` is the managed directory name, `CLIENT_NAME` is the snapshot client
          name,

          and `SUFFIX` is the snapshot suffix.

          The client-visible snapshot name is `CLIENT_NAME.SUFFIX`.

          '
      every:
        type: integer
        format: int64
        description: 'Specifies the interval between snapshots, in milliseconds.

          The `every` value for all rules must be multiples of one another.

          The `every` value must be unique for each rule in the same policy.

          The `every` value must be between 5 minutes and 1 year.

          '
  VolumeSpaceGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where it makes more sense, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/VolumeSpace'
        items:
          type: array
          description: 'Displays a list of all items after filtering.

            If applicable, the values are displayed for each name.

            If `total_only=true`, the `items` list will be empty.

            '
          items:
            $ref: '#/definitions/VolumeSpace'
  _time_window:
    type: object
    properties:
      start:
        type: integer
        format: int64
        example: 3600000
        description: The window start time. Measured in milliseconds since midnight.
          The time must be set on the hour. (e.g., `18000000`, which is equal to 5:00
          AM).
      end:
        type: integer
        format: int64
        example: 46800000
        description: The window end time. Measured in milliseconds since midnight.
          The time must be set on the hour. (e.g., `28800000`, which is equal to 8:00
          AM).
  Saml2Sso:
    allOf:
    - $ref: '#/definitions/_resourceFixedNonUniqueName'
    - type: object
      properties:
        idp:
          $ref: '#/definitions/_saml2SsoIdp'
        sp:
          $ref: '#/definitions/_saml2SsoSp'
        enabled:
          type: boolean
          description: 'If set to `true`, the SAML2 SSO configuration is enabled.

            '
        array_url:
          type: string
          example: https://myarray.mycompany.com
          description: 'The URL of the array.

            '
      description: 'Configuration information for SAML2-based SSO for FlashArray.

        '
  _fleetmemberpostMembers:
    type: object
    properties:
      member:
        $ref: '#/definitions/_fleetmemberpostMember'
      key:
        type: string
        description: 'Provide the `fleet key` generated from any array in the fleet.

          '
  AlertRulesGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/AlertRulesResponse'
    - $ref: '#/definitions/_errorContextResponse'
  LogTargetsResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/LogTarget'
  ProtectionGroupSnapshotTransferGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ProtectionGroupSnapshotTransferResponse'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        total:
          type: array
          description: 'The aggregate value of all items after filtering.

            Where it makes more sense, the average value is displayed instead.

            If applicable, the values are displayed for each field.

            '
          items:
            $ref: '#/definitions/ProtectionGroupSnapshotTransfer'
  AdminCache:
    allOf:
    - $ref: '#/definitions/_fixedNameResourceNoId'
    - type: object
      properties:
        management_access_policies:
          type: array
          description: 'List of management access policies associated with the administrator.

            '
          readOnly: true
          items:
            $ref: '#/definitions/_fixedReferenceWithType'
        role:
          type: object
          title: AdminRole
          allOf:
          - $ref: '#/definitions/_adminRole'
        cached_at:
          type: integer
          format: int64
          description: 'Time when the entry was added to the cache, in milliseconds
            since the

            UNIX epoch.

            '
          readOnly: true
      description: 'A cache entry for the admin cache to quickly look up admin roles.

        '
  FileSystemResponse:
    type: object
    properties:
      items:
        type: array
        description: 'Displays a list of all items after filtering.

          If applicable, the values are displayed for each name.

          '
        items:
          $ref: '#/definitions/FileSystem'
  ResourceAccessesGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/ResourceAccessesResponse'
  AuditGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/AuditResponse'
    - $ref: '#/definitions/_errorContextResponse'
  LogTargetsGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/LogTargetsResponse'
    - $ref: '#/definitions/_errorContextResponse'
  PortGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/_errorContextResponse'
    - type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/definitions/Port'
  ProtectionGroupSnapshotPost:
    allOf:
    - $ref: '#/definitions/_builtIn'
    - $ref: '#/definitions/ProtectionGroupSnapshot'
    - type: object
      properties:
        destroyed:
          type: boolean
          readOnly: true
  RemoteProtectionGroupSnapshotGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/RemoteProtectionGroupSnapshotResponse'
    - $ref: '#/definitions/_errorContextResponse'
  ResourcePerformanceByArray:
    allOf:
    - $ref: '#/definitions/ResourcePerformance'
    - type: object
      properties:
        array:
          type: object
          description: 'The array on which the performance metrics were recorded.

            '
          title: Resource
          allOf:
          - $ref: '#/definitions/_resource'
  _localgroupmembershippostMembers:
    type: object
    properties:
      member_id:
        type: integer
        format: int32
        description: The member ID that should be mapped.
      member:
        type: object
        description: 'Reference to the resource that will be a member of the group.

          The `id` or `name` parameter is required, but cannot be set together.

          '
        title: ReferenceWithType
        allOf:
        - $ref: '#/definitions/_referenceWithType'
  _offloadGoogleCloud:
    type: object
    properties:
      access_key_id:
        type: string
        description: The access key ID of the Google Cloud account used to create
          a connection between the array and a Google Cloud offload target. The access
          key ID is 24 characters in length and is only accepted when creating the
          connection between the array and the Google Cloud offload target. The `access_key_id`,
          `secret_access_key`, and `bucket` parameters must be set together.
      bucket:
        type: string
        description: The name of the Google Cloud Storage bucket to which the data
          will be offloaded. Grant basic read and write access permissions to the
          bucket and verify that the bucket is empty of all objects. The `access_key_id`,
          `secret_access_key`, and `bucket` parameters must be set together.
      profile:
        type: string
        description: 'The offload target profile that will be selected for this target.
          This option

          allows more granular configuration for the target on top of the `protocol`

          parameter. Values include `gcp`.

          '
      secret_access_key:
        type: string
        description: The secret access key that goes with the access key ID of the
          Google Cloud account. The secret access key is 40 characters in length is
          only accepted when creating the connection between the array and the Google
          Cloud offload target. The `access_key_id`, `secret_access_key`, and `bucket`
          parameters must be set together.
  ApiClientPost:
    type: object
    properties:
      public_key:
        type: string
        example: '-----BEGIN PUBLIC KEY-----

          

          -----END PUBLIC KEY-----

          '
        description: 'The API client''s PEM formatted (Base64 encoded) RSA public
          key.

          Include the `-----BEGIN PUBLIC KEY-----` and `-----END PUBLIC KEY-----`
          lines.

          '
      max_role:
        type: string
        example: storage_admin
        description: 'Deprecated. The maximum Admin Access Policy (previously ''role'')
          allowed for ID

          Tokens issued by this API client. The bearer of an access token will be

          authorized to perform actions within the intersection of this policy and

          that of the array user specified as the JWT `sub` (subject) claim.

          `max_role` is deprecated in favor of `access_policies`, but remains for
          backwards

          compatibility. If `max_role` is the name of a valid legacy role, it will
          be

          interpreted as the corresponding access policy of the same name.

          Otherwise, it''s invalid.

          '
      access_policies:
        type: array
        description: 'The access policies allowed for ID Tokens issued by this API
          client.

          The bearer of an access token will be authorized to perform actions

          within the intersection of these policies and those of the array

          user specified as the JWT `sub` (subject) claim.

          '
        items:
          $ref: '#/definitions/_referenceWithType'
      issuer:
        type: string
        example: https://purestorage.idp.okta.com
        description: 'The name of the identity provider that will be issuing ID Tokens
          for this API client.

          The `iss` claim in the JWT issued must match this string.

          If not specified, defaults to the API client name.

          '
      access_token_ttl_in_ms:
        type: integer
        format: int64
        example: 15000
        description: 'The TTL (Time To Live) length of time for the exchanged access
          token.

          Measured in milliseconds. If not specified, defaults to `86400000`.

          '
        x-minimum: 1000
        x-maximum: 86400000
  UsernameResponse:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: '#/definitions/Username'
  AdminApiTokenGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - $ref: '#/definitions/AdminApiTokenResponse'
  _fleetmemberpostMember:
    type: object
    properties:
      name:
        type: string
        description: 'The resource name.

          '
      resource_type:
        type: string
        description: 'Type of the object (e.g. remote-arrays)

          '
      id:
        type: string
        description: 'A globally unique, system-generated ID.

          The ID cannot be modified.

          '
  VirtualMachineGetResponse:
    allOf:
    - $ref: '#/definitions/PageInfo'
    - type: object
      properties:
        items:
          type: array
          description: 'Displays a list of all items after filtering.

            The values are displayed for each name if meaningful.

            '
          items:
            $ref: '#/definitions/VirtualMachine'
parameters:
  From_member_ids:
    name: from_member_ids
    in: query
    description: 'Move the resource from the specified local member realm or array.

      This should be a union of all local realms and arrays from which the resource
      should be removed.

      Enter multiple IDs in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Namespaces:
    name: namespaces
    in: query
    description: 'A comma-separated list of namespaces.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Role_names:
    name: role_names
    in: query
    description: 'This field has been deprecated.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Local_pod_names:
    name: local_pod_names
    in: query
    description: 'A comma-separated list of local pod names. If, after filtering,
      there is not at least one resource

      that matches each of the elements, then an error is returned. This cannot be
      provided together

      with the `local_pod_ids` query parameter.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Encrypted:
    name: encrypted
    in: query
    description: 'If `true`, the returned array connection key will encode an encryption
      key

      used to establish trust and secure replication traffic.

      If `false`, the returned array connection key will not encode an encryption
      key.

      If not specified, defaults to `false`.

      '
    required: false
    type: boolean
  Group_sids:
    name: group_sids
    in: query
    description: 'Performs the operation on the specified group SID.

      Enter multiple group SIDs in comma-separated format.

      For example, `S-1-2-532-582374278-329482934,S-1-2-532-234235245-423425234`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Generate_new_key:
    name: generate_new_key
    in: query
    description: 'If set to `true`, a new private key is generated when generating
      a new certificate with the

      specified attributes. This may not be set to `true` when importing a certificate
      and private key,

      and may not be set to `false` when generating a new self-signed certificate
      to replace a

      certificate that was imported. Default setting is `false`.

      '
    required: false
    type: boolean
  Resource_names:
    name: resource_names
    in: query
    description: 'A comma-separated list of resource names.

      The `resource_ids` or `resource_names` parameter is required, but they cannot
      be set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Initialize:
    name: initialize
    in: query
    description: 'If set to `true`, initializes the Amazon S3/Azure Blob container/Google
      Cloud Storage

      in preparation for offloading. The parameter must be set to `true`

      if this is the first time the array is connecting to the offload target.

      '
    required: false
    type: boolean
  Vchost_names:
    name: vchost_names
    in: query
    description: 'Performs the operation on the unique vchost name specified.

      Enter multiple names in a comma-separated format.

      For example, `vchost01,vchost02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Protocol_endpoint_ids_for_vchost_connections:
    name: protocol_endpoint_ids
    in: query
    description: 'A comma-separated list of protocol endpoint IDs.

      Performs the operation on the protocol endpoints specified.

      For example, `peid01,peid02`.

      Cannot be used in conjunction with `protocol_endpoint_names`.

      If the list contains more than one value,

      then `vchost_ids` or `vchost_names` must have exactly one value.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Alert_rules_parameter:
    name: parameter
    in: query
    description: 'The parameter of the custom alert rule to modify. Values include
      `info` and `warning`.

      Available parameter values can be found in the alert rules catalog.

      '
    required: true
    type: string
  History_start_time:
    name: start_time
    in: query
    description: 'Displays historical performance data for the specified time window,

      where `start_time` is the beginning of the time window,

      and `end_time` is the end of the time window.

      The `start_time` and `end_time` parameters

      are specified in milliseconds since the UNIX epoch.

      If `start_time` is not specified,

      the start time will default to one resolution before the end time,

      meaning that the most recent sample of performance data will be displayed.

      If `end_time`is not specified, the end time will default to the current time.

      Include the `resolution` parameter to display the performance data at the specified
      resolution.

      If not specified, `resolution` defaults to the lowest valid resolution.


      '
    required: false
    type: integer
    format: int64
  NamesSemiRequired:
    name: names
    in: query
    description: 'Performs the operation on the unique name specified.

      For example, `name01`.

      Enter multiple names in comma-separated format.

      The `ids` or `names` parameter is required, but they cannot be set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Block_size:
    name: block_size
    in: query
    description: 'Granularity at which to compare, in bytes. Must be a multiple of
      512.

      '
    required: true
    type: integer
    format: int64
  Local_port_names:
    name: local_port_names
    in: query
    description: 'Performs the operation on the unique local port name specified.

      Enter multiple names in comma-separated format.

      For example, `ct0.eth0,ct1.eth0`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Certificate_ids:
    name: certificate_ids
    in: query
    description: 'A comma-separated list of certificate ids.

      If there is not at least one resource that matches each of the

      elements of `certificate_ids`, then an error is returned.

      This cannot be provided in conjunction with the `certificate_names`

      parameter.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Move_with_host_names:
    name: move_with_host_names
    in: query
    description: 'The hosts to be moved together with the pods to the specified local
      member realm or array.

      Enter multiple names in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Policy_idsUnrestricted:
    name: policy_ids
    in: query
    description: 'Performs the operation on the unique policy IDs specified.

      Enter multiple policy IDs in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Finalize_array_erasure_required:
    name: finalize
    in: query
    description: 'Must be `true` to finalize factory reset.

      '
    required: true
    type: boolean
  Perf_protocol_group:
    name: protocol_group
    in: query
    description: 'Protocol group type. Valid values are `block`, `file`, and `all`.

      '
    required: false
    type: string
  From_member_names:
    name: from_member_names
    in: query
    description: 'Move the resource from the specified local member realm or array.

      This should be a union of all local realms and arrays to be removed from the
      specified resource.

      Enter multiple names in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Move_with_host_group_names:
    name: move_with_host_group_names
    in: query
    description: 'The host groups to be moved together with the pods to the specified
      local member realm or array.

      All the hosts in the host groups will also be moved.

      Enter multiple names in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Next_allocated_block_hint:
    name: next_allocated_block_hint
    in: query
    description: 'If set to `true`, appends offset of the next allocated block in
      bytes

      for the volume specified in `names` or `ids` to the output `items`.

      `base_name` or `base_id` must not be specified if this is set to `true`.

      If not specified, defaults to `false`.

      '
    required: false
    type: boolean
  OnSingle:
    name: 'on'
    in: query
    description: 'Performs the operation on the target name specified.

      For example, `targetName01`.

      '
    required: false
    type: string
  Member_sids:
    name: member_sids
    in: query
    description: 'Performs the operation on the specified member SID.

      Enter multiple member SIDs in comma-separated format.

      For example, `S-1-2-532-582374278-329482934,S-1-2-532-234235245-423425234`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Total_only:
    name: total_only
    in: query
    description: 'If set to `true`, returns the aggregate value of all items after
      filtering.

      Where it makes more sense, the average value is displayed instead.

      The values are displayed for each name where meaningful.

      If `total_only=true`, the `items` list will be empty.

      '
    required: false
    type: boolean
  Source_ids:
    name: source_ids
    in: query
    description: 'Performs the operation on the source ID specified.

      Enter multiple source IDs in comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Volume_names:
    name: volume_names
    in: query
    description: 'Performs the operation on the volume specified.

      Enter multiple names in comma-separated format.

      For example, `vol01,vol02`.

      A request cannot include a mix of multiple objects with multiple names.

      For example, a request cannot include a mix of multiple volume names and host
      names;

      instead, at least one of the objects (e.g., `volume_names`) must be set to only
      one name

      (e.g., `vol01`).

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Destroyed:
    name: destroyed
    in: query
    description: 'If set to `true`, lists only destroyed objects

      that are in the eradication pending state.

      If set to `false`, lists only objects that are not destroyed.

      For destroyed objects, the time remaining is displayed in milliseconds.

      '
    required: false
    type: boolean
  Expose_api_token:
    name: expose_api_token
    in: query
    description: 'If `true`, exposes the API token of the current user.

      '
    required: false
    type: boolean
  Certificate_group_names:
    name: certificate_group_names
    in: query
    description: 'A comma-separated list of certificate group names.

      If no resource matches each of the elements of `certificate_group_names`,

      then an error is returned.

      This cannot be provided in conjunction with the `certificate_group_ids` parameter.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  With_unknown:
    name: with_unknown
    in: query
    description: 'If set to `true`, unstretches the specified pod from the specified
      array by force.

      Use the `with_unknown` parameter in the following rare event&#58; the local
      array goes offline

      while the pod is still stretched across two arrays,

      the status of the remote array becomes unknown,

      and there is no guarantee that the pod is online elsewhere.

      '
    required: false
    type: boolean
  Software_installation_current_step_idRequired:
    name: current_step_id
    in: query
    description: 'The current step `id` of the installation.

      '
    required: true
    type: string
  Placement_names:
    name: placement_names
    in: query
    description: 'Placements from the preset which should be used to compute recommendation.

      Optional parameter if preset has just one placement.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Software_installation_ids:
    name: software_installation_ids
    in: query
    description: 'A comma-separated list of software installation IDs.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  For_replication:
    name: for_replication
    in: query
    description: 'If `true`, destroys and eradicates the snapshot after 1 hour.

      '
    required: false
    type: boolean
  Software_installation_command_required:
    name: command
    in: query
    description: 'A user command that interacts with the upgrade. Commands may only
      be issued when the upgrade

      is paused. Valid values are `continue`, `retry`, and `abort`.

      The `continue` command continues a `paused` upgrade.

      The `retry` command retries the previous step.

      The `abort` command aborts the upgrade.

      '
    required: true
    type: string
  Offset:
    name: offset
    in: query
    description: 'The starting position based on the results of the query

      in relation to the full set of response objects returned.

      '
    required: false
    type: integer
    minimum: 0
    format: int32
  Gids:
    name: gids
    in: query
    description: 'Performs the operation on the specified GIDs.

      Enter multiple GIDs in comma-separated format.

      For example, `4234235,9681923`.

      '
    required: false
    type: array
    items:
      type: integer
      format: int32
    collectionFormat: csv
  Versions:
    name: versions
    in: query
    description: 'A comma-separated list of versions.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Base_name:
    name: base_name
    in: query
    description: 'Name of the volume or volume snapshot that the diff comparison is
      based on.

      If a base volume or volume snapshot is not specified,

      all mapped blocks for the volume or volume snapshot are displayed.

      The `base_name` or `base_id` parameter is required, but they cannot be set together.

      '
    required: false
    type: string
  Keys:
    name: keys
    in: query
    description: 'A comma-separated list of tag keys.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Host_names:
    name: host_names
    in: query
    description: 'Performs the operation on the hosts specified.

      Enter multiple names in comma-separated format.

      For example, `host01,host02`.

      A request cannot include a mix of multiple objects with multiple names.

      For example, a request cannot include a mix of multiple host names and volume
      names;

      instead, at least one of the objects (e.g., `host_names`) must be set to only
      one name

      (e.g., `host01`).

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Skip_quiesce:
    name: skip_quiesce
    in: query
    description: 'Set to `true` to demote the pod without quiescing the `pod-replica-link`
      and allow the pod to

      become a target of the remote pod.

      This stops all pending replication to the remote pod.

      '
    required: false
    type: boolean
  Container_ids:
    name: ids
    in: query
    description: 'Performs the operation on the container ID specified.

      Enter multiple source IDs in comma-separated format.

      The `ids` or `names` parameter is required, but they cannot be set together.

      Specifying `ids` or `names` with no value will perform the operation on local
      array.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Destroy_contents:
    name: destroy_contents
    in: query
    description: 'Set to `true` to destroy contents (e.g., volumes, protection groups,
      snapshots) and containers

      (e.g., realms, pods, volume groups), including eradicating containers with content.

      '
    required: false
    type: boolean
  IdsSemiRequired:
    name: ids
    in: query
    description: 'Performs the operation on the unique resource IDs specified.

      Enter multiple resource IDs in comma-separated format.

      The `ids` or `names` parameter is required, but they cannot be set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  To_member_ids:
    name: to_member_ids
    in: query
    description: 'The resource will be moved to the specified local member realm or
      array.

      Enter multiple IDs in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  On_:
    name: 'on'
    in: query
    description: 'Performs the operation on the target name specified.

      Enter multiple target names in comma-separated format.

      For example, `targetName01,targetName02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Vchost_ids_for_vchost_connections:
    name: vchost_ids
    in: query
    description: 'A comma-separated list of vchost IDs.

      Performs the operation on the vchosts specified.

      For example, `vchostid01,vchostid02`.

      Cannot be used in conjunction with `vchost_names`.

      If the list contains more than one value,

      then `protocol_endpoint_ids` or `protocol_endpoint_names` must have exactly
      one value.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Add_to_protection_group_ids:
    name: add_to_protection_group_ids
    in: query
    description: 'The volumes will be added to the specified protection groups along
      with creation or movement

      across pods and array.

      When a volume is moved, the specified protection groups must be in the target
      pod or array.

      Enter multiple ids in comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Remote_policy_ids:
    name: remote_policy_ids
    in: query
    description: 'A comma-separated list of remote policy IDs. If, after filtering,
      there is not at least one

      resource that matches each of the elements, then an error is returned. This
      cannot be

      provided together with the `remote_policy_names` query parameter.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  With_default_protection:
    name: with_default_protection
    in: query
    description: 'If specified as `true`, the initial protection of the newly created
      volumes will be

      the union of the container default protection configuration and

      `add_to_protection_group_names`.

      If specified as `false`, the default protection of the container will not be
      applied

      automatically. The initial protection of the newly created volumes will be configured

      by `add_to_protection_group_names`.

      If not specified, defaults to `true`.

      '
    required: false
    type: boolean
  Delete_sanitization_certificate_required:
    name: delete_sanitization_certificate
    in: query
    description: 'Set to `true` to finalize factory reset and acknowledge the deletion
      of sanitization certificate.

      '
    required: true
    type: boolean
  Software_versions:
    name: software_versions
    in: query
    description: 'A comma-separated list of target software versions.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Source_names:
    name: source_names
    in: query
    description: 'Performs the operation on the source name specified.

      Enter multiple source names in comma-separated format. For example, `name01,name02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Include_remote:
    name: include_remote
    in: query
    description: 'If set to `true` the response will include remote membership for
      protection groups that belong

      to the remote arrays as well as local volumes.

      Defaults to `false`.

      '
    required: false
    type: boolean
  Base_id:
    name: base_id
    in: query
    description: 'ID of the volume or volume snapshot that the diff comparison is
      based on.

      If a base volume or volume snapshot is not specified,

      all mapped blocks for the volume or volume snapshot are displayed.

      The `base_name` or `base_id` parameter is required, but they cannot be set together.

      '
    required: false
    type: string
  Reinstall_image_required:
    name: reinstall_image
    in: query
    description: 'Set to `true` to reinstall image, or `false` to skip reinstalling
      image.

      '
    required: true
    type: boolean
  Protocol_endpoint_names_for_vchost_connections:
    name: protocol_endpoint_names
    in: query
    description: 'A comma-separated list of protocol endpoint names.

      Performs the operation on the protocol endpoints specified.

      For example, `pe01,pe02`.

      Cannot be used in conjunction with `protocol_endpoint_ids`.

      If the list contains more than one value,

      then `vchost_ids` or `vchost_names` must have exactly one value.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Group_mapping_names_required:
    name: names
    in: query
    description: 'Performs the operation on the unique names specified.

      For example, `GroupRoleMappingName`.

      Enter multiple names in comma-separated format.

      '
    required: true
    type: array
    items:
      type: string
    collectionFormat: csv
  Vchost_names_for_vchost_connections:
    name: vchost_names
    in: query
    description: 'A comma-separated list of vchost names.

      Performs the operation on the vchosts specified.

      For example, `vchost01,vchost02`.

      Cannot be used in conjunction with `vchost_ids`.

      If the list contains more than one value,

      then `protocol_endpoint_ids` or `protocol_endpoint_names` must have exactly
      one value.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Authorization:
    name: Authorization
    in: header
    description: 'Access token (in JWT format) required to use any API

      endpoint (except `/oauth2`, `/login`, and `/logout`)

      '
    required: false
    type: string
  Names:
    name: names
    in: query
    description: 'Performs the operation on the unique name specified.

      Enter multiple names in comma-separated format.

      For example, `name01,name02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Refresh:
    name: refresh
    in: query
    description: 'If set to `true`, the array will attempt to communicate with

      the connection peer in order to update

      the connection attributes on both arrays with any changes that have occurred.

      If set to `true`, other array connection attributes may not be modified in requests.

      Default value is `false`.

      '
    required: false
    type: boolean
  Abort_quiesce:
    name: abort_quiesce
    in: query
    description: 'Set to `true` to promote the pod when the `pod-replica-link` is
      in the `quiescing` state and

      abort when waiting for the `pod-replica-link` to complete the quiescing operation.

      '
    required: false
    type: boolean
  Eradicate_all_data:
    name: eradicate_all_data
    in: query
    description: 'Set to `true` to perform a factory reset.

      '
    required: false
    type: boolean
  Vchost_ids:
    name: vchost_ids
    in: query
    description: 'Performs the operation on the unique vchost IDs specified.

      Enter multiple vchost IDs in a comma-separated format.

      For example, `vchostid01,vchostid02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Fleet_ids:
    name: fleet_ids
    in: query
    description: 'Performs the operation on the unique Fleet IDs specified.

      This query currently accepts only 1 ID.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  To_member_names:
    name: to_member_names
    in: query
    description: 'The resource will be moved to the specified local member realm or
      array.

      Enter multiple names in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Member_ids:
    name: member_ids
    in: query
    description: 'Performs the operation on the unique member IDs specified.

      Enter multiple member IDs in comma-separated format.

      The `member_ids` or `member_names` parameter is required, but they cannot be
      set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Member_names:
    name: member_names
    in: query
    description: 'Performs the operation on the unique member name specified.

      Examples of members include volumes, hosts, host groups, and directories.

      Enter multiple names in comma-separated format. For example, `vol01,vol02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Directory_names:
    name: directory_names
    in: query
    description: 'Performs the operation on the managed directory names specified.

      Enter multiple full managed directory names in comma-separated format.

      For example, `fs:dir01,fs:dir02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Unreachable:
    name: unreachable
    in: query
    description: 'If set to `true`, allows a one-sided disconnect when the connection

      status is `connecting` instead of `connected` or `partially connected`.

      If set to `false`, it works just like normal coordinated disconnect.

      If not specified, defaults to `false`.

      '
    required: false
    type: boolean
  Remote_policy_names:
    name: remote_policy_names
    in: query
    description: 'A comma-separated list of remote policy names. If, after filtering,
      there is not at least one

      resource that matches each of the elements, then an error is returned. This
      cannot be

      provided together with the `remote_policy_ids` query parameter.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Paths:
    name: paths
    in: query
    description: 'Target file path relative to the target directory.

      Enter multiple target file path in a comma-separated format.

      For example, `/dir1/dir2/file1,/dir3/dir4/file2`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Group_mapping_names:
    name: names
    in: query
    description: 'Performs the operation on the unique names specified.

      For example, `GroupRoleMappingName`.

      Enter multiple names in comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Protocol:
    name: protocol
    in: query
    description: 'Protocol type. Valid values are `azure`, `google-cloud`, `nfs`,
      and `s3`.

      '
    required: false
    type: string
  Alert_rules_code:
    name: code
    in: query
    description: 'The alert code that the rule applies to. Available alert codes

      for customization can be found in the alert rules catalog.

      '
    required: true
    type: integer
    format: int32
  Replication_snapshot:
    name: replication_snapshot
    in: query
    description: 'If set to `true`, allow destruction/eradication of snapshots in
      use by replication.

      If set to `false`, allow destruction/eradication of snapshots not in use by
      replication.

      If not specified, defaults to `false`.

      '
    required: false
    type: boolean
  Overwrite:
    name: overwrite
    in: query
    description: 'If set to `true`,

      overwrites an existing object during an object copy operation.

      If set to `false` or not set at all and the target name is an existing object,

      the copy operation fails.

      Required if the `source` body parameter is set

      and the source overwrites an existing object during the copy operation.

      '
    required: false
    type: boolean
  Total_item_count:
    name: total_item_count
    in: query
    description: 'If set to `true`, the `total_item_count` matching the

      specified query parameters is calculated and returned in

      the response. If set to `false`, the `total_item_count`

      is `null` in the response. This may speed up queries

      where the `total_item_count` is large. If not specified,

      defaults to `false`.

      '
    required: false
    type: boolean
  Remote_names:
    name: remote_names
    in: query
    description: 'A comma-separated list of remote array names. If, after filtering,
      there is not at least one

      resource that matches each of the elements, then an error is returned. This
      cannot be provided

      together with the `remote_ids` query parameter.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Remote_pod_names:
    name: remote_pod_names
    in: query
    description: 'A comma-separated list of remote pod names. If, after filtering,
      there is not at least one

      resource that matches each of the elements, then an error is returned. This
      cannot be provided

      together with the `remote_pod_ids` query parameter.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Uids:
    name: uids
    in: query
    description: 'A comma-separated list of local user IDs (UIDs).

      Enter multiple local user IDs in comma-separated format.

      For example, `423,51234`.

      '
    required: false
    type: array
    items:
      type: integer
      format: int32
    collectionFormat: csv
  Ids_without_names:
    name: ids
    in: query
    description: 'Performs the operation on the unique resource IDs specified.

      Enter multiple resource IDs in comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Protection_group_source_names:
    name: source_names
    in: query
    description: 'The name of the protection group or protection group snapshot to
      be copied

      into a new or existing protection group. If the destination protection group

      and all of its volumes already exist, include the `overwrite` parameter to

      overwrite all of the existing volumes with the snapshot contents.

      If including the `overwrite` parameter, the names of the volumes that are

      being overwritten must match the names of the volumes that are being restored.

      If the source is a protection group, the latest snapshot of the protection group

      will be used as the source during the copy operation.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Allow_errors:
    name: allow_errors
    in: query
    description: 'If set to `true`, the API will allow the operation to continue even
      if there are errors.

      Any errors will be returned in the `errors` field of the response.

      If set to `false`, the operation will fail if there are any errors.

      '
    required: false
    type: boolean
    default: false
  Expose_public_key:
    name: expose_public_key
    in: query
    description: 'If `true`, exposes the public key of the current user.

      '
    required: false
    type: boolean
  Join_existing_acct_ad:
    name: join_existing_account
    in: query
    description: 'If specified as `true`, the domain is searched for a pre-existing
      computer account to join to,

      and no new account will be created within the domain.

      The `user` specified when joining a pre-existing account must have permissions
      to

      ''read all properties from'' and ''reset the password of'' the pre-existing
      account.

      `join_ou` will be read from the pre-existing account and cannot be specified

      when joining to an existing account. If not specified, defaults to `false`.

      '
    required: false
    type: boolean
  Pod_replica_link_ids:
    name: pod_replica_link_ids
    in: query
    description: 'A comma-separated list of pod replica link IDs.

      If, after filtering, there is not at least one resource that matches each of
      the elements,

      then an error is returned.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Member_idsUnrestricted:
    name: member_ids
    in: query
    description: 'Performs the operation on the unique member IDs specified.

      Enter multiple member IDs in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Software_ids:
    name: software_ids
    in: query
    description: 'A comma-separated list of software IDs.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  OauthSubjectTokenType:
    name: subject_token_type
    in: formData
    description: 'An identifier that indicates the type of security token specifed
      in the `subject_token` parameter.

      The Pure Storage REST API supports the JSON Web Token (JWT)

      as the means for requesting the access token.

      Set `subject_token_type` to `urn:ietf:params:oauth:token-type:jwt`.

      '
    required: true
    type: string
    default: urn:ietf:params:oauth:token-type:jwt
  Allow_throttle_operation:
    name: allow_throttle
    in: query
    description: 'If set to `true`, allows operation to fail if array health is not
      optimal.

      '
    required: false
    type: boolean
  Member_destroyed:
    name: member_destroyed
    in: query
    description: 'If true, returns only destroyed member objects. Returns an error
      if

      a name of a live member object is specified in the member_names query param.

      If false, returns only live member objects. Returns an error if

      a name of a destroyed member object is specified in the member_names

      query param.

      '
    required: false
    type: boolean
  Software_names:
    name: software_names
    in: query
    description: 'A comma-separated list of software names.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Pgroup_group_ids:
    name: group_ids
    in: query
    description: 'Performs the operation on the unique group id specified.

      Provide multiple resource IDs in comma-separated format.

      The group_ids or names parameter is required, but they cannot be set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Add_to_protection_group_names:
    name: add_to_protection_group_names
    in: query
    description: 'The volumes will be added to the specified protection groups along
      with creation or movement

      across pods and array.

      When a volume is moved, the specified protection groups must be in the target
      pod or array.

      Enter multiple names in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Allow_throttle_snapshot:
    name: allow_throttle
    in: query
    description: 'If set to `true`, allows snapshot to fail if array health is not
      optimal.

      '
    required: false
    type: boolean
  App_names:
    name: app_names
    in: query
    description: 'The name of the installed app.

      Enter multiple names in comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Volume_ids:
    name: volume_ids
    in: query
    description: 'Performs the operation on the specified volume.

      Enter multiple ids in comma-separated format.

      For example, `vol01id,vol02id`.

      A request cannot include a mix of multiple objects with multiple IDs.

      For example, a request cannot include a mix of multiple volume IDs and host
      names;

      instead, at least one of the objects (e.g., `volume_ids`) must be set to only
      one name

      (e.g., `vol01id`).

      Only one of the two between `volume_names` and `volume_ids` may be used at a
      time.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Member_types:
    name: member_types
    in: query
    description: 'Performs the operation on the member types specified.

      The type of member is the full name of the resource endpoint.

      Valid values include `directories`.

      Enter multiple member types in comma-separated format.

      For example, `type01,type02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Replicate_now:
    name: replicate_now
    in: query
    description: 'If set to `true`, replicates the snapshots to each allowed target.

      The `replicate` and `replicate_now` parameters cannot be used together.

      '
    required: false
    type: boolean
  Container_ignore_usage:
    name: ignore_usage
    in: query
    description: 'Set to `true` to set a `quota_limit` that is lower than the existing
      usage.

      This ensures that no new volumes can be created until the existing usage

      drops below the `quota_limit`.

      If not specified, defaults to `false`.

      '
    required: false
    type: boolean
  Context_names:
    name: context_names
    in: query
    description: 'Performs the operation on the context specified. If specified, the
      context names

      must be an array of size 1, and the single element must be the name of an array

      in the same fleet. If not specified, the context will default to the array that

      received this request.


      Other parameters provided with the request, such as names of volumes or snapshots,

      are resolved relative to the provided `context`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Skip_phonehome_check:
    name: skip_phonehome_check
    in: query
    description: 'A flag to skip phonehome connectivity check. If `skip_phonehome_check`
      is not set,

      the factory reset will fail if phonehome connectivity check fails.

      Arrays that intentionally disable phonehome or do not permit connectivity

      to Pure Storage''s cloud servers must set this flag to `true` in order to start
      factory reset.

      '
    required: false
    type: boolean
  Eradicate_all_data_required:
    name: eradicate_all_data
    in: query
    description: 'Set to `true` to perform a factory reset.

      '
    required: true
    type: boolean
  Certificate_names:
    name: certificate_names
    in: query
    description: 'The names of one or more certificates.

      Enter multiple names in comma-separated format.

      For example, `cert01,cert02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Pod_ids:
    name: pod_ids
    in: query
    description: 'A comma-separated list of pod IDs.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Software_idsRequired:
    name: software_ids
    in: query
    description: 'A comma-separated list of software IDs.

      '
    required: true
    type: array
    items:
      type: string
    collectionFormat: csv
  Convert_source_to_baseline:
    name: convert_source_to_baseline
    in: query
    description: 'Set to `true` to have the snapshot be eradicated when it is no longer
      baseline on source.

      '
    required: false
    type: boolean
  Sort:
    name: sort
    in: query
    description: 'Returns the response objects in the order specified.

      Set `sort` to the name in the response by which to sort.

      Sorting can be performed on any of the names in the response,

      and the objects can be sorted in ascending or descending order.

      By default, the response objects are sorted in ascending order.

      To sort in descending order, append the minus sign (`-`) to the name.

      A single request can be sorted on multiple objects.

      For example, you can sort all volumes from largest to smallest volume size,

      and then sort volumes of the same size in ascending order by volume name.

      To sort on multiple names, list the names as comma-separated values.

      '
    required: false
    type: array
    items:
      type: string
      pattern: ^[a-z]+(_[a-z]+)*-?
    collectionFormat: csv
  Policy_ids:
    name: policy_ids
    in: query
    description: 'Performs the operation on the unique policy IDs specified.

      Enter multiple policy IDs in comma-separated format.

      The `policy_ids` or `policy_names` parameter is required, but they cannot be
      set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Ids:
    name: ids
    in: query
    description: 'Performs the operation on the unique resource IDs specified.

      Enter multiple resource IDs in comma-separated format.

      The `ids` or `names` parameter is required, but they cannot be set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Certificate_group_ids:
    name: certificate_group_ids
    in: query
    description: 'A comma-separated list of certificate group ids.

      If there is not at least one resource that matches each of the

      elements of `certificate_group_ids`, then an error is returned.

      This cannot be provided in conjunction with the `certificate_group_names` parameter.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Preset_names:
    name: preset_names
    in: query
    description: 'Create the resource using the preset specified by name. Only one
      preset can be specified.

      One of the `preset_ids` or `preset_names` parameters are required, but they
      cannot be

      set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Remove_from_protection_group_names:
    name: remove_from_protection_group_names
    in: query
    description: 'The volumes will be removed from the specified protection groups
      in the source pod or array along

      with the move.

      This can only be used when moving volumes across pods and arrays and must include
      all protection

      groups that the volumes are members of before the move.

      Enter multiple names in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  XRequestId:
    name: X-Request-ID
    in: header
    description: 'Supplied by client during request or generated by server.

      '
    required: false
    type: string
  Group_names:
    name: group_names
    in: query
    description: 'Performs the operation on the unique group name specified.

      Examples of groups include host groups, pods, protection groups, and volume
      groups.

      Enter multiple names in comma-separated format. For example, `hgroup01,hgroup02`.


      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Namespaces_delete:
    name: namespaces
    in: query
    description: 'A comma-separated list of namespaces. Only one namespace is allowed
      per delete request.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Renew_encryption_key:
    name: renew_encryption_key
    in: query
    description: 'If set to `true`, update array connection with a new encryption
      key.

      If set to `true`, other array connection attributes may not be modified in requests.

      Defaults to `false`.

      '
    required: false
    type: boolean
  Sids:
    name: sids
    in: query
    description: 'Performs the operation on the object SID specified.

      Enter multiple SIDs in comma-separated format.

      For example, `S-1-2-532-582374278-329482934,S-1-2-532-234235245-423425234`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  File_system_ids:
    name: file_system_ids
    in: query
    description: 'Performs the operation on the file system ID specified.

      Enter multiple file system IDs in comma-separated format.

      The `file_system_ids` or `file_system_names` parameter is required,

      but they cannot be set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  On_as_target:
    name: 'on'
    in: query
    description: 'Performs the operation locally with the target names specified as
      the

      destinations of the operation.

      Enter multiple target names in comma-separated format.

      For example, `targetName01,targetName02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Timeout:
    name: timeout
    in: query
    description: 'The duration of API token validity, in milliseconds.

      '
    required: false
    type: integer
    format: int64
  Container_names:
    name: names
    in: query
    description: 'Performs the operation on the unique container name specified.

      Enter multiple names in comma-separated format.

      The `ids` or `names` parameter is required, but they cannot be set together.

      Specifying `ids` or `names` with no value will perform the operation on local
      array.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  OauthSubjectToken:
    name: subject_token
    in: formData
    description: 'An encoded security ID Token representing the identity of the party
      on behalf of

      whom the request is being made. The token must be issued by a trusted

      identity provider which must be either a registered application in Pure1

      or an enabled API client on the array. The token must be a JSON Web Token

      and must contain the following claims:


      > | JWT claim | Location | API Client Field | Description | Required By |


      > |-|-|-|-|-|


      > | kid | Header | key_id | Key ID of the API client that issues the identity
      token. | FlashArray

      and FlashBlade only. |


      > | aud | Payload | id | Client ID of the API client that issues the identity
      token. | FlashArray

      and FlashBlade only. |


      > | sub | Payload | | Login name of the array user for whom the token should
      be issued.

      This must be a valid user in the system. | FlashArray and FlashBlade only. |


      > | iss | Payload | issuer | Application ID for the Pure1 or API client''s trusted
      identity

      issuer on the array. | All products. |


      > | iat | Payload | | Timestamp of when the identity token was issued.

      Measured in milliseconds since the UNIX epoch. | All products. |


      > | exp | Payload | | Timestamp of when the identity token will expire.

      Measured in milliseconds since the UNIX epoch. | All products. |



      Each token must also be signed with the private key that is paired with the

      API client''s public key.


      '
    required: true
    type: string
  Names_single_required:
    name: names
    in: query
    description: 'Performs the operation on the unique resource names specified.

      Only one value is supported.

      '
    required: true
    type: array
    items:
      type: string
    collectionFormat: csv
  Perf_protocol_type:
    name: protocol
    in: query
    description: 'Protocol type. Valid values are `nfs`, `smb`, and `all`.

      '
    required: false
    type: string
  Ids_single:
    name: ids
    in: query
    description: 'Performs the operation on the unique resource IDs specified. Supports
      exactly one value.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Replicate:
    name: replicate
    in: query
    description: 'If set to `true`, queues up and begins replicating to each allowed
      target after all

      earlier replication sessions for the same protection group have been

      completed to that target. The `replicate` and `replicate_now` parameters cannot
      be used together.

      '
    required: false
    type: boolean
  Continuation_token:
    name: continuation_token
    in: query
    description: 'A token used to retrieve the next page of data

      with some consistency guaranteed.

      The token is a Base64 encoded value.

      Set `continuation_token` to the system-generated token taken from the `x-next-token`

      header field of the response.

      A query has reached its last page when the response does not include a token.

      Pagination requires the `limit` and `continuation_token`

      query parameters.

      '
    required: false
    type: string
  PurityDefault:
    name: purity_default
    in: query
    description: 'Set diagnostics tool to default version.

      '
    required: false
    type: boolean
  Remote_ids:
    name: remote_ids
    in: query
    description: 'A comma-separated list of remote array IDs. If, after filtering,
      there is not at least one

      resource that matches each of the elements, then an error is returned. This
      cannot be provided

      together with the `remote_names` query parameter.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Protocol_endpoint_names:
    name: protocol_endpoint_names
    in: query
    description: 'Performs the operation on the protocol endpoints specified.

      Enter multiple names in comma-separated format.

      For example, `pe01,pe02`.

      A request cannot include a mix of multiple objects with multiple names.

      For example, a request cannot include a mix of multiple protocol endpoint names
      and host names;

      instead, at least one of the objects (e.g., `protocol_endpoint_names`) must
      be set to one name

      (e.g., `pe01`).

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Local_group_names:
    name: group_names
    in: query
    description: 'Performs the operation on the group names specified.

      Enter multiple group names in comma-separated format.

      For example, `group1,group2`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Protocol_endpoint_ids:
    name: protocol_endpoint_ids
    in: query
    description: 'Performs the operation on the protocol endpoints specified.

      Enter multiple IDs in comma-separated format.

      For example, `peid01,peid02`.

      A request cannot include a mix of multiple objects with multiple IDs.

      For example, a request cannot include a mix of multiple protocol endpoint IDs
      and host names.

      Instead, at least one of the objects (e.g., `protocol_endpoint_ids`) must be
      set to one ID

      (e.g., `peid01`).

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Local_member_ids:
    name: member_ids
    in: query
    description: 'Performs the operation on the unique local member IDs specified.

      Enter multiple member IDs in comma-separated format. For local group

      IDs refer to group IDs (GID). For local user IDs refer to user IDs (UID).

      The `member_ids` and `member_names` parameters cannot be provided together.

      '
    required: false
    type: array
    items:
      type: integer
      format: int32
    collectionFormat: csv
  Local_pod_ids:
    name: local_pod_ids
    in: query
    description: 'A comma-separated list of local pod IDs. If, after filtering, there
      is not at least one resource

      that matches each of the elements, then an error is returned. This cannot be
      provided together

      with the `local_pod_names` query parameter.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Segment_offset:
    name: segment_offset
    in: query
    description: 'Absolute offset, in bytes, of the segment to compare.

      Must be a multiple of block_size.

      '
    required: false
    type: integer
    format: int64
  Test_type:
    name: test_type
    in: query
    description: 'Specifies the type of test. Valid values are `all`, `phonehome`,
      and `remote-assist`.

      If not specified, defaults to `all`.

      '
    required: false
    type: string
  Resource_ids:
    name: resource_ids
    in: query
    description: 'A comma-separated list of resource IDs.

      The `resource_ids` or `resource_names` parameter is required, but they cannot
      be set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  KeepSessionsOpen:
    name: keep_open_sessions
    in: query
    description: 'If set to `true`, the session does not expire.

      If set to `false`, when the user is disabled or password is changed, the session
      expires.

      If not specified, defaults to `false`.

      '
    required: false
    type: boolean
  Quiesce:
    name: quiesce
    in: query
    description: 'Set to `true` to demote the pod after the `pod-replica-link` goes
      into `quiesced` state and allow

      the pod to become a target of the remote pod.

      This ensures that all local data has been replicated to the remote pod before
      the pod is demoted.

      '
    required: false
    type: boolean
  OauthGrantType:
    name: grant_type
    in: formData
    description: 'The method by which the access token will be obtained.

      The Pure Storage REST API supports the OAuth 2.0 "token exchange" grant type,

      which indicates that a token exchange is being performed.

      Set `grant_type` to `urn:ietf:params:oauth:grant-type:token-exchange`.

      '
    required: true
    type: string
    default: urn:ietf:params:oauth:grant-type:token-exchange
  History_end_time:
    name: end_time
    in: query
    description: 'Displays historical performance data for the specified time window,

      where `start_time` is the beginning of the time window,

      and `end_time` is the end of the time window.

      The `start_time` and `end_time` parameters

      are specified in milliseconds since the UNIX epoch.

      If `start_time` is not specified,

      the start time will default to one resolution before the end time,

      meaning that the most recent sample of performance data will be displayed.

      If `end_time`is not specified, the end time will default to the current time.

      Include the `resolution` parameter to display the performance data at the specified
      resolution.

      If not specified, `resolution` defaults to the lowest valid resolution.


      '
    required: false
    type: integer
    format: int64
  Alert_rules_code_list:
    name: code
    in: query
    description: 'The alert code to display.

      '
    required: false
    type: integer
    format: int32
  Export_names:
    name: export_names
    in: query
    description: 'Performs the operation on the export names specified. Enter multiple
      names in comma-separated

      format. For example, `name01,name02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Segment_length:
    name: segment_length
    in: query
    description: 'Length of the segment, in bytes, to compare.

      '
    required: true
    type: integer
    format: int64
  Group_ids:
    name: group_ids
    in: query
    description: 'A comma-separated list of group IDs.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Truncate:
    name: truncate
    in: query
    description: 'If set to `true`, reduces the size of a volume

      during a volume resize operation.

      When a volume is truncated, Purity automatically takes an undo snapshot,

      providing a 24-hour window during which the previous contents

      can be retrieved. After truncating a volume,

      its provisioned size can be subsequently increased,

      but the data in truncated sectors cannot be retrieved.

      If set to `false` or not set at all and the volume is being reduced in size,

      the volume copy operation fails.

      Required if the `provisioned` parameter is set to a volume size

      that is smaller than the original size.

      '
    required: false
    type: boolean
  Endpoints:
    name: endpoints
    in: query
    description: 'Performs the operation on the unique endpoints specified.

      Enter multiple endpoints in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Ignore_usage:
    name: ignore_usage
    in: query
    description: 'Flag used to override checks for quota management operations.

      If set to `true`, directory usage is not checked against the `quota_limits`
      that are set.

      If set to `false`, the actual logical bytes in use are prevented from exceeding

      the limits set on the directory. Client operations might be impacted.

      If the limit exceeds the quota, the client operation is not allowed.

      If not specified, defaults to `false`.

      '
    required: false
    type: boolean
  Remote_pod_ids:
    name: remote_pod_ids
    in: query
    description: 'A comma-separated list of remote pod IDs. If, after filtering, there
      is not at least one resource

      that matches each of the elements, then an error is returned. This cannot be
      provided together

      with the `remote_pod_names` query parameter.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Modify_resource_access_on_move:
    name: modify_resource_access
    in: query
    description: 'Describes how to modify a resource accesses of a resource when that
      resource is moved.

      Possible values are: `none`, `create`, and `delete`.

      The `none` value indicates that no resource access should be modified.

      The `create` value is used when a resource is moving out of a realm into the
      array and it needs to

      create a resource access of the moved resource to the realm from which it is
      moving.

      The `delete` value is used when a resource that is moving from an array into
      a realm already has

      a resource access into that realm.

      This is a required parameter when a resource is being moved to another member.

      '
    required: false
    type: string
  Names_required:
    name: names
    in: query
    description: 'Performs the operation on the unique name specified.

      For example, `name01`.

      Enter multiple names in comma-separated format.

      '
    required: true
    type: array
    items:
      type: string
    collectionFormat: csv
  Remove_all_entries:
    name: remove_all_entries
    in: query
    description: 'If set to `true`, removes all entries from the administrator cache.

      '
    required: true
    type: boolean
  Context_names_get:
    name: context_names
    in: query
    description: 'Performs the operation on the unique contexts specified.

      If specified, each context name must be the name of an array in the same fleet.

      If not specified, the context will default to the array that received this request.


      Other parameters provided with the request, such as names of volumes or snapshots,

      are resolved relative to the provided `context`.


      Enter multiple names in comma-separated format.

      For example, `name01,name02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Remove_from_protection_group_ids:
    name: remove_from_protection_group_ids
    in: query
    description: 'The volumes will be removed from the specified protection groups
      in the source pod or array along

      with the move.

      This can only be used when moving volumes across pods and arrays and must include
      all protection

      groups that the volumes are members of before the move.

      Enter multiple ids in a comma-separated format.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Preset_ids:
    name: preset_ids
    in: query
    description: 'Create the resource using the preset specified by the ID. Only one
      preset can be specified.

      One of the `preset_ids` or `preset_names` parameters are required, but they
      cannot be

      set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Policy_names:
    name: policy_names
    in: query
    description: 'Performs the operation on the policy names specified.

      Enter multiple policy names in comma-separated format.

      For example, `name01,name02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Eradicate_contents:
    name: eradicate_contents
    in: query
    description: 'Set to `true` to eradicate contents (e.g., volumes, protection groups,
      snapshots) and containers

      (e.g., realms, pods, volume groups). This enables you to eradicate containers
      with contents.

      '
    required: false
    type: boolean
  Pod_names:
    name: pod_names
    in: query
    description: 'Performs the operation on the unique pod name specified.

      Enter multiple names in comma-separated format. For example, `pod01,pod02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Names_single:
    name: names
    in: query
    description: 'Performs the operation on the unique resource names specified.

      Only one value is supported.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Factory_reset_token:
    name: factory_reset_token
    in: query
    description: 'A token required to perform a factory reset.

      '
    required: false
    type: integer
    format: int64
  Host_group_names:
    name: host_group_names
    in: query
    description: 'Performs the operation on the host group specified.

      Enter multiple names in comma-separated format.

      A request cannot include a mix of multiple objects with multiple names.

      For example, a request cannot include a mix of multiple host group names and
      volume names;

      instead, at least one of the objects (e.g., `host_group_names`) must be set
      to only one name

      (e.g., `hgroup01`).

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  File_system_names:
    name: file_system_names
    in: query
    description: 'Performs the operation on the file system name specified.

      Enter multiple file system names in comma-separated format.

      For example, `filesystem01,filesystem02`.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Allow_stretched_multi_vchost:
    name: allow_stretched_multi_vchost
    in: query
    description: 'If set to `true`, users are allowed to create a new

      vchost-connection to a stretched container that already has

      a vchost-connection. In principle, a stretched container can

      only have one vchost-connection at a time.

      '
    required: false
    type: boolean
    default: false
  Directory_ids:
    name: directory_ids
    in: query
    description: 'Performs the operation on the unique managed directory IDs specified.

      Enter multiple managed directory IDs in comma-separated format.

      The `directory_ids` or `directory_names` parameter is required, but they cannot
      be set together.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Group_gids:
    name: group_gids
    in: query
    description: 'Performs the operation on the specified GIDs.

      Enter multiple GIDs in comma-separated format.

      For example, `4234235,9681923`.

      '
    required: false
    type: array
    items:
      type: integer
      format: int32
    collectionFormat: csv
  Historical_total_only:
    name: total_only
    in: query
    description: 'If set to `true`, only return the aggregate value of all items after
      filtering.

      For real-time performance, the values are aggregated for the latest timestamp.

      For historical performance, the values are aggregated for each timestamp from

      `start_time` to `end_time`. Where it makes more sense, the average value is

      displayed instead. The values are displayed for each name where meaningful.

      If `total_only=true`, the `items` list will be empty.

      '
    required: false
    type: boolean
  Resource_destroyed:
    name: resource_destroyed
    in: query
    description: 'If set to `true`, returns only objects from destroyed resources.
      Returns an error if

      the name of a live resource is specified in the `resource_names` query parameter.

      If set to `false`, returns only objects from live resources. Returns an error
      if the

      name of a destroyed resource is specified in the `resource_names` query parameter.

      '
    required: false
    type: boolean
  Apply_retention:
    name: apply_retention
    in: query
    description: 'If `true`, applies the local and remote retention policy to the
      snapshots.

      '
    required: false
    type: boolean
  Local_only_ad:
    name: local_only
    in: query
    description: 'If specified as `true`, only delete the Active Directory configuration
      on the local array, without

      deleting the computer account created in the Active Directory domain. If not
      specified, defaults

      to `false`.

      '
    required: false
    type: boolean
  Fleet_names:
    name: fleet_names
    in: query
    description: 'Performs the operation on the unique Fleet names specified.

      Enter multiple names in comma-separated format.

      For example, `name01,name02`.

      This query currently accepts only 1 name.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  Filter:
    name: filter
    in: query
    description: 'Narrows down the results to only the response objects

      that satisfy the filter criteria.

      '
    required: false
    type: string
  Admit:
    name: admit
    in: query
    description: 'If `true`, admits any `unadmitted` drives into the system.

      '
    required: false
    type: boolean
  History_resolution:
    name: resolution
    in: query
    description: 'The number of milliseconds between samples of historical data.

      For array-wide performance metrics (`/arrays/performance` endpoint),

      valid values are `1000` (1 second), `30000` (30 seconds), `300000` (5 minutes),

      `1800000` (30 minutes), `7200000` (2 hours), `28800000` (8 hours), and `86400000`
      (24 hours).

      For performance metrics on storage objects (`<object name>/performance` endpoint),

      such as volumes,

      valid values are `30000` (30 seconds), `300000` (5 minutes), `1800000` (30 minutes),

      `7200000` (2 hours), `28800000` (8 hours), and `86400000` (24 hours).

      For space metrics, (`<object name>/space` endpoint),

      valid values are `300000` (5 minutes), `1800000` (30 minutes), `7200000` (2
      hours),

      `28800000` (8 hours), and `86400000` (24 hours).

      Include the `start_time` parameter to display the performance data

      starting at the specified start time.

      If `start_time` is not specified,

      the start time will default to one resolution before the end time,

      meaning that the most recent sample of performance data will be displayed.

      Include the `end_time` parameter to display the performance data until the specified
      end time.

      If `end_time`is not specified, the end time will default to the current time.

      If the `resolution` parameter is not specified

      but either the `start_time` or `end_time` parameter is,

      then `resolution` will default to the lowest valid resolution.

      '
    required: false
    type: integer
    minimum: 0
    format: int64
    x-example: 30000
  Protection_group_source_ids:
    name: source_ids
    in: query
    description: 'The id of the protection group or protection group snapshot to be
      copied

      into a new or existing protection group. If the destination protection group

      and all of its volumes already exist, include the `overwrite` parameter to

      overwrite all of the existing volumes with the snapshot contents.

      If including the `overwrite` parameter, the names of the volumes that are

      being overwritten must match the names of the volumes that are being restored.

      If the source is a protection group, the latest snapshot of the protection group

      will be used as the source during the copy operation.

      '
    required: false
    type: array
    items:
      type: string
    collectionFormat: csv
  All_vchosts:
    name: all_vchosts
    in: query
    description: 'If set to `true`, the storage container represented by the protocol
      endpoint

      is accessible to all vchosts.

      Users should not specify `vchost_ids` or `vchost_names` in the request.

      If set to `false`, the storage container represented by the protocol endpoint

      is only accessible to the vchosts that have explicit vchost-connections with

      the protocol endpoint. Users need to specify `vchost_ids` or `vchost_names`
      in the request.

      '
    required: false
    type: boolean
    default: false
  Preserve_configuration_data_required:
    name: preserve_configuration_data
    in: query
    description: 'A comma-separated list of configuration data types to preserve.

      A value of `all` will preserve all configuration

      data types. Valid values include `all`.

      '
    required: true
    type: array
    items:
      type: string
    collectionFormat: csv
  Promote_from:
    name: promote_from
    in: query
    description: 'The `undo-demote` pod that should be used to promote the pod.

      After the pod has been promoted, it will have the same data as the

      `undo-demote` pod and the `undo-demote` pod will be eradicated.

      '
    required: false
    type: string
  Limit:
    name: limit
    in: query
    description: 'Limits the size of the response to the specified number of objects
      on each page.

      To return the total number of resources, set `limit=0`.

      The total number of resources is returned as a `total_item_count` value.

      If the page size requested is larger than the system maximum limit,

      the server returns the maximum limit, disregarding the requested page size.

      '
    required: false
    type: integer
    minimum: 0
    format: int32
    x-example: 10
  Flagged:
    name: flagged
    in: query
    description: 'If set to `true`, lists only flagged messages.

      If set to `false`, lists only unflagged messages.

      if not specified, lists all messages.

      '
    required: false
    type: boolean
responses:
  OauthToken401:
    description: UNAUTHORIZED
    schema:
      type: object
      properties:
        error_description:
          type: string
          example: 'Invalid identity: JWT validation failed.'
        error:
          type: string
          example: invalid_client
  OauthToken400:
    description: BAD REQUEST
    schema:
      type: object
      properties:
        error_description:
          type: string
          example: 'Unsupported subject_token_type: urn:ietf:params:oauth:token-'
        error:
          type: string
          example: invalid_request
  OauthToken200:
    description: OK
    schema:
      type: object
      properties:
        access_token:
          type: string
          example: eyJraWQiOiJqTlBzL1Ria2c4U2Vua3F3ZmkvbnI4bWxxQ3NPIiwidHlwIjoiSldUIiwiYWxnIjoiUlMyNTYifQ.eyJhdWQiOiI5NDcyMTkwOC1mNzkyLTcxMmUtYTYzOS0wODM5ZmE4MzA5MjIiLCJzdWIiOiJqb2UiLCJyb2xlIjoic3RvcmFnZV9hZG1pbiIsImlzcyI6Imh0dHA6Ly9sb2NhbGhvc3Q6OTAwMCIsImlhdCI6MTU0NDAzNjA1MiwiZXhwIjoxNTQ0MDcyMDUyLCJqdGkiOiJjOTg0MjgyNS1mNGM3LTRiNGUtODdkNy03OWFiZmIxYTUzNDgifQ.pnuYAx0CkmkIG0LDrMAQGRr5Ci4-t5yMto3A7BkmyKnAVHBXG5ZIWwvNkWDLhqbA4YjmG7LZmWHrCVehLy2zy2vRLahFURsd3GTnOaWSyWFIyrwpoO81jQRtOQATtseweuMT_-C8o3oa4MgBNBsuKrhwKQS3oDDpeRPaCRTGev1_xRDxh_K6CWJBTAeOP2pcR4LW6zIJkCLzzkMuyL4aTJWWUjSbl04mcFbyw8r8W1GURrmaDVOvvpT634Hk9-GGh9OMIRlS6OOq7cJKc-RRWn18IK2Gs53V_KYshXTBbnRr990Y_qOX8MaTWOJTqgzsojY02MSVuJ9XDJWoIU3TQytr4K1vM2EvwDZDgl7LuUYUn7vWhbKktFzpeZyyhOjq3eX-ViugYKpIjBcG2f_-fcTPceEWGV82rd6TyVNB5A-v9u2kxCdW198t_kesgVQfuupDeS02cZe0ABLCzEHPiVF17JfiVr6sjkciioxN7Wj_j18ga4U0mdSukauT8yhbgCW1ijTVTFu1VwWebW0s8z3BWMtXdTtZ3BhcZVAdKRF8bOq7nfEbUQGhTn9g7dK-yF050winjtp-VTL2oUtkF5j1v_N8vPNiN9ZdkGJZr7VVZ-qeOJZcjdaRbxL6YB__yT1wkTcKPh8RHz6GUq7Jbyw8VIlcBE2nvJ63d0tH9C4
          description: 'The serialized OAuth 2.0 Bearer token used to perform authenticated
            requests.

            The access token must be added to the Authorization header of all API
            calls.

            '
        issued_token_type:
          type: string
          example: urn:ietf:params:oauth:token-type:access_token
          description: 'The type of token that is issued.

            The Pure Storage REST API supports OAuth 2.0 access tokens.

            '
        token_type:
          type: string
          example: Bearer
          description: 'Indicates how the API client can use the access token issued.

            The Pure Storage REST API supports the `Bearer` token.

            '
        expires_in:
          type: integer
          format: uint32
          example: 35999
          description: 'The duration after which the access token will expire.

            Measured in seconds. This differs from other duration

            fields that are expressed in milliseconds.

            '
      title: oauth_token_response
tags:
- name: Auth Helpers
  description: A set of endpoints not in the Purity API, but to make OAuth using this
    tool simple and easy to understand.
- name: Authorization
  description: 'Pure Storage uses the OAuth 2.0 Token Exchange authorization grant
    and JSON Web Tokens (JWTs)

    to authenticate to the Pure Storage REST API.

    Before you can exchange the ID token for an access token, create and enable the
    API client

    to generate the `key_id`, `id`, and `issuer` values.

    These values will be used as JWT claims for the `subject_token` parameter.

    '
- name: Active Directory
  description: 'Active Directory configuration authenticates users for NFS using Kerberos
    or SMB using Kerberos

    or New Technology LAN Manager (NTLM). Active Directory is also used to authorize
    users by

    mapping identities across the NFS and SMB protocols by using LDAP queries.

    '
- name: Administrators
  description: 'The FlashArray has a single default administrative account named pureuser.
    The administrator can

    add, delete, and modify administrators on the array. Administrators are assigned
    management

    access policies to give different levels of permissions.

    '
- name: Alerts
  description: 'Alerts indicate significant events that occur on an array, including
    whenever a component

    degrades or the capacity threshold of the component is reached.

    '
- name: Alert Watchers
  description: 'Alert watchers receive email notifications when alerts occur on an
    array.

    '
- name: API Clients
  description: 'An API client represents an identity type. API clients are created
    on the array.

    To create an API client, register and then enable it on the array.

    After the API client has been created, the user name (`sub`) and identity tokens
    (`kid` and

    `aud` tokens) that are issued by the API client are used as claims for the JSON
    Web Token

    that you create to authenticate into the REST API.

    '
- name: Apps
  description: 'Apps that extend array functionality can be integrated into the Purity//FA
    operating system.

    '
- name: Arrays
  description: 'Array features provide the ability to configure settings that affect
    the operation

    of the array as a whole and monitor array I/O performance.

    '
- name: Array Connections
  description: 'Manages connections between arrays.

    '
- name: Audits
  description: 'Audit trail records describe administrative actions performed by a
    user to modify the

    configuration of an array.

    '
- name: Certificates
  description: 'Purity//FA creates a self-signed certificate and private key when
    you start the system for the

    first time. You can use the default certificate, change the certificate attributes,
    create a new

    self-signed certificate, or import an SSL certificate signed by a certificate
    authority.

    '
- name: Certificate Groups
  description: 'Certificate groups, also known as certificate bundles, are collections
    of digital certificates.

    Each certificate can belong to one or more groups in order to serve different
    purposes.

    '
- name: Connections
  description: 'A connection gives hosts access to volumes on the array.

    '
- name: Container Default Protections
  description: 'Default protection is a list of protection groups that are applied
    to newly created volumes.

    Volumes can opt out of the default protection at creation. The pod''s `default_protections`

    defaults to the array''s `default_protections` at pod creation.

    Default protection can be configured on the array and the pod.

    '
- name: Controllers
  description: 'Controller data includes the name, mode, FlashArray model, Purity//FA
    software version, and

    status of each controller in the array.

    '
- name: Directories
  description: 'Important file system directories should be set up as managed directories.
    Managed directories

    can have policies attached to them. Managed directories differ from standard directories
    in that

    they provide space reporting and metrics.

    '
- name: Directory Exports
  description: 'Managed directory exports are created by adding NFS or SMB export
    policies to managed

    directories.

    '
- name: Directory Quotas
  description: 'Directory quotas provide the ability to set capacity limits on managed
    directories.

    '
- name: Directory Services
  description: 'Manages directory service configurations for integration with LDAP
    servers (e.g. Active

    Directory and OpenLDAP) in order to support various array services.

    '
- name: Directory Snapshots
  description: 'Directory snapshots are created manually or by adding snapshot policies
    to managed directories.

    Each snapshot policy can be re-used for multiple directories.

    '
- name: DNS
  description: 'DNS attributes include the domain suffix, static name servers, mode
    (static or DHCP), and search

    domain. The configured attributes can be listed.

    '
- name: Drives
  description: 'Drive data includes the name, type, status, capacity, protocol and
    other information

    for all flash, NVRAM, and cache modules in an array.

    '
- name: File Systems
  description: 'A FlashArray can contain up to 24 separate file systems, each with
    a number of directories that

    can be exported via supported protocols. Clients, using Active Directory or LDAP,
    can connect

    and access these exports using SMB or NFS.

    '
- name: Fleets
  description: 'A fleet is a collection of Arrays.

    '
- name: Hardware
  description: 'Operational status is reported by most of the hardware components
    in an array, including the

    chassis, controller, and storage shelf.

    '
- name: Host Groups
  description: 'Host groups implement consistent connections between a set of hosts
    and one or more volumes.

    Connections are consistent in the sense that all hosts associated with a host
    group

    address a volume connected to the group by the same LUN. Host groups are typically

    used to provide a common view of storage volumes to the hosts in a clustered application.

    '
- name: Hosts
  description: 'Hosts organize the storage network addresses (iSCSI Qualified Names,
    NVMe qualified names, or

    Fibre Channel world wide names) of client computers to identify the host''s intiators.
    Hosts also

    control communications between clients and volumes. After a volume has been created,
    establish a

    host-volume connection so that the host can read data from and write data to the
    volume.

    '
- name: KMIP
  description: 'The Key Management Interoperability Protocol (KMIP) server is used
    in combination with the Pure

    Storage Rapid Data Locking (RDL) feature and EncryptReduce feature to further
    secure the

    encrypted data on a FlashArray.

    '
- name: Log Targets
  description: 'Log Targets to be used to send management or data audit logs.

    '
- name: Maintenance Windows
  description: 'During a maintenance window, alerts are suppressed that are related
    to connections, paths,

    ports, and other resources that are down during maintenance.

    '
- name: Network Interfaces
  description: 'Manages the interfaces and the network connection attributes of the
    array.

    '
- name: Offloads
  description: 'The offload feature enables system administrators to replicate point-in-time
    volume snapshots

    from the array to an external storage system for long-term retention.

    Each offload target represents an external storage system, such as an Azure Blob
    container,

    NFS device, or S3 bucket, to where Purity//FA volume snapshots can be replicated.

    '
- name: Presets
  description: 'Presets are reusable templates that provision resources.

    '
- name: Pods
  description: 'Synchronous replication is managed through pods. A pod representing
    a collection of protection

    groups and volumes is created on one array and stretched to another array, resulting
    in fully

    synchronized writes between the two arrays. A pod can contain a mix of volumes,
    and protection

    groups with member volumes. Writes to the pod coming into either array are immediately

    synchronized and seen on both arrays.

    '
- name: Pod Replica Links
  description: 'Pod replica links are created by associating a source pod with a demoted
    pod, making

    the demoted pod the target pod of the source pod. The direction of the replica
    link is from the

    promoted source pod to the demoted target pod. Replica links can be created in
    either direction

    between the same two FlashArrays. The target pod of a replica link cannot be on
    the same

    FlashArray as the source pod.

    '
- name: Policies
  description: 'Policies are used to create exports (i.e., shares) and schedule snapshots.
    NFS and SMB policies

    can be created and have one or more rules applied to them. Each policy can be
    reused, creating

    exports for a number of managed directories.

    '
- name: Ports
  description: 'The ports on a FlashArray are assigned iSCSI Qualified Names (IQNs),
    NVMe Qualified Names

    (NQNs), and Fibre Channel World Wide Names (WWNs).

    '
- name: Protection Groups
  description: 'A protection group defines a set of volumes, hosts, or host groups
    (called members) that are

    protected together through snapshots with point-in-time consistency across the
    member volumes.

    The members within the protection group have common data protection requirements
    and the same

    snapshot, replication, and retention schedules.

    '
- name: Protection Group Snapshots
  description: 'Protection group snapshots capture the content of all volumes on the
    source array for the

    specified protection group at a single point in time.

    '
- name: Realms
  description: A realm is an administrative domain, a data container, and a namespace
    for pods, hosts, and host groups.
- name: Remote Arrays
  description: 'Remote arrays provide the ability to list and manage all the remote
    arrays known to an array.

    '
- name: Remote Pods
  description: 'A remote pod represents a pod that is on a connected array but not
    stretched to this array.

    '
- name: Remote Protection Groups
  description: 'A remote protection group represents a protection group that resides
    on an offload target with

    the source side of the remote protection group being another array that is connected
    to the

    local array. The local array can only see the remote protection groups of other
    arrays if the

    two arrays are connected.

    '
- name: Remote Protection Group Snapshots
  description: 'A remote protection group snapshot represents a protection group snapshot
    that resides on an

    offload target with the source side of the remote protection group snapshot being
    another array

    that is connected to the local array. The local array can only see the remote
    protection group

    snapshots of other arrays if the two arrays are connected.

    '
- name: Remote Volume Snapshots
  description: 'A remote volume snapshot represents a volume snapshot that resides
    on an offload target with the

    source side of the remote volume snapshot being another array that is connected
    to the local

    array. The local array can only see the remote volume snapshots of other arrays
    if the two

    arrays are connected.

    '
- name: SAML2 SSO
  description: 'SAML2 SSO allows customers to configure settings of SAML2 service
    provider

    and identity provider. It provides a multi-factor authentication (MFA)

    mechanism for customers to log in to FlashArray.

    '
- name: Sessions
  description: 'Manages Purity//FA login and user session data.

    '
- name: SMI-S
  description: 'Manages the Pure Storage Storage Management Initiative Specification
    (SMI-S).

    '
- name: SMTP
  description: 'Manages connections to Simple Network Management Protocol (SMTP) managers.

    '
- name: SNMP Agents
  description: 'Manages connections to Simple Network Management Protocol (SNMP) agents.

    '
- name: SNMP Managers
  description: 'Manages connections to Simple Network Management Protocol (SNMP) managers.

    '
- name: Software
  description: 'Software to be installed on the array.

    '
- name: Subnets
  description: 'Manages the subnets and VLANs used to organize the network interfaces.

    '
- name: Subscriptions
  description: 'Provides information about subscription offerings.

    '
- name: Subscription Assets
  description: 'Provides information about subscription assets.

    '
- name: Support
  description: 'Enables Support to fix bugs and help customers solve problems. Support
    tools include proxy,

    phonehome, and remote assist.

    '
- name: Syslog
  description: 'Copied to Log Targets/Syslog for more organized way to handle all
    log targets.

    Both endpoints are identical. We encourage our users to use Log Targets/Syslog.

    '
- name: Vchost Connections
  description: 'A vchost connection is between a protocol endpoint and vchost.

    In the context of vchost-connection, the vchost represents a vCenter,

    and the protocol endpoint is used to represent a storage container.

    Creating a vchost connection gives the vCenter access to the storage container.

    If `all_vchosts` is set to `true`, the container will be accessible to all vchosts.

    '
- name: Volumes
  description: 'A volume represents a container that manages the storage space on
    the array. After a volume has

    been created, host-volume connections must be established so that the host can
    read data from

    and write data to the volume. Volume data should be protected using asynchronous
    replication to

    a remote array, synchronous replication between remote arrays, and replication
    to external

    storage systems.

    '
- name: Volume Groups
  description: 'Volume groups organize volumes into logical groupings. If virtual
    volumes are configured, each

    volume group on the FlashArray array represents its associated virtual machine,
    and inside each

    of those volumes groups are the FlashArray volumes that are assigned to the virtual
    machine.

    Volume groups that are associated with virtual machines have names that begin
    with `vvol-` and

    end with the virtual machine name.

    '
- name: Volume Snapshots
  description: 'Volume snapshots are immutable, point-in-time images of the contents
    of one or more volumes.

    There are two types of volume snapshots&#58; volume snapshots and protection group
    volume

    snapshots. A volume snapshot is a snapshot that captures the contents of a single
    volume.

    A protection group volume snapshot is a volume snapshot that is created from a
    group of

    volumes that are part of the same protection group. All of the volume snapshots
    created from a

    protection group snapshot are point-in-time consistent with each other.

    '
- name: Workloads
  description: 'Workloads organize storage resources (such as volumes) and their related
    configuration and

    policy objects into logical groupings. Workloads can be deployed from workload
    presets.

    '
