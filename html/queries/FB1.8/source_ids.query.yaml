allOf:
- $ref: ../../queries/FB1.6/_ids_query_base.query.yaml
- name: source_ids
description: >-
  A comma-separated list of source file system IDs.
  If after filtering, there is not at least one resource that matches
  each of the elements of `source_ids`, then an error is returned.
  This cannot be provided together with the `names` or `sources` query parameters.
