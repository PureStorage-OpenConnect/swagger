name: fleet_names
in: query
description: Performs the operation on the unique Fleet names specified. Enter multiple
  names in comma-separated format. For example, `name01,name02`. This query currently
  accepts only 1 name.
type: array
collectionFormat: csv
items:
  type: string
