name: client_names
in: query
description: A comma-separated list of ip addresses of clients. For IPv6 both the
  extended format (x:x:x:x:x:x:x:x) and the shortened format are supported.
type: array
collectionFormat: csv
items:
  type: string
