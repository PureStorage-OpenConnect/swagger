name: resource_ids
in: query
description: A comma-separated list of resource IDs. The `resource_ids` or `resource_names`
  parameter is required, but they cannot be set together.
type: array
items:
  type: string
collectionFormat: csv
