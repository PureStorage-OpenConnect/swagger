collectionFormat: csv
description: A comma-separated list of resource IDs. The `resource_ids` and `resource_names`
  parameters cannot be provided together.
in: query
items:
  type: string
name: resource_ids
type: array
