name: remote_pod_ids
in: query
description: A comma-separated list of remote pod IDs. If, after filtering, there
  is not at least one resource that matches each of the elements, then an error is
  returned. This cannot be provided together with the `remote_pod_names` query parameter.
type: array
items:
  type: string
collectionFormat: csv
