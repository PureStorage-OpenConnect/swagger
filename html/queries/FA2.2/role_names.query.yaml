name: role_names
in: query
description: Performs the operation on the unique roles specified. For example, `array_admin`.
  Enter multiple roles in comma-separated format.
type: array
collectionFormat: csv
items:
  type: string
