name: names
in: query
description: Performs the operation on the unique container name specified. Enter
  multiple names in comma-separated format. The `ids` or `names` parameter is required,
  but they cannot be set together. Specifying `ids` or `names` with no value will
  perform the operation on local array.
type: array
collectionFormat: csv
items:
  type: string
