name: policy_names
in: query
description: >-
  A comma-separated list of policy names.
  If there is not at least one policy that matches each of the elements of
  `policy_names`, an error is returned.
  Single quotes are required around all strings.
type: array
collectionFormat: csv
items:
  type: string
