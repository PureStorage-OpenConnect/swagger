collectionFormat: csv
description: A comma-separated list of marketplace partner reference IDs. If there
  is not at least one resource that matches each `marketplace_partner.reference_id`
  element, an error is returned. Single quotes are required around all strings.
in: query
items:
  type: string
name: marketplace_partner_reference_ids
type: array
x-quoted: true
