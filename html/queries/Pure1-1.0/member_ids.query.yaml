name: member_ids
in: query
description: A comma-separated list of member IDs. If there is not at least one resource
  that matches each `member_id` element, an error is returned. Single quotes are required
  around all strings.
x-quoted: true
type: array
collectionFormat: csv
items:
  type: string
