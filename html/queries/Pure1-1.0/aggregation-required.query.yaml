name: aggregation
in: query
description: Aggregation needed on the metric data. Valid values are `avg` and `max`.
  Single quotes are required around all strings. Latency metrics averages are weighted
  by the IOPS.
x-quoted: true
type: string
required: true
