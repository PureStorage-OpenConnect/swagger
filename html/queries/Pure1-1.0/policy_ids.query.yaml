name: policy_ids
in: query
description: >-
  A comma-separated list of policy IDs.
  If there is not at least one policy that matches each of the elements
  of `policy_ids`, an error is returned.
  Single quotes are required around all strings.
type: array
collectionFormat: csv
items:
  type: string
