name: subscription_ids
in: query
description: A comma-separated list of subscription IDs. If there is not at least
  one resource that matches each `subscription.id` element, an error is returned.
  Single quotes are required around all strings.
x-quoted: true
type: array
collectionFormat: csv
items:
  type: string
