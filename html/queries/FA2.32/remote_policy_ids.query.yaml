name: remote_policy_ids
in: query
description: A comma-separated list of remote policy IDs. If, after filtering, there
  is not at least one resource that matches each of the elements, then an error is
  returned. This cannot be provided together with the `remote_policy_names` query
  parameter.
type: array
collectionFormat: csv
items:
  type: string
