collectionFormat: csv
description: A comma-separated list of local file system names. If there is not at
  least one resource that matches each of the elements, then an error is returned.
  This cannot be provided together with `local_file_system_ids` query parameter.
in: query
items:
  type: string
name: local_file_system_names
type: array
