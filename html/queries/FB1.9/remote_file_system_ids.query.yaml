collectionFormat: csv
description: A comma-separated list of remote file system IDs. If there is not at
  least one resource that matches each of the elements, then an error is returned.
  This cannot be provided together with the `remote_file_system_names` query parameter.
in: query
items:
  type: string
name: remote_file_system_ids
type: array
