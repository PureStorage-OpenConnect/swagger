name: local_file_system_ids
description: A comma-separated list of local file system IDs. If after filtering,
  there is not at least one resource that matches each of the elements, then an error
  is returned. This cannot be provided together with the `local_file_system_names`
  query parameter.
in: query
type: array
collectionFormat: csv
items:
  type: string
