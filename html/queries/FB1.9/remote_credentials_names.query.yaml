collectionFormat: csv
description: A comma-separated list of remote credentials names. If there is not at
  least one resource that matches each of the elements, then an error is returned.
  This cannot be provided together with the `remote_credentials_ids` query parameter.
in: query
items:
  type: string
name: remote_credentials_names
type: array
