collectionFormat: csv
description: Performs the operation on the unique name specified. For example, `name01`.
  Enter multiple names in comma-separated format. The `ids` or `names` parameter is
  required, but both cannot be set together.
in: query
items:
  type: string
name: names
type: array
