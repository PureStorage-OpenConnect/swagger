name: group_names
in: query
description: Performs the operation on the unique group name specified. Examples of
  groups include host groups, pods, protection groups, and volume groups. Enter multiple
  names in comma-separated format. For example, `hgroup01,hgroup02`.
type: array
collectionFormat: csv
items:
  type: string
