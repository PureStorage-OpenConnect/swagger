name: member_names
in: query
description: Performs the operation on the unique member name specified. Examples
  of members include volumes, hosts, host groups, and directories. Enter multiple
  names in comma-separated format. For example, `vol01,vol02`.
type: array
collectionFormat: csv
items:
  type: string
