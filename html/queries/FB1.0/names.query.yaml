allOf:
- $ref: ../../queries/FB1.0/_names_query_base.query.yaml
- name: names
- description: >-
    A comma-separated list of resource names.
    If there is not at least one resource that matches
    each of the elements of `names`, then an error is returned.
