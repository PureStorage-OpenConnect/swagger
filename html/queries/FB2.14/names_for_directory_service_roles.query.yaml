name: names
description: A comma-separated list of resource IDs. If after filtering, there is
  not at least one resource that matches each of the elements of `names`, then an
  error is returned. This cannot be provided together with the `ids`, `role_names`,
  or `role_ids` query parameters.
in: query
type: array
collectionFormat: csv
items:
  type: string
