name: group_names
description: A comma-separated list of group names. If there is not at least one resource
  that matches each of the elements of `group_names`, then an error is returned. This
  cannot be provided together with `gids` query parameter.
in: query
type: array
collectionFormat: csv
items:
  type: string
