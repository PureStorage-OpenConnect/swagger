name: gids
description: A comma-separated list of group IDs. If there is not at least one resource
  that matches each of the elements of `gids`, then an error is returned. This cannot
  be provided together with `group_names` query parameter.
in: query
type: array
collectionFormat: csv
items:
  type: integer
  format: int32
