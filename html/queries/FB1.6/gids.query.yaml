allOf:
- $ref: ../../queries/FB1.6/_ids_query_base.query.yaml
- name: gids
description: >-
  A comma-separated list of group IDs.
  If there is not at least one resource that matches
  each of the elements of group IDs, then an error is returned.
  This cannot be provided together with group_names query parameter.
