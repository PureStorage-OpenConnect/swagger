name: remote_bucket_names
in: query
description: A comma-separated list of remote bucket names. If there is not at least
  one resource that matches each of the elements, then an error is returned.
type: array
collectionFormat: csv
items:
  type: string
