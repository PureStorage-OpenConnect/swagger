collectionFormat: csv
description: A comma-separated list of keytab names. If there is not at least one
  resource that matches each of the elements, then an error is returned. This cannot
  be provided together with `keytab_ids` query parameter.
in: query
items:
  type: string
name: keytab_names
type: array
