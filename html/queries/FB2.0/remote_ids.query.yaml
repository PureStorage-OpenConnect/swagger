collectionFormat: csv
description: A comma-separated list of remote array IDs. If after filtering, there
  is not at least one resource that matches each of the elements, then an error is
  returned. This cannot be provided together with the `remote_names` query parameter.
in: query
items:
  type: string
name: remote_ids
type: array
