name: remote_credentials_ids
description: A comma-separated list of remote credentials IDs. If after filtering,
  there is not at least one resource that matches each of the elements, then an error
  is returned. This cannot be provided together with the `remote_credentials_names`
  query parameter.
in: query
type: array
collectionFormat: csv
items:
  type: string
