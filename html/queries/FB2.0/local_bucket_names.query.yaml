collectionFormat: csv
description: A comma-separated list of local bucket names. If there is not at least
  one resource that matches each of the elements, then an error is returned. This
  cannot be provided together with `local_bucket_ids` query parameter.
in: query
items:
  type: string
name: local_bucket_names
type: array
