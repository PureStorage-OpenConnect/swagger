collectionFormat: csv
description: A comma-separated list of remote array names. If there is not at least
  one resource that matches each of the elements, then an error is returned. This
  cannot be provided together with `remote_ids` query parameter.
in: query
items:
  type: string
name: remote_names
type: array
