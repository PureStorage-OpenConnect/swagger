collectionFormat: csv
description: A comma-separated list of resource names. If there is not at least one
  resource that matches each of the elements of `names`, then an error is returned.
in: query
items:
  type: string
name: names
type: array
