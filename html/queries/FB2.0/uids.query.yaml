collectionFormat: csv
description: A comma-separated list of user IDs. If there is not at least one resource
  that matches each of the elements of `uids`, then an error is returned. This cannot
  be provided together with `user_names` query parameter.
in: query
items:
  format: int32
  type: integer
name: uids
type: array
